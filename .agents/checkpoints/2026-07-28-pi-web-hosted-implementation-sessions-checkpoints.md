# Pi Web-Hosted Implementation Sessions Checkpoints

Plan: `.agents/plans/2026-07-28-pi-web-hosted-implementation-sessions.md`

## Handoff

**Source:** Pi Subagents run `6121e246-b04b-48cc-afd8-1d796dca575f`, `context-builder` child 0 (Pi Web runtime and browser seams); artifact `.pi-subagents/artifacts/outputs/6121e246-b04b-48cc-afd8-1d796dca575f/parallel-0/0-context-builder/context.md`.
**Purpose:** Map the exact Pi Web registry, startup lock, wrapper lifecycle, prompt dispatch, control/SSE routes, discovery cache, browser refresh path, SDK cleanup seam, and focused test harness against the approved hosted-session contract.
**Outcome:** The existing process-global registry, per-ID startup promise, fire-and-forget prompt path, running accounting, routes, and SSE are the correct reusable ownership/control plane. Four implementation blockers were confirmed: startup has no final cancellation/publication transaction or unpublished-native cleanup; existing/in-flight reuse validates only ID rather than exact JSONL/cwd; idle expiry destroys active wrappers; and cache invalidation/running SSE does not make an unknown hosted target row refresh into the browser. Native `AgentSession.dispose()` exists but the wrapper does not call it, and registry deletion is not identity guarded. A narrow versioned capability lifecycle, hosted background prompt lifecycle diagnostics, active-safe cleanup, exact identity checks, and ordinary list reload on an unknown running ID are required; no new target API or session model is justified.
**Evidence:** Decisive ranges are `lib/rpc-manager.ts:107-335,603-613,952-1104`, `lib/pi-types.ts:116-160`, `lib/session-reader.ts:16-100`, `app/api/agent/[id]/route.ts:17-55`, `app/api/agent/[id]/events/route.ts:14-60`, `app/api/agent/running/events/route.ts:16-29`, and `components/SessionSidebar.tsx:363-444`. Installed SDK evidence confirms synchronous `AgentSession.dispose()` in `node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.d.ts:271-292`. Existing deferred-binding/prompt/compaction fixtures in `lib/rpc-manager.test.mjs:120-223` provide the focused race harness. Counterevidence against broader work: command routing and native JSONL discovery already work once the exact wrapper is published, and selected-session IDs already isolate source and target controls.
**Uncertainty / gaps:** The plan leaves the literal symbol/version/owner constants and the browser refresh carrier as engineering details; they must be identical in both repositories and remain structural. Browser interaction and process-lifetime behavior still require runtime evidence. Any inability to dispose an unpublished native session or validate exact identity is a stop condition.
**Recommended use:** Implement cleanup and cancellable exact registration first, then the same-process capability and background kickoff, then ordinary discovery refresh and deterministic race/privacy tests. Preserve existing routes, SSE authority, and source-selected URL behavior.

## Handoff

**Source:** Pi Subagents run `6121e246-b04b-48cc-afd8-1d796dca575f`, `context-builder` child 1 (dot_files launcher, policy, docs, and tests); artifact `.pi-subagents/artifacts/outputs/6121e246-b04b-48cc-afd8-1d796dca575f/parallel-0/1-context-builder/context.md`.
**Purpose:** Trace the dot_files Start/Orchestrate materialization and launch boundary, direct-root guard, detached fallback, prompt/depth policy, maintained documentation, tests, and cross-repository delivery constraints from captured base `8c09ba6e2cae79795390a9049788d6f942be23b4` without using dirty-main changes.
**Outcome:** Preparation through JSONL materialization must remain unchanged; only the final launch seam should select a compatible Pi Web capability or, on true absence, the existing detached print launcher. Any present malformed, incompatible, foreign, invalidated, or rejecting capability is a bounded no-fallback failure. The hosted request must contain only target ID/file/cwd, kickoff, literal `start | orchestrate`, and the source signal; Orchestrate's `targetEnvironment` remains detached-only. Result messaging must permit Pi Web selection/control only for hosted ownership and preserve the detached no-control warning otherwise. The process-global preparation guard already spans Start and Orchestrate but its source-session wording is false. The tracked depth ceiling must become two while global policy, the finalized embedded orchestrator workflow, and `milestone-implementer` state the non-mechanical ordinary one-level rule and the root depth 0 → implementer depth 1 → read-only support depth 2 → no further delegation exception.
**Evidence:** Core seams are `ai/.pi/agent/extensions/start-implementation/transaction.ts:31-71,87-95,96-255`, `print.ts:6-78`, `launcher-extension.ts:23-45,58-100,108-172`, and `orchestrate-implementation/profile.ts:5-13`. Policy surfaces are `ai/.pi/agent/extensions/subagent/config.json`, `ai/.pi/agent/AGENTS.md:65-75`, `skills/orchestrator/SKILL.md:10-79`, and `agents/milestone-implementer.md:13-39`. Existing detached regression ranges are `start-implementation.test.ts:1168-1717,1858-2074`; capability, result, guard, depth, prompt, README, and wiki tests require extension. Before this handoff, the parent captured dirty dot_files main at `8c09ba6...` and created the clean registered companion worktree `/Users/xin/Documents/repos/dot_files/.agents/worktrees/2026-07-28-pi-web-hosted-implementation-sessions` on branch `2026-07-28-pi-web-hosted-implementation-sessions`; unrelated main changes remain untouched.
**Uncertainty / gaps:** The repository has no root package/tsconfig, so the prior strict one-shot TypeScript validation command must be recovered and recorded. Full link audits may retain an unrelated missing Kimi source baseline. Prompt policy tests prove wording/topology, not universal model compliance. Dot_files cannot prove Pi Web registry atomicity and must use faithful structural fakes without claiming end-to-end ownership evidence.
**Recommended use:** Freeze shared capability constants first; add a dependency-free structural client and absent-only selector; thread launch kind/ownership through the existing transaction; correct messaging/guard lifetime tests; then update depth policy, prompt contracts, READMEs/wiki, and deterministic suites. Use the dot_files main mutex for closeout and stop on any overlap with dirty main.

## Handoff

**Source:** Pi Subagents run `6121e246-b04b-48cc-afd8-1d796dca575f`, `reviewer` child 2 (VC-001 through VC-010 validation audit); transcript `.pi-subagents/artifacts/6121e246-b04b-48cc-afd8-1d796dca575f_reviewer_2_transcript.jsonl`, session `eb6ddc4b/run-2/session.jsonl` under the parent session artifact tree.
**Purpose:** Adversarially map every approved validation obligation to reusable evidence, smallest deterministic additions, manual runtime checks, and two-repository closeout safeguards.
**Outcome:** The reviewer independently confirmed the same P0 blockers: unconditional active idle eviction, absent cancellation/publication transfer, stale browser discovery, superseded depth-one global config/tests, and over-broad RPC eligibility without positive capability detection. It additionally highlighted identity-guarded registry removal, invalidation-before-cleanup, privacy-safe diagnostics, hosted/detached message divergence, post-publication source isolation, and both automatic discovery and hard-refresh runtime flows. The evidence matrix requires executable ownership races and control isolation rather than source-regex-only P0 tests, retained real TUI fallback coverage, exact prompt-topology tests, separate scoped commits, and guarded integration dispositions.
**Evidence:** VC mappings cite the same runtime surfaces plus dot_files fallback tests `start-implementation.test.ts:1168-1411,1858-2074`, depth tests in `agents/test/milestone-implementer.test.mjs`, `skills/orchestrator/test/orchestrator-contract.test.mjs`, and `orchestrate-implementation/test/discovery-depth.test.mjs`, and mutex behavior in `.agents/scripts/main-branch-lock.sh`. Captured bases are Pi Web `2e4160a899ce07433f7dd787eaf1da34c36bd01f` and dot_files `8c09ba6e2cae79795390a9049788d6f942be23b4`; both task worktrees are registered and initially clean. Counterevidence against new infrastructure is the existing live-wrapper route/SSE fast path and native resume format.
**Uncertainty / gaps:** Pi Web has no main-branch lock helper, so final closeout needs a documented single-coordinator/no-race proof or must stop. Browser runtime evidence is not replaceable by static tests. Arbitrary exception text may be inherently unsafe to log; generic bounded messages are preferable if redaction cannot be proved. The dirty dot_files main makes final overlap assessment mandatory.
**Recommended use:** Treat VC-001/002/003/005 executable race and routing tests, VC-004 exact topology/config tests, VC-007 retained TUI tests, and VC-008 captured-log redaction tests as blockers. Run repository-specific full static gates, a realistic Pi Web development pass without `next build`, independent fresh review, separate commits, final checkpoint summary naming both commits, then guarded closeout or exact recovery disposition.

## Handoff

**Source:** Pi Subagents run `388d4e4f-de83-4c48-8a9d-224835ec0217`, `context-builder` (installed Pi 0.82.1 SDK and extension lifecycle); artifact `.pi-subagents/artifacts/outputs/388d4e4f-de83-4c48-8a9d-224835ec0217/context.md`, session `809457b1/run-0/session.jsonl` under the parent session artifact tree.
**Purpose:** Read the installed Pi extension, SDK, session-format, and RPC documentation completely, follow relevant examples and implementation declarations, and confirm or challenge the same-process construction, binding, prompt, cancellation, and cleanup assumptions.
**Outcome:** Public SDK evidence confirms the planned same-process seam: exact `SessionManager.open`, cwd-bound `createAgentSessionServices`, one `createAgentSessionFromServices` owner, subscription, RPC-mode binding, and host-owned fire-and-forget `prompt()` are supported without child RPC or upstream change. `prompt()` normally resolves only after the turn, while Pi RPC deliberately invokes it without awaiting and distinguishes later events; hosted acknowledgement must therefore mean scheduled, never preflight-accepted or settled. Two lifecycle limitations must remain explicit: direct idle extension commands normally expose `ctx.signal` as undefined, so the structural request must preserve the optional source signal and real cancellation can only be honored when one exists; and supported bare `AgentSession.dispose()` aborts/disconnects but does not emit `session_shutdown`, while the runtime API that does so is an explicit plan non-goal. SDK construction also accepts no signal, so cancellation can be checked after awaited creation and immediately before publication but cannot interrupt a hung extension factory.
**Evidence:** Authoritative ranges include installed `docs/sdk.md:184-231,719-815,1116-1129`, `docs/extensions.md:170-224,880-930,989-1032,2866-2875`, `docs/session-format.md:386-404`, `docs/rpc.md:1-7,42-111,672-741,1134-1165`, `dist/core/agent-session-services.d.ts:15-76`, `dist/core/agent-session-services.js:45-130`, `dist/core/sdk.js:245-266`, `dist/core/agent-session.js:517-570,792-916,1158-1175,1741-1776,1888-1907`, and `dist/core/agent-session-runtime.js:285-292`. Examples `examples/sdk/06-extensions.ts`, `11-sessions.ts`, `13-session-runtime.ts`, and extension input/user-message examples corroborate construction, cleanup, and source/streaming semantics. Counterevidence to a mandatory concrete source signal is the installed optional context type and idle preflight ordering; counterevidence to graceful bare-session shutdown is the runtime-only shutdown emission.
**Uncertainty / gaps:** The plan wording names a source `AbortSignal`, but the actual direct idle API can supply no signal. Implementation can carry `sourceSignal: AbortSignal | undefined`, honor injected/available signals through the exact pre-publication check, and record the normal-idle limitation; fabricating cancellation or coupling source Stop through internals would violate the approved architecture. Per-wrapper extension `session_shutdown` parity is likewise unavailable without the excluded runtime adoption. These are SDK constraints, not authorization for upstream or runtime expansion.
**Recommended use:** Keep the capability request field exact but optional in type and value, never race or abandon SDK startup, test both undefined and abortable signals, validate exact ID/file/cwd, initiate binding before kickoff, describe success only as scheduled, call native `dispose()` exactly once on real cleanup, and record the missing graceful shutdown emission as residual risk rather than using undocumented internals.

## Implementation Summary

**Plan section:** Design / Implementation Strategy §§1–5; Pi Web portions of Test Strategy, Telemetry / Debuggability, and VC-001, VC-002, VC-003, VC-005, VC-006, VC-008, and VC-009.
**Work and outcome:** Added the versioned process-local Pi Web hosted-session capability with same-runtime replacement, foreign/incompatible preservation, invalidated callback rejection, exact request validation, and content-free diagnostics. Refactored native startup through the existing per-ID lock into an atomic final-check/subscribe/cache/publish/bind boundary with unpublished disposal and exact hosted ID/file/cwd validation. Added wrapper-owned kickoff scheduling that claims running state before unresolved binding, retains no source signal or target promise, emits bounded target lifecycle outcomes, and prevents duplicate hosted kickoff on one wrapper. Idle cleanup now defers across prompt claims, compaction claims, native streaming, and native compaction, resets a full idle window after settlement, conditionally removes only the same registry owner, and calls native `dispose()` exactly once. Hosted publication invalidates ordinary discovery and emits `sessions_changed` over the existing running SSE route; the sidebar reloads native `/api/sessions` without selection or URL mutation. Existing target routes/SSE remain the sole control path. Updated Pi Web architecture instructions and durable memory.
**Validation / evidence:** Focused capability/wrapper/sidebar tests and the full Pi Web Node suite pass (129/129); TypeScript, lint, and `git diff --check` pass. Executable tests cover startup sharing, pre-publication cancellation/disposal, post-publication source isolation, unresolved binding/target acknowledgement, exact owner mismatch, target error/resume, active-safe idle cleanup, source/target command isolation, HMR invalidation, foreign preservation, privacy, and discovery signaling. A realistic isolated Next development/headless-Chromium pass used a committed synthetic plan and local faux provider: the target appeared without browser refresh or source URL change, a second source prompt completed while it remained active, source Stop was isolated, sidebar selection reused the owner, steer/follow-up queued on the target, target Stop settled it, diagnostics had one acceptance/schedule/dispatch/native-start/settlement event and zero payload markers, process exit logged cleanup, and native `SessionManager.open()` resumed the JSONL. A second Pi Web-hosted Orchestrate runtime used the tracked global max-two config and finalized workflow to observe root depth 0/max 2, the designated implementer at depth 1/max 2 with nested tools, fresh support at depth 2/max 2 without `subagent`/`edit`/`write`, real child run/session IDs, no depth above two, one target owner/start, and zero diagnostic payload markers. Durable evidence is `.agents/reports/2026-07-29-pi-web-hosted-runtime-validation.md`.
**Departures from approved obligations:** Installed Pi 0.82.1 supplies `ctx.signal` as optional and normally undefined for exact idle commands, so the exact capability field is `AbortSignal | undefined`; tests prove cancellation when a signal exists, and runtime proves source isolation after publication, but normal idle source Stop cannot cancel SDK construction. SDK construction itself is not signal-aware, so the caller awaits any slow extension factory before the final check as required. Bare public `AgentSession.dispose()` does not emit `session_shutdown`; adopting `AgentSessionRuntime` remains explicitly excluded, so graceful per-wrapper extension shutdown parity is a residual limitation. An initial synthetic `/tmp` versus `/private/tmp` alias split project grouping; the accepted canonical-realpath browser run passed, and arbitrary symlink-root canonicalization remains outside scope. No other Pi Web obligation is incomplete.
**Implementation commit:** Pending.

## Implementation Summary

**Plan section:** Design / Implementation Strategy §§2, 6, and 7; dot_files portions of Test Strategy, VC-004, VC-005, VC-007, VC-009, and VC-010.
**Work and outcome:** Added the dot_files structural client for the same symbol/protocol/version/owner contract and an absent-only hosted-or-detached selector after native JSONL materialization. Direct TUI always stays detached; unrelated RPC with true absence stays detached; every present malformed, foreign, incompatible, invalidated, rejecting, or malformed-acknowledgement record fails without print retry. Hosted requests carry only target ID/file/cwd, kickoff, literal `start | orchestrate`, and optional source signal; Orchestrate environment overrides remain inside detached print construction. Launch results now identify Pi Web versus detached ownership, user notices permit ordinary Pi Web controls only for hosted targets, and the process-global preparation guard describes its real Pi-process scope and ends after registration/spawn. Raised tracked global subagent depth to two, preserved the explicit Orchestrate detached override, and made global policy, the finalized embedded orchestrator workflow, and `milestone-implementer` state the non-mechanical ordinary one-level rule and exact depth-zero root → depth-one writer → depth-two read-only support → no-delegation topology. Updated Start/Orchestrate READMEs, current-state wiki, and append-only wiki log.
**Validation / evidence:** New pure capability tests pass with exact request/no-environment assertions, TUI and unrelated-RPC fallback, present-record no-fallback cases, and no caller-side signal race. The expanded Start transaction suite passed 29 of 30 on its first post-change run; all behavioral launch/TUI/runtime tests passed and the sole stale static assertion was corrected to allow documentation to say target settlement is not awaited. Prompt/policy/agent/discovery suites pass 22/22. Strict combined TypeScript for Start/Orchestrate production and changed tests passes under ESNext/Bundler resolution, and `git diff --check` passes. Full corrected reruns, mission trace, link/profile checks, privacy scan, and independent review remain pending before commit.
**Departures from approved obligations:** The hosted request preserves the real optional Pi extension signal rather than fabricating a concrete one; available-signal cancellation remains exact and caller-owned racing remains prohibited. Durable dot_files memory files are intentionally untouched because local main has unrelated modifications on their exact paths; maintained current state is recorded in wiki instead. No launch, depth, prompt-topology, or detached-fallback obligation is waived.
**Implementation commit:** Pending.

## Handoff

**Source:** Pi Subagents run `ff223059-97ff-4052-a0d7-82a3c778b2c8`, `reviewer` child 0 (Pi Web ownership/concurrency); output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/ff223059-97ff-4052-a0d7-82a3c778b2c8/output-0.log`.
**Purpose:** Adversarially review capability lifecycle, atomic registration, kickoff ownership, idle cleanup, control routing, discovery, and SDK boundaries.
**Outcome:** Exact identity, atomic final check/publication, source detachment, HMR invalidation/foreign preservation, wrapper-owned kickoff, active-safe cleanup, native disposal, and registry identity guards were confirmed. Three defects were found: `sessions_changed` is live-listener-only and can be lost across EventSource disconnection; overlapping browser list requests can let an older stale response overwrite a newer refresh; and a duplicate hosted launch reports successful scheduling even when the wrapper rejects the duplicate kickoff.
**Evidence:** Findings cite `lib/rpc-manager.ts:1119-1125,1295-1302`, `app/api/agent/running/events/route.ts:20-39`, `components/SessionSidebar.tsx:363-423`, and `lib/hosted-implementation-session.ts:212-229`. The reviewer ran a late-subscriber probe that received no refresh. Counterevidence is the successful connected runtime flow and correct existing startup/HMR/cleanup tests.
**Uncertainty / gaps:** Process shutdown ordering remained source-asserted rather than active-target runtime-tested at review time. Pre-publication cancellation uses the shared startup seam rather than a cold full native factory. File mismatch had direct coverage while ID/cwd mismatch used the same validation function without separate cases.
**Recommended use:** Make session-list refresh generation replayable on initial/reconnected SSE, guard client responses by request generation, make duplicate scheduling a target-owned no-duplicate result that does not falsely acknowledge the second request, and add executable regressions.

## Handoff

**Source:** Pi Subagents run `ff223059-97ff-4052-a0d7-82a3c778b2c8`, `reviewer` child 1 (dot_files capability/fallback/policy); output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/ff223059-97ff-4052-a0d7-82a3c778b2c8/output-1.log`.
**Purpose:** Review exact cross-repository protocol compatibility, selection/failure ambiguity, detached compatibility, guard behavior, depth topology, and maintained documentation.
**Outcome:** No production blocker was found. The reviewer independently matched symbol/protocol/version/owner and request/response shapes, confirmed RPC-only hosted selection, TUI/absent fallback, every present-record no-fallback case, no environment leak, unchanged materialization and detached argv/env/spawn behavior, correct ownership notices/guard, global max two, non-mechanical ordinary policy, finalized-master topology, and current docs. It requested only more literal complete hosted Orchestrate transaction/message and post-host guard-release tests.
**Evidence:** Decisive ranges are dot_files `start-implementation/hosted.ts:51-133`, `transaction.ts:105-239`, `launcher-extension.ts:58-147`, `orchestrate-implementation/profile.ts:5-14`, global policy lines 70-71, orchestrator lines 38-40, and milestone implementer line 19. Direct protocol comparison and all focused suites passed. Worktree-root link audit failures were correctly attributed to live-main links and the known unrelated missing Kimi source.
**Uncertainty / gaps:** At review time, full hosted Orchestrate wiring was split between selector-discriminator and hosted Start transaction tests; result-message coverage was source-level. Post-integration main-root link audits remained pending.
**Recommended use:** Add a full hosted Orchestrate transaction assertion and, if practical, a direct hosted-result message/guard-release regression; retain every production boundary unchanged unless another reviewer identifies a concrete defect.

## Handoff

**Source:** Pi Subagents run `ff223059-97ff-4052-a0d7-82a3c778b2c8`, `reviewer` child 2 (cross-repository VC/simplicity); output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/ff223059-97ff-4052-a0d7-82a3c778b2c8/output-2.log`, session `6ac5b0d3/run-2/session.jsonl`.
**Purpose:** Map current diffs and evidence to VC-001 through VC-010, challenge tests/checkpoint claims, and assess commit/closeout readiness.
**Outcome:** A P0 Stop race was reproduced: target Stop after hosted scheduling but before extension binding only aborted an idle native session, then binding completion still dispatched kickoff. The reviewer also corroborated reconnect-lossy discovery and missing duplicate-scheduling failure injection. It correctly marked commits/final summary/closeout pending. Its claim that hosted Orchestrate depth runtime evidence was absent was accurate for the reviewed snapshot but has since been resolved by the parent: the runtime report now records root depth 0/max 2 → implementer depth 1/max 2 → read-only nondelegating support depth 2/max 2 with real child identities and no depth above two.
**Evidence:** Stop race spans `lib/rpc-manager.ts:355-364,409-411`; discovery spans running SSE and sidebar lines above; duplicate scheduling spans `rpc-manager.ts:1295-1302`. Correctly accepted areas include VC-001, VC-002, active cleanup/resume, detached TUI, privacy, and scoped quality gates. The later hosted Orchestrate evidence is `.agents/reports/2026-07-29-pi-web-hosted-runtime-validation.md` under `Hosted Orchestrate nested-depth flow`.
**Uncertainty / gaps:** The browser evidence is a parent attestation rather than a committed reusable harness, though it records synthetic setup, outcomes, lifecycle counts, privacy scan, process cleanup, and native reopen. Active-target process termination remained less direct than settled-target cleanup. Link audits and guarded integrations remain pending.
**Recommended use:** Cancel a scheduled-but-undispatched hosted kickoff on target abort with exactly-once running release; add the deferred-binding Stop regression. Make discovery reconnect-safe and response-order-safe, test duplicate kickoff scheduling as target-owned/no-retry, add the requested dot_files wiring checks, rerun full gates and fresh focused review, then commit and close out each repository independently.

## Handoff

**Source:** Pi Subagents run `6e3c6ff2-7c91-49f1-b27c-b02dc8e2f884`, `reviewer` child 0 (post-fix Pi Web lifecycle/discovery); output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/6e3c6ff2-7c91-49f1-b27c-b02dc8e2f884/output-0.log`, parent run log `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/6e3c6ff2-7c91-49f1-b27c-b02dc8e2f884/subagent-log-6e3c6ff2-7c91-49f1-b27c-b02dc8e2f884.md`.
**Purpose:** Fresh adversarial review of the corrected capability, wrapper lifecycle, cancellation, discovery replay, control isolation, cleanup, and privacy behavior.
**Outcome:** The reviewer confirmed atomic publication, source detachment, exact owner reuse, active-safe cleanup, Stop-before-binding cancellation, bounded diagnostics, and stale-response suppression. It reproduced one new high-severity sibling defect: deleting or otherwise destroying a wrapper while extension binding remained unresolved did not change the scheduled kickoff state, so binding completion could dispatch against a disposed native owner. It also found that a failed `/api/sessions` load consumed its replay generation before success and that duplicate rejection emitted a false second `ownership_accepted` diagnostic.
**Evidence:** Decisive pre-correction ranges were `lib/rpc-manager.ts:356-403,704-724,1333-1337`, `app/api/sessions/[id]/route.ts:232-235`, and `components/SessionSidebar.tsx:369-437`. The reviewer reproduced one prompt call after deferred binding resolved post-destroy; focused tests were 30/30 and the then-current full suite was 133/133, providing counterevidence for already-covered behavior but not the reproduced deletion race.
**Uncertainty / gaps:** The reproduced path used a controlled wrapper probe rather than browser deletion against a real indefinitely delayed SDK binding. The runtime report remains a durable attestation rather than a reusable browser harness.
**Recommended use:** Cancel a scheduled kickoff synchronously during destruction, require wrapper liveness at dispatch, make discovery generations successful-load aware, move ownership acceptance behind the atomic kickoff claim, and add deterministic regressions before acceptance.

## Handoff

**Source:** Pi Subagents run `6e3c6ff2-7c91-49f1-b27c-b02dc8e2f884`, `reviewer` child 1 (post-fix cross-repository protocol/policy); output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/6e3c6ff2-7c91-49f1-b27c-b02dc8e2f884/output-1.log`, session recorded in the parent run log above.
**Purpose:** Independently compare the dot_files capability client, Start/Orchestrate transactions, detached fallback, result/guard contracts, global depth policy, and Pi Web structural protocol.
**Outcome:** No Start/Orchestrate, fallback, cancellation-transfer, prompt-topology, diagnostic, or depth-policy defect was found. Symbol/protocol/version/owner and the six normal request fields matched exactly, hosted Orchestrate and detached TUI behavior were fully covered, and target environments remained detached-only. One medium protocol-hardening defect remained in Pi Web: request validation accepted additional own fields such as `targetEnvironment`, silently widening the approved narrow capability even though the paired client never sends them. The reviewer also suggested aligning server and client non-empty runtime-ID checks.
**Evidence:** Findings cite dot_files `start-implementation/hosted.ts:51-133`, `transaction.ts:220-251`, `launcher-extension.ts:58-147`, Orchestrate `profile.ts:5-14`, and Pi Web `lib/hosted-implementation-session.ts:121-139`. A direct probe proved an extra `targetEnvironment` reached `startTarget`. Start/hosted 37/37, policy/depth 17/17, mission 5/5, Pi Web 133/133, typecheck, lint, and diff checks otherwise passed.
**Uncertainty / gaps:** Delivery commits, guarded integration, and main-root link audits were still pending. The browser report does not directly exercise the later reconnect/response-order or deferred-binding corrections, which have deterministic tests.
**Recommended use:** Reject every extra own request key before registration, test environment-shaped fields, require non-empty runtime IDs on both sides, retain the otherwise accepted production boundaries, and finish two-repository delivery.

## Handoff

**Source:** Pi Subagents run `6220a8b4-0ff5-429b-8159-748383260bee`, fresh follow-up `reviewer`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/6220a8b4-0ff5-429b-8159-748383260bee/output-0.log`, session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-28-pi-web-hosted-implementation-sessions--/2026-07-29T04-36-24-460Z_019fac28-d30c-72dc-85ba-760c63107aae/fb42d70b/run-0/session.jsonl`.
**Purpose:** Re-review the actual correction diff for destruction safety, duplicate lifecycle accuracy, failed-discovery retry, exact request shape, and sibling regressions.
**Outcome:** No blocker or fix-worth-doing-now remained. Destruction now synchronously cancels/releases before disposal and the deferred continuation checks liveness; duplicate rejection emits no second acceptance; generations become applied only after the latest successful list response and failed generations remain replayable; every extra own request key is rejected before registration; runtime IDs are non-empty; and source isolation, active cleanup, cross-repository compatibility, and privacy remain intact.
**Evidence:** The reviewer traced `lib/rpc-manager.ts:356-403,704-723`, `lib/hosted-implementation-session.ts:75-82,131-153`, and `components/SessionSidebar.tsx:20-39,392-466` plus their tests. Focused Pi Web tests passed 32/32; full tests passed 135/135; TypeScript, lint, and `git diff --check` passed; the dot_files hosted client passed 5/5. No staged files or `next build` were used.
**Uncertainty / gaps:** Actual-browser deletion during indefinitely delayed real SDK binding remains unexercised; deterministic deferred-binding destruction coverage proves the state transition and zero prompt dispatch. Full process restart survival remains excluded. Non-enumerable/symbol extras are rejected structurally through `Reflect.ownKeys()` but lack separate literal tests.
**Recommended use:** Accept the correction round, preserve the bounded runtime gaps, run the final repository gates, then create scoped commits and perform guarded independent closeout.

## Implementation Summary

**Plan section:** Design / Implementation Strategy §§1–7; post-review corrections for VC-002, VC-005, VC-006, VC-008, VC-009, and the completed dot_files portions of VC-004 and VC-007.
**Work and outcome:** Closed every fresh-review finding without expanding architecture. Wrapper destruction now cancels and releases a scheduled-but-undispatched kickoff before native disposal, and dispatch requires both liveness and scheduled state. The atomic wrapper claim emits ownership acceptance only for a launch it accepts. Browser discovery tracks pending and successfully applied generations separately, so failure is replayable and older responses or generations cannot overwrite newer discovery. Pi Web accepts exactly the six approved own request fields and requires non-empty runtime identities. Added focused deletion/destruction, duplicate-lifecycle, failed-load retry, widened-request, and runtime-identity regressions. The dot_files hosted client, detached fallback, full Orchestrate transaction, ownership messages, global depth-two ceiling, and prompt-policy topology remain unchanged and accepted.
**Validation / evidence:** Parent-focused post-correction tests passed 32/32 with TypeScript and `git diff --check`; the fresh follow-up reviewer independently passed the full Pi Web suite 135/135, TypeScript, lint, diff check, and the paired hosted client. Parent dot_files validation passed Start/hosted 37/37, policy/agent/orchestrator/discovery 17/17, mission trace 5/5, Plan Context Compaction 30/30, strict combined TypeScript, and `git diff --check`. The first parent full-suite attempt was invalidated by a concurrent `npm ci` temporarily removing shared main-root dependencies; after that install completed, the same gate passed and no tracked dependency file changed.
**Departures from approved obligations:** Browser deletion during an indefinitely delayed real SDK binding is not separately exercised; the deterministic wrapper regression reproduces the causal race and proves zero post-destroy dispatch. The normal idle source-signal and bare-session shutdown limitations recorded earlier remain. Main-root link audits, implementation commits, final summary, and guarded closeout are delivery steps still pending, not waived.
**Implementation commit:** Pending.
