# S1 Global Running and Discovery WebSocket Checkpoints

Plan: `.agents/plans/2026-07-31-s1-global-running-websocket.md`

## Handoff

**Source:** Pi subagents run `642ae71c-2ec9-4771-ab73-86e85fdf5ae6`, `context-builder` child 0 (S1 server/gateway reconnaissance); recoverable artifact `.pi-subagents/artifacts/outputs/642ae71c-2ec9-4771-ab73-86e85fdf5ae6/.agents/reports/s1-server-context.md`. Recorder: orchestration root. Kind: read-only milestone context. Governing plan: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S1; Mode: investigation.
**Purpose:** Reconcile the accepted M0 server/gateway/runtime foundation with the master's S1 admission, production-channel, global-status, security, lifecycle, package, and validation obligations without editing project source or entering S2+ scope.
**Outcome:** Confirmed that M0 is intact and dormant: the V1 gateway owns registered handlers and one-use tickets, the custom server alone owns `/_pi/websocket`, and no production channel exists. The reserved upgrade currently consumes a ticket and dispatches without direct-peer admission, so gateway-instance-wide 64-per-address/256-total accounting and exact release are mandatory before registration. The current global SSE subscribes before synchronous snapshots and replays both running state and current discovery generation; hosted publication depends on that replay. The current running getter enumerates wrappers and calls `isRunning()`, while de-duplication is module-local. The root adopts the stronger literal ORCH-VC-002 boundary: wrapper-owned transitions must maintain a process-global running projection, and browser attach/reconnect must not create, enumerate, get, or invoke wrappers/native sessions. An identity-aware lazy `running` registration through the ticket bootstrap is the narrow supported App Router seam; no evidence requires a second port or CommonJS import of TypeScript application runtime.
**Evidence:** Decisive source verified by the root includes `bin/pi-web-transport-gateway.js` ticket/registration state and global identity; `bin/pi-web-server.js` upgrade order and owned close; `app/api/transport/ticket/route.ts` exact one-field bootstrap; `lib/rpc-manager.ts` wrapper transitions, broadcaster globals, current getter, and hosted discovery; `app/api/agent/running/events/route.ts` subscribe/snapshot/replay; existing gateway/ticket/server/runtime/real-Next tests; M0 plans/checkpoints and `.agents/memory/custom-server-lifecycle.md`. The child independently ran the focused gateway/ticket/server/sidebar suite with 27 passing tests. Counterevidence is explicit: `WebSocketServer.clients.size` is diagnostic only, the 16 KiB inbound limit is not admission/backpressure, module-local `lastRunningSnapshot` is not HMR state, and copied production artifacts cannot prove fresh channel inclusion.
**Uncertainty / gaps:** Exact internal API names, reconnect constants, and admission rejection HTTP status are implementation judgments so long as behavior and bounded diagnostics satisfy the immutable milestone. Cross-engine 1/5/10 browser inventory and responsiveness were not available from the read-only child and remain root acceptance gates. The subagent runtime marked inferred acceptance rejected because it did not parse the saved file-only report's structured block; execution completed and the root read and verified the artifact directly rather than treating that metadata as acceptance.
**Recommended use:** Implement one gateway-owned idempotent admission token used after ticket consumption and before upgrade dispatch; publish deterministic running state from wrapper-owned transitions; add one HMR/restart-safe static handler with exact unsubscribe; preserve M0 security/lifecycle and per-session SSE; require exact boundary/re-admission, no-wrapper-touch, real-Next, package, privacy, and browser evidence before S1 acceptance.

## Handoff

**Source:** Pi subagents run `642ae71c-2ec9-4771-ab73-86e85fdf5ae6`, `context-builder` child 1 (S1 browser/global-status reconnaissance); recoverable artifact `.pi-subagents/artifacts/outputs/642ae71c-2ec9-4771-ab73-86e85fdf5ae6/.agents/reports/s1-browser-context.md`. Recorder: orchestration root. Kind: read-only milestone context. Governing plan: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S1; Mode: investigation.
**Purpose:** Trace the exact page/component lifetime, global EventSource state machine, sidebar discovery/running authority, unread and refresh edges, client test seams, and removal boundary needed for one page-global WebSocket without changing per-session or file-watch behavior.
**Outcome:** The global state machine currently combines native list loads, EventSource delivery, and running-transition policy inside `SessionSidebar`; `AppShell`/`app/page.tsx` is the stable page lifetime above the always-mounted sidebar and keyed `ChatWindow`. A thin provider over a pure injected controller can own one ticket fetch/socket/reconnect timer per page while the sidebar retains list/unread policy. Discovery replay must be delivered as an event, not a latest numeric React scalar: a failed generation intentionally remains retryable and an equal reconnect replay must be observed. Server restart creates a new generation namespace keyed by `serverInstanceId`; replacing the tracker isolates pending old completion. Transport loss must retain the last running set, and stale fetch/socket/timer callbacks require epoch plus exact-resource checks to prevent duplicate sockets and false idle edges.
**Evidence:** The root verified `app/page.tsx` and `components/AppShell.tsx` owner/remount seams; `components/SessionSidebar.tsx` list request IDs, fallback authority, generation tracker, SSE handling, unread persistence, background completion, and selected-session exception; `hooks/useAgentSession.ts` and the per-session route as an explicit preservation boundary; M0 ticket/gateway/server contracts; and focused component/runtime tests. The child ran 50 focused current-state tests with zero failures/skips and found exactly the expected global, per-session, four file-watch, and OAuth EventSource caller classes. Counterevidence is retained: no browser transport owner or component harness exists, `useAgentSession` has unrelated per-session connection/reconciliation semantics that must not be refactored in S1, and React state equality alone cannot preserve discovery delivery.
**Uncertainty / gaps:** No Playwright/jsdom dependency is installed; deterministic controller tests must carry most race coverage while Firefox/Chromium inventory remains a real acceptance layer. Registration timing and bounded reconnect constants were not predetermined by M0; the milestone selects lazy identity-aware registration and requires injectable bounded backoff. Fresh production bundle inclusion remains blocked by the repository's `next build` prohibition. As with child 0, file-only output caused inferred runtime acceptance metadata to report rejected despite a complete artifact; the root independently inspected the evidence.
**Recommended use:** Mount one provider above `AppShell`; keep exactly one fetch/socket/timer with epoch invalidation; validate minimal versioned frames containing server identity; expose running authority plus event-preserving discovery subscriptions; retain sidebar request/generation/unread logic; remove only the global route/caller after parity tests; require static EventSource inventory, hosted discovery reconnect, cross-engine 1/5/10 page counts, and HTTP schedulability before acceptance.

## Handoff

**Source:** Pi subagents run `7249a8c7-eba8-4cd0-96aa-222e443e8f2d`, `reviewer` child 0 (draft S1 containment and validation review). Recorder: orchestration root. Kind: independent read-only plan review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; Mode: review.
**Purpose:** Adversarially check the draft milestone and checkpoint for contained authority, implementation ambiguity, validation gaps, privacy violations, later-milestone leakage, and executable acceptance before root approval.
**Outcome:** The review passed the selected S1 objective, admission order, literal no-wrapper-touch projection, registration/reconnect/server-instance/equal-generation semantics, EventSource removal boundary, and exclusions of S2/S6/S7 implementation. It blocked approval on five concrete draft defects: absolute child-session locations in committed evidence; existing raw identifier diagnostics exercised by required tests but omitted from source scope; a placeholder focused-test command; a total-cap test that did not explicitly distribute connections across peer keys; and wording that risked advancing S7's subjective responsiveness gate into S1. The root accepted all findings and corrected the draft without changing product direction: checkpoint Sources now use recoverable run/child plus repository-relative artifacts; narrow identifier removal in runtime/hosted diagnostics is explicit; focused test filenames are exact; the 256/257 gate uses at least four direct peer keys with none above 64; and S1 gates only cross-engine inventory plus bounded request completion while reserving user responsiveness acceptance for S7.
**Evidence:** The reviewer inspected the complete master, draft, checkpoint, current gateway/server/runtime/sidebar/hosted diagnostics, and EventSource inventory; ran 50 focused baseline tests with zero failures/skips; and verified draft whitespace. Root source checks confirmed raw identifiers in `lib/rpc-manager.ts` extension-binding logs and `lib/hosted-implementation-session.ts` lifecycle diagnostics, current admission absence, and no implementation diff. The review's recoverable output is run `7249a8c7-eba8-4cd0-96aa-222e443e8f2d`; no private runtime path or raw session identity is committed here.
**Uncertainty / gaps:** Browser tooling and multi-loopback-source support remain empirical implementation/acceptance facts. If the required direct multi-peer integration or cross-engine inventory proves unavailable, S1 remains blocked; the corrected plan supplies no waiver. This review predates implementation and is not source acceptance.
**Recommended use:** Recheck the corrected draft sections and executable commands, approve and commit only after no blocker remains, then issue the immutable plan to one `milestone-implementer`. After source changes, require a new fresh no-edit review rather than reusing this plan review.
