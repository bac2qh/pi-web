# S1 Global Running and Discovery WebSocket Checkpoints

Plan: `.agents/plans/2026-07-31-s1-global-running-websocket.md`

## Handoff

**Source:** Pi subagents run `642ae71c-2ec9-4771-ab73-86e85fdf5ae6`, `context-builder` child 0 (S1 server/gateway reconnaissance); recoverable artifact `.pi-subagents/artifacts/outputs/642ae71c-2ec9-4771-ab73-86e85fdf5ae6/.agents/reports/s1-server-context.md`. Recorder: orchestration root. Kind: read-only milestone context. Governing plan: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S1; Mode: investigation.
**Purpose:** Reconcile the accepted M0 server/gateway/runtime foundation with the master's S1 admission, production-channel, global-status, security, lifecycle, package, and validation obligations without editing project source or entering S2+ scope.
**Outcome:** Confirmed that M0 is intact and dormant: the V1 gateway owns registered handlers and one-use tickets, the custom server alone owns `/_pi/websocket`, and no production channel exists. The reserved upgrade currently consumes a ticket and dispatches without direct-peer admission, so gateway-instance-wide 64-per-address/256-total accounting and exact release are mandatory before registration. The current global SSE subscribes before synchronous snapshots and replays both running state and current discovery generation; hosted publication depends on that replay. The current running getter enumerates wrappers and calls `isRunning()`, while de-duplication is module-local. The root adopts the stronger literal ORCH-VC-002 boundary: wrapper-owned transitions must maintain a process-global running projection, and browser attach/reconnect must not create, enumerate, get, or invoke wrappers/native sessions. An identity-aware lazy `running` registration through the ticket bootstrap is the narrow supported App Router seam; no evidence requires a second port or CommonJS import of TypeScript application runtime.
**Evidence:** Decisive source verified by the root includes `bin/pi-web-transport-gateway.js` ticket/registration state and global identity; `bin/pi-web-server.js` upgrade order and owned close; `app/api/transport/ticket/route.ts` exact one-field bootstrap; `lib/rpc-manager.ts` wrapper transitions, broadcaster globals, current getter, and hosted discovery; `app/api/agent/running/events/route.ts` subscribe/snapshot/replay; existing gateway/ticket/server/runtime/real-Next tests; M0 plans/checkpoints and `.agents/memory/custom-server-lifecycle.md`. The child independently ran the focused gateway/ticket/server/sidebar suite with 27 passing tests. Counterevidence is explicit: `WebSocketServer.clients.size` is diagnostic only, the 16 KiB inbound limit is not admission/backpressure, module-local `lastRunningSnapshot` is not HMR state, and copied production artifacts cannot prove fresh channel inclusion.
**Uncertainty / gaps:** Exact internal API names, reconnect constants, and admission rejection HTTP status are implementation judgments so long as behavior and bounded diagnostics satisfy the immutable milestone. Cross-engine 1/5/10 browser inventory and responsiveness were not available from the read-only child and remain root acceptance gates. The subagent runtime marked inferred acceptance rejected because it did not parse the saved file-only report's structured block; execution completed and the root read and verified the artifact directly rather than treating that metadata as acceptance.
**Recommended use:** Implement one gateway-owned idempotent admission token used after ticket consumption and before upgrade dispatch; publish deterministic running state from wrapper-owned transitions; add one HMR/restart-safe static handler with exact unsubscribe; preserve M0 security/lifecycle and per-session SSE; require exact boundary/re-admission, no-wrapper-touch, real-Next, package, privacy, and browser evidence before S1 acceptance.

## Handoff

**Source:** Pi subagents run `642ae71c-2ec9-4771-ab73-86e85fdf5ae6`, `context-builder` child 1 (S1 browser/global-status reconnaissance); recoverable artifact `.pi-subagents/artifacts/outputs/642ae71c-2ec9-4771-ab73-86e85fdf5ae6/.agents/reports/s1-browser-context.md`. Recorder: orchestration root. Kind: read-only milestone context. Governing plan: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S1; Mode: investigation.
**Purpose:** Trace the exact page/component lifetime, global EventSource state machine, sidebar discovery/running authority, unread and refresh edges, client test seams, and removal boundary needed for one page-global WebSocket without changing per-session or file-watch behavior.
**Outcome:** The global state machine currently combines native list loads, EventSource delivery, and running-transition policy inside `SessionSidebar`; `AppShell`/`app/page.tsx` is the stable page lifetime above the always-mounted sidebar and keyed `ChatWindow`. A thin provider over a pure injected controller can own one ticket fetch/socket/reconnect timer per page while the sidebar retains list/unread policy. Discovery replay must be delivered as an event, not a latest numeric React scalar: a failed generation intentionally remains retryable and an equal reconnect replay must be observed. Server restart creates a new generation namespace keyed by `serverInstanceId`; replacing the tracker isolates pending old completion. Transport loss must retain the last running set, and stale fetch/socket/timer callbacks require epoch plus exact-resource checks to prevent duplicate sockets and false idle edges.
**Evidence:** The root verified `app/page.tsx` and `components/AppShell.tsx` owner/remount seams; `components/SessionSidebar.tsx` list request IDs, fallback authority, generation tracker, SSE handling, unread persistence, background completion, and selected-session exception; `hooks/useAgentSession.ts` and the per-session route as an explicit preservation boundary; M0 ticket/gateway/server contracts; and focused component/runtime tests. The child ran 50 focused current-state tests with zero failures/skips and found exactly the expected global, per-session, four file-watch, and OAuth EventSource caller classes. Counterevidence is retained: no browser transport owner or component harness exists, `useAgentSession` has unrelated per-session connection/reconciliation semantics that must not be refactored in S1, and React state equality alone cannot preserve discovery delivery.
**Uncertainty / gaps:** No Playwright/jsdom dependency is installed; deterministic controller tests must carry most race coverage while Firefox/Chromium inventory remains a real acceptance layer. Registration timing and bounded reconnect constants were not predetermined by M0; the milestone selects lazy identity-aware registration and requires injectable bounded backoff. Fresh production bundle inclusion remains blocked by the repository's `next build` prohibition. As with child 0, file-only output caused inferred runtime acceptance metadata to report rejected despite a complete artifact; the root independently inspected the evidence.
**Recommended use:** Mount one provider above `AppShell`; keep exactly one fetch/socket/timer with epoch invalidation; validate minimal versioned frames containing server identity; expose running authority plus event-preserving discovery subscriptions; retain sidebar request/generation/unread logic; remove only the global route/caller after parity tests; require static EventSource inventory, hosted discovery reconnect, cross-engine 1/5/10 page counts, and HTTP schedulability before acceptance.

## Handoff

**Source:** Pi subagents run `7249a8c7-eba8-4cd0-96aa-222e443e8f2d`, `reviewer` child 0 (draft S1 containment and validation review). Recorder: orchestration root. Kind: independent read-only plan review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; Mode: review.
**Purpose:** Adversarially check the draft milestone and checkpoint for contained authority, implementation ambiguity, validation gaps, privacy violations, later-milestone leakage, and executable acceptance before root approval.
**Outcome:** The review passed the selected S1 objective, admission order, literal no-wrapper-touch projection, registration/reconnect/server-instance/equal-generation semantics, EventSource removal boundary, and exclusions of S2/S6/S7 implementation. It blocked approval on five concrete draft defects: absolute child-session locations in committed evidence; existing raw identifier diagnostics exercised by required tests but omitted from source scope; a placeholder focused-test command; a total-cap test that did not explicitly distribute connections across peer keys; and wording that risked advancing S7's subjective responsiveness gate into S1. The root accepted all findings and corrected the draft without changing product direction: checkpoint Sources now use recoverable run/child plus repository-relative artifacts; narrow identifier removal in runtime/hosted diagnostics is explicit; focused test filenames are exact; the 256/257 gate uses at least four direct peer keys with none above 64; and S1 gates only cross-engine inventory plus bounded request completion while reserving user responsiveness acceptance for S7.
**Evidence:** The reviewer inspected the complete master, draft, checkpoint, current gateway/server/runtime/sidebar/hosted diagnostics, and EventSource inventory; ran 50 focused baseline tests with zero failures/skips; and verified draft whitespace. Root source checks confirmed raw identifiers in `lib/rpc-manager.ts` extension-binding logs and `lib/hosted-implementation-session.ts` lifecycle diagnostics, current admission absence, and no implementation diff. The review's recoverable output is run `7249a8c7-eba8-4cd0-96aa-222e443e8f2d`; no private runtime path or raw session identity is committed here.
**Uncertainty / gaps:** Browser tooling and multi-loopback-source support remain empirical implementation/acceptance facts. If the required direct multi-peer integration or cross-engine inventory proves unavailable, S1 remains blocked; the corrected plan supplies no waiver. This review predates implementation and is not source acceptance.
**Recommended use:** Recheck the corrected draft sections and executable commands, approve and commit only after no blocker remains, then issue the immutable plan to one `milestone-implementer`. After source changes, require a new fresh no-edit review rather than reusing this plan review.

## Handoff

**Source:** Pi subagents run `a2e39a89-deac-4a39-aa95-ca465a8dfbf8`, `milestone-implementer` child 0; read-only support scout run `bdcd31b2`. Recorder: orchestration root. Kind: milestone implementation handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; Mode: implementation.
**Purpose:** Implement all immutable S1 obligations as the sole source writer, run the required automated gates, preserve later persistent streams and M0 lifecycle/security, and return a VC-mapped special handoff without committing or editing orchestration state.
**Outcome:** The writer added gateway admission, central upgrade reservation/release, an HMR-global wrapper-owned running projection, the static global-status protocol/channel, lazy ticket registration, an injected browser controller and page provider, sidebar migration, identifier-free runtime/hosted diagnostics, focused tests, and deletion of only the global running SSE route. It reported 224 full tests, 81 focused tests, four real-Next tests, typecheck, lint, package dry run, and diff check passing without skips. Root inspection confirmed the claimed changed-path boundary and protocol/controller/projection structure. The result was not accepted from the handoff: fresh review subsequently found callback-less handshake admission leakage, closed-before-handler subscriber leakage, a sidebar authority race, and a total-cap fixture that used configured local interface addresses rather than the milestone's required loopback-only mechanism.
**Evidence:** Recoverable implementation output is run `a2e39a89-deac-4a39-aa95-ca465a8dfbf8`; support evidence is scout run `bdcd31b2` and repository-local artifact `.pi-subagents/artifacts/outputs/bdcd31b2/context.md`. Root directly read the new protocol/channel/client/provider and decisive gateway/server/runtime/sidebar diffs, verified the global EventSource route is deleted while the per-session, four file-watch, and OAuth callers remain, confirmed no staged files or milestone/checkpoint mutation, and inspected the automated test additions. The child's direct-source test demonstrated current-host 64/65 and distributed 256/257 behavior with capacity reuse, but that evidence does not disposition the later review findings.
**Uncertainty / gaps:** Required Chromium/Firefox inventory, hosted reconnect/running-edge flows, and five-page/seven-run plus ten-page/ten-run bounded request evidence remain absent. Fresh production bundle inclusion remains release-owned under the build prohibition. The writer's raw child response contained a runtime session identity; this committed synthesis intentionally identifies only the recoverable run and child.
**Recommended use:** Keep the implementation uncommitted; classify the fresh findings under the same immutable S1; issue one focused fix writer; rerun root verification and a new independent review; then collect the browser layer before acceptance.

## Handoff

**Source:** Pi subagents run `dd74c45e-5eab-40c2-a228-cbe543fc1010`, parallel `reviewer` children 0 (server/security/lifecycle) and 1 (browser/sidebar/migration). Recorder: orchestration root. Kind: independent implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; Mode: review.
**Purpose:** Independently inspect the actual uncommitted S1 diff and adversarially validate server admission/release, projection/registration/subscriber lifecycle, browser ownership/reconnect, sidebar authority, migration boundaries, privacy, M0 lifecycle, and evidence quality.
**Outcome:** Both reviewers agreed the global SSE removal boundary, no-wrapper-touch projection, controller epoch/resource guards, protocol/registration direction, identifier-free diagnostics, and M0 one-port lifecycle were substantially correct. They reproduced three bounded source defects: a real valid-ticket malformed WebSocket handshake can close the raw socket without `handleUpgrade` throwing or invoking its callback, leaking one reservation; a socket closed before deferred channel-handler dispatch can install and retain both subscriptions because the non-open send path does not clean up; and sidebar WebSocket authority is written only in a passive effect, allowing a late HTTP fallback to transiently drive stale running/unread/refresh behavior. They also found the 256/257 fixture depends on six configured local interfaces rather than the immutable plan's recorded loopback-source mechanism, and that the ticket-before-admission test does not explicitly trace order/reuse. Missing browser evidence remains an acceptance blocker rather than a source fix.
**Evidence:** Reviewer child 0 independently passed typecheck, lint, 81 focused tests, an isolated 224-test full suite, four real-Next tests, package dry run, and diff check, then reproduced retained admission after callback-less raw-handshake close and retained subscribers on an already-closed channel socket. Child 1 independently corroborated the subscriber leak and identified the passive-effect authority window from `SessionSidebar` data flow. Both verified no staged files and the expected EventSource inventory. An earlier full-suite invocation overlapped another real-Next run and failed from test contention; isolated reruns passed and the concurrency failure is not acceptance evidence.
**Uncertainty / gaps:** A nonprivileged, portable loopback-only source-address mechanism providing at least five distinct direct peer keys is not yet proven on this host. Browser tooling and active-run workload evidence remain empirical. The sidebar mounted-race test seam must avoid adding an out-of-scope dependency merely to simulate React.
**Recommended use:** In one sequential S1 fix attempt, attach reservation release to raw-socket close, clean up channel subscriptions whenever initial send sees a non-open socket, make sidebar stream authority atomic with fallback arbitration, strengthen order/reuse and race tests, and investigate a compliant nonprivileged loopback fixture. Stop and escalate rather than mutate host networking, add a waiver, or substitute configured external interface addresses if no compliant fixture exists.

## Handoff

**Source:** Pi subagents run `e342cb3f-bb68-4374-87c6-50049fa27e6c`, `milestone-implementer` child 0; read-only loopback investigation scout run `4ea8e2b8`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; Mode: fix.
**Purpose:** Correct only the accepted S1 admission-release, subscriber-cleanup, sidebar-authority, and validation-fixture findings without changing the immutable milestone, later streams, M0 lifecycle, host networking, or orchestration state.
**Outcome:** The fix attaches idempotent reservation release to the accepted raw socket before `handleUpgrade`, so callback-less malformed handshake close converges with accepted WebSocket, handler-failure, catch, and gateway-close paths. Global-channel non-open send paths now clean both subscriptions exactly once and avoid reterminating an already closed socket. The provider exposes a synchronous controller snapshot, and the sidebar checks that authority at the HTTP commit point while deriving rendered and transition state directly from provider data once authoritative. Tests now explicitly trace ticket consume/reserve/upgrade/handler order, reject reuse after cap consumption, reproduce malformed-handshake release/re-admission, cover close-before-deferred dispatch and all non-open states, and execute the late-HTTP false-completion race. The misleading configured-interface distributed fixture was removed. Nonprivileged investigation found this host supplies only two usable distinct direct-loopback peer keys, below the four needed to hold 256 real sockets without exceeding 64 per key.
**Evidence:** The writer reported 85 focused tests, 228 full tests, four real-Next tests, typecheck, lint, package dry run, and diff check passing without skips after one disclosed intermediate timing-test failure. Root independently read every fix and test, then reran the same 85 focused tests, 228 full tests, four real-Next tests, typecheck, lint, package inclusion assertion, and diff/staging checks successfully. Pure gateway tests still prove 256/257 across four synthetic peer keys; real integration now honestly proves loopback 64/65, malformed-handshake release, and re-admission only. Recoverable fix output is run `e342cb3f-bb68-4374-87c6-50049fa27e6c`; scout evidence is run `4ea8e2b8`.
**Uncertainty / gaps:** S1-VC-002/S1-VC-011 real distributed 256/257 loopback evidence is infeasible on this host without a different environment or privileged/external networking mutation, neither authorized. Chromium/Firefox page inventory, hosted reconnect/running-edge flows, and five-page/seven-run plus ten-page/ten-run bounded request evidence remain unexecuted because the workflow stops at the first material divergence. No plan or validation waiver is implied.
**Recommended use:** Preserve the reviewed source diff and checkpoint, perform one fresh no-edit verification, then stop for a user decision: supply/authorize a compliant loopback environment or approve a separate follow-up plan that lawfully changes the over-specific milestone evidence requirement. Do not commit an S1 implementation boundary or start S2.

## Handoff

**Source:** Pi subagents run `fc46d4e9-7a11-46f8-8621-d549e7698364`, `reviewer` child 0. Recorder: orchestration root. Kind: fresh independent fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; Mode: review.
**Purpose:** Reinspect the actual post-fix diff, search for sibling release/cleanup/authority defects, verify M0/privacy/EventSource boundaries, and independently test the claimed environment limitation without host mutation.
**Outcome:** The reviewer passed all three source fixes with no blocker, major, minor, or sibling defect worth doing now. It confirmed raw-socket and WebSocket release convergence, exact ticket ordering/non-reuse, non-open and close-before-dispatch subscription cleanup, synchronous sidebar authority and transition derivation, generation behavior, honest removal of the non-loopback claim, M0 lifecycle, privacy, and expected EventSource inventory. It independently confirmed that fewer than four nonprivileged direct-loopback peer keys are usable, so the immutable real 256/257 gate remains unsatisfied. It also retained the separate browser acceptance blocker.
**Evidence:** The reviewer independently passed 85 focused tests, 228 full tests, four real-Next tests, typecheck, lint, package dry run, diff check, and no-staged-file inspection. Its sanitized loopback inventory/bind probes confirmed the environment limit. Source ranges and test seams are recoverable in run `fc46d4e9-7a11-46f8-8621-d549e7698364`; no private runtime path, peer value, ticket, or raw identifier is copied here.
**Uncertainty / gaps:** Fresh production inclusion remains release-workflow evidence under the `next build` prohibition. The browser layer could likely be exercised with installed Firefox and a Chromium-based browser, but continuing it before dispositioning the immutable loopback gate would violate the master's material-divergence stop rule.
**Recommended use:** Treat bounded source defects as resolved but S1 as unaccepted. Keep S1/S2 progression stopped until explicit direction provides an authorized route to the real distributed loopback evidence or a separately approved follow-up plan changes authority.

## Implementation Summary

**Plan section:** S1 `Design / Implementation Strategy` §§1–7; S1-VC-001 through S1-VC-011; master S1 outcome and ORCH-VC-001/002/008/009/010/012.
**Work and outcome:** Implemented and independently reviewed the intended S1 source boundary: gateway admission and exact release, wrapper-owned running projection, static global-status protocol/channel, browser controller/provider, sidebar migration and authority arbitration, privacy correction, tests, and global SSE removal with later streams preserved. The initial review's three source blockers were corrected in one sequential fix attempt and passed fresh follow-up review. The milestone is not accepted or complete: its immutable real distributed 256/257 direct-loopback evidence cannot run on this host because fewer than four nonprivileged peer keys are available, and the remaining browser matrix has not been started after that material divergence.
**Validation / evidence:** Root and fresh review passed the exact 85-test focused suite, 228-test full suite, four-test real-Next suite, TypeScript, lint, package inclusion, and diff/staging checks with no skips. Real loopback 64/65, malformed-handshake release/re-admission, callback-less close, subscriber cleanup, controller reconnect races, sidebar fallback race, registration/replay, privacy, production reuse, and EventSource boundaries are covered. Pure gateway tests prove 256/257 across four synthetic keys. The required real distributed 256/257 loopback test and Chromium/Firefox 1/5/10 plus workload evidence remain missing.
**Departures from approved obligations:** S1-VC-002/S1-VC-011 and master ORCH-VC-002/008 remain blocked by the observed host capability; satisfying them here would require unauthorized privileged/external networking mutation or weakening the immutable milestone. S1-VC-005/006/010/011 browser evidence is incomplete because work stopped at material divergence. No obligation is waived, reinterpreted, or transferred to S2; no later milestone may start. The unresolved human decision is whether to provide/authorize a compliant loopback environment/external side effect or approve a separate follow-up plan that changes the over-specific milestone validation authority.
**Implementation commit:** pending; no S1 implementation boundary commit is permitted while the milestone remains blocked.

## Handoff

**Source:** User-approved follow-up `.agents/plans/2026-08-01-s1-direct-peer-evidence-correction.md` at local-main commit `8f7ded851e1241ee81631d49f443091f1e02bb49`, delivered to this existing orchestration root with the explicit instruction `resume under 8f7ded8`. Recorder: orchestration root. Kind: material-divergence resolution and validation-authority amendment. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; active milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; Mode: resume.
**Purpose:** Resolve the recorded host-capability divergence without mutating the immutable master or S1 plan, starting another implementation session, changing production limits, or authorizing host-network side effects.
**Outcome:** The real integration that opens 256 WebSockets across at least four direct client addresses and rejects connection 257 is explicitly user-authorized as `waived`, not passed or silently skipped. Deterministic pure 256/257 admission accounting; real single-address 64/65, release/re-admission, malformed-handshake, ordering, cleanup, forwarded-header, lifecycle, package, privacy, browser, hosted-discovery, HTTP-schedulability, and fresh-review gates remain mandatory. Production limits remain 64 active Pi Web WebSockets per direct peer and 256 total. S1 resumes from the preserved reviewed source diff; S2 remains prohibited until every retained S1 gate passes and the coherent boundary is accepted and committed.
**Evidence:** The root read the complete exact follow-up, verified `Status: approved`, verified the current plan content at commit `8f7ded851e1241ee81631d49f443091f1e02bb49`, confirmed its narrow VC-001 through VC-004 contract, and confirmed the current file contains no raw session identifier or private absolute path. The user supplied the required explicit resume instruction. No privileged command, loopback alias, interface change, route change, or other host-network mutation was attempted.
**Uncertainty / gaps:** The accepted residual evidence gap is that pure 256/257 accounting plus real 64/65 integration is weaker than 256 live sockets through the complete Node upgrade path. The amendment does not waive Chromium/Firefox 1/5/10 inventory, hosted discovery/reconnect, running-edge behavior, five-page/seven-run or ten-page/ten-run bounded HTTP completion, fresh review, or any later full-master requirement.
**Recommended use:** Rerun all retained automated S1 gates, collect the required sanitized browser and workload evidence, obtain fresh independent no-edit/no-delegation review, reconcile every S1 and follow-up obligation, and commit the S1 boundary only if all retained gates pass.

## Handoff

**Source:** Pi subagents run `1a98b657-941a-4f5e-a8b0-5148fffe149a`, `scout` child 0. Recorder: orchestration root. Kind: fresh read-only browser-validation investigation. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; active milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; amendment: local-main commit `8f7ded851e1241ee81631d49f443091f1e02bb49`; Mode: validation investigation.
**Purpose:** Identify a no-project-edit, no-new-dependency path for the retained Chromium/Firefox 1/5/10 inventory, reconnect, hosted discovery, running edge, and five-page/seven-run plus ten-page/ten-run HTTP completion gates.
**Outcome:** The scout confirmed that project-local browser automation dependencies are absent, a globally installed Playwright Chromium is usable, system Firefox is installed, and the existing real-Next test supplies safe owned-child/deadline/cleanup patterns. It initially found no compatible Playwright Firefox driver. The root followed official Firefox WebDriver BiDi documentation and verified that installed Firefox exposes its built-in loopback-only BiDi endpoint directly, so no browser/driver installation or manual gate was required. The resulting temporary harness used real top-level browser pages, one fresh browser context/profile per case, pathname-only WebSocket/EventSource classification, and an isolated temporary Pi home/session/cwd with local command fixtures.
**Evidence:** Durable aggregate results are in `.agents/reports/2026-08-01-s1-global-websocket-validation.md`. Both engines passed 1/5/10 inventory with exactly one active Pi Web global socket per page and zero global EventSource/request; five-page restart/reconnect and discovery replay; two real hosted capability launch kinds; and 5/7 plus 10/10 local active-run workloads with every ordinary HTTP request completing in the bounded under-one-second diagnostic class. The fixture made zero provider requests, attempted zero privileged operations, made zero host-network mutations, stopped every owned child/browser, and removed temporary state. The scout output is recoverable by run identity; no private path or raw session identity is copied here.
**Uncertainty / gaps:** Synthetic command-only sessions exercised wrapper running claims, hosted registration, session-list reload, and persisted unread transition state but did not render ordinary session rows, so no visual unread dot is claimed. Component tests cover indicator policy; subjective visual/user acceptance remains S7. The real distributed four-address 256/257 run remains explicitly waived rather than passed.
**Recommended use:** Have a fresh independent reviewer inspect the actual diff, exact amendment, automated partition and pinned-fixture rationale, temporary harness methodology, aggregate browser report, privacy boundary, and every retained S1 VC. Treat any concrete in-scope defect through the existing sequential fix loop; otherwise complete the root summary and S1 boundary commit.

## Handoff

**Source:** Pi subagents run `0584087c-8631-4c38-a81c-f6ca55cd8e0e`, `reviewer` child 0. Recorder: orchestration root. Kind: final fresh independent S1 implementation/evidence review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; active milestone: `.agents/plans/2026-07-31-s1-global-running-websocket.md`; amendment: local-main commit `8f7ded851e1241ee81631d49f443091f1e02bb49`; Mode: review.
**Purpose:** Adversarially re-evaluate the actual final source/test diff, narrow waiver, all retained automated and browser evidence, privacy/source boundaries, and S1-VC-001 through S1-VC-011 before any implementation commit.
**Outcome:** The reviewer recommends accepting S1 with no blocker or major defect. It passed admission order/release/re-admission, pure total-cap accounting under the narrow waiver, literal no-wrapper-touch projection, channel cleanup/identity, browser controller ownership/reconnect, sidebar authority/generation/completion behavior, global SSE removal, privacy, M0 lifecycle/package preservation, both browser engines, hosted launch kinds, and 5/7 plus 10/10 schedulability. It judged both disclosed qualifications non-blocking under the exact plans: the release-owned production-manifest preflight is fully covered by the 224-test non-real-Next partition plus four-test pinned-fixture lifecycle run, and persisted unread plus component policy is sufficient for S1 while subjective rendered/visual acceptance remains S7. One non-functional minor remains: two `rpc-manager` comments still say SSE; chasing that optional comment cleanup would require an unnecessary writer/review loop and is deferred to later maintained-documentation/source-touch work.
**Evidence:** The reviewer independently reran TypeScript, lint, 85 focused tests, 224 non-real-Next tests, package dry run with both changed runtime files present, diff/cached-diff checks, and staged-state inspection successfully with no skips in the executed partitions. It inspected the retained four-test pinned-fixture real-Next result, raw aggregate browser evidence and temporary harness, actual source/tests, checkpoint/report, and exact amendment. Its explicit tables mark every S1 VC and follow-up VC satisfied at the pre-commit boundary and relevant ORCH rows satisfied for S1. Recoverable output is run `0584087c-8631-4c38-a81c-f6ca55cd8e0e`; no private runtime path or raw session identity is copied here.
**Uncertainty / gaps:** The user-accepted live 256-socket integration gap, release-owned fresh production inclusion, and absent visual unread-row observation remain explicit residual evidence limits, not hidden passes. S7 still owns combined 30-socket, rich visual/capability, provider-backed/user responsiveness, and final user acceptance. The two stale SSE comments are misleading but not behavioral and do not block this milestone.
**Recommended use:** Accept the retained S1 obligations under the narrow amendment, create the coherent implementation boundary commit excluding `.pi-subagents/` and temporary validation files, append the mandatory post-commit summary naming that commit, and then immediately select S2 under the master.

## Implementation Summary

**Plan section:** S1 `Design / Implementation Strategy` §§1–7 and S1-VC-001 through S1-VC-011; amendment VC-001 through VC-004 at local-main commit `8f7ded851e1241ee81631d49f443091f1e02bb49`; master S1 outcome and ORCH-VC-001/002/008/009/010/011/012.
**Work and outcome:** S1 is accepted at the pre-commit boundary. The source implements gateway-wide direct-peer admission and exact release, an HMR-stable wrapper-owned running projection, one static minimal global-status channel, a page-lifetime browser controller/provider, sidebar authority and replay migration, identifier-free exercised diagnostics, and removal of only the global running EventSource route/caller. One sequential fix corrected all three independently reproduced source defects. Both browser engines then passed real page ownership, reconnect, hosted discovery, running-edge, unread-state, and HTTP-schedulability validation. No S2+ production surface changed.
**Validation / evidence:** TypeScript and lint pass; focused S1 tests pass 85/85; the non-real-Next full partition passes 224/224; the pinned cached production lifecycle/HMR fixture passes 4/4; package dry run includes both changed plain runtime files; diff, source-boundary, privacy, and no-staged-file checks pass. Pure tests prove 256/257 accounting; real tests prove 64/65, malformed-handshake release, exact order/non-reuse, cleanup, and re-admission. Chromium and Firefox each prove 1/5/10 pages with exactly one active global socket per page and zero global EventSource, five-page server restart/discovery replay, both hosted launch kinds, and 5/7 plus 10/10 workloads with every ordinary HTTP request completing in the under-one-second diagnostic class. Final fresh review run `0584087c-8631-4c38-a81c-f6ca55cd8e0e` recommends acceptance with no blocker or major.
**Departures from approved obligations:** The real distributed four-address 256/257 integration is explicitly user-authorized as `waived` by the approved follow-up; it is never described as passed. This is the only validation waiver. The literal full command's release-artifact preflight is transparently partitioned because fresh production artifacts are release-owned and `next build` is prohibited; every test executes in the 224-plus-4 partition with zero skips. Synthetic command-only sessions persisted the exact unread state but produced no ordinary rendered rows, leaving the subjective visual observation to S7 as planned. Fresh production inclusion also remains release-owned. No implementation obligation is incomplete, no source behavior diverges, and the optional stale SSE comments are deferred rather than treated as a behavior departure.
**Implementation commit:** pending at the coherent S1 boundary commit.

## Implementation Summary

**Plan section:** Final S1 disposition for `Design / Implementation Strategy` §§1–7 and S1-VC-001 through S1-VC-011; amendment VC-001 through VC-004 at local-main commit `8f7ded851e1241ee81631d49f443091f1e02bb49`; master S1 outcome and ORCH-VC-001/002/008/009/010/011/012.
**Work and outcome:** Accepted S1 is committed. Pi Web now enforces gateway-wide direct-peer admission before its first production channel, maintains global running state through wrapper-owned projection, serves one minimal HMR-safe running/discovery WebSocket channel, owns one global socket per browser page above keyed chat lifetimes, preserves sidebar fallback/replay/completion semantics, sanitizes exercised lifecycle diagnostics, and removes only the migrated global SSE route/caller. Initial independent findings were corrected in one bounded fix pass; final fresh review found no blocker or major. S2 remained unstarted through this boundary.
**Validation / evidence:** Root and independent evidence passed TypeScript, lint, 85/85 focused tests, 224/224 non-real-Next tests, 4/4 pinned-fixture real-Next tests, package inclusion, diff/privacy/source-boundary checks, pure 256/257, real 64/65 and release/re-admission, and the complete retained browser matrix. Chromium and Firefox each passed 1/5/10 page inventory, five-page restart/discovery replay, both hosted launch kinds, and 5/7 plus 10/10 workloads with all ordinary HTTP requests completing. Durable aggregate evidence is `.agents/reports/2026-08-01-s1-global-websocket-validation.md`; final independent review is run `0584087c-8631-4c38-a81c-f6ca55cd8e0e`.
**Departures from approved obligations:** The real distributed four-address 256/257 integration is `waived` only under the user-approved follow-up; deterministic total-cap behavior and real per-peer/release behavior remain passing, and no production limit changed. The literal full-suite production preflight was partitioned transparently because local main lacks release-owned manifests; all 224 other tests and all four lifecycle tests executed without skip against the allowed pinned stale fixture, and no fresh inclusion claim is made. Synthetic sessions proved persisted unread transitions but not a rendered unread row; S7 retains the planned subjective visual/user gate. Fresh production inclusion remains release-owned. Two stale SSE comments are deferred as optional non-functional cleanup. There are no other incomplete, blocked, superseded, waived, or divergent S1 obligations.
**Implementation commit:** `f0fc1ac7296065dfca0aeb0038e2b6e2ed04837a` (`feat: migrate global status delivery to websockets`).
