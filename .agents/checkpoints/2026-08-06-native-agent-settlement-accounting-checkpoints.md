# Native Agent Settlement Accounting Checkpoints

Plan: `.agents/plans/2026-08-06-native-agent-settlement-accounting.md`

## Handoff

**Source:** Pi subagents run `5f7fd3b5-d393-4cc0-af47-3f98ad0bb49d`, fresh read-only `scout`; mission `21303e2a-ed19-4e1c-bf64-050be63a9a21`; child session `75e927fa/run-0`; recoverable result `.pi-subagents/artifacts/outputs/5f7fd3b5-d393-4cc0-af47-3f98ad0bb49d/context.md`, output log `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/5f7fd3b5-d393-4cc0-af47-3f98ad0bb49d/output-0.log`, and native session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-08-06-native-agent-settlement-accounting--/2026-08-06T19-14-36-022Z_019fd87f-b576-7d98-a82e-9ea4df6fec95/75e927fa/run-0/session.jsonl`.
**Purpose:** Independently map the causal-claim and receipt model, confirm the approved defect and smallest production seam, identify deterministic multi-start, receipt, reentrancy, manual-compaction, and authority tests, and report any plan conflict without editing or delegation.
**Outcome:** The scout confirmed that `agent_settled` currently reserves only the first unreserved native claim, while Pi continuation loops can emit multiple `agent_start` events before one session-level settlement. It recommended snapshotting every unreserved native claim before terminal raw fanout, resolving that exact batch from the shared projection receipt, and retaining one-claim reservation for current and legacy manual compaction. It found no plan conflict or reason to change projection, transport, browser, persistence, or publisher-authority semantics.
**Evidence:** The parent verified the decisive paths in `lib/rpc-manager.ts`, `lib/session-event-hub.ts`, `lib/session-projector.ts`, and the focused lifecycle suite. Baseline `NODE_ENV=test node --test lib/rpc-manager.test.mjs` passed `119/119`. A new multi-start/single-settlement regression then failed on unchanged production code with one remaining native claim (`1 !== 0`) and passed after exact pre-fanout batch reservation was implemented. Focused delayed commit/rejection, reentrant-start, nested-terminal, manual one-to-one, and same-ID replacement cases pass.
**Uncertainty / gaps:** The scout noted that per-claim completion performs repeated idle checks, though the active predicate permits projected finality only after the last eligible claim; final adversarial review must confirm this remains safe under delayed and nested receipts. Exact full-suite, static, lint, whitespace, documentation, and closeout gates remain pending.
**Recommended use:** Keep batching exclusive to `agent_settled`, retain the captured claim objects through raw fanout and receipt resolution, preserve publisher identity/epoch checks unchanged, and require fresh review plus the full Validation Contract before commit.

## Implementation Summary

**Plan section:** Objective; Design / Implementation Strategy; Test Strategy items 1–5; Validation Contract VC-001 through VC-004.
**Work and outcome:** Changed native terminal reservation from one claim to the exact set of all unreserved native-agent claims captured before raw `agent_settled` fanout. The shared terminal receipt now commits or restores that complete batch, while current and legacy standalone manual compaction still reserve and release one claim per terminal. Starts created during terminal fanout remain outside the captured batch. Expanded deterministic coverage for Pi-style continuation attempts, delayed committed and rejected terminal receipts, terminal/start and nested terminal reentrancy, manual one-to-one accounting, projected-final-before-global-idle ordering, and same-ID replacement authority.
**Validation / evidence:** The required red baseline failed before production changes with one leaked native claim. After the change, the new root regression and all added focused receipt/reentrancy/manual cases pass; the complete focused RPC suite passed `120/120` before the final added focused cases, and those later focused cases also pass. `git diff --check` passes. Literal worktree-local TypeScript was not yet runnable because this retained worktree has no `node_modules` entry; Node tests resolve dependencies from the main checkout, and the approved static gate remains pending with the established temporary worktree symlink procedure.
**Departures from approved obligations:** None. Production changes remain confined to `lib/rpc-manager.ts`; regression changes remain confined to `lib/rpc-manager.test.mjs`; no browser, protocol, reducer, persistence, Pi dependency, or user-control behavior changed.
**Implementation commit:** Pending independent review and complete validation.

## Handoff

**Source:** Pi subagents workflow `call_n9sJArYHVARIrQMMStgcDWKZ|fc_000fe24c1c40f134016a74df09aee881979e79462e8ab6ab1e`, two fresh read-only `reviewer` children; correctness run `9bd4d8bb` / mission `9fc6771a-725f-4913-acd3-bb92d0180156`, validation run `d8c460d3` / mission `ebfc8a17-a8fa-4cb6-829d-b99f3a1b259d`; recoverable aggregate result `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-results/call_n9sJArYHVARIrQMMStgcDWKZ|fc_000fe24c1c40f134016a74df09aee881979e79462e8ab6ab1e.json` and child sessions named by that result.
**Purpose:** Adversarially review transactional batch correctness, pending and delayed receipt behavior, terminal reentrancy, destruction, projected/global finality, same-ID authority, test fidelity, scope, durable documentation, and Validation Contract completion without editing or delegation.
**Outcome:** Both reviewers found no production correctness defect. They confirmed that pre-fanout capture, exact shared-receipt commit/rollback, manual one-to-one isolation, per-claim completion, finality ordering, destruction, and publisher identity/epoch authority are sound. The correctness reviewer identified one low-severity coverage gap: multiple pending start receipts captured by one queued terminal were proven only by its temporary probe, not a retained regression. The parent added committed and rejected queued multi-start subtests; both pass. The validation reviewer correctly blocked completion until the broad suite is rerun with a worktree-local dependency link held for the entire command.
**Evidence:** Reviewers cited the exact production and focused-test paths and independently ran focused validation. The new retained queued-receipt regression verifies two pending starts are reserved by one terminal, committed FIFO as one batch, or completely restored when the shared terminal receipt rejects. A preliminary broad run reached `709/711` but lost its temporary `node_modules` symlink while a concurrent static-validation command completed; the only failing real-Next child requires that exact local path. This was a validation-setup race, not accepted evidence for VC-005, and must be replaced by a clean sequential run.
**Uncertainty / gaps:** Exact focused, broad, TypeScript, lint, whitespace, hygiene, commit, and closeout gates remain pending after the reviewer-requested test addition. Existing wrappers already stranded in a live process may require restart, as disclosed by the approved plan.
**Recommended use:** Retain the implementation and new pending-receipt regression, keep `.pi-subagents/` out of tracked state, hold the temporary dependency symlink for each complete validation command, then obtain a final diff review before commit and guarded closeout.

## Handoff

**Source:** Pi subagents run `c9bc304d-f354-4d3b-b8a7-dd69109ef94e`, fresh read-only `reviewer`; mission `35b3e0b6-db86-486e-bd09-c75caa18e9c4`; recoverable result `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-results/c9bc304d-f354-4d3b-b8a7-dd69109ef94e.json`, output log `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/c9bc304d-f354-4d3b-b8a7-dd69109ef94e/output-0.log`, and native session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-08-06-native-agent-settlement-accounting--/2026-08-06T19-14-36-022Z_019fd87f-b576-7d98-a82e-9ea4df6fec95/efcc2c55/run-0/session.jsonl`.
**Purpose:** Provide final independent signoff on the post-review diff, queued pending-start regression, complete Validation Contract evidence, durable documentation, scope, hygiene, and closeout prerequisites without editing or delegation.
**Outcome:** The reviewer returned an explicit no-findings signoff. It confirmed exact pre-fanout native batch capture, transactional shared-receipt resolution, pending and reentrant start handling, nested terminal isolation, one-to-one manual compaction, projected-final-before-global-idle ordering, same-ID authority, and server-only scope. It accepted the parent-observed broad/static gates and independently reran the focused suite and whitespace check.
**Evidence:** Final focused RPC validation passed `127/127`; the exact broad repository suite passed `714/714`; TypeScript, lint, and `git diff --check` passed with the temporary dependency symlink held for the complete command and removed afterward. The reviewer independently passed focused RPC `127/127`, whitespace, status, and dependency-link absence checks, and found no staged files or unintended source surfaces.
**Uncertainty / gaps:** Correctness continues to depend on Pi retaining its documented session-level `agent_settled` contract. Wrappers already stranded in an existing live process may require server restart. Implementation commit, mandatory final summary, and guarded local-main closeout remain pending.
**Recommended use:** Commit the reviewed implementation and evidence without `.pi-subagents/`, append and commit the mandatory final Implementation Summary naming that implementation commit, then perform guarded closeout while preserving unrelated main dirt.
