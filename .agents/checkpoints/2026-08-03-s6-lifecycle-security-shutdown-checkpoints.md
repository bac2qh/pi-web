# S6 Lifecycle, Security, and Owned Shutdown Checkpoints

Plan: `.agents/plans/2026-08-03-s6-lifecycle-security-shutdown.md`

## Handoff

**Source:** Pi subagents parallel run `c017ddf8`, `scout` child 0; session `019fc8ef-6c0f-7eb4-96f5-87118f759c4d`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/c017ddf8/run-0/session.jsonl`; output `.pi-subagents/artifacts/c017ddf8_scout_0_output.md`. Recorder: orchestration root. Kind: read-only S6 wrapper/idle/bounds/ownership planning investigation. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S6; Mode: investigation.
**Purpose:** Map current semantic idle, command/native/wrapper touch behavior, active causal states, exact-once wrapper disposal, session replay/output bounds, channel owners, runtime process cleanup, tests, and privacy without editing or delegation.
**Outcome:** Current default idle is ten minutes. Startup and every native event touch; `send()` currently touches even unsupported types; wrapper-generated `emit()` has no common idle touch. The simple expiry predicate covers prompt/compaction claims plus native streaming/compacting but omits already tracked native causal/reserved/fanout/projected activity. Destruction itself is synchronous/idempotent and native disposal is guarded once. Session replay is already 4 MiB or 8,192 units and subscriber output is already 4 MiB; these should be revalidated, not redesigned. The process-global wrapper registry installs independent `exit`/`SIGINT`/`SIGTERM` cleanup and is not joined to custom-server close.
**Evidence:** Root verified `lib/rpc-manager.ts` constants, native subscription, `send()`, causal counters, projected settlement, idle callback, destroy path, registry publication/removal, and process handlers. Root also inspected accepted session/file channel owners and protocol constants. The scout identifies fake-clock seams for exact touch/expiry/settlement, repeated disposal/recreation, subscriber reference release, and in-flight shutdown publication.
**Uncertainty / gaps:** The master leaves engineering values/rules to freeze. Root resolves them inside S6 authority: recognized command types touch while unsupported types do not; all native/wrapper domain events touch; passive transport/heartbeat/file/global traffic does not; active protection includes existing causal/reserved/fanout/projected state. The 4 MiB output rule is session-subscriber-specific; file-watch retains its fixed-frame coalesced bound. No product choice or material divergence remains.
**Recommended use:** Make semantic touch and active-state rules explicit and exhaustive in the draft; replace process handlers with a generation-bound custom-server owner; require shutdown/start races and exact passive non-touch evidence.

## Handoff

**Source:** Pi subagents parallel run `c017ddf8`, `scout` child 1; session `019fc8ef-6bfc-7718-b76c-833c6a422c32`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/c017ddf8/run-1/session.jsonl`; output `.pi-subagents/artifacts/c017ddf8_scout_1_output.md`. Recorder: orchestration root. Kind: read-only S6 heartbeat/admission/custom-server shutdown planning investigation. Governing master and candidate milestone as above; Mode: investigation.
**Purpose:** Map the exact one-port gateway/server/launcher ownership, all three channel teardown gaps, public `ws` ping/pong APIs, admission release, current immediate-force close, production/development evidence, and ten-second graceful seam without editing or delegation.
**Outcome:** The upgrade path already consumes tickets before direct-peer admission and releases reservations idempotently across raw close, accepted WebSocket close, handler failure, and handshake failure. The server owns the WSS client set and tracked HTTP connections but currently terminates/destroys both immediately. Gateway close clears registrations without invoking uniform channel owner teardown. Global status has only per-socket cleanup; session has wrapper-keyed subscribers but no registration-wide close; file-watch already has the right registration owner pattern. The correct heartbeat root is one server-level WSS sweep, and the correct force boundary is only that WSS client set plus this HTTP server's tracked sockets.
**Evidence:** Root verified `bin/pi-web-server.js` upgrade and close ordering, `bin/pi-web-transport-gateway.js` ticket/admission/maps/close behavior, `bin/pi-web.js` terminal-only signal policy, typed gateway access, all three channel registration records, server/gateway/real-Next tests, and installed public `ws` `ping`/`pong`/`terminate` behavior. Historical M0 proves copied production lifecycle only; current release manifests may be absent and cannot be rebuilt during development.
**Uncertainty / gaps:** Heartbeat cadence is not numerically fixed by the master. Root adopts the standard contained policy of one unrefed 30-second sweep and termination only after one complete missed response window. The ten seconds is a natural-drain grace for proven Pi Web sockets/connections, not force authority over public/private Next state. Current-bin injected/real-child production-mode reuse remains mandatory even when copied-artifact preflight is unavailable. No material conflict remains.
**Recommended use:** Freeze server-level heartbeat, explicit finite owner callbacks, begin-shutdown versus final-close gateway state, exact 9,999/10,000 ms behavior, launcher-only signals, real production repetition, real development/HMR, and one-page Chromium/Firefox automatic-pong smoke.

## Handoff

**Source:** Orchestration-root reconciliation of the exact master, accepted S5 implementation/finality `e82e9bdc6c020d0663eec12515bf41c3aa5d6ad8`/`c7f4a4c3bf488c6ef6ad5da2caebc96cdc1c8174`, current repository state, M0/S3/S5 plans/checkpoints, custom-server memory, root source reads, and planning run `c017ddf8`. Recorder: orchestration root. Kind: S6 milestone selection and draft decisions. Governing master and candidate milestone as above; Mode: planning.
**Purpose:** Select the next single contained outcome, disposition implementation-level ambiguities without expanding the master, and establish a complete draft/review boundary before any writer starts.
**Outcome:** S6 is the next and only active milestone. The draft defines 30-minute semantic idle and an exhaustive touch matrix, protects all already modeled active states, adds one 30-second server heartbeat, preserves direct-peer admission and existing session/file bounds, creates four finite generation-bound lifecycle owners, removes runtime process handlers, and applies an exact ten-second natural drain followed by force only for residual Pi-owned sockets/connections. It preserves public Next cleanup, process-scoped development, production reuse, application protocols/browser ownership, and S7 boundaries.
**Evidence:** HEAD is accepted S5 finality `c7f4a4c3bf488c6ef6ad5da2caebc96cdc1c8174`; worktree state is clean except excluded `.pi-subagents/` plus the new draft/checkpoint. The exact master authorizes all selected outcomes under S6 and ORCH-VC-007/008/009/012. No reference-pointer companion exists. The plan includes Objective, seven implementation sections, scope estimate, exact tests, diagnostics, ten numbered validation contracts, risks, residual production-artifact honesty, and stop conditions.
**Uncertainty / gaps:** Implementation and test evidence is intentionally absent while status remains `draft`. A fresh independent no-edit/no-delegation review must challenge generation gating, heartbeat timing, owner close/HMR, in-flight starts, 10-second force ordering, callback convergence, process listeners, production evidence, browser smoke, and S7 containment before approval.
**Recommended use:** Obtain fresh independent draft review. Correct only bounded planning defects, keep status `draft` until clean, then approve and commit plan/checkpoint before launching one sole S6 implementer.

## Handoff

**Source:** Pi subagents parallel run `c725f36a`, two fresh read-only `reviewer` children; sessions `019fc8f8-a122-77d1-a542-01d99c5207d3` and `019fc8f8-a136-722d-97ce-3429ffe3de08`; recoverable session files `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/c725f36a/run-0/session.jsonl` and `run-1/session.jsonl`; outputs `.pi-subagents/artifacts/c725f36a_reviewer_0_output.md` and `c725f36a_reviewer_1_output.md`. Recorder: orchestration root. Kind: independent initial S6 draft architecture/validation review. Governing master and S6 draft as above; Mode: review.
**Purpose:** Challenge the complete draft for semantic-idle completeness, generation ownership, HMR/pre-dispatch races, extension binding, output bounds, heartbeat evidence, exact shutdown convergence, production/development truth, privacy, and S7 containment before approval.
**Outcome:** Verdict `FINDINGS — DO NOT APPROVE`; no material authority gap. Reviewers accept the command list, core active predicate, 30-second heartbeat feasibility, existing session bounds, launcher-only signals, production-manifest honesty, S7 boundary, and most shutdown ordering. They find bounded draft defects: five direct projected wrapper inputs bypass `emit()`; cached hosted capability and shared start locks are not generation-safe; accepted sockets can race unregister before deferred handler enlistment; binding-dependent commands and binding completion can continue after destroy; injected missing close callbacks cannot literally be forced to fire; global status output is unbounded; the S5 destroy-during-binding risk was not explicit; the draft allowed an ambiguous alternative lifecycle capability; browser survival alone could hide reconnects; and wrapper clocks lacked exact timer injection.
**Evidence:** Root verified the cited direct hub inputs (`wrapper_activity_started`, `wrapper_settled`, `extension_dialog_closed`, `extension_status_cleared`, `extension_widget_cleared`), module-evaluation-only hosted registration, shared lock finalizer, upgrade Promise dispatch gap, binding waits, global channel unrestricted stringify/send, and current ambient idle timers. Installed Node/`ws` public heartbeat APIs and accepted S2/S3 constants remain feasible. All findings are planning-level and contained by S6.
**Uncertainty / gaps:** The corrected draft still needs fresh review. No implementation exists and no writer is authorized. The gateway lifecycle design now must be treated as mandatory rather than one of two alternatives; implementation must stop if synchronous enlistment, generation gating, or public callback settlement cannot satisfy it.
**Recommended use:** Review the corrected draft, which now routes every wrapper-originated hub input through one touch seam; generation-scopes RPC owner/locks/hosted activation and binding-dependent continuations; mandates gateway `ownerLifecycleVersion: 1` with synchronous pre-dispatch enlistment; gives global subscribers a 4 MiB bounded pump; distinguishes real callback convergence from injected omission; requires injected wrapper clocks; and proves original browser sockets by server-observed finite channel-class pongs without reconnect.

## Handoff

**Source:** Pi subagents parallel run `81ef497b`, two fresh read-only `reviewer` children; sessions `019fc903-3dd0-74e4-96f2-30162acfefec` and `019fc903-3dbc-7a93-8d38-e894370e24b2`; recoverable session files `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/81ef497b/run-0/session.jsonl` and `run-1/session.jsonl`; outputs `.pi-subagents/artifacts/81ef497b_reviewer_0_output.md` and `81ef497b_reviewer_1_output.md`. Recorder: orchestration root. Kind: independent corrected S6 draft architecture/validation review. Governing master and S6 draft as above; Mode: review.
**Purpose:** Recheck every `c725f36a` disposition and search for new contradictions before immutable approval.
**Outcome:** Architecture reviewer returns `APPROVE` and confirms every prior finding resolved. Validation reviewer accepts all those corrections but returns `FINDINGS — DO NOT APPROVE` for one bounded shutdown-order defect: channel-local 1,000 ms terminate fallbacks or close helpers could force a resistant socket before the plan's exact 10,000 ms server-owned force boundary.
**Evidence:** Root verified current session fallback and global/file close helper behavior. The draft now gives owner callbacks exact finite `owner_replaced` versus `server_shutdown` reasons. HMR, policy, and slow-consumer paths retain bounded local fallback. `server_shutdown` immediately releases application resources and cancels/suppresses every local terminate fallback, requests close handshakes only, leaves already-closing sockets alone, and reserves all terminate/raw destroy operations for the server coordinator at 10,000 ms. Integrated all-channel tests require zero local force through 9,999 ms, graceful natural close at 9,999, and one coordinator force per residual owner at 10,000.
**Uncertainty / gaps:** One fresh review of the reason-specific shutdown correction remains. No material divergence exists and no implementation has started.
**Recommended use:** Obtain a final fresh no-edit/no-delegation draft review focused on reason-specific owner closure and the complete corrected contract. Approve only on a clean verdict.

## Handoff

**Source:** Pi subagents run `c401b36d`, fresh read-only `reviewer`; session `019fc90a-45f4-791b-b5ad-f8639e42c75a`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/c401b36d/run-0/session.jsonl`; output `.pi-subagents/artifacts/c401b36d_reviewer_0_output.md`. Recorder: orchestration root. Kind: independent final S6 draft review. Governing master and S6 plan as above; Mode: review.
**Purpose:** Verify the complete corrected draft and exact reason-specific owner/shutdown semantics after all bounded findings.
**Outcome:** Verdict `APPROVE`. The reviewer confirms `owner_replaced` retains bounded local fallback while `server_shutdown` releases application resources, suppresses local force, and reserves residual force for the server coordinator at 10,000 ms. It finds idle semantics, generation ownership, binding/start races, heartbeat, admission/backpressure, global output, privacy, production reuse, browser evidence, and S7 containment complete, feasible, and testable with no undecided implementation policy.
**Evidence:** The clean review maps the correction to the current session 1,000 ms fallback and global/file close helpers, verifies installed Node 24.18.0 and `ws` 8.21.0 public APIs, and rechecks the full draft/validation contract. Root re-read the final owner/shutdown sections, exact 9,999/10,000 tests, prior finding dispositions, source references, and `git diff --check`.
**Uncertainty / gaps:** Implementation complexity remains high around generation and deadline races, but every risk has an explicit blocker/test path. All source, real-child, and browser evidence is correctly implementation-stage.
**Recommended use:** Approve and commit the existing S6 plan/checkpoint boundary, record its immutable blob, then launch exactly one fresh sole S6 implementer with the complete special handoff contract.

## Handoff

**Source:** Orchestration-root final planning reconciliation after investigations `c017ddf8`, finding reviews `c725f36a`/`81ef497b`, and clean final review `c401b36d`. Recorder: orchestration root. Kind: milestone approval. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; milestone: `.agents/plans/2026-08-03-s6-lifecycle-security-shutdown.md`; Mode: planning.
**Purpose:** Freeze one contained S6 source-writing contract only after every lifecycle/security/shutdown ambiguity and review finding was dispositioned.
**Outcome:** S6 is approved. The status changed from `draft` to `approved`; its frozen pre-commit blob is `ce7244da61b274b70032e1217a9f3c8582ad19e3`. The immutable master blob remains `195253256996ab4a35dfaa44f2e21175efbaf4d9`. No implementation source has changed, no S6 writer has launched, and `.pi-subagents/` remains excluded runtime state.
**Evidence:** Accepted predecessor implementation/finality is `e82e9bdc6c020d0663eec12515bf41c3aa5d6ad8`/`c7f4a4c3bf488c6ef6ad5da2caebc96cdc1c8174`. The approved plan defines exact semantic touches/active states, generation-scoped owner/start/binding behavior, mandatory gateway lifecycle and synchronous socket enlistment, 30-second heartbeat, session/global/file bounds, reason-specific HMR versus shutdown closure, exact ten-second owned force, production/development/browser evidence, privacy, and S6-VC-001 through S6-VC-010. Root verified complete sections, hashes, status, and whitespace.
**Uncertainty / gaps:** Implementation-stage source, tests, real-child, browser, package, and independent acceptance evidence remains intentionally outstanding. Fresh production route inclusion remains release-owned. Any need for private Next/Pi changes, application heartbeat frames, weaker bounds, early force, or S7 scope requires a stop.
**Recommended use:** Commit only the plan/checkpoint boundary without `.pi-subagents/`; verify the committed plan blob; issue the immutable packet to one fresh `milestone-implementer` as sole source writer.

## Handoff

**Source:** Pi subagents run `9b414848-d85a-44fc-bff0-8df79c310c03`, sole fresh `milestone-implementer`; session `019fc910-138b-7ec3-953f-1210a82f6243`; recoverable run session `.pi-subagents` run directory `93028db5/run-0` and session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/93028db5/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone implementation handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; milestone: `.agents/plans/2026-08-03-s6-lifecycle-security-shutdown.md`; Mode: implementation.
**Purpose:** Implement the complete immutable S6 contract, including S6-VC-001 through S6-VC-010, as the only implementation-source writer without staging, committing, changing orchestration files, or entering S7 scope.
**Outcome:** The implementer reports a structural owner-lifecycle gateway, generation-bound RPC registry and starts, exact 30-minute semantic idle, one server heartbeat, reason-specific channel teardown, bounded global-status output, and a ten-second owned-resource shutdown coordinator. Production changes are confined to the two `bin` runtime modules plus `lib/websocket-gateway.ts`, `lib/rpc-manager.ts`, and the three persistent channel modules; tests add `lib/s6-lifecycle.test.mjs` and update focused runtime/server/channel integration fixtures. No browser product component, protocol schema, dependency, plan, documentation, memory, or release artifact was changed. The handoff reports no scope departure or blocker.
**Evidence:** Reported results are focused S6/runtime/server/channel `210/210`, exact non-real-Next `601/601`, literal `603/605` with only the permitted copied-production missing-`.next/BUILD_ID` pair, separate real-Next `2/4` with the same permitted pair, typecheck, lint, package dry run, current-bin same-port production reuse, real `ws` automatic-pong versus `autoPong:false`, and one-page Chromium and Firefox 65-second three-channel heartbeat/restart smoke. It reports immutable blobs `195253256996ab4a35dfaa44f2e21175efbaf4d9` and `ce7244da61b274b70032e1217a9f3c8582ad19e3`, empty index, and preserved `.pi-subagents/`. Root has independently confirmed HEAD `8f5ef8cc3a61c0f93b52c210f9b7343b736ca03d`, both blobs, the unstaged intended file set, and `git diff --check`; all behavioral and browser claims remain subject to root reproduction and independent review.
**Uncertainty / gaps:** Fresh copied-production inclusion remains release-owned because manifests are absent. Real development reportedly reaches the intended coordinator-forced result for its tracked HMR connection before public Next cleanup. The implementer's attempted read-only support review stopped because that child received forbidden writer tools and made no inspection or mutation. Root diff scrutiny has identified review questions around exact fallback-reference release, bounded global encoding, and the accepted-before-owner-replacement socket failure path; these are not yet classified. No implementation is accepted, staged, or committed until decisive root gates and a fresh no-edit/no-delegation reviewer resolve them.
**Recommended use:** Inspect the complete source/test diff and owner/timer graph, reproduce decisive gates and browser evidence, then obtain a fresh independent implementation review. Route any bounded defect through one sequential fix attempt under this unchanged milestone; stop only for material divergence.

## Handoff

**Source:** Pi subagents parallel run returned in the orchestration session after implementation run `9b414848-d85a-44fc-bff0-8df79c310c03`; two fresh no-edit/no-delegation `reviewer` children, one architecture/correctness and one validation/security specialist. Recorder: orchestration root. Kind: independent S6 implementation review. Governing master and approved S6 milestone as above; Mode: review.
**Purpose:** Independently inspect the complete production/test diff and untracked S6 test against S6-VC-001 through S6-VC-010, reproduce adversarial owner/generation/bounds races, and challenge the implementer's validation claims before acceptance.
**Outcome:** Both reviewers return `FINDINGS — DO NOT APPROVE`; all findings are bounded implementation or test defects inside immutable S6, not material divergence. Blocking code findings are: a consumed/admitted upgrade that loses its registration before the delayed `handleUpgrade` callback receives only a close handshake and can retain a resistant socket/admission; synchronous native `subscribe()` reentrancy can shut down the RPC generation before a wrapper is published, after which startup still publishes an undisposed dead owner; the shutdown drain schedules a fresh ten seconds after synchronous owner/setup work instead of enforcing one deadline from close initiation; and global-status encoding/reference work can allocate an oversized string, retain consumed queue entries, and retain fired fallback closures. Additional bounded findings are missing heartbeat listener removal, the analogous session fallback-set retention, attacker-controlled diagnostic error names, a copied-production lifecycle fixture still expecting pre-S6 `ok`, and incomplete injected matrices for every active/passive semantic-idle and new admission-release path.
**Evidence:** Architecture review reproduced failed-enlist admission retention, stale RPC publication without native disposal, fired fallback retention, and heartbeat listener retention; focused tests remained `155/155` and `49/49`, with typecheck/lint/whitespace clean. Validation review reproduced the pre-enlist HMR leak, exact non-real-Next `601/601`, focused `204/204`, real-Next `2/4` with only the permitted missing-manifest parent/child, and package/typecheck/lint/hash/no-stage gates. Root verified the cited control flow in `bin/pi-web-server.js`, `lib/rpc-manager.ts`, `lib/global-status-channel.ts`, `lib/session-channel.ts`, and the stale `entry.outcome === "ok"` copied-production fixture. Both reviews accept the basic 30-second heartbeat policy, complete active-state predicate in source, binding-dependent post-await guards, existing session/file bounds, launcher signal ownership, current-bin reuse, real development behavior, S7 containment, and recoverable Chromium/Firefox three-socket smoke.
**Uncertainty / gaps:** The global running-list bound needs a contained implementation that preserves exact running semantics while avoiding whole oversized serialization/snapshot work; no protocol change or weaker 4 MiB bound is authorized. The browser smoke need not be rerun until source fixes stabilize. Fresh copied-production execution remains unavailable and release-owned, but its latent expectation must still be corrected. No implementation evidence is accepted and nothing is staged.
**Recommended use:** Issue one focused sequential S6 fix attempt to the sole milestone implementer covering every reproduced code defect and missing deterministic test. Require exact consume/admit/replace, synchronous-start shutdown, absolute-deadline, reference-release, hostile encoding/diagnostic, complete active/passive, admission-recovery, listener cleanup, and copied-fixture tests; then repeat root verification and fresh independent review.

## Handoff

**Source:** Pi subagents run `a067949b-0329-4eb6-8cd0-82baab92ff53`, fresh sole `milestone-implementer` fix writer; session `019fc948-a727-73fa-9181-8668cbda1e68`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/362fb87d/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Resolve the complete bounded ten-finding implementation-review packet under the unchanged S6 plan, preserve all accepted transport/product boundaries, add missing adversarial evidence, and leave acceptance/commit decisions to the root.
**Outcome:** The fix handoff reports all ten findings corrected: reason-aware failed-enlist cleanup and pre-enlist admission release; post-reentrant RPC generation checks; one absolute monotonic shutdown deadline; shape-checked incremental global encoding and bounded canonicalization; structural running-state change publication without whole-array stringify; consumed queue/fired fallback reference release; exact heartbeat listener removal; finite launcher/server error classes and fixed opener warning; complete injected active/passive idle matrices and terminal-path admission evidence; and the copied-production fixture's S6 `graceful` expectation. Fix production edits add only `bin/pi-web.js` to the original contained production set; no browser product, protocol schema, dependency, plan, documentation, memory, release artifact, or S7 surface changed.
**Evidence:** The handoff reports focused `222/222`, global focused `11/11`, exact non-real-Next `615/615`, literal `617/619` and real-Next `2/4` with only the permitted missing-`.next/BUILD_ID` parent/child, typecheck, lint, current-bin two-cycle/three-channel same-port production reuse, real automatic-pong versus `autoPong:false` with admission recovery, package dry run, hashes/privacy/source/whitespace/no-stage checks, and fresh Chromium plus Firefox original-three-socket 65-second/two-sweep survival and restart recovery. Root has independently reconfirmed HEAD `8f5ef8cc3a61c0f93b52c210f9b7343b736ca03d`, both immutable blobs, empty index, intended unstaged file set, no imported runtime process handlers/exits, and clean whitespace; behavioral claims remain subject to root scrutiny and fresh review.
**Uncertainty / gaps:** Fresh copied-production execution remains release-owned because required manifests are absent; the fixture expectation is now source-reviewable but cannot run here. S7 scale, visual/capability matrix, maintained documentation, user acceptance, full-master validation, and closeout remain untouched. The root checkpoint is the only orchestration file changed and was not edited by the writer. Nothing is accepted, staged, or committed yet.
**Recommended use:** Inspect every final disposition in source and tests, reproduce decisive focused/full/real-child/browser gates, and obtain a fresh independent no-edit/no-delegation review. If clean, record acceptance and commit the coherent S6 implementation/finality boundary; otherwise continue the bounded sequential fix loop.

## Handoff

**Source:** Orchestration-root post-fix scrutiny plus two fresh no-edit/no-delegation `reviewer` children, architecture/race and validation/security specialists, after fix run `a067949b-0329-4eb6-8cd0-82baab92ff53`. Recorder: orchestration root. Kind: independent S6 post-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Verify every first-review disposition, reproduce the fixed races/bounds, inspect equivalent owner transitions, and determine whether S6 can be accepted or needs another bounded sequential fix.
**Outcome:** Verdict remains `FINDINGS — DO NOT APPROVE`; no material divergence. Root focused tests pass `223/223`, typecheck/lint/whitespace pass, and reviewers confirm the prior RPC reentrancy, absolute-deadline, queue/fallback reference release, heartbeat-listener cleanup, launcher/server diagnostics, copied-fixture, and production-restart corrections. One P0 owner-transition defect remains: a registration removed by HMR retains its gateway one-second fallback, while `beginShutdown()` visits only current registrations, so replacement followed by server shutdown still terminates at one second. Retired global/session policy fallbacks have the analogous reachability problem. Additional bounded findings are that global canonicalization still reads/builds/sorts the whole valid source before encoded one-over and rejects some duplicate-heavy inputs before canonicalization; runtime publishes its mutable backing running `Set`; a heartbeat whose `terminate()` throws can resume pinging instead of remaining terminal-pending; extension-binding diagnostics still accept arbitrary identifier-like names; and behavioral/passive test evidence remains weaker than the plan on real reentrant lifecycle seams.
**Evidence:** Root independently reproduced unregister → `beginShutdown()` → early gateway terminate with a resistant enlisted socket. Both reviewers reproduced it and mapped affected running/session/file, pre-enlist, policy, and slow paths. Validation review also reproduced a valid wide source fully read before global one-over, duplicate canonical rejection, hostile mutation of the published Set, and ping resumption after a throwing terminate. Exact non-real-Next remains `615/615`; focused reviewer sets report `136/136`; real-Next retains only the permitted missing-manifest parent/child. Reviewers found no new problem in RPC generation/disposal, absolute deadline, session/file bounds, production/dev lifecycle, or S7 containment.
**Uncertainty / gaps:** The owner correction must preserve one semantic owner-close callback while giving shutdown a separate idempotent way to cancel active and retired force fallbacks. Global correction must preserve exact unique canonical running semantics and stop iterating once a newly unique encoded ID crosses the bound. Browser smoke should be rerun only after final source stabilization. Copied production remains release-owned. No state is staged or accepted.
**Recommended use:** Issue one second focused S6 fix attempt: retain retired network registrations until terminal release; centralize owner-replacement force fallback in the gateway while channel owners cancel local fallbacks on either owner close; cancel active/retired gateway fallbacks at shutdown without a second semantic owner callback; use a runtime read-only projection façade and incremental unique-byte global canonicalization; make heartbeat failure terminal-pending; finite-map extension binding errors; and add integrated HMR→shutdown, mid-iteration one-over/duplicates/mutation, throwing-terminate, and behavioral active/passive tests. Then repeat decisive root gates and one fresh independent review.

## Handoff

**Source:** Pi subagents run `aebe73af-f16e-4bc8-b2d2-1bd8cdb76d0a`, fresh sole `milestone-implementer` second fix writer; session `019fc969-5473-72d1-82f4-e481cce9c3de`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/6612d3a8/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Correct the six remaining post-fix findings under the immutable milestone: retired-owner shutdown crossover, true global one-over canonicalization, mutable running authority, heartbeat terminal-pending behavior, extension diagnostic classes, and behavioral active/passive validation.
**Outcome:** The handoff reports retained active/retired registration ownership with shutdown cancellation and prompt terminal retirement; centralized gateway replacement fallback with channel owner-close suppression; incremental unique-byte global encoding that stops on the first newly unique one-over ID; a frozen non-mutating running projection façade; irreversible heartbeat terminal-pending retries without resumed pings; finite extension-binding error classes; and integrated actual lifecycle/passive tests. Production edits remain inside the approved S6 surfaces, with no browser product, protocol schema, dependency, plan, documentation, memory, release, or S7 change.
**Evidence:** Reported results are changed-surface `146/146`, broader focused `232/232`, exact non-real-Next `625/625`, literal `627/629` and real-Next `2/4` with only the permitted missing-production-manifest pair on the final runs, typecheck, lint, package, current-bin production reuse, real pong/admission recovery, hashes/privacy/source/no-stage, and fresh Chromium/Firefox original-three-socket two-sweep plus restart smoke. An earlier literal run reportedly exposed one timing-sensitive file-watch failure and failed to drain; later focused, exact, and literal reruns were clean except the permitted pair. Root has independently reconfirmed HEAD, immutable blobs, intended unstaged file set, no imported runtime process handlers/exits, and clean whitespace; all behavioral claims remain subject to final root reproduction and fresh review.
**Uncertainty / gaps:** The transient file-watch load failure needs disposition through repeated root evidence rather than omission. Fresh copied production remains release-owned. Root must verify retained-registration collection, semantic callback exact-once behavior, no shutdown-local force, true encoder tail stopping, façade immutability, terminal-pending retries, and actual passive no-touch before acceptance. Nothing is staged or committed.
**Recommended use:** Run decisive owner/encoder/heartbeat/runtime tests repeatedly, inspect the final source owner graph and reference release, rerun exact/literal/real-Next/browser gates as needed, and obtain one fresh no-edit/no-delegation implementation review. Continue the bounded fix loop on any finding; otherwise accept and commit S6.

## Handoff

**Source:** Orchestration-root second-fix scrutiny plus Pi subagents parallel review run `8dc3c961`, two fresh no-edit/no-delegation `reviewer` children; recoverable sessions `8dc3c961/run-0` and `run-1`. Recorder: orchestration root. Kind: independent S6 second-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Recheck all six second-fix dispositions, every consume/enlist terminal, retained-owner collection, global/heartbeat/runtime correctness, and final gate stability before S6 acceptance.
**Outcome:** Verdict `FINDINGS — DO NOT APPROVE`; findings remain bounded and do not change S6 authority. Reviewers accept the retired enlisted-socket HMR→shutdown correction, semantic callback exact once, gateway fallback suppression, global unique sorted one-over behavior, frozen running façade, heartbeat terminal-pending, extension diagnostic mapping, RPC lifecycle, absolute deadline, session/file bounds, production/dev lifecycle, and browser evidence. One P0 ownership gap remains: `consumeTicket()` increments `pendingConsumed`, but admission rejection, synchronous handshake throw, and callback-less malformed handshake never settle it because neither enlist nor failed-enlist callback runs. A later HMR therefore retains one registration/reference until final gateway close. One reviewer also reproduced the previously reported real file-watch convergence test failing and then hanging because its `finally` removes the directory without closing the socket/watcher.
**Evidence:** Root independently reproduced cap rejection and callback-less handshake each leaving `retiredRegistrationCount: 1` and `pendingConsumedCount: 1`; the fixed HMR→shutdown path now shows zero local terminate through 9,999 ms. Reviewers reproduced cap, thrown-handshake, and real callback-less malformed cases; focused sets passed `201/201` and `160/160`, exact non-real-Next `625/625`, typecheck/lint/package/whitespace passed, and real-Next retained only the permitted absent-manifest pair. The architecture reviewer also identifies an unsafe post-final-close callback: the current closed branch requests only a close handshake after the coordinator is gone and does not enlist the resistant socket. Validation review reproduced the file-watch test failure on the fifth isolated run and its 120-second hang.
**Uncertainty / gaps:** The consumed authorization needs one explicit idempotent pre-callback terminal/abandon operation bound to the raw accepted socket, called on admission rejection, raw close, synchronous handshake throw, and callback-less rejection; enlist/failure must settle the same record once. Late callback behavior must not create unowned sockets after final force. The file-watch production implementation is not implicated by current evidence, but the OS fixture must always close its watcher and report phase-specific convergence; repeated evidence must distinguish a robust fixture from a product defect. Browser rerun waits until source stabilizes.
**Recommended use:** Issue one third bounded fix attempt for the consumed-authorization state machine and terminal-path matrix, including exact zero owner/admission counters and safe late callbacks, plus unconditional file-watch test teardown and robust phase-labeled convergence. Repeat that fixture under isolation and full parallel load, then perform fresh final review.

## Handoff

**Source:** Pi subagents run `8d6eab84-e3aa-4518-a1c4-2c359c796367`, fresh sole `milestone-implementer` third fix writer; session `019fc993-f091-7575-9633-f19ab3672b79`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/7f95728e/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Resolve the remaining consumed-authorization exact-terminal leak and file-watch fixture failure/hang while preserving every accepted S6 lifecycle, transport, security, and product boundary.
**Outcome:** The handoff reports an idempotent consumed-authorization state machine with raw-socket binding and explicit abandonment, exact settlement on every admission/handshake/enlist terminal, fail-closed post-abandonment callbacks, coordinator-owned shutdown callbacks, and immediate terminal closure for hostile callbacks after final close. It also reports unconditional real file-watch socket/watcher teardown, phase-labelled convergence, state/change-count baselines, bounded mutation retries for OS coalescing, and a quiet sibling boundary. Production edits remain confined to gateway/server typed lifecycle surfaces; the file-watch production implementation is unchanged by this attempt.
**Evidence:** Reported results are focused authorization/server/file-watch `59/59`, broader focused `237/237`, twenty sequential real watcher passes without hang, exact non-real-Next `629/629`, literal `631/633` and real-Next `2/4` with only the permitted absent-manifest pair, typecheck, lint, package, current-bin reuse, real pong/admission recovery, hashes/privacy/source/no-stage, and fresh Chromium/Firefox original-three-socket two-sweep plus restart smoke. Root has independently reconfirmed HEAD, immutable blobs, intended unstaged scope, no imported process handlers/exits, and clean whitespace; state-machine and stability claims remain subject to root reproduction and fresh review.
**Uncertainty / gaps:** Fresh copied production remains release-owned. The new state machine must be scrutinized for idempotence, raw-close/callback ordering, abandonment before/after enlist, shutdown/final-close force authority, pending-record collection, and no reentrant repopulation. The watcher fixture's retry strategy must still prove the required states rather than mask them. Nothing is staged or accepted.
**Recommended use:** Inspect the final authorization and test control flow, reproduce exact-zero matrices and repeated watcher runs, run full final gates, and obtain one fresh no-edit/no-delegation review. Accept and commit S6 only on a clean verdict.

## Handoff

**Source:** Pi subagents fresh final-review parallel run after fix `8d6eab84-e3aa-4518-a1c4-2c359c796367`; two no-edit/no-delegation `reviewer` children, architecture/security and validation/acceptance. Recorder: orchestration root. Kind: independent S6 third-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Validate the consumed-authorization state machine, reentrant close ownership, watcher fixture semantics, and all S6 contracts before acceptance.
**Outcome:** Verdict `FINDINGS — DO NOT APPROVE`; all three findings are bounded. First, `bindRawSocket()` settles an already-destroyed raw socket but returns `true`, so the server reserves admission after the close event has passed and can retain one admission/peer count. Second, `server.close()` assigns `closePromise` only after constructing an async coordinator whose synchronous owner cleanup may reenter `close()`, producing a second promise/coordinator. Third, the watcher fixture's recreate retry appends when the file already exists, so a modify notification can satisfy the recreate predicate without proving another absent→present transition.
**Evidence:** Architecture review reproduced the already-destroyed raw admission leak and distinct nested/outer close promises, while confirming public configured `ws.handleUpgrade` callback synchrony. Validation review passed focused `237/237`, exact `629/629`, twenty watcher repetitions, literal and real-Next with only the permitted pair, typecheck/lint/package, source/privacy/no-stage, and accepted the browser evidence; it isolated only the recreate-proof weakness. Root focused final-candidate evidence independently passed `188/188`, twenty watcher repetitions, typecheck and lint. Reviewers otherwise accept every prior lifecycle, bounds, heartbeat, RPC, shutdown, production/dev, privacy, and browser disposition.
**Uncertainty / gaps:** The raw binding return must represent whether ownership is still bound after the destroyed check, and the server must require exact `true` before admission. The close latch must be installed before any coordinator code can invoke callbacks. Recreate retries must themselves perform an absent→present cycle. Browser rerun remains deferred until these final source changes stabilize.
**Recommended use:** Issue one fourth narrow fix attempt for these three items and adversarial tests, then run final gates and a fresh clean review.

## Handoff

**Source:** Pi subagents run `59180714-ed2c-424b-87c8-33f523c62064`, fresh sole `milestone-implementer` fourth fix writer; session `019fc9a7-1a26-7350-8e3a-e6248272305b`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/21153df3/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Correct the final three bounded third-review findings: terminal raw-binding admission, synchronous reentrant close latching, and genuine absent-to-present file-watch recreate proof.
**Outcome:** `bindRawSocket()` now returns true only while the exact raw socket remains live and bound after listener installation and destroyed-state recheck, and the upgrade server requires exact true before admission. `server.close()` now installs one deferred shared latch before any owner cleanup or coordinator code can reenter it, then one coordinator resolves or rejects that latch. Every recreate attempt and retry now removes any present file, asserts absence, and recreates it, with counters proving one observed absence per cycle. Focused adversarial tests cover already-destroyed and hostile synchronously-closing raw sockets, strict nested/outer close-promise identity with one cleanup coordinator, and the recreate cycle invariant. No production behavior outside those adjacent S6 control paths, browser product, protocol, dependency, plan, documentation, memory, release, or S7 surface changed.
**Evidence:** The implementer reports focused `50/50`, broader S6 `239/239`, exact non-real-Next `631/631`, literal `633/635` and real-Next `2/4` with only the permitted missing-`.next/BUILD_ID` copied-production parent/child, twenty isolated watcher repetitions, typecheck, lint, package, current-bin/real-`ws`, privacy/source/hash/no-stage gates, and fresh Chromium plus Firefox original-three-socket 65-second/two-sweep survival with restart recovery. Root verified HEAD `8f5ef8cc3a61c0f93b52c210f9b7343b736ca03d`, immutable blobs `195253256996ab4a35dfaa44f2e21175efbaf4d9` and `ce7244da61b274b70032e1217a9f3c8582ad19e3`, empty index, intended unstaged scope, exact `bound !== true` rejection, latch-before-coordinator construction, all three adversarial fixtures, clean whitespace, focused `50/50`, an independently selected broader S6 set `228/228`, typecheck, lint, and OAuth login as the sole production `EventSource`.
**Uncertainty / gaps:** Fresh copied-production execution remains release-owned because required manifests are absent. The root-selected broader test list differs from the implementer's ten-file count but passed every selected test; exact whole-suite evidence and browser/package claims still require final acceptance reconciliation. A fresh independent no-edit/no-delegation review remains mandatory. Nothing is staged, committed, or accepted.
**Recommended use:** Obtain one fresh final review of the complete S6 diff, concentrating on terminal binding truth, reentrant latch settlement/error semantics, recreate proof, and regression of every prior finding. Accept and commit S6 only on a clean verdict.

## Handoff

**Source:** Pi subagents parallel final-review run after fourth fix `59180714-ed2c-424b-87c8-33f523c62064`; two fresh no-edit/no-delegation `reviewer` children, architecture/security and validation/acceptance; outputs `final-s6-architecture-review.md` and `final-s6-validation-review.md` with recoverable run sessions in `.pi-subagents/`. Recorder: orchestration root. Kind: independent S6 fourth-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Recheck the complete S6 implementation and every prior finding, with decisive scrutiny of terminal raw binding, reentrant close latching, watcher recreate proof, and cumulative lifecycle/security regressions.
**Outcome:** Verdict remains `FINDINGS — DO NOT APPROVE`; two findings are accepted as bounded S6 defects and one reported finding is rejected by direct source evidence. Both reviewers accept the fourth-fix raw-binding truth and exact-true admission gate, shared close latch and one coordinator, consumed-authorization terminal matrix, owner/fallback shutdown model, global/session bounds, semantic idle, heartbeat, diagnostics, production/development lifecycle, and S7 containment. The architecture reviewer identifies a P0 same-process generation collision: a late unpublished generation-A wrapper is destroyed after generation B has published the same session ID as running, and A's unconditional ID-only false publication removes B's valid running projection. The validation reviewer identifies a test-honesty gap: watcher phase baselines are recorded without quiet/drain boundaries, so delayed prior OS work sampled in the next state can satisfy a later phase despite the mutation helper performing an actual recreate cycle. Its separate claim that idempotent running publications emit duplicate frames is false: `publishRunningSessionState()` computes `changed` and returns before publication when membership is unchanged, and the current deterministic projection test expects only the two actual membership transitions.
**Evidence:** Root reproduced the generation collision in an isolated real-module Node script: generation B was alive and running before late A resolution; A rejected `rpc_runtime_generation_closed` and disposed once, after which the global projection changed from `[sessionId]` to `[]` and emitted a false empty transition while B remained alive. Root verified unconditional `publishRunningSessionState(this.sessionId, false)` in `AgentSessionWrapper.destroy()`, ID-only global projection state, and the lack of an ownership token. Root also verified that file-watch production coalescing observes current filesystem state when the timer fires, while the fixture records each next baseline immediately after one qualifying frame without draining prior events. Reviewers report focused `179/179` and `239/239`, exact non-real-Next `631/631`, real-Next `2/4` with only the permitted missing-manifest pair, typecheck, lint, package, watcher `20/20`, source/privacy/hash/no-stage gates, and no final-fix defect in gateway/server close. Root directly read the duplicate-suppression branch and test expectations, disproving the no-op publication finding.
**Uncertainty / gaps:** Running ownership must be made publisher-identity-safe without changing the ID-only wire projection, initial/changed semantics, or HMR state. The watcher fixture needs causally honest quiet boundaries and a separately observed absent state before recreate, while retaining retries and unconditional teardown. Fresh copied production remains release-owned. No material divergence exists; both accepted findings are bounded under S6-VC-003/005/010 and revalidated ORCH-VC-002/007. Nothing is staged, committed, or accepted.
**Recommended use:** Issue one fifth narrow fix attempt for identity-safe running projection ownership plus the watcher phase-causality fixture only. Add an exact generation-A-late/generation-B-running same-ID regression and quiet/drain boundaries around every watcher phase; then repeat root verification and obtain another fresh independent review.

## Handoff

**Source:** Pi subagents run `1eb9be76-3998-4c6a-9e1a-bca60c7f1124`, fresh sole `milestone-implementer` fifth fix writer; recoverable transcript `.pi-subagents/artifacts/1eb9be76-3998-4c6a-9e1a-bca60c7f1124_milestone-implementer_transcript.jsonl` and session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/ea350e7d/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Resolve the confirmed same-ID cross-generation running-projection collision and make the real watcher phases causally honest, while preserving no-op suppression and every established S6/product boundary.
**Outcome:** Each wrapper now owns one opaque stable running-publication identity, with an HMR-stable process-global map from projected session ID to its current publisher. A current true publication may replace internal authority without an external membership transition; a false publication removes only its own or explicitly ownerless compatibility state; projection replacement and membership removal release publisher references. The external projection remains ID-only and duplicate membership publications remain suppressed. A real two-gateway test holds generation B running while late unpublished generation A resolves/rejects/disposes, proving A cannot remove B and B later settles/removes exactly once. The watcher fixture now reaches a successful quiet boundary before every new phase baseline, proves observed and filesystem absence before recreate, retains real absent-to-present retries, proves observed/filesystem presence before later modification, and preserves unconditional watcher teardown.
**Evidence:** The implementer reports focused generation/projection tests, broader `229/229`, exact non-real-Next `632/632`, literal `634/636` and real-Next `2/4` with only the permitted missing-manifest pair, watcher `20/20`, current-bin/real-`ws`, typecheck, lint, package, Chromium and Firefox original-three-socket two-sweep/restart, hashes/privacy/source/whitespace/no-stage gates. Root verified the exact three-file fifth-fix scope inside the cumulative diff, opaque identity calls on wrapper true/false publication, authority replacement/no-op/removal logic, current-projection reference reconciliation, the real A/B race assertions, every watcher quiet/state boundary, HEAD and immutable blobs, empty index, clean whitespace, focused race/projection `2/2`, watcher `20/20`, exact non-real-Next `632/632`, typecheck, lint, and package dry-run (9 files; 20,384-byte archive; 72,608 bytes unpacked).
**Uncertainty / gaps:** The ownerless path is intentionally limited to inherited compatibility state; current wrappers establish opaque authority on true publication. Filesystem notification timing remains OS-dependent, but phase acceptance now follows a successful quiet period and exact expected state rather than an immediately reused count. Fresh copied production remains release-owned because `.next/BUILD_ID` is absent. Fresh independent review remains mandatory; no source is accepted, staged, or committed.
**Recommended use:** Obtain a fresh no-edit/no-delegation review focused on authority replacement/removal under all wrapper races, bounded reference lifetime and HMR behavior, exact global changed semantics, and watcher phase causality. If clean, finish full root validation and accept/commit S6.

## Handoff

**Source:** Pi subagents parallel fifth-fix review; two fresh no-edit/no-delegation `reviewer` children with outputs `s6-fifth-fix-architecture-review.md` and `s6-fifth-fix-validation-review.md`, recoverable under `.pi-subagents/`. Recorder: orchestration root. Kind: independent S6 fifth-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Validate publisher-identity authority, exact global transition ordering, bounded reference/HMR behavior, watcher phase causality, and all cumulative S6 dispositions before acceptance.
**Outcome:** Validation reviewer returns `APPROVE` and accepts the real A/B race, identity-safe ordinary ownership, duplicate suppression, watcher quiet/state proof, all repeated gates, privacy, and scope. Architecture reviewer returns `FINDINGS — DO NOT APPROVE`; root accepts two reproduced P0 defects and one adjacent performance correction. First, replacing the process-global projection Set while preserving an ID currently clears all publisher authority; a later stale ownerful false then treats the member as ownerless and removes the current wrapper. Second, listener fanout receives a live Set view and nested publication mutates it before later listeners consume the outer transition, so an `a` edge can become two `a,b` frames. Adjacent authority reconciliation scans every publisher on every routine event publication. The watcher fix is accepted and needs no further change.
**Evidence:** Root independently reproduced both P0 traces through Jiti. For reentrant fanout, listener one published `b=true` while handling `a=true`; listener two observed `[[a,b],[a,b]]` rather than `[[a],[a,b]]`. For projection replacement, A published true, B replaced authority, the backing Set was replaced with a new Set preserving the ID, and stale A false emitted an empty frame and removed the still-current B ID. Root verified that `getRunningPublisherAuthority()` clears the complete map on projection identity change and otherwise iterates all entries on every call, while `createRunningSessionIdsView()` closes over the mutable Set and fanout has no nested-mutation barrier. Reviewers report focused `127/127`, `116/116`, lifecycle `23/23`, exact non-real-Next `632/632`, watcher `20/20`, typecheck, lint, package, hashes/privacy/source/no-stage gates; prior raw binding, close latch, authorization, owner, idle, heartbeat, bounds, shutdown, launcher, and S7 dispositions remain accepted.
**Uncertainty / gaps:** Production code does not ordinarily replace an existing global projection Set, but the S6 implementation explicitly introduced replacement reconciliation and the invariant must remain safe across process-global/HMR compatibility state. An old wrapper cannot ordinarily publish `true` once its generation guard retires it, yet the authority API should make supersession monotonic rather than relying on every caller ordering. Nested publication must be serialized without restoring an unbounded whole-list clone. Fresh copied production remains release-owned. No material divergence exists; these are bounded S6 global-status/runtime defects. Nothing is staged or accepted.
**Recommended use:** Issue one sixth narrow fix: retain publisher entries for IDs preserved across projection-object replacement; reject an identity once superseded; perform ordinary authority work by exact key rather than full-map scan; and serialize/defer nested projection mutations until each listener batch has consumed one stable transition. Add preserved-ID replacement, stale true/false, two-listener reentrant ordering, queue/reference cleanup, and no-routine-scan regressions, then repeat final review.

## Handoff

**Source:** Pi subagents run `3f3085eb-9048-4dbc-ade5-3a605ceb694d`, fresh sole `milestone-implementer` sixth fix writer; recoverable transcript `.pi-subagents/artifacts/3f3085eb-9048-4dbc-ade5-3a605ceb694d_milestone-implementer_transcript.jsonl` and session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/947c31c7/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Correct preserved-ID projection replacement, monotonic publisher supersession, reentrant transition ordering, and routine authority-scan cost without changing the accepted watcher or any other S6/product surface.
**Outcome:** Projection-object replacement now reconciles once and retains publisher entries for IDs still present while releasing absent entries. Superseded wrapper identities enter a process-global weak retirement set, so stale true or false cannot reclaim or remove the successor; ordinary current settlement does not retire the identity and the same wrapper may run again. A process-global transient dispatch queue defers nested mutations until a snapshotted listener batch consumes one stable live view, then drains FIFO iteratively; consumed/abandoned update and publisher references are cleared. Ordinary publication uses exact-key authority work, with full-map iteration only on actual projection identity replacement. Focused tests cover preserved and removed IDs, stale true/false, false-before-true, inherited ownerless state, current identity reuse, listener add/remove/throw/reentrancy, queue cleanup, and instrumented zero routine scans.
**Evidence:** The implementer reports new focused `6/6`, full RPC `107/107`, broader S6 `232/232`, watcher `20/20`, exact non-real-Next `635/635`, literal `637/639` and real-Next `2/4` with only the permitted missing-manifest pair, current-bin/real-`ws`, typecheck, lint, package, Chromium/Firefox original-three-socket two-sweep/restart, hashes/privacy/source/whitespace/no-stage gates. Root verified the two-file sixth-fix scope inside the cumulative diff, process-global weak retirement and dispatch declarations, reconciliation-only iteration, exact-key ordinary paths, listener snapshot and FIFO drain, queue clearing in `finally`, preserved no-clone ID-only views, focused authority/reentrant `10/10`, typecheck, lint, immutable hashes, empty index, and clean whitespace. Root reran the two prior reproductions: reentrant listener fanout now yields `[[a],[a,b]]` with an empty inactive queue; preserved-ID replacement plus stale A true/false retains B with no extra frame, and B settlement emits exactly one empty frame and releases the authority map.
**Uncertainty / gaps:** Synchronous callbacks are the running-subscription contract; a callback that retains a live view beyond its batch may observe later canonical state, while every listener inside the captured batch now receives the same stable transition. The weak retirement set does not strongly retain wrappers. Nested queue growth is bounded by synchronous internal publications and no browser/content data enters it. Fresh copied production remains release-owned. Fresh independent review remains mandatory; nothing is staged, committed, or accepted.
**Recommended use:** Obtain one final fresh no-edit/no-delegation review focused on weak retirement, projection reconciliation, FIFO fanout stability/reference cleanup, HMR/process-global compatibility, and all cumulative S6 contracts. Accept and commit only on a clean verdict.

## Handoff

**Source:** Pi subagents parallel sixth-fix review; two fresh no-edit/no-delegation `reviewer` children with outputs `s6-sixth-fix-architecture-review.md` and `s6-sixth-fix-validation-review.md`, recoverable under `.pi-subagents/`. Recorder: orchestration root. Kind: independent S6 sixth-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Challenge final publisher lineage, replacement reconciliation, FIFO fanout, reference lifetime, and every cumulative S6 contract before acceptance.
**Outcome:** Validation reviewer returns `APPROVE`; architecture reviewer returns `FINDINGS — DO NOT APPROVE`. Root accepts two bounded compatibility/authority defects. First, ordinary false deliberately makes one identity reusable but drops all last-owner lineage; after the empty gap, B can claim the ID and stale A can then publish true, supersede B, and suppress B's legitimate false. Second, projection-object replacement that removes an ID updates canonical state and authority lazily but emits no changed frame; a later re-add can therefore emit the same visible list twice. Both paths are inside compatibility behavior explicitly claimed and tested by S6 even though direct backing-Set replacement is not an ordinary product mutation. All ordinary wrapper-generation, watcher, authorization, owner, shutdown, idle, heartbeat, bounds, privacy, launcher, and S7 dispositions remain accepted.
**Evidence:** Root reproduced both traces through Jiti. `A:true, A:false, B:true, A:true, B:false` leaves the ID running under stale A, with frames `[id],[],[id]` and B weakly retired. During an `id:true` listener batch, replacing the projection with an empty Set and queuing `id:false` yields only `[id]` while the final canonical projection is empty; re-adding later produces a second `[id]` without the missing empty transition. Root verified ordinary false deletes current authority without retaining weak per-ID lineage, and projection-pointer reconciliation deletes absent authority but neither detects membership difference nor fans out the replacement state. Reviewers report focused `232/232`, exact `635/635`, real-Next `2/4` with only the permitted missing-manifest pair, typecheck, lint, package, whitespace/hash/no-stage gates; validation also reconfirmed watcher `20/20` and all other S6 evidence.
**Uncertainty / gaps:** A destroyed wrapper's runtime guard blocks ordinary stale true callbacks, but the exported identity authority and claimed HMR/ownerless compatibility require monotonic behavior across empty membership gaps. Last-owner lineage must not strongly retain wrappers or session IDs indefinitely; wrapper terminal destruction provides an explicit cleanup seam. Replacement membership changes are rare and may scan old/new Sets once, but routine publication must remain exact-key and clone/stringify-free. Fresh copied production remains release-owned. No material divergence exists and nothing is staged or accepted.
**Recommended use:** Issue one seventh narrow fix: retain weak per-ID last-owner lineage across ordinary false gaps, retire the previous lineage when a different identity claims, and explicitly retire/clear lineage on wrapper destruction using a terminal queued update safe under reentrant fanout. On projection-object replacement, compare old/new membership once and emit the replacement canonical state as its own FIFO transition before the triggering update, including replacement during a listener batch. Add gap, terminal destroy, removed/re-add, during/between-batch, no-duplicate/no-lost-frame, cleanup, and routine-cost tests, then obtain fresh review.

## Handoff

**Source:** Pi subagents run `8168dbc2-dcff-415a-a3be-ed8c323819cf`, fresh sole `milestone-implementer` seventh fix writer; session `019fc9dc-1b93-7dec-9267-a853d515d8f4`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/d190f061/run-0/session.jsonl`; transcript `.pi-subagents/artifacts/8168dbc2-dcff-415a-a3be-ed8c323819cf_milestone-implementer_transcript.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Make publisher supersession monotonic across ordinary empty gaps, add reentrancy-safe terminal destruction cleanup, and publish exact projection-replacement membership transitions.
**Outcome:** A process-global exact-ID lineage map now stores only `WeakRef` publisher identities. Ordinary false releases strong authority but preserves weak latest-owner lineage; a different later owner retires that identity even across an empty gap, while the same live identity can run again. Wrapper destruction enqueues a terminal update after destruction callbacks, removes only that identity's current or compatible ownerless membership, preserves successors, retires the destroyed identity after applying its edge, and removes its lineage key only when still latest. Projection-pointer replacement compares old/new membership once, reconciles strong authority and weak lineage, and emits a changed replacement state as its own stable FIFO batch before triggering or nested updates; equal replacement emits nothing. Routine updates remain exact-key and clone/sort/stringify-free.
**Evidence:** The implementer reports full RPC `112/112`, broader S6 `248/248`, watcher `20/20`, exact non-real-Next `640/640`, literal `642/644` and real-Next `2/4` with only the permitted missing-manifest pair, typecheck, lint, package, current-bin/real-`ws`, separate Firefox and Chromium original-three-socket two-sweep/restart, hashes/privacy/source/whitespace/no-stage gates. Root verified the two-file seventh-fix scope inside the cumulative diff, weak lineage declarations and exact-key helpers, terminal update ordering from `AgentSessionWrapper.destroy()`, successor-safe terminal conditions, replacement membership comparison and transition loop, listener-stable Set capture, queue clearing, focused authority/replacement `11/11`, typecheck, lint, immutable hashes, empty index, and clean whitespace. Root reran the prior traces: the empty-gap sequence now leaves B settled and A retired with exact `[id],[],[id],[]`; replacement removal during fanout now emits `[id],[]`, and re-add emits `[id],[],[id]` rather than duplicate visible states.
**Uncertainty / gaps:** Weak collection itself is nondeterministic; tests prove structural `WeakRef` storage, release of strong active authority, and exact lineage-key removal on terminal destruction rather than forcing GC. Direct backing-Set replacement remains a rare compatibility path but now has deterministic canonical transition ordering. Fresh copied production remains release-owned. Fresh independent review remains mandatory; nothing is staged, committed, or accepted.
**Recommended use:** Obtain a fresh final no-edit/no-delegation review focused on weak lineage/terminal cleanup lifetime, replacement transitions under reentrancy, exact external ordering, and all cumulative S6 contracts. If clean, complete root validation and accept/commit S6.

## Handoff

**Source:** Pi subagents parallel seventh-fix review; two fresh no-edit/no-delegation `reviewer` children with outputs `s6-seventh-fix-architecture-review.md` and `s6-seventh-fix-validation-review.md`, recoverable under `.pi-subagents/`. Recorder: orchestration root. Kind: independent S6 seventh-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Validate weak lineage, terminal cleanup, projection replacement ordering and availability, exact global state, and every cumulative S6 contract.
**Outcome:** Both reviewers return `FINDINGS — DO NOT APPROVE`; root accepts two bounded compatibility defects. First, `publishPendingRunningProjectionReplacements()` has an unbounded loop: a listener that replaces the projection on every replacement notification can make one synchronous publication never return. Second, remove/re-add projection replacement currently drops both strong authority and weak lineage for the re-added ID; terminal cleanup permits removal when neither exists, so a never-published or retired same-ID wrapper can erase ownerless membership while the current B wrapper remains running. All ordinary weak-gap lineage, terminal serialization, successor protection, exact replacement transitions, tests, and previous S6 fixes are otherwise accepted.
**Evidence:** Architecture review demonstrated 10,000 alternating replacements in one synchronous call and showed removing its artificial limit would not return. Root verified the unbounded `for (;;)` and one-shot guard in existing replacement tests. Validation review reproduced real same-ID wrappers: B alone held a prompt run; replacement removed then re-added B's ID and reconciliation made it ownerless; never-published A destruction removed the ID while B stayed `isRunning() === true`. Root independently reproduced the same trace, observing frames `[B],[],[B],[]` and an empty final projection with B active. The cause is introduced-ID lineage deletion plus terminal `mayRemove` treating complete lack of provenance as permission. Reviewers otherwise report focused `222/222` and `237/237`, exact non-real-Next `640/640`, literal/real-Next only the permitted missing-manifest pair, watcher `20/20`, typecheck, lint, package, hashes/privacy/whitespace/no-stage gates.
**Uncertainty / gaps:** Direct projection replacement is not ordinary product mutation, but S6 now explicitly supplies this compatibility path; it must fail boundedly and preserve live provenance rather than blocking or authorizing arbitrary terminal deletion. A direct mutating listener is internal, not remotely supplied, yet synchronous nontermination violates server availability. Fresh copied production remains release-owned. No material divergence exists; nothing is staged or accepted.
**Recommended use:** Issue one eighth narrow fix. Bound/contain listener-driven replacement oscillation, preferably isolating any listener that directly changes the projection and retaining a finite per-dispatch replacement budget with content-free fail-closed cleanup. For IDs re-added by replacement, restore a live non-retired weak latest publisher as provenance when available; never let terminal cleanup remove ownerless membership without a positive exact current or latest identity match. Add never-published/retired A versus running B remove/re-add tests, B terminal cleanup, alternating replacement bounded-return/queue cleanup, and routine-cost regressions, then obtain fresh review.

## Handoff

**Source:** Pi subagents run `c2b2d265-0d10-4189-b692-4642c4eb76c7`, fresh sole `milestone-implementer` eighth fix writer; session `019fc9f4-9246-7d3a-adaf-0123e8448b7b`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/85aeda6f/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Bound listener-driven replacement compatibility and require positive publisher provenance for terminal cleanup after projection remove/re-add.
**Outcome:** A listener that directly changes the process-global projection pointer is now removed by exact function identity after completing its captured stable batch, while the one canonical replacement it caused is still reconciled and published. Each dispatch also has a finite 32-replacement transition budget; exhaustion adopts/reconciles the latest pointer, suppresses further compatibility callbacks, abandons queued updates fail-closed, and returns through existing reference-clearing `finally` without throwing into agent work. Ordinary nested publications remain subscribed and FIFO. Reintroduced IDs restore active authority only from an exact live non-retired weak latest publisher; otherwise they remain ownerless. Terminal cleanup now removes membership only on positive equality with current strong authority or weak latest lineage, so absence of provenance never authorizes an arbitrary terminal.
**Evidence:** The implementer reports focused `4/4`, full RPC `115/115`, broader S6 `251/251`, watcher `20/20`, exact non-real-Next `643/643`, literal `645/647` and real-Next `2/4` with only the permitted missing-manifest pair, current-bin/real-`ws`, typecheck, lint, package, separate Chromium and Firefox original-three-socket two-sweep/restart, hashes/privacy/source/whitespace/no-stage gates. Root verified the two-file eighth-fix scope inside the cumulative diff, exact listener mutation detection/isolation, finite constant and exhaustion branch, queue clearing, non-mutating nested-publication distinction, live/non-retired provenance restoration, positive-only terminal predicate, focused projection/provenance `12/12`, typecheck, lint, immutable hashes, empty index, and clean whitespace.
**Uncertainty / gaps:** Budget exhaustion is an intentionally fail-closed internal-compatibility condition: the final canonical pointer may be newer than the last delivered frame and queued nested updates are abandoned, but each later dispatch is independently finite and ordinary product listeners never mutate the projection pointer. The next uninvoked chained mutator can remain subscribed until a later bounded dispatch. Fresh copied production remains release-owned. Fresh independent review remains mandatory; nothing is staged, committed, or accepted.
**Recommended use:** Obtain a fresh final no-edit/no-delegation review focused on oscillation availability, mutator isolation, budget cleanup, remove/re-add provenance, terminal positive authority, and every cumulative S6 contract. If clean, complete root validation and accept/commit S6.

## Handoff

**Source:** Pi subagents parallel eighth-fix review; two fresh no-edit/no-delegation `reviewer` children with outputs `s6-eighth-fix-architecture-review.md` and `s6-eighth-fix-validation-review.md`, recoverable under `.pi-subagents/`. Recorder: orchestration root. Kind: independent S6 eighth-fix review. Governing master and approved milestone as above; Mode: review.
**Purpose:** Validate compatibility failure containment, semantic-update preservation, provenance safety, exact wrapper disposal, and every cumulative S6 contract.
**Outcome:** Both reviewers return `FINDINGS — DO NOT APPROVE`; root accepts three bounded defects. First, replacement-budget exhaustion currently abandons all queued semantic running updates, so a legitimate nested `real-running=true` behind a mutator chain is lost. Second, a direct mutator can install a hostile Set whose membership operations throw; reconciliation propagates into wrapper prompt admission, leaves the hostile pointer canonical, and poisons later dispatches. Third, `AgentSessionWrapper.destroy()` invokes native unsubscribe without isolation after setting `destroying=true`; a throw leaves the wrapper alive, undisposed, unobservable by terminal cleanup, and permanently immune to retry, while generation shutdown can stop before later wrappers and registry/lock cleanup. Provenance restoration and positive-only terminal cleanup themselves are accepted.
**Evidence:** Validation review reproduced the chain case: the call returned boundedly but the queued live ID, strong authority, and lineage were all absent. Architecture review reproduced hostile replacement causing prompt rejection before native dispatch and repeated later publication failure despite queue-state reset. It also reproduced a throwing native unsubscribe leaving `alive:true`, `disposeCalls:0`, no destruction observer, and a no-op second destroy. Root verified the exhaustion break plus final queue clear, uncontained reconciliation/apply exceptions against the retained global pointer, unguarded unsubscribe call, and unisolated generation destroy loop. Reviewers otherwise report focused `191/191` and `138/138`, exact non-real-Next `643/643`, real-Next only the permitted missing-manifest pair, watcher `20/20`, typecheck, lint, package, hashes/privacy/whitespace/no-stage gates; all other cumulative fixes remain accepted.
**Uncertainty / gaps:** Projection-pointer mutation remains internal compatibility, but it must neither throw into agent work nor permanently poison canonical state. A finite fallback may suppress malicious replacement callbacks, but accepted semantic state/terminal updates must still drain. Native unsubscribe is SDK-supplied and therefore an external cleanup boundary even if normal implementations do not throw. Fresh copied production remains release-owned. No material divergence exists; nothing is staged or accepted.
**Recommended use:** Issue one ninth narrow fix: quarantine an offending direct mutator together with listeners it synchronously installs; on replacement-budget exhaustion suppress further compatibility callbacks but continue draining semantic/terminal FIFO updates. Contain hostile replacement/reconciliation failures, restore the last trusted canonical projection, clear hostile state/references, and recover automatically without throwing into wrappers. Make destroy and generation shutdown converge despite throwing unsubscribe/cleanup stages, then add chain-plus-live-update, hostile Set/map recovery, prompt survival, throwing-unsubscribe exact disposal, multi-wrapper shutdown continuation, and cleanup tests before fresh review.

## Handoff

**Source:** Orchestration-root audit after the user paused ninth fix run `6033bdf2-d7fd-40ba-8771-ece7fbb98da9`; the run is paused before implementation after an initial checkpoint-read failure. Recorder: orchestration root. Kind: material-boundary reconciliation. Governing master and approved S6 milestone as above; Mode: review.
**Purpose:** Determine why the sequential fix loop continued, distinguish supported product/HMR behavior from synthetic internal compatibility, and prevent further source work until the acceptance surface is explicit.
**Outcome:** The root finds that original S6 plus fixes one through five address supported lifecycle/security/validation paths and should remain. Fixes six through eight increasingly harden arbitrary reassignment of `globalThis.__piRunningSessionIds` and listener mutation of that backing global. Production assigns the Set only on first initialization and its sole production subscribers are global-status channel callbacks that encode/send without mutating or republishing running state. The approved master requires one HMR-stable global projection, not arbitrary replacement by subscribers. Treating the synthetic replacement path as mandatory expanded the state machine into weak lineage, replacement reconciliation, mutator quarantine, and budget semantics, which then generated new defects. This was orchestration drift; the root should have challenged the premise after fix five. The ninth review also found one separate supported defect—throwing SDK unsubscribe can strand wrapper and generation cleanup—which remains unfixed and is small enough to retain.
**Evidence:** Completed writers are original implementation `9b414848-d85a-44fc-bff0-8df79c310c03` plus fixes `a067949b-0329-4eb6-8cd0-82baab92ff53`, `aebe73af-f16e-4bc8-b2d2-1bd8cdb76d0a`, `8d6eab84-e3aa-4518-a1c4-2c359c796367`, `59180714-ed2c-424b-87c8-33f523c62064`, `1eb9be76-3998-4c6a-9e1a-bca60c7f1124`, `3f3085eb-9048-4dbc-ade5-3a605ceb694d`, `8168dbc2-dcff-415a-a3be-ed8c323819cf`, and `c2b2d265-0d10-4189-b692-4642c4eb76c7`. Production source search finds no reassignment of `__piRunningSessionIds` after initialization; replacement and mutating-listener uses are tests. The actual same-process A/B generation collision from fix five remains reproduced and requires only active publisher identity, not replacement/ownerless compatibility. Current S6 is uncommitted and unstaged; S5 remains the accepted boundary.
**Uncertainty / gaps:** The user must decide whether to clarify unsupported internal behavior and authorize simplification, continue arbitrary replacement hardening, or stop S6 for broader replanning. Simplification would retain per-wrapper identity-safe active ownership, ordinary initial/changed semantics, bounded global output, and all earlier lifecycle fixes; remove synthetic replacement/lineage/budget machinery and tests; and fix throwing unsubscribe/generation cleanup. Fresh copied production remains release-owned.
**Recommended use:** Do not resume a writer until the user selects the acceptance boundary. The root recommends the simplification path and a final review limited to product-reachable or explicitly master-required behavior.

## Handoff

**Source:** Explicit user direction in the orchestration session after the material-boundary report. Recorder: orchestration root. Kind: human scope clarification and waiver. Governing master and approved S6 milestone as above; Mode: review.
**Purpose:** Freeze the supported S6 acceptance surface and decide whether the throwing SDK-unsubscribe hardening remains required.
**Outcome:** The user directs the root to retain original S6 plus fixes one through five and discard fixes six through eight's synthetic running-projection compatibility machinery. Arbitrary replacement/corruption of `globalThis.__piRunningSessionIds`, projection-mutating subscribers, stale retired `running=true` calls, ownerless remove/re-add provenance, and recovery after those mutations are unsupported and waived. The user also accepts the existing SDK unsubscribe behavior and waives the newly proposed throwing-unsubscribe/generation-cleanup hardening as a low-probability robustness edge case. Pi Web must continue to call the unsubscribe function supplied by `AgentSession.subscribe()`; the waiver applies only if that SDK callback itself throws.
**Evidence:** The production SDK supplies the unsubscribe closure when `AgentSessionWrapper.start()` calls `inner.subscribe(...)`; Pi Web stores and invokes it during destruction. No observed provider or product failure shows that callback throwing, and the behavior predates this WebSocket migration. The reproduced failure required an injected hostile unsubscribe. The real fix-five A/B generation collision, ordinary initial/changed running state, bounded output, HMR-stable unchanged global Set, and all fixes one through five remain required.
**Uncertainty / gaps:** If the SDK unsubscribe callback unexpectedly throws, one wrapper's destruction and same-process generation cleanup may be incomplete until process exit; this is an accepted residual risk, not a claim that unsubscribe is optional. Fresh copied production remains release-owned. S7 remains untouched.
**Recommended use:** Perform one rollback-only source pass that removes fixes six through eight and their synthetic tests, restores the minimal active-publisher fix-five model without arbitrary projection replacement, preserves every earlier fix, and makes no unsubscribe hardening change. Validate the resulting cutoff and report any product-reachable defect to the user rather than entering another automatic fix loop.

## Handoff

**Source:** Pi subagents rollback run `bafbb6b0-cb1b-46b5-90a8-a12ce3c9f406`, fresh sole `milestone-implementer`; session `019fca43-f0c5-7b39-8e9e-beabfb164c6d`; recoverable session file `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-orchestration-2026-07-30-persistent-stream-websocket-migration--/2026-07-31T06-15-57-590Z_019fb6d0-af96-7b6f-b3af-d5ad71e149e2/b3423043/run-0/session.jsonl`. Recorder: orchestration root. Kind: milestone rollback/fix handoff. Governing master and approved S6 milestone as above; Mode: fix.
**Purpose:** Apply the human-authorized fix-five cutoff by removing synthetic running-projection compatibility while preserving every required original S6 and fix-one-through-five behavior.
**Outcome:** Only `lib/rpc-manager.ts` and `lib/rpc-manager.test.mjs` changed. The runtime now retains one opaque identity per wrapper and one exact-key active-publisher map. Ownerful true claims/replaces active ownership; ownerful false/destruction removes only the exact current owner; ownerless false cannot remove an active owner; membership no-ops do not emit. The process-global Set remains one unchanged HMR-stable object with a read-only façade. Weak lineage, retired identities, projection-pointer reconciliation, generated replacement transitions, dispatch queue, mutator quarantine, replacement budget, terminal compatibility update, and eleven synthetic tests were removed. The SDK unsubscribe call remains unchanged under the explicit waiver. The authentic late-A/current-B race, inherited ownerless unchanged-Set behavior, ordinary settle/rerun/destroy, façade immutability, and failure-isolated synchronous fanout remain covered.
**Evidence:** The implementer reports focused `4/4`, RPC `104/104`, broader S6 `241/241`, watcher `5/5`, exact non-real-Next `633/633`, literal `635/637` and real-Next `2/4` with only the permitted missing-manifest pair, typecheck, lint, package, synthetic-symbol/test absence, exact-key hot-path, production single-initialization assignment, hashes/privacy/source/whitespace/no-stage gates. Root verified exact HEAD and immutable blobs, empty index, clean whitespace, removed synthetic symbols, minimal exact-key publisher logic, unchanged unisolated unsubscribe, focused cutoff `4/4`, typecheck, and lint. All non-RPC S6 files and fix-five watcher evidence remain unchanged.
**Uncertainty / gaps:** Recursive running publication and backing-Set replacement/corruption are intentionally unsupported. If the SDK unsubscribe callback throws, wrapper/generation cleanup may be incomplete; the user accepts this residual. Fresh copied production remains release-owned. Browser evidence is reused from the fix-five checkpoint. S7 remains untouched.
**Recommended use:** Obtain one final independent read-only containment review limited to product-reachable and master-required behavior. Its job is to verify the rollback boundary and preservation of fixes one through five, not to reopen waived synthetic globals. Report any supported-path blocker to the user without an automatic fix loop; otherwise accept and commit S6.

## Handoff

**Source:** Pi subagents fresh final containment review, output `s6-final-containment-review.md` under `.pi-subagents/artifacts/outputs/0f627de6/`; recoverable reviewer session through the root subagent run. Recorder: orchestration root. Kind: independent final S6 review. Governing master and approved S6 milestone as above; Mode: review.
**Purpose:** Verify the human-authorized fix-five cutoff, preservation of every supported original S6 and fix-one-through-five obligation, and absence of synthetic fix-six-through-eight machinery without reopening waived internal-global behavior.
**Outcome:** Verdict `APPROVE`. The reviewer confirms minimal exact-key active publisher ownership, authentic late-A/current-B safety, ordinary settle/rerun/destroy and ownerless unchanged-Set behavior, immutable façade, production single-initialization/global-channel subscriber boundaries, and removal of weak lineage, replacement reconciliation, queue, mutator, and budget machinery. It independently accepts all cumulative semantic-idle, generation, authorization, admission, owner, backpressure, heartbeat, shutdown, launcher, privacy, file-watch, and S7-containment dispositions. No supported-path blocker remains.
**Evidence:** Independent focused S6 `230/230` plus gateway `11/11` (`241/241` combined), exact non-real-Next `633/633` across 55 files, real-Next `2/4` with only the documented missing-`.next/BUILD_ID` child/parent, typecheck, lint, package dry-run (9 files, 20,384 bytes packed, 72,608 unpacked), whitespace, hashes, empty index, synthetic-symbol absence, and production assignment/subscriber inventory all passed. Root previously verified the cutoff diff, focused `4/4`, typecheck, lint, exact immutable blobs, empty index, and clean whitespace. Browser evidence remains recoverable from fix five.
**Uncertainty / gaps:** The user accepts that a throwing SDK unsubscribe may interrupt wrapper/generation cleanup; normal unsubscribe remains invoked. Arbitrary backing-Set replacement/corruption and synthetic recursive subscribers remain unsupported. Fresh copied-production inclusion remains release-owned. S7 scale, capability/visual validation, documentation, user acceptance, full-master validation, and closeout remain pending.
**Recommended use:** Accept S6 under the explicit human boundary, commit the coherent implementation and checkpoint evidence, then record finality. Honor the user's pause and do not start S7 automatically.

## Implementation Summary

**Plan section:** Approved S6 lifecycle, security, backpressure, heartbeat, generation ownership, and owned-shutdown implementation sections; S6-VC-001 through S6-VC-010.
**Work and outcome:** Implemented exact 30-minute semantic idle and active-state protection; generation-scoped RPC ownership/start/binding/hosted guards; bounded global output; uniform running/session/file/RPC owner teardown; heartbeat terminal-pending behavior; exact direct-peer admission and consumed-authorization settlement; retained active/retired HMR ownership; one reentrant-safe absolute-deadline server close coordinator; launcher-only signal ownership; privacy-safe diagnostics; and robust file-watch lifecycle evidence. Corrected every supported fix-one-through-five finding. Final containment retains only minimal per-wrapper active running ownership for the reproduced late-generation race and removes synthetic projection-replacement hardening.
**Validation / evidence:** Root and independent evidence includes focused S6 `241/241`, exact non-real-Next `633/633`, literal `635/637` and separate real-Next `2/4` with only the permitted absent copied-production manifest pair, typecheck, lint, package dry-run, current-bin/real-`ws`, watcher repetitions, sanitized Chromium/Firefox original-three-socket two-sweep/restart evidence, immutable hashes, privacy/source inventory, whitespace, and no-stage checks. Final independent verdict: `APPROVE`.
**Departures from approved obligations:** User-authorized boundary: arbitrary replacement/corruption of the internal running projection and synthetic recursive subscribers are unsupported rather than hardened. The user also accepts the residual that an SDK unsubscribe callback which itself throws may interrupt wrapper/generation cleanup; normal unsubscribe remains required and invoked. Fresh copied-production inclusion is release-owned because manifests are absent. No product protocol, browser ownership, dependency, Next-private cleanup, S7, or other scope departure occurred.
**Implementation commit:** Pending.
