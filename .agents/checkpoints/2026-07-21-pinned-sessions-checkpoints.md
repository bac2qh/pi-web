# Pinned, Recent, and Hidden Sessions Sidebar — Checkpoints

Plan: `.agents/plans/2026-07-21-pinned-sessions.md`
Worktree: `/Users/xin/Documents/repos/pi-web/.agents/worktrees/2026-07-21-pinned-sessions`
Branch: `2026-07-21-pinned-sessions`
Implementation base: `3c6d242e117e`

## Handoff

**Source:** Async run `ea0939cc-40f9-4385-8168-e0c0e5bcfbf7`, child 0, `context-builder` (frontend/UI reconnaissance); recoverable session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-21-pinned-sessions--/2026-07-29T19-53-54-520Z_019faf70-d258-7ae5-86da-a0c581378e41/fabc966c/run-0/session.jsonl`.
**Purpose:** Map the existing sidebar, navigation, status, worktree, Explorer, layout, accessibility, and test seams before source mutation.
**Outcome:** Keep runtime/session loading, SSE generation protection, unread state, cwd/worktree synchronization, and Explorer ownership in `SessionSidebar`; replace component-local graph/sort logic with the client-safe helper; reuse one session row for flat global and recursive Project presentations; route every global selection through the existing cwd-aware click path; split only the title/New/Refresh/Show-hidden controls into the fixed header and move unchanged project/worktree controls into Project. Pinned and Recent need independently collapsible, shrinkable, approximately five-row scroll regions while Project retains a minimum; Explorer must default collapsed. Existing hover-only rows are not keyboard/touch accessible, so selection and action controls need real focus semantics and focus/touch-visible actions.
**Evidence:** Decisive local seams are `components/SessionSidebar.tsx` session loading and SSE handling, cwd/worktree synchronization, combined header, session/Explorer flex split, recursive row surface, and `components/AppShell.tsx` selection/cwd callbacks; existing `components/SessionSidebar.test.mjs` protects stale-load and discovery behavior. The child identified the current direct-parent cycle failure and the 260 px action-crowding risk. The parent independently read these sources and confirmed the findings before implementation.
**Uncertainty / gaps:** No DOM harness exists; responsive/touch/cross-project behavior needs browser evidence. Deeply indented 260 px rows are geometrically constrained, so compact focus/touch-visible actions must be verified rather than replacing them with an unapproved overflow menu. Cross-project global selection must be runtime-tested because `AppShell` can clear a mismatched selection if cwd synchronization drifts.
**Recommended use:** Implement domain derivation first, then metadata loading/queue, shared row, global sections, header split, and responsive layout; preserve the existing load/SSE/cwd callbacks and validate desktop/mobile plus cross-project/worktree flows.

## Handoff

**Source:** Async run `ea0939cc-40f9-4385-8168-e0c0e5bcfbf7`, child 1, `context-builder` (domain/persistence/tests/docs reconnaissance); recoverable session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-21-pinned-sessions--/2026-07-29T19-53-54-520Z_019faf70-d258-7ae5-86da-a0c581378e41/fabc966c/run-1/session.jsonl`.
**Purpose:** Establish pure-helper, state-store, API, diagnostics, test, and maintained-documentation contracts.
**Outcome:** Use strict versioned validation with bounded unique opaque IDs; preserve newest-first pin order and idempotent retries; build hidden closure from raw global sessions with cycle guards; include the exact ten-day equality boundary and first excluded millisecond; sort Project families by maximum visible-descendant activity with deterministic tie-breaks; serialize operation-only writes under an exclusive lock and same-directory atomic rename; refuse malformed/unsupported state byte-for-byte; and reconcile stale IDs only from a successful `listAllSessions()` result. Keep metadata errors isolated from ordinary session browsing and use Refresh—not polling or SSE—for cross-client convergence.
**Evidence:** `lib/session-reader.ts` supplies the complete cached global listing, resolved parent IDs, `projectRoot`, and worktree metadata; `lib/types.ts` already contains the needed native `SessionInfo` fields; existing agent-directory JSON handling is intentionally not reusable because it silently defaults malformed input and writes directly. Repository tests use Node’s runner plus Jiti and temporary directories. Documentation seams are the feature/data/file-map sections in `README.md`, `README.zh-CN.md`, and `AGENTS.md`.
**Uncertainty / gaps:** The plan leaves numeric ID/file/lock bounds and listing-failure availability as implementation judgments. The chosen implementation uses named conservative bounds and fails only the metadata GET/PATCH when a complete listing is unavailable, leaving `/api/sessions` and ordinary browsing independent. The API returns the authoritative state directly rather than wrapping it; the plan fixes semantics, not a wire wrapper. Browser/two-client validation remains required.
**Recommended use:** Test pure state/graph/time/prefix/tree behavior and temporary-directory persistence before UI integration; keep API errors stable and sanitized; add source-level sidebar assertions only where no DOM harness exists; update all three maintained docs after behavior is validated.

## Handoff

**Source:** Async run `a331f586-b52e-47e9-93e2-e6be7800b85c`, three `reviewer` children; recoverable sessions `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-21-pinned-sessions--/2026-07-29T19-53-54-520Z_019faf70-d258-7ae5-86da-a0c581378e41/c77fbf6a/run-0/session.jsonl`, `run-1/session.jsonl`, and `run-2/session.jsonl` under the same directory.
**Purpose:** Adversarial review of domain correctness, persistence races and bounds, frontend lifecycle/layout/accessibility, diagnostics, documentation, and required validation.
**Outcome:** The review found a stale-scan reconciliation race, missing background-completion activity refresh, request-start ordering that could discard a newer authoritative revision, asymmetric read/write byte bounds, insufficient diagnostic assertions, short-mobile Project sizing, hybrid-touch action visibility, weak accessibility source assertions, and absent browser evidence. The implementation was corrected with generation-current list retries and a lock-time generation recheck; background running-to-idle refresh; revision-based authoritative acceptance; a pre-write byte limit; privacy-focused diagnostics tests; viewport-relative Project sizing; `(any-pointer: coarse)` action visibility; stronger semantic assertions; and the browser matrix. A suggestion to automatically delete a lock after owner death was rejected because process-local PID evidence cannot prove that another host sharing the agent directory is dead; bounded timeout and operator cleanup avoid deleting a live writer's lock.
**Evidence:** Fixes and regressions are in `lib/session-reader.ts`, `lib/sidebar-state-store.ts`, `components/SessionSidebar.tsx`, `app/globals.css`, and their focused tests. The full post-fix browser and static evidence is retained under `.agents/reports/2026-07-21-pinned-sessions/`.
**Uncertainty / gaps:** Generation freshness remains process-local and therefore does not coordinate destructive stale-ID reconciliation between multiple independent Pi Web server processes sharing one agent directory. A process that exits while holding the metadata lock can leave a lock requiring operator cleanup; automatic reclamation remains intentionally excluded as unsafe without a cross-host lease protocol.
**Recommended use:** Treat the process-local generation and crash-stranded-lock cases as documented residual operational risks, not reasons to weaken serialization or add unsafe stale-lock deletion.

## Handoff

**Source:** Async run `ed753e0e-bd6f-464a-9a55-160f4383e039`, two `reviewer` children; recoverable sessions `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-21-pinned-sessions--/2026-07-29T19-53-54-520Z_019faf70-d258-7ae5-86da-a0c581378e41/3fd7e9e7/run-0/session.jsonl` and `run-1/session.jsonl` under the same directory.
**Purpose:** Re-review the corrected domain/store and frontend surfaces against the approved plan and the first browser report.
**Outcome:** Reviewers confirmed the generation race, background activity, revision authority, byte bounds, diagnostic privacy, interactive markup, touch media query, short-height sizing, hidden initial selection, and maintained documentation were corrected, with no further source fix recommended. The frontend reviewer requested stronger evidence than the synthetic running-state fixture for hide safety and actual touch activation. The parent then ran a real in-process AgentSession against a delayed local OpenAI-compatible fixture: touch input hid the genuinely streaming selected session, its wrapper remained streaming, its JSONL digest did not change during Hide, Show hidden revealed running indicators, and the prompt settled normally after release with no fallback click.
**Evidence:** `.agents/reports/2026-07-21-pinned-sessions/live-validation.json` records `genuineStreamingBeforeHide`, `genuineStreamingAfterHide`, normal settlement, unchanged JSONL, preserved selection, two revealed running rows, and `touchPointerInputActivatedHide`, all passing. `.agents/reports/2026-07-21-pinned-sessions/live-running-touch.png` captures the revealed live state.
**Uncertainty / gaps:** Headless Firefox did not report document focus in the general matrix, so focus evidence combines active-element traversal with the compiled 2 px focus rule. The live validation removed the separate touch uncertainty. Multi-process generation freshness remains the only material architecture risk.
**Recommended use:** Accept the focused review as resolved; retain the reports and screenshot as the independent evidence for the previously identified runtime/touch gap.

## Implementation Summary

**Plan section:** 1. Derive all sidebar presentations from shared pure helpers.
**Work and outcome:** Added `lib/sidebar-session-state.ts` with strict versioned state/operation parsing, idempotent optimistic reduction, cycle-safe raw-session graph and hidden closure, redundant marker canonicalization, exact inclusive ten-day Recent derivation and next-expiry scheduling, shortest-unique project suffixes, and deterministic newest-visible-descendant Project trees. Pinned, Recent, and Project now derive hidden-first from one model while Show hidden classifies explicit and inherited rows.
**Validation / evidence:** `lib/sidebar-session-state.test.mjs` covers strict bounds, pin order and replay, descendant/future-fork/cycle behavior, stale reconciliation, exact Recent boundaries and uncapped ordering, prefix collisions, and family promotion. The final full run passed all 160 tests; exact output is retained in `.agents/reports/2026-07-21-pinned-sessions/automated-validation.txt`.
**Departures from approved obligations:** None.
**Implementation commit:** Pending.

## Implementation Summary

**Plan section:** 2. Persist shared state through one operation API.
**Work and outcome:** Added the server-only `lib/sidebar-state-store.ts` and `GET/PATCH /api/sidebar-state`. Shared metadata lives only in `pi-web-sidebar.json`; mutations are operation-only, serialized by a bounded exclusive lock, reread and validated under lock, bounded before write, and published by same-directory atomic rename. Malformed or unsupported state is preserved. Generation-current global listings and a post-lock generation check prevent stale scans from pruning newer metadata.
**Validation / evidence:** Temporary-directory tests cover defaults, revisions/no-ops, concurrent operations, malformed preservation, lock timeout, successful-only reconciliation, sanitized error categories, invalidated scans, byte-limit symmetry, and ancestor canonicalization. `lib/session-reader.test.mjs` covers invalidated in-flight scan retry. Review run `ed753e0e-bd6f-464a-9a55-160f4383e039` found no remaining focused domain fix.
**Departures from approved obligations:** None. Crash-stranded locks intentionally time out rather than being auto-deleted because safe cross-host ownership proof is outside the approved design.
**Implementation commit:** Pending.

## Implementation Summary

**Plan section:** 3. Recompose the existing sidebar without changing session runtime behavior.
**Work and outcome:** Refactored `SessionSidebar` into fixed global controls followed by independently collapsible and scrollable Pinned and Recent sections, a selected Project tree ordered by visible descendant activity, and an initially collapsed Explorer. One row implementation now serves flat and recursive presentations with shared selection, cwd/worktree synchronization, running/unread state, rename/delete, pin, hide/restore, hidden classification, keyboard semantics, focus styling, and touch-visible actions. Optimistic metadata operations are queued and acknowledged in order while authoritative revisions govern overlapping responses; metadata errors remain non-blocking. Background completions refresh activity-derived sections.
**Validation / evidence:** `.agents/reports/2026-07-21-pinned-sessions/validation.json` and the four desktop/mobile screenshots demonstrate ordered sections, independent overflow, short-height shrinking, visible Explorer at 640×360, explicit/inherited restore semantics, two-client concurrent pins, hidden selected-session stability, duplicate running/unread state, zero browser errors, and unchanged fixture JSONL. `live-validation.json` and `live-running-touch.png` additionally prove a genuine running AgentSession survives touch-initiated Hide, remains selected and unchanged on disk, is revealed with running status, and settles normally.
**Departures from approved obligations:** None. The approved waiver for introducing a new automated DOM framework remains; Firefox WebDriver BiDi supplied runtime automation instead. Safari WebDriver was unavailable, but the plan did not require a specific browser.
**Implementation commit:** Pending.

## Implementation Summary

**Plan section:** 4. Update maintained documentation; Validation Contract VC-001 through VC-009.
**Work and outcome:** Updated `README.md`, `README.zh-CN.md`, and `AGENTS.md` for global Pinned/Recent, presentation-only Hide/Show hidden, the shared state file/API, helper/store boundaries, reconciliation safety, and Refresh-based convergence. Retained the complete validation outputs and screenshots under `.agents/reports/2026-07-21-pinned-sessions/`.
**Validation / evidence:** `node --test lib/*.test.mjs components/*.test.mjs` passed 160/160; `/Users/xin/Documents/repos/pi-web/node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check` passed. Runtime reports have no failures; desktop is 260 px, mobile is 280 px, Pinned and Recent overflow independently, short/mobile layouts preserve Project and Explorer, concurrent clients preserve pins, malformed metadata does not block browsing, and genuine running/touch Hide safety passes. Evidence files are `automated-validation.txt`, `validation.json`, `live-validation.json`, `desktop-tall.png`, `desktop-short.png`, `mobile.png`, `mobile-landscape.png`, and `live-running-touch.png` in the report directory.
**Departures from approved obligations:** None. No production build was run, as required. The worktree used the main checkout's installed TypeScript binary because it has no separate `node_modules` directory.
**Implementation commit:** Pending.

## Implementation Summary

**Plan section:** Final validation and closeout readiness; Validation Contract VC-001 through VC-009.
**Work and outcome:** Completed every approved implementation section, resolved both adversarial review rounds, retained desktop/mobile/live-running evidence, removed generated subagent artifacts, and reverted the development server's generated `tsconfig.json` include. The implementation is ready for guarded integration into local `main`.
**Validation / evidence:** Commit `22d194e71fa8a4a7c51bc5b230fc256e8024730d` contains the implementation and evidence. The final run passed 160/160 Node tests, TypeScript, ESLint, and `git diff --check`. Both `validation.json` and `live-validation.json` report `passed: true` with zero failures; screenshots cover desktop tall/short, mobile portrait/landscape, and a genuinely running touch-hidden session. Independent review is recoverable through runs `a331f586-b52e-47e9-93e2-e6be7800b85c` and `ed753e0e-bd6f-464a-9a55-160f4383e039`.
**Departures from approved obligations:** None. The plan's explicit automated-DOM-framework waiver was honored and replaced with Firefox WebDriver BiDi runtime coverage. Safari automation was unavailable but was never a required browser. Residual architecture risks are limited to process-local generation freshness across hypothetical multiple server processes and operator cleanup for a crash-stranded metadata lock; neither changes approved behavior or leaves an obligation incomplete.
**Implementation commit:** `22d194e71fa8a4a7c51bc5b230fc256e8024730d`.
