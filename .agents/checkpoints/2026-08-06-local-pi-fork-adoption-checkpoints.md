# Local Pi Fork Adoption Checkpoints

Plan: `.agents/plans/2026-08-06-local-pi-fork-adoption.md`

## Handoff

**Source:** Read-only scout run `9db652b4` in workflow `call_gj8WFRvVSduIvCQi6YfuYazy` (preserved artifact `.agents/reports/2026-08-06-local-pi-fork-adoption/fork-packaging-recon.md`).
**Purpose:** Inspect the exact fork commit’s release machinery, metadata/shrinkwrap identity, compaction implementation, faux-provider regression, and reproducibility risks.
**Outcome:** The fork’s `scripts/local-release.mjs` already runs generated-model hydration, checks, all workspace builds, the isolated full suite, and package packing. The recommended adoption seam is two fresh exact-object builds, an explicit focused faux regression, a separate metadata-only coding-agent stage, scripts-disabled final packing, raw-byte digest equality, and atomic publication. The declared overlay is package name/version, `bac2qh/pi` repository, conventional full-commit provenance, and the four shrinkwrap root identity fields. The fork’s actual in-run sequence is one agent run containing a completed tool batch, threshold compaction start/end, continuation, agent end, and final settlement.
**Evidence:** Exact-object references and line ranges are recorded in the artifact, notably `scripts/local-release.mjs`, coding-agent package/shrinkwrap, `src/core/agent-session.ts`, and `test/suite/agent-session-between-turn-compaction.test.ts`. The handoff identifies strict model-generation/network, Biome write, npm pack, path-layout, and source-harness risks.
**Uncertainty / gaps:** The scout preferred `git archive` and did not require checkout HEAD/cleanliness because object materialization is sufficient. The approved plan separately requires an exact clean sibling checkout and actionable wrong-commit failure; implementation therefore keeps that stricter preflight while materializing a detached worktree directly from the exact object. Actual archive reproducibility and installed-wrapper behavior were not yet run.
**Recommended use:** Invoke the fork release path only in disposable exact-commit state and external output; use the official tarball as a build reference, then verify that the final overlaid stage changes only its two metadata files and that both final archives are byte-identical.

## Handoff

**Source:** Read-only reviewer run `8800084f` in workflow `call_gj8WFRvVSduIvCQi6YfuYazy` (preserved artifact `.agents/reports/2026-08-06-local-pi-fork-adoption/pi-web-compatibility-recon.md`).
**Purpose:** Compare Pi Web’s Pi-facing contracts with exact Pi `0.84.0` and identify the smallest actual packaged-fork lifecycle integration test.
**Outcome:** Session factories, wrapper-facing AgentSession APIs, SessionManager clone primitives, resource/package/settings APIs, TUI keybindings, hosted boundaries, and in-process delta events remain source-compatible. Delta-only `message_update` is a JSON/RPC wire change and does not require a Pi Web projector change. Existing lifecycle coverage is synthetic; the missing decisive test should construct an installed AgentSession with the exported faux provider, seed compactable history, drive tool/summary/continuation responses through `AgentSessionWrapper`, and assert one final projection plus global idle only after native `agent_settled`. The reviewer also identified Pi `0.84.0`’s committed-but-unsynchronized credential error as a route-cache boundary requiring characterization.
**Evidence:** The artifact cites Pi Web factory use at `lib/rpc-manager.ts`, lifecycle accounting tests, projector handling, clone/resource/auth routes, and exact fork source ranges for AgentSession, ModelRuntime, faux provider, and compaction tests.
**Uncertainty / gaps:** No built artifact, alias lock, compile, runtime, auth partial-commit test, or installed package graph had yet been exercised. Source compatibility does not replace those gates.
**Recommended use:** Add one isolated installed-artifact wrapper integration test, retain existing imports, run focused Pi boundary suites, and test whether model cache invalidation must occur for `CredentialSynchronizationError` after a credential mutation commits.

## Implementation Summary

**Plan section:** Design / Implementation Strategy 1–6; Validation Contract VC-001, VC-002, VC-004, VC-005, VC-007, and VC-008 implementation surfaces.
**Work and outcome:** Added the exact-commit, standard-library-only local build helper and focused helper tests; produced two fresh full fork builds; applied only the `@bac2qh` package/shrinkwrap identity overlay; proved byte-identical archives; atomically published the ignored sibling tarball; advanced the npm manifest/lock to the compatibility alias plus exact official `0.84.0` companion graph; retained all existing Pi import paths; added an actual packaged-fork faux-provider lifecycle test through `AgentSessionWrapper`; fixed the demonstrated Pi `0.84.0` Theme constructor drift with inert backgrounds; and made every auth credential mutation invalidate Pi Web’s cache even when post-commit synchronization fails.
**Validation / evidence:** Both fork runs passed check, all workspace builds, the complete isolated fork suite (including coding-agent `1877` passed / `49` skipped), and the explicit 14-test between-turn regression. Their final 5,072,824-byte archives were identical at SHA-256 `f5a5caf1f5d34e9830480afb5707b5dc412a0e10a3016349240904bc12a47de2` and SHA-512 `sha512-T/AN5JeYYvGiDU7uupAkgiibNLD3Q8iUzQTpG401YJ9IbCIMsZE8ICtTdEcnGWaPqtwsP1UkNFH9VMOeJnchYw==` with 956 packed files. A clean disposable sibling-layout `npm ci --ignore-scripts` succeeded; npm reports `@earendil-works/pi-coding-agent@npm:@bac2qh/pi-coding-agent@0.84.0-bac2qh.734502cb8`; every official Pi node is `0.84.0`; runtime `VERSION`, repository, `gitHead`, shrinkwrap roots, `dist`, and fork marker match. TypeScript, lint, helper tests, the installed lifecycle test, and credential-cache tests pass.
**Departures from approved obligations:** None. Live model catalogs had drifted and made the exact commit fail its own typecheck; to satisfy the approved exact-source and byte-reproducibility obligations, the helper integrity-pins ignored generated model data to unchanged official `@earendil-works/pi-ai@0.84.0` and suppresses only the local-release script’s mutable regeneration command. All exact fork checks, tests, builds, local-release packing, and focused regression still execute.
**Implementation commit:** Pending.

## Handoff

**Source:** Fresh four-lane review workflow `call_CAr61bgG10Cr0amq9oXm8iF5|fc_028afed9d9f29597016a760392102481939f936ed93b202b8a` (helper safety, lock integrity, runtime/auth, lifecycle/docs).
**Purpose:** Adversarially review the large npm lock transition, exact-source helper and publication safety, Pi 0.84 runtime/auth adaptations, installed-fork lifecycle proof, and local-only documentation before commit.
**Outcome:** Lock review found the alias/actual identity, local integrity, exact companion graph, and removal/dedupe churn coherent; runtime/auth and documentation reviews found no regressions. Helper and lifecycle reviewers found actionable gaps: PATH Node was not independently pinned, origin matching was too loose, verify-only tolerated a non-adopted lock, publication needed no-replace/symlink handling, checkout transforms needed proof, and the lifecycle test needed explicit installed `VERSION` plus native-before-projected ordering. All accepted findings were fixed. The lifecycle reviewer’s initial 0.82.1 failure came from the intentionally uninstalled nested worktree and usefully demonstrated that the test rejects upstream/old bytes; authoritative execution remained the disposable installed layout.
**Evidence:** Recoverable child sessions are recorded in the workflow result; final fixes and reruns are synthesized in `.agents/reports/2026-08-06-local-pi-fork-adoption/final-validation.md`.
**Uncertainty / gaps:** Route-level mocked credential synchronization coverage remains absent, but helper-level error identity, route source review, TypeScript, and integrated runtime tests cover the adopted boundary. A same-user malicious ancestor-directory swap during artifact publication is outside the cooperative local-helper threat model.
**Recommended use:** Treat the local artifact/lock/runtime identity as accepted only when the strict verify-only command and installed lifecycle regression run from a real sibling-layout installation.

## Handoff

**Source:** Fresh follow-up workflow `call_VnT260OWyZDJGoXPs5VJwA0n|fc_028afed9d9f29597016a76082371288193b87d5f6f05b8cbcb` (helper and lifecycle) plus final reviewer `8451c27e-1f6b-48cf-9a50-ad4aada6c59d`.
**Purpose:** Verify first-round fixes, re-audit exact Git object materialization and finality ordering, and identify any remaining blocker after complete build/test evidence.
**Outcome:** Lifecycle follow-up found no VC-005 gap. Helper follow-up drove disabling global/system attributes and hooks, rejecting info attributes, enforcing exactly two declared CRLF paths, checking all tracked blob bytes and executable modes before and after build, and retaining atomic no-replace publication. The final reviewer found only Git replacement-object exposure; every helper Git command and nested fork validation now force `GIT_NO_REPLACE_OBJECTS=1`. A parent cleanup audit then found and fixed macOS `/tmp` versus `/private/tmp` worktree-registration identity; six exact stale task registrations were removed. The current sibling had no replacement refs, and the final successful two-build run retained the exact digest with no residual helper worktree.
**Evidence:** Recoverable workflow/single-run results above; helper output attested 1,340 exact tracked files per build, two declared CRLF transforms, full fork suites, and byte-identical final archives. Focused helper/lifecycle tests and the 736-test Pi Web suite passed afterward.
**Uncertainty / gaps:** None within the approved cooperative local workflow. The artifact remains intentionally ignored and machine-layout-dependent.
**Recommended use:** Keep replacement objects, external attributes, repository info attributes, hooks, unexpected checkout transforms, tracked source mutations, and destination replacement as hard failures in future helper revisions.

## Implementation Summary

**Plan section:** Design / Implementation Strategy 7–8; Test Strategy 1–7; Validation Contract VC-001 through VC-010.
**Work and outcome:** Closed every accepted review finding; made exact-source verification cover Git replacement objects, attributes, hooks, all 1,340 tracked blobs/modes, and post-build state; made publication stable-symlink-safe and atomic no-replace; made verify-only require the adopted lock; made clean development installation deterministic under ambient production; strengthened the installed-fork lifecycle test’s identity and native/projected/global ordering; and completed English/Chinese/local-release guidance. The ignored sibling artifact remains byte-identical to every successful rebuild.
**Validation / evidence:** `.agents/reports/2026-08-06-local-pi-fork-adoption/final-validation.md` records the full evidence. Final gates include two independent complete fork builds, coding-agent `1877 passed / 49 skipped` per run, focused fork compaction `14/14` per run, clean `npm ci --ignore-scripts --include=dev`, exact fork identity/graph inspection, installed lifecycle finality, TypeScript, lint, Pi Web `736/736`, audits against original and same-time old-lock baselines, production build, two launcher cycles, and isolated real development/production lifecycle tests. The global Pi binary/target and named configuration metadata were preserved; only volatile agent-directory mtime changed, and three provably task-owned npm debug logs were removed.
**Departures from approved obligations:** None. Live npm audit data added a DOMPurify advisory during implementation; same-time re-audit of the untouched old lock contains the same unchanged dependency finding, while the adopted graph removes the old coding-agent/Undici findings. Invalid ambient `NODE_ENV=production` and `TURBOPACK=auto` attempts were corrected and rerun under the canonical test/build environments. One post-cleanup fork run hit the unrelated reftable watcher timeout after 1,876 coding-agent passes; its temporary state was fully removed and the immediate unchanged two-build retry passed all 1,877 tests in both runs at the exact digest.
**Implementation commit:** Pending.

## Implementation Summary

**Plan section:** Final implementation and validation closeout for Design / Implementation Strategy 1–8 and Validation Contract VC-001 through VC-010.
**Work and outcome:** Adopted the exact local `bac2qh/pi` coding-agent fork through the existing compatibility key; locked every official companion at `0.84.0`; delivered a strict, repeatable, byte-reproducible sibling-artifact workflow; preserved Pi Web’s wrapper, projection, hosted, clone, auth, plugin, skill, and server behavior; documented the local-only/non-publishable boundary; and removed all task-owned temporary process, validation, npm-log, and stale-worktree state while retaining only the intended ignored sibling artifact.
**Validation / evidence:** Commit `be385ddb31dbad01de3197c70b7fe401f7112dca` contains the implementation and the complete evidence report at `.agents/reports/2026-08-06-local-pi-fork-adoption/final-validation.md`. Required gates passed: exact two-build fork validation and byte identity, clean supported reinstall, actual packaged-fork lifecycle ordering, exact companion tree, TypeScript, lint, 736/736 Pi Web tests, production/full audit comparison, disposable production build, development/production lifecycle, two launcher cycles, fork/global-state preservation, scoped diff, unchanged Bun lock, and unchanged release implementation.
**Departures from approved obligations:** None. No obligation is incomplete, blocked, waived, superseded, or divergent. The integrity-pinned official AI model-data hydration is the documented deterministic implementation of the approved exact-source build after the mutable live catalog failed; the transient watcher timeout and invalid ambient test/build attempts were rerun successfully and left no residual state. The same-time old-lock audit proves the newly published DOMPurify advisory is external database drift on unchanged bytes rather than a task regression.
**Implementation commit:** `be385ddb31dbad01de3197c70b7fe401f7112dca` (`feat: adopt reproducible local bac2qh Pi fork`).
