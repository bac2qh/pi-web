# M0 Development Lifecycle Recovery Checkpoints

Plan: `.agents/plans/2026-07-29-m00-development-lifecycle-recovery.md`

## Handoff

**Source:** pi-subagents run `aff40fd7-84ee-4a3d-ba47-ff57d956be40`, context-builder child 0 (preserved-M0 implementation reconnaissance); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-29-m00-development-lifecycle-recovery--/2026-07-30T05-13-22-001Z_019fb171-0551-78e1-9719-126180483b01/3db9da61/run-0/session.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/aff40fd7-84ee-4a3d-ba47-ff57d956be40/output-0.log`.
**Purpose:** Map the preserved partial M0 implementation against the approved recovery contract before parent-owned edits, including exact launcher/server/gateway/route/package/test/documentation gaps and scope boundaries.
**Outcome:** The partial gateway, ticket route, one-port custom server, scripts/dependencies, and isolated security tests were coherent, while recovery remained concentrated in terminal-only signal ownership, `130`/`143` first-signal exits, repeated injected development/production cleanup, serialized real-Next child coverage, copied-artifact production lifecycle, diagnostics, documentation, state, and final gates. The child confirmed no private Next/Watchpack workaround, second port, browser/SSE migration, production channel, or framework upgrade. It also confirmed the recovery plan copy now matches the approved source, the original plans and stopped checkpoint still match their references, task `.next` has no production build, and main's stale production artifact contains pinned Next 16.2.11 plus a manifest-listed `/api/home` route suitable only for copied lifecycle probing.
**Evidence:** Decisive ranges and test seams are synthesized in the recoverable output: prior launcher `bin/pi-web.js:38-132`, server `bin/pi-web-server.js:168-308`, gateway `bin/pi-web-transport-gateway.js:31-281`, route `app/api/transport/ticket/route.ts:1-129`, package `package.json:14-66`, and the then-skipped real test `lib/pi-web-server.test.mjs:211-333`. The child verified an empty index, unchanged browser/SSE/runtime boundaries, `git diff --check`, direct `ws`/dev-only `@types/ws`, all runtime `bin/` files in dry-run packaging, and main's required production manifests. Counterevidence was explicit: raw HMR acceptance alone cannot establish functional browser HMR, the stale artifact cannot contain the new ticket route, and fake-Next cleanup cannot prove real development watcher drain.
**Uncertainty / gaps:** At handoff time the real child suite, production natural drain, manual browser HMR, memory/checkpoint updates, final reviewer gate, commits, and guarded closeout were still outstanding. Main had advanced beyond the commit named in the immutable recovery plan and had unrelated dirt; the main lock helper remained absent.
**Recommended use:** Keep one parent writer, preserve the old checkpoint byte-for-byte, replace the skipped in-process case with one serialized child file, use only public Next cleanup, treat every timeout/kill/skip as failure, preflight and copy rather than mutate main's artifact, record manual HMR separately, and stop before any unsafe or racing main write.

## Handoff

**Source:** pi-subagents run `aff40fd7-84ee-4a3d-ba47-ff57d956be40`, reviewer child 1 (adversarial lifecycle/security/closeout audit); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-29-m00-development-lifecycle-recovery--/2026-07-30T05-13-22-001Z_019fb171-0551-78e1-9719-126180483b01/3db9da61/run-1/session.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/aff40fd7-84ee-4a3d-ba47-ff57d956be40/output-1.log`.
**Purpose:** Challenge the evolving recovery implementation for lifecycle leaks, signal/exit mistakes, security state disclosure, production-fixture weakness, history drift, and closeout hazards without editing project files.
**Outcome:** The reviewer confirmed the parent had corrected imported-versus-terminal ownership, first-signal latching, `130`/`143` selection, focused tests, package boundaries, ticket redaction, and plan/checkpoint hashes. It found two implementation blockers: gateway installation preceded unguarded WebSocket-server acquisition, so constructor/setup failure could leave the exact global installed; and ordinary terminal startup failure only set `exitCode`, which could hang after real development Watchpack initialization instead of explicitly terminating with `1`. It also recommended validating Origin before gateway availability, using `npm ls --include=dev --depth=0`, making timer counts explicit, and preserving copied-artifact fingerprints. Closeout findings were that main's advanced memory log may conflict, the lock helper is absent, and the M1 handoff must identify the retained foundation implementation commit truthfully without post-closeout narrative.
**Evidence:** Findings cite the evolving launcher/server/route/test ranges in the raw output and were corroborated by parent source reads. The reviewer independently observed focused 28/28 tests, typecheck, lint, `git diff --check`, dry-run inclusion of all runtime `bin/` modules, absence of browser consumers/production channel/private cleanup, a usable main artifact, and byte-identical original plan/checkpoint/recovery-plan copies. Counterevidence and risk were retained: the existing startup test covered `app.prepare()` failure only, configured npm omit settings hid `@types/ws` from the unqualified dependency command, and no manual browser HMR or settled-diff review had yet occurred.
**Uncertainty / gaps:** The review raced with active parent edits and therefore was not final acceptance. The exact closeout merge result and no-race condition cannot be known until guarded preflight; successful merge must not be followed by new checkpoint narrative. Fresh production ticket-route coverage remains not applicable because building is prohibited.
**Recommended use:** Guard every post-install acquisition and test constructor failure; explicitly exit `1` only from the executed terminal entry after startup cleanup; make invalid-origin responses independent of gateway state; run the include-dev dependency command; integrate current main memory content before the durable append where safe; obtain fresh review of the settled diff; and stop with `Closeout Recovery` rather than guess through any guarded merge conflict.

## Implementation Summary

**Plan section:** `Design / Implementation Strategy` §§1–3 and §4 implementation portion; REC-VC-001 through REC-VC-005; original M0 §§1–5 and M0-VC-001 through M0-VC-007.
**Work and outcome:** Resumed the preserved M0 branch without reset/stash/rebase/clean, copied the approved recovery plan byte-for-byte, and kept the original plans and stopped checkpoint unchanged. Completed the dormant V1 process gateway, bounded same-origin ticket route, one-port Node/Next server, terminal launcher, direct dependencies/scripts, diagnostics, architecture notes, and focused tests. Programmatic close now stops acceptance, releases and reports all Pi-owned WebSocket/HTTP/gateway/ticket/timer/global state, awaits public Next close, aggregates failures, is idempotent, and never exits. Only the executed terminal entry latches signals and exits after one cleanup (`130`/`143`, or `1` on cleanup/startup failure). All real Next cases now live in one sequential child integration file: real development route/gateway/security/frame/HMR coexistence and same-port restart, the terminal signal matrix, and copied-artifact production start/close/restart/natural drain.
**Validation / evidence:** Focused gateway/route/options/launcher/server tests passed 29/29 with no skips. The sequential real-Next file passed all four reported tests/subtests, including real post-prepare startup failure exit, direct-child orderly shutdown/rebind, and production natural drain without `process.exit()`. Focused ESLint, `node_modules/.bin/tsc --noEmit`, syntax checks, and `git diff --check` passed. An actual Chromium browser was opened before a temporary `app/page.tsx` marker edit; Next recompiled, browser refresh rendered the visible marker (sanitized screenshot SHA-256 `2b2782e4472474b6887c9a236af32c7e2c91f84b5ba24fef134150cc4fa1f99a`), a second refresh proved the exact source revert, `app/page.tsx` returned clean, and SIGTERM cleanup reported success and released the test port. Full repository/package gates and settled-diff review remain the next validation boundary.
**Departures from approved obligations:** None. Fresh production ticket-route coverage remains explicitly not applicable under the build prohibition; the copied stale artifact was used only for pinned production lifecycle evidence. No private Next/Watchpack cleanup, arbitrary handle enumeration, second product port, framework upgrade, browser/SSE migration, production channel, or later forced-shutdown policy was added.
**Implementation commit:** pending.

## Handoff

**Source:** pi-subagents run `264c85a6-d62d-49e9-bc27-616bdc2b60ea`, reviewer child 0 (lifecycle correctness and regression review); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-29-m00-development-lifecycle-recovery--/2026-07-30T05-13-22-001Z_019fb171-0551-78e1-9719-126180483b01/4720a815/run-0/session.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/264c85a6-d62d-49e9-bc27-616bdc2b60ea/output-0.log`.
**Purpose:** Independently review the settled launcher/server lifecycle, signal races/statuses, cleanup ordering, real development boundary, production reuse, and test validity against recovery and original-M0 obligations.
**Outcome:** Passed the lifecycle implementation with no code blocker. The reviewer confirmed guarded post-install acquisition cleanup, one idempotent aggregate close, public-Next-only cleanup, terminal-only first-signal ownership and statuses, real development direct-child exit/rebind, and production same-process restart/natural drain. It identified one P2 coverage note—signals were sent only after readiness—and the inherent environment-coupled production-artifact prerequisite. The parent subsequently added a pending-startup mixed-signal child case while preserving the approved local-artifact prerequisite.
**Evidence:** The child independently ran the full suite (129/129 at its snapshot, zero skips), focused 29/29 tests, real-Next integration, TypeScript, lint, `git diff --check`, include-dev dependency verification, and package dry run. It cited lifecycle source and test ranges in the recoverable output and found no private handle enumeration, Watchpack manipulation, browser migration, or SSE/runtime diff. Counterevidence was explicit that clean clones without the approved external production fixture cannot run that lifecycle case.
**Uncertainty / gaps:** Durable memory, implementation/final-summary commits, M1 handoff, and guarded closeout were intentionally still pending. The production fixture remains stale and lifecycle-only; no fresh ticket-route bundle claim is available.
**Recommended use:** Retain the added startup-signal assertion, document the production-fixture prerequisite and stale-route limitation, rerun all gates after final bookkeeping, and treat this result as implementation review rather than closeout authorization.

## Handoff

**Source:** pi-subagents run `264c85a6-d62d-49e9-bc27-616bdc2b60ea`, reviewer child 1 (security and test-validity review); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-29-m00-development-lifecycle-recovery--/2026-07-30T05-13-22-001Z_019fb171-0551-78e1-9719-126180483b01/4720a815/run-1/session.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/264c85a6-d62d-49e9-bc27-616bdc2b60ea/output-1.log`.
**Purpose:** Adversarially validate ticket/origin/global-slot security, reserved routing, body/frame limits, redaction, product-scope boundaries, and whether child-process evidence used only approved public seams.
**Outcome:** Blocked its reviewed snapshot on an undocumented `Module._load` test monkeypatch, malformed empty-userinfo Host acceptance (`@localhost:30141`), and occupied falsy global-slot overwrite. The parent accepted all three findings: fake terminal tests now execute a byte-identical copied launcher beside a public fake server module; real gateway coverage now uses a standalone public `startPiWebServer` child harness; Host authority shape rejects userinfo, empty/nonnumeric/out-of-range ports, and malformed IPv6 suffixes before URL canonicalization; and own-slot presence is distinguished from falsy values in CommonJS and typed accessors. Focused probes and the full real integration pass after those changes. Production immutability evidence was also strengthened from five manifests to a digest/count of every copied production artifact file/symlink outside deliberately excluded cache/dev/source-map state.
**Evidence:** The review confirmed correct 32-byte random tickets, 30-second expiry, delete-before-validation consumption, timer cleanup, 1 KiB body and 16 KiB frame limits, exact reserved-path routing, no CORS, sanitized product diagnostics, and unchanged browser/SSE scope. Its focused counterexamples reached a ticket with malformed Host and overwrote an own `null` slot before the fix. Post-fix gateway/route/server/launcher tests pass 29/29; the standalone real-Next file passes all four reported tests/subtests; targeted probes now reject malformed Host and preserve/reject occupied falsy slots; searches find no loader monkeypatch or private lifecycle workaround.
**Uncertainty / gaps:** Query tickets can appear in infrastructure outside Pi Web, as already recorded. The copied production artifact remains stale and contains embedded build metadata, but it runs only from copied test-owned state. Manual HMR evidence is attested in this checkpoint; fresh production route coverage remains not applicable.
**Recommended use:** Keep the public fixture/harness approach, retain malformed-authority and falsy-slot regression cases, rerun a fresh security review if later changes touch these seams, and never reintroduce private loader or handle manipulation.

## Handoff

**Source:** pi-subagents run `264c85a6-d62d-49e9-bc27-616bdc2b60ea`, reviewer child 2 (scope, package, documentation, and execution-state review); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-29-m00-development-lifecycle-recovery--/2026-07-30T05-13-22-001Z_019fb171-0551-78e1-9719-126180483b01/4720a815/run-2/session.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/264c85a6-d62d-49e9-bc27-616bdc2b60ea/output-2.log`.
**Purpose:** Verify immutable history, scope, maintainability, package contents/lock churn, documentation, checkpoint discipline, memory/commit sequencing, exact M1 handoff, and guarded-closeout prerequisites.
**Outcome:** Found no source correctness or unnecessary-complexity blocker and confirmed the large sequential integration file is justified by the anti-contention contract. It correctly blocked closeout—not implementation—until durable memory, explicit staging excluding `.pi-subagents/`, implementation commit, separately committed final summary, exact M1 entry handoff, live no-race preflight, and guarded integration are complete. It highlighted that M1 should enter at the provider/global-running-status migration because M0 now retains the dependencies/custom server/launcher/gateway/ticket foundation, while per-session SSE remains and fresh production ticket-route bundle coverage is unavailable.
**Evidence:** The reviewer verified original plan/master and old checkpoint identity, recovery-plan equality with main, minimal package-lock churn, unchanged `bun.lock`, all four runtime `bin/` modules in dry-run packaging, direct runtime `ws`, development-only `@types/ws`, no browser/hooks/SSE/rpc-manager diff, and no production channel/consumer. It independently ran 129 tests with no skips plus typecheck/lint/package/dependency/diff gates at its snapshot. Counterevidence was explicit: main has advanced unrelated dirt, no lock helper exists, raw `.pi-subagents/` artifacts are unignored, and task memory lacked main's newer hosted-session history.
**Uncertainty / gaps:** Closeout race state and merge cleanliness must be re-evaluated immediately before main write. A normal merge is required because main advanced; any actual conflict triggers guarded recovery rather than guessed resolution. The local production artifact remains an approved environment prerequisite.
**Recommended use:** Integrate current committed main memory content into task state before the durable append, stage only enumerated paths, identify the implementation commit as the retained M1 foundation entry in the final summary, record a no-race exception only after live verification, and add no checkpoint narrative after successful closeout.

## Handoff

**Source:** pi-subagents run `a5548001-3b54-4252-b031-b06a3fff7ef5`, reviewer child 0 (post-security-fix validation); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-29-m00-development-lifecycle-recovery--/2026-07-30T05-13-22-001Z_019fb171-0551-78e1-9719-126180483b01/b8c3a3cf/run-0/session.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/a5548001-3b54-4252-b031-b06a3fff7ef5/output-0.log`.
**Purpose:** Verify the accepted public-harness, malformed-Host, falsy-slot, whole-artifact fingerprint, and pending-startup-signal fixes and search for regressions before commit.
**Outcome:** Confirmed all requested security/test-validity fixes and their regression coverage, then found one independent request-dispatch blocker: `Promise.resolve(requestHandler(req, res))` evaluated a synchronous handler throw before promise containment. It also noted that `Number()` accepted non-decimal port strings. The parent accepted both: request invocation now occurs inside `Promise.resolve().then(...)`, and port normalization accepts only in-range numbers or digit-only decimal strings. Focused tests now prove bounded 500 responses for both synchronous throws and asynchronous rejections, sanitized diagnostics, cleanup afterward, valid decimal ports, and rejection of whitespace/exponent/hex/fraction/signed/out-of-range/non-string forms.
**Evidence:** Before the final request fix, the child independently passed the then-full 129-test suite, TypeScript, lint, dependencies, package dry run, artifact fingerprint inspection (430 included files, no symlinks), malformed-authority probes, and no-private-workaround searches. Its focused counterprobe observed an uncaught synchronous request error and timeout. After the parent fix, `lib/pi-web-server.test.mjs` passes 7/7, focused ESLint/typecheck/`git diff --check` pass, and direct source inspection confirms the deferred invocation and unchanged close path.
**Uncertainty / gaps:** The request/port fix still requires an independent follow-up verdict and final full-suite rerun before commit. The approved stale production, manual HMR, external-query-log, and process-scoped development limitations remain unchanged.
**Recommended use:** Obtain focused independent verification of the request and port fixes, then rerun final gates; do not commit while any synchronous request path can escape the bounded response handler.

## Handoff

**Source:** pi-subagents run `e15e16c8-0227-45a8-bbe1-c6c19d59208c`, reviewer child 0 (request-dispatch/port follow-up); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-29-m00-development-lifecycle-recovery--/2026-07-30T05-13-22-001Z_019fb171-0551-78e1-9719-126180483b01/8aa1bd43/run-0/session.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/e15e16c8-0227-45a8-bbe1-c6c19d59208c/output-0.log`.
**Purpose:** Independently verify the synchronous/rejected request containment and strict decimal port fixes without reopening unrelated review scope.
**Outcome:** Passed with no blocker. The reviewer confirmed handler invocation is deferred inside the promise chain, both synchronous throws and rejected promises return bounded empty 500 responses, malformed port forms are rejected, and cleanup/idempotence/global removal/connection drain/port reuse remain intact.
**Evidence:** Direct ranges are recorded in the recoverable output for `bin/pi-web-server.js` request containment, port normalization, and close flow plus `lib/pi-web-server.test.mjs` regression cases. The reviewer ran the focused file: 7/7 passed with no skips or failures.
**Uncertainty / gaps:** The child intentionally ran only the focused server file; the parent remains responsible for the final full repository, real-Next, package, typecheck, lint, and diff gates.
**Recommended use:** Treat the prior request-dispatch blocker and port note as resolved, then use the final full-suite result and parent diff inspection for implementation acceptance.
