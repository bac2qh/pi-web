# M0 Custom Server and Secure Gateway Foundation Checkpoints

Plan: `.agents/plans/2026-07-24-m00-baseline-transport-feasibility.md`

## Handoff

**Source:** pi-subagents run `721745a8-70b1-41e3-8163-beb53c9020c3`, scout child 0 (launcher/package/test reconnaissance); artifact `.pi-subagents/artifacts/outputs/721745a8-70b1-41e3-8163-beb53c9020c3/parallel-0/0-scout/context.md`; runtime output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/721745a8-70b1-41e3-8163-beb53c9020c3/output-0.log`.
**Purpose:** Map the existing launcher, option parsing, package/lock state, test conventions, documentation, and compatibility history before the parent changes the approved M0 surfaces.
**Outcome:** The current published launcher unconditionally requires `.next`, spawns the Next CLI, opens the browser only after matching `Ready` on child stdout, and has no owned signal or close path. `npm run dev` and `npm start` bypass that launcher. The parser preserves CLI-over-environment precedence, tolerant argument parsing, nullable hostname binding, and case-insensitive no-open values. `bin/` is already fully published. Root lock state contains transitive `ws@8.21.0`, but neither `ws` nor `@types/ws` is direct. The recommended order is options/dependencies, gateway, typed route bridge, server, launcher/scripts/docs, then integration validation.
**Evidence:** Exact source ranges are recorded for `bin/pi-web.js:13-74`, `bin/pi-web-options.js:12-27`, `package.json:14-64`, `package-lock.json:7-45,17784-17790`, `lib/pi-web-options.test.mjs:1-41`, `AGENTS.md:3-30,113-116`, and `lib/rpc-manager.ts:952-1001`. Launcher history confirms readiness-gated opening, nonfatal opener errors, and no-open behavior are intentional compatibility obligations. The child found no production packaging change beyond direct dependencies because the existing `files` list includes all of `bin/`.
**Uncertainty / gaps:** The child did not resolve the exact Next 16 owned-server API or run tests; real development HMR and the newly added production route remain empirical/build-blocked gates. It recorded no counterevidence to the approved one-port design.
**Recommended use:** Preserve parser and opener semantics, make `.next` production-only, load Next after mode selection, keep non-Pi upgrades literally untouched, review npm-generated lock churn, and retain the approved prohibition on `next build` and all M1/browser/SSE exclusions.

## Handoff

**Source:** pi-subagents run `721745a8-70b1-41e3-8163-beb53c9020c3`, context-builder child 1 (programmatic Next/server lifecycle); artifact `.pi-subagents/artifacts/outputs/721745a8-70b1-41e3-8163-beb53c9020c3/parallel-0/1-context-builder/context.md`; runtime output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/721745a8-70b1-41e3-8163-beb53c9020c3/output-1.log`.
**Purpose:** Resolve the installed Next 16.2.11 CommonJS custom-server contract, upgrade/HMR coexistence, Node 24 teardown primitives, and focused test seams.
**Outcome:** Installed Next supports `require("next")` and `next({ dev, dir, hostname, port, httpServer })`, followed by `prepare()`, `getRequestHandler()`, and `close()`. The request handler is asynchronous and needs a bounded rejection path. Next attaches its `upgrade` listener lazily on the first ordinary request, owns `/_next/webpack-hmr` in development, and intentionally ignores unmatched upgrades. Node invokes every upgrade listener, so the Pi listener must synchronously do nothing for nonreserved paths. Basic close cannot rely on `closeAllConnections()` or public `app.close()` alone for upgraded sockets; the owner should track raw connections, stop accepts, close Pi sockets/WSS, close Next, destroy remaining owned sockets, and prove port rebind. No blocker to the one-port design was found.
**Evidence:** Decisive installed-source ranges include `node_modules/next/dist/server/next.d.ts:10-30`, `next.js:267-332,384-453`, `router-server.js:604-692`, Node HTTP/net types for `close`, `closeAllConnections`, and async disposal, and authoritative Node documentation that `closeAllConnections()` excludes upgraded sockets. The artifact records the subtle `port: 0` metadata fallback to 3000 inside Next and the custom-server Turbopack-auto default. Repository test conventions support direct CJS loading, Jiti for routes, `t.after()` cleanup, and real loopback tests.
**Uncertainty / gaps:** Public Next close uses `Promise.allSettled` and does not expose its private `closeUpgraded()` hook, so observable socket/port release—not internal rejection propagation—is the reliable lifecycle evidence. A browser-level HMR observation remains required unless a raw protocol probe proves enough; the new production route cannot be built under repository rules.
**Recommended use:** Construct and pass the owned server before `prepare`, normalize the CLI string port, catch request-handler rejection, issue an ordinary request before HMR validation, track raw sockets, expose only the planned readiness/address/gateway/close surface plus narrow test seams, and stop rather than using a private Next patch or second port if real coexistence fails.

## Handoff

**Source:** pi-subagents run `721745a8-70b1-41e3-8163-beb53c9020c3`, context-builder child 2 (gateway/bootstrap/upgrade security); artifact `.pi-subagents/artifacts/outputs/721745a8-70b1-41e3-8163-beb53c9020c3/parallel-0/2-context-builder/context.md`; runtime output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/721745a8-70b1-41e3-8163-beb53c9020c3/output-2.log`.
**Purpose:** Derive the minimal fail-closed V1 process gateway, bounded ticket route, reserved upgrade validation, `ws` message-limit behavior, and adversarial test matrix from project and installed-source evidence.
**Outcome:** The safest bridge is a plain-CommonJS installer writing one explicitly versioned `globalThis.__piWebTransportGatewayV1` object and a TypeScript structural accessor that reads that slot without importing or instantiating another runtime. Recommended invariants are 32-byte base64url tickets, 30-second server-side records containing the exact handler, synchronous delete-before-validation/dispatch, generic invalid-ticket failures, identity-checked global uninstall, idempotent close, and optional fail-closed revocation on unregister. The route should require the exact custom header, canonical same-host HTTP(S) Origin, JSON content type, a 1 KiB incrementally read body, one valid channel field, no-store caching, closed errors, and no CORS. The upgrade path must parse only enough to classify the exact reserved path, validate Origin/Host and one ticket, consume before `handleUpgrade`, attach a socket error listener before invoking the stored handler, and never log raw URLs/tickets. `ws` enforces `maxPayload` across complete/fragmented messages and emits a socket error plus close code 1009 on overflow.
**Evidence:** The artifact traces process-global patterns in `lib/allowed-roots.ts`, `lib/models-cache.ts`, and `lib/rpc-manager.ts`; direct-route tests in `lib/rpc-manager.test.mjs`; installed `ws` server/client tracking and close behavior in `ws/lib/websocket-server.js:31-198,213-457`; receiver accumulation/overflow in `ws/lib/receiver.js:437-560`; socket error emission in `ws/lib/websocket.js:1196-1219`; and installed Next unmatched-upgrade handling. It provides route, gateway, upgrade, secret-redaction, oversize-message, reuse/expiry, and cleanup tests.
**Uncertainty / gaps:** The immutable plan does not fix the bounded-body size, channel grammar, exact closed error codes, or unregister-ticket behavior. The parent adopts the handoff's narrow 1 KiB body cap, lowercase 64-character channel grammar, generic closed error set, and unregister revocation as fail-closed implementation judgments inside approved scope. Query tickets can still appear in infrastructure logs outside Pi Web; Pi Web itself must not log them. Real process-global sharing/HMR and production bundle freshness remain empirical/residual gates.
**Recommended use:** Keep the gateway authoritative state wholly inside the global object, reject occupied or version-mismatched slots, collapse missing/expired/reused tickets without secret-bearing messages, use only a temporary test channel, test exact 16 KiB/16 KiB+1 behavior and 1009 closure, and adversarially search the final diff for raw URL/query/ticket/error logging, CORS allowances, production registrations, browser consumers, and out-of-scope SSE/runtime changes.

## Handoff

**Source:** pi-subagents run `3c9f8ffb-076f-4624-8af5-ca9f740cba46`, oracle child 0 (lifecycle blocker decision review); session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-07-24-m00-baseline-transport-feasibility--/2026-07-29T05-52-28-773Z_019fac6e-7865-78c7-89cf-a86aa53d015b.jsonl`; output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/3c9f8ffb-076f-4624-8af5-ca9f740cba46/output-0.log`.
**Purpose:** Decide whether the empirically reproduced Next 16.2.11 development Watchpack leak has an in-scope public-API resolution or blocks the immutable M0 lifecycle contract.
**Outcome:** The leak is a genuine VC-006 blocker. Public `NextCustomServer.close()` closes its rendered server and registered callbacks, but `setup-dev-bundler.js` creates a separate local Watchpack instance without returning it or registering `wp.close()`. Both default Turbopack and explicit webpack reproduce approximately 52 referenced `FSWatcher` handles after `await app.close()`. Awaited orderly cleanup followed by terminal `process.exit`, as Next's own CLI does, is defensible for the published command but does not make exported `startPiWebServer().close()` reusable or leak-free in the same process. Fake-Next repetitions and child-process exits cannot satisfy the approved same-process truth.
**Evidence:** The oracle independently traced `node_modules/next/dist/server/next.js:384-390`, `node_modules/next/dist/server/lib/router-utils/setup-dev-bundler.js:286+`, and Next's terminal signal cleanup/exit in `node_modules/next/dist/server/lib/start-server.js:321-389`. Parent probes reproduced the leak with a minimal direct public-API call, with no Pi Web server involved, under both bundlers; public close left 52 referenced FSWatchers. Closing those handles through `process._getActiveHandles()` made the test process exit but relies on an undocumented Node API and masks rather than fixes the production defect.
**Uncertainty / gaps:** Production custom-server close may be clean, and an explicit terminal exit after awaited cleanup can make the CLI signal path bounded, but neither changes the approved obligation that the exported server close release Next resources and permit leak-free same-process repetition in development. A future Next version may fix this; changing the pinned framework version requires separately approved investigation and validation.
**Recommended use:** Stop this implementation without commit or closeout, remove/quarantine any test-only handle manipulation, preserve the useful partial source state, and obtain a revised approved lifecycle contract or separately approved Next-version investigation. Do not reach into `app.init`, import private Next server helpers, monkeypatch `fs.watch`, enumerate/close process handles, add a second port/process boundary, or claim terminal process exit as VC-006 evidence.

## Implementation Summary

**Plan section:** `Design / Implementation Strategy` §§1–4 and basic lifecycle obligations; `Test Strategy`; M0-VC-001 through M0-VC-006.
**Work and outcome:** Drafted the direct `ws`/`@types/ws` dependency changes, V1 process gateway, typed App Router accessor, bounded same-origin ticket route, one-port custom server, thin launcher/options/scripts, focused gateway/route/launcher/server tests, and launcher architecture notes. Baseline and isolated security/launcher work progressed, and a real Next development probe proved same-process route compilation and began exercising the shared gateway. Implementation stopped before completion when public Next 16.2.11 development close was shown to retain its Watchpack handles; no private workaround or scope reinterpretation was applied. The real-Next integration test is explicitly skipped in the preserved partial state rather than masking the leak with test-only handle closure.
**Validation / evidence:** Baseline before tracked source changes: `node --test lib/*.test.mjs` passed 101/101, `node_modules/.bin/tsc --noEmit` passed, and `npm run lint` passed. During implementation, the isolated gateway/route/options/launcher suite passed 25/25. After stopping and quarantining the blocked real-Next case, the focused partial-state suite passed 27 tests with the real-Next lifecycle case explicitly skipped (28 total), `node_modules/.bin/tsc --noEmit` passed, `npm run lint` passed, and `git diff --check` passed. These checks characterize the preserved draft and are not final plan acceptance. Direct real-Next probes showed ordinary App Router responses and route-issued tickets on the owned port, then `await app.close()` left 52 referenced `FSWatcher` handles under both Turbopack and webpack. A private-handle test cleanup temporarily demonstrated the difference, was rejected as invalid evidence, and was removed.
**Departures from approved obligations:** M0-VC-006 and the plan's reusable development close/repeated-start resource guarantee are blocked by pinned Next 16.2.11's public custom-server lifecycle. Consequently final real-Next HMR/upgrade/oversize validation, full required gates, memory distillation, coherent implementation commit, successor handoff, and ordinary closeout are incomplete and must not be inferred from the draft. Terminal `process.exit` after awaited CLI cleanup is retained as defensible signal behavior but is not treated as a waiver. No second port, private Next API, browser/SSE migration, production channel, or final forced-shutdown mechanism was added.
**Implementation commit:** pending; blocked before a coherent implementation commit.
