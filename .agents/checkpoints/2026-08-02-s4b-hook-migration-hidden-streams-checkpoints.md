# S4B Hook Migration and Retained Hidden Session Streams Checkpoints

Plan: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md`

## Handoff

**Source:** Pi subagents parallel batch `3648e333`, `scout` child 0, recoverable output `.pi-subagents/artifacts/outputs/3648e333/s4b-browser-integration-context.md`. Recorder: orchestration root. Kind: read-only S4B browser ownership/integration investigation. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S4B; Mode: investigation.
**Purpose:** Reconcile accepted S4A registry ownership with the current AppShell selection, keyed ChatWindow, new-ID promotion, hook state machine, draft, sidebar, branch, queue, compaction, extension, completion, and per-session SSE lifetimes without editing or delegation.
**Outcome:** The scout derives a page-local view owner above keyed chat, one stable exact-ID binding, acquire-B-before-changing-A transitions, minimum active/local-prompt hidden retention, and projected snapshot/effect adaptation as the sole persistent live writer. It identifies the HTTP-to-first-frame gap, final-snapshot-before-effect release hazard, new-ID same-binding promotion, hidden completion/global-unread division, and exact product preservation list. The root adopts its contained minimum: retain only selected, local-prompt-claimed, or canonical-active views; do not acquire from global running/sidebar state or retain arbitrary idle history.
**Evidence:** Root verified `SessionRegistryProvider`, `SessionRegistry`, AppShell’s seven selected-session mutations, promotion without `sessionKey` bump, keyed `ChatWindow`, current EventSource connect-before-prompt, run-id reconciliation, sidebar global running→idle authority, draft keys, branch/fork/clone semantics, and the removable route. Accepted S4A implementation/finality is `11036f6`/`598f804`. S4A’s stable client/registry/effect contract supports the derived owner without server change.
**Uncertainty / gaps:** The current UI has one mounted chat view, so hidden retention preserves transport/canonical recovery rather than inventing a multi-chat product. A local prompt claim is necessary before first activity but remains browser continuity only. The draft must define an existing-V1 attachment gate and must not journal effects or let hidden settlement drop the final same-sequence effect.
**Recommended use:** Review the draft’s view-controller API, prompt claim, strict-ready gate, deferred hidden release, promotion, completion/unread boundaries, exact product tests, and no-run-ownership exclusions.

## Handoff

**Source:** Pi subagents parallel batch `3648e333`, `scout` child 1, recoverable output `.pi-subagents/artifacts/outputs/3648e333/s4b-recovery-context.md`. Recorder: orchestration root. Kind: read-only S4B cursor/recovery/test investigation. Governing master as above; candidate milestone: S4B; Mode: investigation.
**Purpose:** Derive the exact existing-V1 projected/HTTP composition, recovery, final equality, SSE-removal, test, and source-boundary contract from accepted S2-S4A and the current hook/HTTP routes without editing or delegation.
**Outcome:** The scout confirms that committed `SessionClientSnapshot` identity/epoch/cursor/revision is the only available browser composition token; V1 has no server-side HTTP read barrier or marker acknowledgement. It derives cursor-observed stale suppression and eventual retry/final equality, sticky-marker coalescing, canonical live-state authority with HTTP durable repair, initial/exact/recovery/zero-subscriber behavior, and a mounted hook/browser test matrix. It confirms per-session SSE removal is safe only after all visible/hidden/reconnect/HTTP/compatibility gates pass.
**Evidence:** Root verified projected state/effect fields, target-gated committed snapshots, client reconnect semantics, registry atomic first effect consumer, session/context/state HTTP routes, current transcript/runtime load and 15-second/visibility/online/prompt-settlement paths, and SSE route behavior. The draft explicitly rejects a false linearizability claim and preserves polling/reconciliation.
**Uncertainty / gaps:** No HTTP response carries a cursor. The root resolves “cursor-bound” as the only contained existing-V1 interpretation: unchanged observed cursor generation plus after-apply recheck and later-delta retry. If implementation requires a server-stamped generation, that is material divergence. Sticky runtime markers need coalesced initial/recovery/settlement triggers rather than a per-frame fetch loop.
**Recommended use:** Challenge request-token/view/run/leaf/cursor predicates, projected-over-HTTP authority, active coalescing, recovery markers, final equality, real browser obligations, and exact SSE/file-watch/OAuth inventory before approval.

## Handoff

**Source:** Pi subagents run `4202dc66-860a-48a4-8910-96b8dace3b96`, `reviewer` child 0; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/4202dc66-860a-48a4-8910-96b8dace3b96/output-0.log`. Recorder: orchestration root. Kind: independent initial S4B draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md`; Mode: review.
**Purpose:** Test the draft’s containment, existing-V1 attach/HTTP interpretation, hidden/local-claim lifecycle, keyed selection/promotion, projected adaptation, SSE removal, test boundaries, privacy, browser gates, and material-divergence judgment without editing or delegating.
**Outcome:** Verdict `FINDINGS — DO NOT APPROVE AS WRITTEN`. The V1 `recovering|connected` gate, minimum hidden policy, eventual cursor-observed HTTP interpretation, projected authority, SSE/later-scope boundaries, and browser/privacy gates are sound. Four bounded draft defects require correction: an idle pre-prompt baseline snapshot must not settle an uncovered claim; late new-ID promotion must not steal selection/URL from a newer B view; every transcript/context replacement must capture prompt/UI message generation even before cursor advance; and focused source/tests must include the S1 EventSource inventory test plus S3 session-channel integration test-only evidence.
**Evidence:** Root verified the cited client ordering: `recovering` publishes after ready but before initial/recovery snapshot commit, so a selected-before-prompt idle snapshot may follow prompt dispatch and precede ordered `activity_started`. Current `handleSessionCreated` unconditionally selects/rewrites URL after async prompt response, making stale promotion real. Current transcript loads replace complete messages/entry IDs, so a prompt-run advance alone must stale them. `components/GlobalStatusProvider.test.mjs` still asserts per-session EventSource presence and `lib/session-channel-integration.test.mjs` owns the required zero-subscriber/API seam.
**Uncertainty / gaps:** These are contained claim-relative ordering, new-screen generation, request-token, and test-inventory rules; no new product choice, server protocol, weaker gate, or material divergence is required. Browser/provider execution remains a later acceptance fact.
**Recommended use:** Correct the still-draft plan and obtain a fresh independent corrected-draft review before approval.

## Handoff

**Source:** Pi subagents run `f84245ab-6ec1-4bbd-aecd-ac0130f3b973`, `reviewer` child 0; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/f84245ab-6ec1-4bbd-aecd-ac0130f3b973/output-0.log`. Recorder: orchestration root. Kind: final independent corrected S4B draft review. Governing master as above; reviewed milestone: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md`; Mode: review.
**Purpose:** Verify all initial findings and the complete corrected draft for claim-relative baseline ordering, stale promotion, unconditional transcript/UI generation capture, exact SSE/integration tests, contained architecture, browser gates, privacy, and executable validation.
**Outcome:** The reviewer returns `APPROVE AS WRITTEN` with no blocker, major, or minor defect. It confirms an uncovered claim survives an idle selected-before-prompt baseline; stale A promotion cannot displace B; every transcript/context replacement captures prompt/UI generation before cursor advance; required GlobalStatusProvider and session-channel integration tests are exact; and API/state/effect ordering, eventual HTTP composition, source boundaries, browser evidence, privacy, and all S4B VCs remain contained and testable.
**Evidence:** The reviewer traced corrections to plan sections and current `AppShell`/hook replacement sites, verified accepted S4A lineage `11036f6`/`598f804`, and confirmed server protocol, file watch, lifecycle, and S7 scope are protected. Root verifies the plan remains `Status: draft`, both planning files alone are untracked besides unrelated `.pi-subagents/`, and no advisory pointer companion exists.
**Uncertainty / gaps:** Browser/provider execution and implementation behavior remain mandatory future evidence, not plan ambiguity. No human decision, scope change, validation waiver, or material divergence remains.
**Recommended use:** Change only the plan status to `approved`, commit this exact plan/checkpoint boundary excluding `.pi-subagents/`, record the immutable blob, and launch one fresh sole S4B milestone implementer with every VC, browser obligation, source boundary, special handoff, and stop condition.

## Handoff

**Source:** Pi subagents run `8633079d-9f3f-4e4c-90b5-ea9416d5bf49`, `milestone-implementer`; child session `019fc1ff-7a48-7fb4-98c4-f874e9324e17`; recoverable output `.pi-subagents/artifacts/8633079d-9f3f-4e4c-90b5-ea9416d5bf49_milestone-implementer_output.md` and run log `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/8633079d-9f3f-4e4c-90b5-ea9416d5bf49/output-0.log`. Recorder: orchestration root. Kind: failed S4B implementation attempt with a recoverable late handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md` at blob `7fc9e8e9a3960d5eeaa9c38c8f6741493d643bb4`; Mode: implementation.
**Purpose:** Implement S4B-VC-001 through S4B-VC-011 as the sole source writer, preserve protected S2-S4A/server/file-watch/OAuth boundaries, run the complete validation matrix, attempt sanitized browser/provider flows, and return the special implementation handoff without staging or committing.
**Outcome:** The run is terminal and failed mechanically after exceeding its 120-turn budget, so its runtime acceptance is rejected. Before termination it nevertheless produced a complete recoverable handoff and left an unstaged, uncommitted S4B diff: three new browser view/projection/reconciliation modules and tests; provider/AppShell/ChatWindow/hook integration; focused test updates; and deletion of the per-session SSE route. The writer reports S4B-VC-001 through 007 and 010 source behavior implemented, automated compatibility coverage for VC-008, and all required command classes exercised. It explicitly leaves S4B-VC-008/009/011 unaccepted because full Chromium, Firefox, and authenticated-provider evidence is missing. No milestone acceptance follows from this failed attempt or its prose.
**Evidence:** Root confirmed process-terminal status, no matching writer process, all implementation-file modification times preceding run termination, exact milestone blob, no staged files, `git diff --check`, the expected sixteen source/test paths only, no protected production drift, route absence, and exactly four file-watch plus one OAuth production `EventSource` callers. The handoff reports TypeScript and lint passes; focused `102/102`; exact non-real-Next `468/468`; literal `470` passes plus only the allowed missing-`.next/BUILD_ID` child/parent failures; real development/terminal passes with the same allowed production failure; package and development smoke passes; and a basic isolated-profile Firefox headless page smoke. Those command claims remain subject to root reproduction. The failed runtime metadata is `.pi-subagents/artifacts/8633079d-9f3f-4e4c-90b5-ea9416d5bf49_milestone-implementer_meta.json`.
**Uncertainty / gaps:** Root has not yet accepted the source, reproduced the command matrix, completed full-diff scrutiny, or collected the required browser/provider evidence. Chromium and browser automation were reported unavailable; the Firefox attempt was only a page smoke, not the required attach/hidden/reveal/reconnect/network inventory. Fresh production inclusion remains release-owned under the no-build rule. `.pi-subagents/` remains unrelated runtime output and excluded.
**Recommended use:** Preserve and independently inspect the current source state. Reproduce decisive ownership/prompt/reconciliation/SSE cases and the complete root matrix, obtain a fresh independent root review, and collect the exact sanitized Chromium/Firefox/provider evidence. Use a sequential bounded S4B continuation writer only for a root- or reviewer-established in-scope defect; do not resume the broken descriptor or accept from the failed run alone.

## Handoff

**Source:** Nested fresh read-only reviewer run `d010bb7d`, launched by S4B writer `8633079d-9f3f-4e4c-90b5-ea9416d5bf49`; recoverable output `.pi-subagents/artifacts/d010bb7d_reviewer_0_output.md`. Recorder: orchestration root. Kind: implementation review during the failed S4B writer attempt. Governing master and immutable S4B milestone as above; Mode: review.
**Purpose:** Adversarially inspect the then-current S4B diff for ownership, attachment and claim ordering, hidden final-effect release, promotion, projected adaptation, HTTP convergence, SSE removal, compatibility, privacy, and protected-boundary defects without editing or delegation.
**Outcome:** The reviewer found one source blocker, four major defects, and one minor comment defect: tokenless prompt-settlement polling could retire an uncovered claim from stale HTTP; dirty repairs could lose their final retry; pre-canonical HTTP did not seed running state; extension projection chose the oldest request; a new compaction retained stale prior result/error; and two comments still named SSE. It separately identified missing browser/provider evidence as an acceptance blocker. The same sole writer reports applying bounded fixes before its final source state.
**Evidence:** File/line findings and VC mappings are preserved in `.pi-subagents/artifacts/d010bb7d_reviewer_0_output.md`; metadata `.pi-subagents/artifacts/d010bb7d_reviewer_0_meta.json` records exit zero, no mutation attempt, fresh context, and read-only constraints. The writer's later diff includes targeted reconciliation, projection, compaction, and comment changes, but root verification remains required.
**Uncertainty / gaps:** This review predates the final writer changes and cannot accept them. Browser/provider evidence was intentionally not fabricated. The root has not yet independently searched for sibling defects or reproduced each fix.
**Recommended use:** Treat the six concrete source findings as mandatory root verification targets and require a fresh post-writer review after root validation.

## Handoff

**Source:** Nested fresh read-only reviewer run `a7b62df3`, launched by S4B writer `8633079d-9f3f-4e4c-90b5-ea9416d5bf49`; recoverable output `.pi-subagents/artifacts/a7b62df3_reviewer_0_output.md`. Recorder: orchestration root. Kind: focused post-fix implementation re-review during the failed S4B writer attempt. Governing master and immutable S4B milestone as above; Mode: review.
**Purpose:** Reinspect the focused fixes for tokenized settlement polling, bounded dirty repair, pre-canonical running seed, latest extension selection, compaction reset, and SSE terminology, then report any remaining S4B source defect without editing or delegation.
**Outcome:** The reviewer reported no remaining blocker, major, or minor source finding in the inspected state. It verified the focused reconciliation, projection, attachment, claim, and static EventSource-removal surfaces. It explicitly retained missing Chromium, full Firefox, and authenticated-provider evidence as an acceptance blocker and did not independently rerun the full validation matrix.
**Evidence:** `.pi-subagents/artifacts/a7b62df3_reviewer_0_output.md` gives the inspected file ranges and conclusions; `.pi-subagents/artifacts/a7b62df3_reviewer_0_meta.json` records exit zero, fresh context, no mutation attempt, and read-only constraints. The output was budget-wrapped at eight turns and labels itself potentially partial.
**Uncertainty / gaps:** This nested re-review is useful advisory evidence but is not the mandatory fresh independent root review and cannot replace root command/browser validation. Its partial-budget note and narrow focus leave full-diff scrutiny outstanding.
**Recommended use:** Verify its decisive claims directly, then launch one fresh root-owned no-edit/no-delegation review only after the root matrix and browser evidence are ready enough to assess final acceptance.

## Handoff

**Source:** Pi subagents run `a387edb0-db76-4ce3-8c93-93389436d56a`, fresh `reviewer`; child session `c97f9625/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/a387edb0-db76-4ce3-8c93-93389436d56a/output-0.log`. Recorder: orchestration root. Kind: mandatory fresh independent root-owned S4B implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md` at blob `7fc9e8e9a3960d5eeaa9c38c8f6741493d643bb4`; Mode: review.
**Purpose:** Inspect the complete current tracked and untracked S4B diff independently of the failed writer's claims, map source correctness and validation adequacy to S4B-VC-001 through S4B-VC-011, search for sibling defects, distinguish bounded fixes from material divergence, and preserve the browser/provider gap without editing or delegation.
**Outcome:** Verdict `REJECT`; one bounded continuation writer is required before acceptance-oriented browser validation. The review confirms immutable-plan/source boundaries, acquire-before-release, deferred final effect, mounted prompt ordering, HTTP token dimensions, source-level per-session SSE removal, privacy, and the root's TypeScript/lint/focused/non-real/package/whitespace passes. It finds two source blockers and four major issues: an uncovered claim cannot be settled after the initiating keyed hook disappears when recovery skips `active:true`; completion notification can duplicate under projected/HTTP races and loses page-local lineage across A→B→A; initial/pre-canonical HTTP handling omits standalone compaction and needs an exact committed-state boundary; stale old-screen materialization can mark itself controller-selected after a newer blank screen; dirty HTTP repair has no eventual selected-idle trigger after its single retry; and the pure/mounted suites omit much of the immutable explicit matrix. It separately retains missing full Chromium, Firefox, and authenticated-provider evidence as a blocker. The reviewer reports no material divergence: all corrections remain above accepted S2-S4A seams.
**Evidence:** The output cites `lib/session-view-transport.ts`, `lib/session-http-reconciliation.ts`, `hooks/useAgentSession.ts`, `components/AppShell.tsx`, and the four new test files with VC mappings and smallest contained directions. Root independently confirms the stale-old-screen selection case, hook-local hidden-claim and completion-lineage gaps, possible projected/HTTP duplicate completion race, missing pre-canonical standalone-compaction seed, selected-idle retry gap, immediate composer clearing without failure restoration, and the large difference between the immutable mounted/pure matrix and the current five-case pure suites plus one happy mounted flow. Root also verified from `lib/session-stream-state.ts` that initial snapshot ready retains a null committed epoch, while exact/recovery ready may expose a previously committed epoch; the continuation must use the actual committed-state contract rather than assuming every non-null epoch is uncommitted.
**Uncertainty / gaps:** The reviewer did not supply browser evidence and did not treat the basic Firefox page smoke as sufficient. Its broad `streamEpoch !== null` criticism needs the implementation writer to reconcile the accepted client contract precisely: a prior committed epoch remains real canonical state during recovery, but initial HTTP must seed standalone compaction and other available live fields until the first canonical commit. Current developer documentation still names removed agent SSE, but S7 owns maintained documentation; do not widen this S4B fix into S7 docs. Full literal/real-Next reruns remain for the corrected source.
**Recommended use:** Launch exactly one fresh sole S4B continuation writer under the same immutable milestone. Require transferable page-entry claim/completion lineage without run ownership, stale-screen-safe retained acquisition/adoption, eventual bounded selected repair, prompt failure draft/image preservation, exact pre-canonical compaction seeding with projected authority after commit, atomic visible-once completion, and authentic expansion of every named pure/mounted integration obligation. Preserve server/base-registry/file-watch/OAuth/docs boundaries; then repeat root scrutiny, the complete matrix, real browser evidence, and a new independent review.

## Handoff

**Source:** Pi subagents run `2101d14b-4ffa-4a48-b081-82ca9fe74984`, `milestone-implementer`; child session `019fc238-f4ec-7c76-a865-9735d1611be4`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/2101d14b-4ffa-4a48-b081-82ca9fe74984/output-0.log`. Recorder: orchestration root. Kind: bounded S4B continuation/fix implementation handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md` at blob `7fc9e8e9a3960d5eeaa9c38c8f6741493d643bb4`; Mode: fix.
**Purpose:** Correct the root-reviewed hidden-claim, page-lineage completion, stale-screen acquisition, pre-canonical fallback, selected-idle repair, prompt draft/image rollback, comment, and explicit test-matrix defects while preserving accepted S2-S4A and later-milestone boundaries.
**Outcome:** The writer reports completing all seven issued bounded fixes. Stable page entries now carry claim/completion lineage and a bounded hidden settlement monitor; projected and HTTP settlement share one visible-once decision; stale materializations acquire retained rather than selected; committed canonical authority is distinguished from genuinely initial HTTP seed; selected dirty repairs use coalesced backed-off retries and idle recovery triggers; ChatInput awaits prompt acceptance and preserves unchanged text/images/drafts on failure; and the pure/mounted tests were materially expanded. It reports no protected production change, protocol addition, server ownership, effect journal, dependency, or material divergence. S4B-VC-009 and final S4B-VC-011 remain explicitly unaccepted pending root browser/provider evidence and fresh review.
**Evidence:** Root confirmed the writer process is terminal, the immutable plan hash and HEAD are unchanged, no files are staged, `git diff --check` passes, the expected S4B source/test set plus the root-owned checkpoint are the only non-runtime changes, protected production/docs/package paths have no diff, the per-session route is absent, and exactly four file-watch plus one OAuth `EventSource` remain. The handoff reports TypeScript and lint passes; focused `124/124`; mounted `13/13`; exact non-real-Next `490/490`; literal `492` passes with only the allowed absent-`.next/BUILD_ID` child/parent failures; isolated real development/terminal passes with the same allowed production failure; and package/hash/privacy/no-stage gates. Root reproduction and source scrutiny remain authoritative.
**Uncertainty / gaps:** No Chromium, complete Firefox, authenticated-provider, or user evidence was produced by this fix run, by design. A fresh root-owned reviewer has not inspected the corrected state. Fresh production route inclusion remains release-owned. The writer's page-lineage, monitor, retry, composer, and test-coverage claims require direct root inspection, and `.pi-subagents/` remains excluded runtime state.
**Recommended use:** Inspect the complete corrected diff, reproduce decisive claim/completion/stale-screen/canonical/retry/composer tests and the full command matrix, then obtain a fresh independent no-edit/no-delegation review. Only after source acceptance should the root collect the sanitized Vivaldi-base-Chromium, Firefox BiDi, and authenticated-provider browser matrix.

## Handoff

**Source:** Pi subagents run `b53eede1-6336-4109-9562-3e81e2cf53a8`, fresh `reviewer`; child session `45c2fea2/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/b53eede1-6336-4109-9562-3e81e2cf53a8/output-0.log`. Recorder: orchestration root. Kind: mandatory fresh post-fix independent S4B implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md` at blob `7fc9e8e9a3960d5eeaa9c38c8f6741493d643bb4`; Mode: review.
**Purpose:** Independently inspect the continuation result for page-owned claim/completion lineage, canonical/HTTP authority, stale-screen acquisition, bounded repair, composer rollback, selected-effect delivery, retained slash-command state, SSE removal, protected boundaries, and validation adequacy before browser/provider acceptance work.
**Outcome:** Verdict `REJECT`; one further bounded sequential S4B fix is required before acceptance-oriented browser validation. The reviewer confirms the previously rejected hidden-settlement, completion atomicity, stale materialization, initial HTTP seeding, draft/image rollback, and source-removal directions materially improved, but finds two P0 source defects and three major bounded defects: a later pre-commit transport publication can erase HTTP-seeded live state; an HTTP prompt failure still rolls back after ordered canonical activity covered the claim; slash-command classification remains hook-local across A→B→A remount; retry exhaustion omits leaf/UI/run/lineage/transcript tuple changes and has no eventual selected-idle quiescence; and AppShell starts a selected binding before the keyed hook installs its transient-effect consumer. The mounted suite does not reproduce those races and still substitutes source assertions or hand-built bindings for actual AppShell/ChatInput/reconnect behavior. Chromium, full Firefox, authenticated-provider, and sanitized network evidence remain independently absent.
**Evidence:** Root reproduced the writer's TypeScript, lint, exact focused `124/124`, literal `492` pass plus only the two permitted absent-`.next/BUILD_ID` failures, package dry-run, whitespace, plan-hash, protected-boundary, route-removal, and EventSource gates. Root then verified each source finding directly: `applyProjectedSnapshot` replaces live state without checking `canonicalCommitted`; the catch path unconditionally calls `failed()` and rolls back while `failPrompt` has no covered outcome; `promptClassificationRef` is hook-local; `FailureTuple` excludes leaf/prompt/transcript dimensions and stops after three failures while the 15-second interval exists only for active runs; and `AppShell` calls `sessionViews.select()` before React mounts the effect subscriptions while the controller relay discards a delivery with no listener. The review's focused command matrix also passed and its output cites exact source/test lines and VC mappings.
**Uncertainty / gaps:** The selected-effect correction must preserve acquire-B-before-A and the prohibition on a transient-effect journal; the writer should use an atomic consumer/selection handoff or another demonstrably equivalent contained seam rather than weakening delivery. Eventual repair must remain one-at-a-time and rate/memory bounded, not become a per-frame or tight infinite loop. An ambiguous failed HTTP response may suppress rollback only after the exact lineage has canonical activity/settlement proof. No finding requires a V1/server/base-registry change, browser run ownership, or material divergence. Preparatory browser probes proved bundled Chromium and stock Firefox BiDi can connect, but they are not product-flow evidence and are intentionally not acceptance claims.
**Recommended use:** Launch one fresh sole S4B fix writer under the same immutable plan. Require focused source and mounted tests for all five defects, including actual AppShell/ChatInput or an equivalent honest mounted boundary, preserve every protected/later-milestone surface, then repeat root scrutiny, the full command matrix, and another fresh independent review before real browser/provider validation.

## Handoff

**Source:** Pi subagents run `ecfc3e09-ebb2-4c20-ac1d-60476e42e2a5`, `milestone-implementer`; child session `361059fa/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/ecfc3e09-ebb2-4c20-ac1d-60476e42e2a5/output-0.log`. Recorder: orchestration root. Kind: interrupted partial S4B bounded-fix attempt. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Implement the five post-fix review defects: pre-canonical seed preservation, canonically covered prompt-failure semantics, page-stable slash classification, full-tuple/eventual idle repair, and journal-free selected-effect handoff, with honest mounted coverage.
**Outcome:** The writer made a partial unstaged source/test correction, including a `prepareSelection` seam, richer page lineage/retry behavior, and expanded mounted tests, but returned no handoff and is unaccepted. Its focused command hung for more than 90 seconds in `components/SessionAgentTransport.test.mjs` after two reported mounted cases. The root soft-interrupted the run; its background process became terminal but left the owned test subprocesses orphaned, which the root terminated. Routed resume then failed mechanically with `Invalid async recovery descriptor ... unknown field 'launchContractDigest'`. No writer or test process remains active.
**Evidence:** Runtime status recorded 38/80 turns, 61 tools, source edits, then a long-running focused test. Transcript evidence shows earlier expected stale-test failures were being corrected and the hanging command was `tsc --noEmit` followed by the four focused source/mounted files. Root verified the interrupted run changed only the current S4B source/test set, did not stage files, did not touch protected production/plans/docs/packages, and left the immutable plan hash/HEAD unchanged. Process inspection identified owned PIDs `4491/4494/4495`; bounded TERM cleanup stopped all three. The recoverable child session/output and current diff preserve the partial work.
**Uncertainty / gaps:** None of the partial source behavior or revised tests is accepted. The hang may be a production lifecycle leak or test-harness leak involving prepared selection, saturated repair timers, renderers, subscriptions, fake clocks, or pending promises; it must be isolated rather than hidden with forced process exit or weakened behavior. The recovery-descriptor failure is tooling-only and supplies no source verdict or material divergence.
**Recommended use:** Launch one fresh sole continuation writer from the preserved current diff. First isolate the hanging test with bounded test-name runs, correct lifecycle cleanup honestly, then finish and validate the same five issued obligations. Do not resume the invalid descriptor, overlap writers, or broaden scope.

## Handoff

**Source:** Pi subagents run `b8173cfc-3f33-4df8-b2ae-979d6547e777`, `milestone-implementer`; child session `b060c805/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/b8173cfc-3f33-4df8-b2ae-979d6547e777/output-0.log` and `.pi-subagents/artifacts/b8173cfc-3f33-4df8-b2ae-979d6547e777_*`. Recorder: orchestration root. Kind: completed S4B bounded-fix continuation handoff. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Recover the interrupted partial source, isolate the mounted hang, and finish the five verified defects without journal/protocol/protected-boundary changes.
**Outcome:** The writer reports all five corrections complete. Prepared selection now defers raw acquisition until the keyed hook has installed consumers and then acquires B before changing A; page lineage owns slash classification and covered/rollback prompt-failure truth; HTTP seed survives pre-commit publications; reconciliation uses a complete observation tuple with a saturated one-at-a-time slow retry; and hook settlement waits cancel on unmount. The hang was traced to an early test assertion plus an uncancelled hook-local slash settlement loop; cleanup now uses `finally` and cancellable per-hook waits without process exit or timer unref. Tests add precommit→canonical, exact replay/recovery, covered/uncovered failure, A→B→A classification/effect ordering, mounted ChatInput, full-tuple backoff, and rapid/reentrant prepared-selection cases. No material divergence or protected-source change is reported.
**Evidence:** The handoff reports focused `131/131`, non-real-Next `497/497`, literal `499` plus only two permitted absent-`.next/BUILD_ID` failures, isolated real development/terminal success with the same production-artifact exception, typecheck, lint, package dry-run, whitespace, hash, privacy, no-stage, route, and EventSource gates. Root verified the run is terminal with no orphan, HEAD/blob/no-stage/protected-source/EventSource/route gates, inspected the complete revised source, and independently reproduced TypeScript, lint, whitespace, and the exact focused `131/131` suite in 2.1 seconds with normal exit. Root confirms the prepare→subscribe effects/snapshots/completions→activate→attach→HTTP prompt order, page classification, covered failure outcome, precommit guard, full failure tuple, saturated delay, and unmount cancellation are present.
**Uncertainty / gaps:** Acceptance remains withheld pending a fresh independent review and browser/provider evidence. Root inspection found one untested sibling risk for review: when a current new session's prompt POST fails only after canonical activity covers its lineage, `handleSend` returns accepted from the catch path before `promoteNewSession()`, potentially leaving the executing materialized session bound to the blank/new-screen UI and URL. Existing covered-failure coverage uses an already selected session; the new-ID mounted test covers only successful POST. The synchronous raw-client-start effect edge also deserves reviewer scrutiny against the journal-free prepared-selection contract. The mounted ChatInput case exercises text acceptance directly while image rollback remains split across the mounted hook and pure composer identity tests.
**Recommended use:** Obtain one fresh independent no-edit/no-delegation review focused on the five fixes plus current-new covered-error promotion and synchronous-start effect ordering. Route any confirmed bounded defect through one final sequential fix, then repeat decisive/full validation before real Chromium/Firefox/provider evidence.

## Handoff

**Source:** Pi subagents run `ef4d7a2d-491b-4fc2-8962-b33c21588d66`, fresh `reviewer`; child session `78d7fa72/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/ef4d7a2d-491b-4fc2-8962-b33c21588d66/output-0.log`. Recorder: orchestration root. Kind: fresh independent post-fix S4B implementation review. Governing master and immutable S4B milestone/blob as above; Mode: review.
**Purpose:** Adversarially inspect the complete continuation result, with explicit focus on current-new covered failure, synchronous acquire-time state/effect ordering and reentrancy, unaccepted HTTP settlement, prepared selection, page lineage, canonical/HTTP authority, repair, coverage honesty, and protected/privacy boundaries.
**Outcome:** Verdict `REJECT`; one final bounded continuation writer is required before browser validation. The reviewer confirms the ordinary prepared B-before-A transition, hidden monitoring/release, full HTTP tuple and saturated retry, seed/prior-epoch authority, page classification/completion, and SSE removal. It confirms the root's P0 sibling defect: a current materialized new-session prompt whose exact claim is canonically covered before the HTTP response fails returns accepted before `promoteNewSession()`, stranding the executing real binding behind AppShell's new-screen state and `/` URL. It finds two additional P0 defects: accepted `SessionRegistry.acquire()` can synchronously publish a snapshot/effect before the view controller has installed its raw snapshot observation, causing effect-before-view-snapshot/drop and allowing reentrant pending-selection cancellation to leak/corrupt the just-acquired owner; and idle HTTP reconciliation can settle an unaccepted lineage, after which a definitive POST failure is falsely treated as covered. Mounted coverage masks all three.
**Evidence:** The reviewer ran the exact focused suite `131/131` and whitespace gate, traced the source ordering, and used sanitized real registry/view probes to observe effect at view cursor zero before snapshot cursor one and a three-client/two-stop reentrant leak. Root independently confirms normal promotion occurs only after successful POST while the covered catch returns first; `settlePrompt()` lacks accepted/activity proof while runtime reconciliation can call it during pre-acceptance idle; and the registry starts the client inside `acquire()` before `SessionViewTransport` assigns the returned handle or subscribes to raw snapshots. Existing tests cover successful new-ID promotion, covered failure only on an existing session, HTTP settlement only after `accepted()`, and effects only after post-acquire snapshot publication.
**Uncertainty / gaps:** The synchronous-start correction may use the milestone's explicitly permitted demonstrated bounded S4B integration-defect exception to make the accepted base registry expose an atomic pre-start snapshot consumer alongside the existing pre-start effect consumer, provided the change is minimal, preserves S4A behavior, and adds exhaustive ordering/reentrancy/rollback tests. It must not add an effect journal, change wire/server ownership, or weaken selected delivery. Current-new covered promotion must preserve generation/binding guards. HTTP settlement must require accepted lineage or exact canonical activity/settlement proof without losing recovery-snapshot settlement. Browser/provider evidence and true mounted AppShell/draft/image coverage remain absent.
**Recommended use:** Launch one final fresh sole S4B fix writer. Correct all three P0s with honest real-registry and current-new AppShell-adoption tests, add mounted draft/image behavior if feasible, run the full matrix, then perform root verification and a final fresh independent review before browser/provider validation.

## Handoff

**Source:** Pi subagents run `773616d2-444f-4930-bd50-5e96895f45ee`, `milestone-implementer`; child session `6b73a512/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/773616d2-444f-4930-bd50-5e96895f45ee/output-0.log`. Recorder: orchestration root. Kind: final bounded S4B fix implementation handoff. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Correct current-new covered promotion, synchronous registry/view snapshot-effect ordering and reentrant acquisition cleanup, and pre-acceptance idle settlement, with behavior-level tests.
**Outcome:** The writer reports all three P0s corrected. Covered current-new HTTP failure now crosses the ordinary exact-once promotion callback, leaving stale generation handling to AppShell's existing discovery-only guard. The narrowly authorized S4B integration exception adds optional `SessionAcquireOptions.onSnapshot`, installed with `onEffect` before client subscribe/start; view activation consumes atomic raw snapshots, rechecks exact currency after acquisition, and releases invalidated handles. Page settlement now requires POST acceptance or ordered canonical activity. Tests cover current/stale covered promotion, pre-acceptance idle rejection, accepted recovery idle, real registry synchronous snapshot→effect order, reentrant B activation/exact stops, and actual mounted ChatInput text/image/draft retention. No journal, wire, server, client, or broader ownership change entered scope.
**Evidence:** The handoff reports blocker/source `71/71`, exact focused `140/140`, mounted `32/32`, S4A registry/client `52/52`, non-real-Next `506/506`, literal `508` plus only the two permitted missing-build-manifest failures, isolated development/terminal passes, package, typecheck, lint, whitespace, hash, no-stage, route, EventSource, and privacy gates. Root verified terminal/no-orphan state, immutable HEAD/blob, no staging, source boundaries with only the explicit `lib/session-registry.ts` exception, inspected the registry/view/hook/tests, and independently reproduced TypeScript, lint, whitespace, and exact focused `140/140` in 2.1 seconds. Root confirms atomic current-and-future snapshot consumer installation, subscribe/start reentrancy cleanup, post-acquire currency/release, accepted-or-activity settlement guard, generation-guarded covered promotion, and mounted image/draft assertions.
**Uncertainty / gaps:** Final source acceptance still requires a fresh independent review. The optional registry seam changes S4A's public acquisition options when used but preserves omitted behavior; review must challenge existing-entry delivery, new-entry subscribe/start rollback, nested notification ordering, diagnostics, and exact cleanup. Browser/provider/network evidence remains missing. Fresh production inclusion remains release-owned because builds are prohibited and `.next/BUILD_ID` is absent.
**Recommended use:** Obtain the final fresh no-edit/no-delegation source review. If accepted, reproduce the complete matrix once on final source, then run the root-owned sanitized Chromium/Firefox/authenticated-provider S4B browser report before milestone acceptance.

## Handoff

**Source:** Pi subagents run `914796e5-dbec-427b-a8a9-ef17488ef27b`, fresh `reviewer`; child session `3fec9b9c/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/914796e5-dbec-427b-a8a9-ef17488ef27b/output-0.log`. Recorder: orchestration root. Kind: final-registry-seam independent S4B review. Governing master and immutable S4B milestone/blob as above; Mode: review.
**Purpose:** Scrutinize the atomic registry snapshot seam, reentrant view activation, covered-new promotion, unaccepted settlement, full S4B behavior, and test honesty before browser validation.
**Outcome:** Verdict `REJECT`; another bounded source fix is required, with no material divergence. The reviewer accepts containment, ordinary prepared B-before-A, current/stale covered promotion, pre-acceptance settlement guard, HTTP seed/repair, page lineage, hidden lifecycle, SSE removal, and the intended optional snapshot seam. It finds three blockers: synchronous invalidation inside `client.subscribeEffects()` leaves its returned cleanup unowned and still calls `client.start()` after registry deletion; reentrant same-ID activation while `entry.handle` is not yet assigned acquires a second raw handle that the outer activation overwrites/leaks; and keeping the submitted composer visible while optimistic `agentRunning` exposes steer/follow-up can duplicate the initial prompt, while a newer follow-up draft can be lost when current new-ID promotion changes `draftKey`.
**Evidence:** The reviewer passed TypeScript, lint, a focused `82/82`, non-real-Next `506/506`, package, whitespace, plan/source/route/EventSource/no-stage gates. Sanitized probes observed missing effect cleanup with an invalidated client still started, and a same-ID acquisition leaving raw listeners/client alive after view disposal until base-registry disposal. Root verifies `subscribeEffects()` lacks the snapshot path's unowned-cleanup/post-subscribe check; same-entry reentrancy sees `entry.handle === null`; and ChatInput retains the submitted state across `await onSend()` while ChatWindow immediately maps optimistic running to streaming controls and draft-key transition reloads the new key before the late clear guard.
**Uncertainty / gaps:** Registry fixes must mirror the snapshot unowned-cleanup pattern and add an explicit same-entry acquisition-in-progress state that coalesces/upgrades ownership without a second handle; A→B/null reentrancy and exact stops must remain. Composer correction must keep definitive-failure text/images/draft and allow real steer/follow-up during long POST acceptance without duplicating the initial prompt. A contained approach may visually detach the submitted composer into a pending snapshot, treat the displayed composer as empty/new, merge it back on definitive failure, and migrate newer state across same-mounted new-ID draft-key promotion; implementation must own image preview cleanup and unmount races. Browser evidence remains blocked.
**Recommended use:** Launch one focused continuation writer for these three defects and behavior tests, then repeat root verification and a fresh independent review. Do not weaken queue/draft behavior, force process cleanup, or begin browser validation first.

## Handoff

**Source:** Pi subagents run `93f7dc0a-ec60-4d5f-89c4-a34d8a70d0b1`, `milestone-implementer`; child session `1654db47/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/93f7dc0a-ec60-4d5f-89c4-a34d8a70d0b1/output-0.log` and `.pi-subagents/artifacts/93f7dc0a-ec60-4d5f-89c4-a34d8a70d0b1_milestone-implementer_transcript.jsonl`. Recorder: orchestration root. Kind: bounded S4B registry/composer fix handoff. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Fix effect-subscribe invalidation cleanup/start, same-entry reentrant acquisition, and pending prompt queue/draft/image ownership without weakening long-run follow-up behavior.
**Outcome:** The writer reports all three blockers corrected. Registry effect subscription now owns its returned cleanup before a post-subscribe currency check and skips start after invalidation. View entries carry one mutable acquisition intent so same-ID reentrancy joins/upgrades one raw handle while different/null invalidation still releases the outer owner. ChatInput immediately detaches ordinary submitted state into a private pending owner, leaving an empty live queue editor; success/covered acceptance disposes only submitted previews/state, definitive failure restores or deterministically prepends the failed submission to newer input, and same-mounted `new:`→real draft-key promotion migrates the newer composer. Tests cover effect-subscribe cleanup, nested omitted behavior, same-ID visible/retained upgrades, B/null invalidation, fresh follow-up-only delivery, promotion migration, merged image failure, exact preview revocation, unmount-late resolution, and late image decode.
**Evidence:** The handoff reports exact focused `152/152`, non-real-Next `518/518`, literal `520` plus only two permitted missing-build failures, isolated development/terminal success, typecheck, lint, package, whitespace, hash, no-stage, route, EventSource, and privacy gates. Root verified terminal/no-orphan state, immutable HEAD/blob/no-stage/protected boundaries, inspected all three implementations and tests, and independently reproduced TypeScript, lint, whitespace, and exact focused `152/152` in 2.1 seconds. Root confirms the unowned effect cleanup/post-check precedes start; same-entry activation coalesces and visible intent wins; submitted state is synchronously removed from value/image refs before `onSend`; real queue input remains active; failure merge and promotion migration persist drafts; and blob ownership has success/failure/unmount/late-decode tests.
**Uncertainty / gaps:** Fresh independent acceptance of these final seams remains required. Reviewer should challenge effect-subscribe throw/invalidation variants, retained→visible and same/null reentrancy, pending submission with synchronous/late resolution, user follow-up queueing, stale/new promotion, draft-key destination collisions, unmount-before-resolution, delayed image decoding, and exact preview ownership. Browser/provider/network evidence remains absent, and fresh production inclusion remains release-owned.
**Recommended use:** Launch a fresh no-edit/no-delegation review. If source is accepted, run the complete final automated matrix and then collect real sanitized Chromium/Firefox/authenticated-provider S4B evidence.

## Handoff

**Source:** Pi subagents run `3d24aafe-c4e1-4968-9050-5cf95ed3eb51`, fresh `reviewer`; child session `5367245e/run-0`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/3d24aafe-c4e1-4968-9050-5cf95ed3eb51/output-0.log`. Recorder: orchestration root. Kind: final independent S4B source review. Governing master and immutable S4B milestone/blob as above; Mode: review.
**Purpose:** Verify every prior source defect and the final registry acquisition, view reentrancy, pending composer, promotion, settlement, recovery, SSE, privacy, and test seams before browser validation.
**Outcome:** Verdict `ACCEPTED FOR BROWSER VALIDATION`; no blocker, major, or minor source defect and no material divergence remain. The reviewer accepts atomic registry snapshot/effect setup and effect-subscribe invalidation cleanup; same-ID visible/retained acquisition coalescing and B/null invalidation; B-before-A ownership; hidden/no-journal/final-effect/completion semantics; attach/promotion/pre-acceptance settlement; pending composer queue/draft/image ownership; projected/HTTP authority and repair; per-session SSE removal; privacy; and the narrow registry exception. Source supports S4B-VC-001 through 008 and 010. S4B-VC-009 and final VC-011 remain blocked only by missing real browser/provider/network evidence and final orchestration disposition.
**Evidence:** The reviewer independently ran TypeScript, lint, exact focused `152/152`, and whitespace, inspected production registry/view/hook/composer and behavior tests, and verified exact plan/HEAD/no-stage/source/EventSource boundaries. It cites real-registry ordering/reentrancy tests, mounted current/stale new promotion and settlement tests, mounted pending/follow-up/promotion/failure/image/draft/preview tests, pure/mounted recovery, and diagnostic bounds. Root independently reproduced the same focused/type/lint/whitespace gates and had already inspected the final source.
**Uncertainty / gaps:** The complete broad matrix must be reproduced once by the root on this exact source. Real Chromium, Firefox, authenticated provider, hidden/reveal, refresh/offline/reconnect/server restart, no-SSE connection inventory, and ordinary HTTP responsiveness evidence remain mandatory. Fresh production route inclusion remains release-owned due absent manifests and the build prohibition.
**Recommended use:** Run the final broad automated/package/real-development matrix, then collect and commit a sanitized S4B browser report. Accept and commit the milestone only if those gates pass; otherwise route any observed bounded browser defect through the immutable fix loop.

## Handoff

**Source:** Orchestration-root Chromium reruns and direct sanitized HTTP/WebSocket-ticket probe on the root-owned development server; recoverable sanitized result `/tmp/pi-web-s4b-chromium-result.json` and harnesses `/tmp/pi-web-s4b-browser-common.cjs`, `/tmp/pi-web-s4b-chromium.cjs`. Recorder: orchestration root. Kind: real-browser validation failure and bounded S4B integration-defect diagnosis. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4b-hook-migration-hidden-streams.md` at blob `7fc9e8e9a3960d5eeaa9c38c8f6741493d643bb4`; Mode: validation/fix trigger.
**Purpose:** Retry the required Chromium new-session hidden-first-prompt flow after correcting the harness baseline-settlement race, then determine whether failure was harness-only or a bounded product integration defect before accepting S4B.
**Outcome:** Chromium again failed before the first new-session prompt POST. The first retry exposed a harness error: `GET /api/agent/[id]` reports `running:true` while a live wrapper exists, so activity must instead use `state.isPromptRunning || state.isStreaming || state.isCompacting`. After that correction, the browser reached the new-session flow, completed one `ensure_session` request successfully, attempted the session ticket repeatedly, opened no session socket, and never issued the prompt POST. A direct sanitized probe proved the circular boundary exactly: ensure returned 200; the session ticket returned `404 session_not_found` before prompt; the prompt HTTP command returned 200 and real activity was observed; an immediate ticket still returned 404; only after provider settlement did the ticket return 200. The hook is required to await `binding.waitUntilAttached()` before prompt POST, while the S3 ticket issuer currently requires a discoverable materialized session file/header that the ensured wrapper does not publish until prompt work persists it. This blocks every browser first prompt on a newly ensured session.
**Evidence:** Sanitized failed-browser counters: one successful ensure, zero prompt starts/finishes, one active global socket, zero active session sockets, repeated session-ticket starts, and zero agent EventSources. Root traced the wait to `hooks/useAgentSession.ts` (`ensureNewSession` → prepare/begin claim → `waitUntilAttached` → prompt) and the 404 to `app/api/transport/ticket/route.ts` resolving/reading the session file before considering the live ensured wrapper. The direct probe emitted only HTTP statuses, finite error classes, and activity booleans—no IDs, tickets, paths, prompts, provider payloads, or credentials. No implementation source was edited by the root.
**Uncertainty / gaps:** The smallest safe correction remains for the sole writer to determine. It may materialize the ensured native owner before returning or securely authorize the already-live exact wrapper without weakening ticket metadata/identity checks; it must preserve same-host one-use tickets, existing-session bootstrap, attach-before-prompt, exact wrapper identity, no new wire/protocol, and no browser run ownership. This is an observed bounded S4B integration defect, not material divergence, unless implementation proves those requirements incompatible with accepted S3/S4B seams.
**Recommended use:** Stop browser acceptance, launch one focused sole S4B fix writer under the immutable milestone, add a real new-ensure→ticket→ready→first-prompt regression at the actual route/runtime boundary, then repeat root verification, independent review, and Chromium/Firefox/provider validation.

## Handoff

**Source:** Pi subagents run `47cfe680-8296-4711-89a6-207235adb99b`, `milestone-implementer`; child session `019fc4e7-524e-791e-87c2-f3e86692f5d4`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/47cfe680-8296-4711-89a6-207235adb99b/output-0.log`. Recorder: orchestration root. Kind: bounded S4B ensure/ticket integration-fix handoff. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Break the verified new-session attach-before-prompt circularity without synthetic persistence, weaker authorization, a new wire/barrier, or changes to the accepted browser/session protocol.
**Outcome:** The writer chose secure admission of an explicitly marked, already-live exact native owner. `AgentSessionWrapper` now captures an immutable manager-derived session ID/file/cwd target only after successful `ensure_session`; the ticket issuer admits that target before disk discovery only while the exact live wrapper remains the current registry owner and its manager, exposed file, hub, and compatibility checks still match. Ordinary persisted-session resolution/start remains intact. A dependency-injected new-agent route seam and actual custom-server route test cover ensure → ticket → consume/ready/snapshot → first HTTP prompt without fabricating a header. Adversarial tests cover stale/dead/mismatched/closed/incompatible/replaced targets and failed identity establishment. No hook, browser registry, V1 wire/client/reducer/hub, file-watch, OAuth, package, dependency, documentation, or checkpoint source was changed.
**Evidence:** Root confirmed terminal writer/no overlap, unchanged HEAD `86049ae`, plan blob `7fc9e8e`, no staging, the six-file fix boundary, route absence, exact four file-watch plus one OAuth EventSource inventory, and `git diff --check`. Root inspected the production diff and independently reproduced TypeScript, lint, and changed-surface `112/112`. A real root-owned development-server probe with the actual provider path then produced only sanitized outcomes: ensure 200, pre-prompt ticket 200, ready first, snapshot complete, prompt 200, native active edge observed, projected activity frame observed, and clean settlement. The writer additionally reports exact S4B focused `154/154`, non-real-Next `526/526`, literal `528` plus only the two permitted missing-`.next/BUILD_ID` failures, isolated terminal/development success, package dry-run, privacy/source/hash/no-stage gates, and no build.
**Uncertainty / gaps:** A fresh independent review remains mandatory, especially for failed-ensure cleanup before identity marking, current-owner/replacement races, lifetime of the ensured marker after persistence, ordinary persisted fallback, route export/test seams, and ticket consume-time ownership. Real Chromium/Firefox hidden/reveal/reconnect/provider evidence remains unaccepted; fresh production inclusion remains release-owned.
**Recommended use:** Obtain one fresh no-edit/no-delegation review of the six-file fix and its interaction with the complete S4B diff. Route any confirmed bounded defect through one sequential fix; otherwise rerun the previously failing Chromium flow before the broader browser matrix.

## Handoff

**Source:** Pi subagents run `2010a6ce-8913-4bff-8e2a-c239e2848181`, fresh `reviewer`; child session `019fc4ef-7a6b-7930-9870-eb9fa29bd665`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/2010a6ce-8913-4bff-8e2a-c239e2848181/output-0.log`. Recorder: orchestration root. Kind: mandatory fresh independent ensure/ticket fix review. Governing master and immutable S4B milestone/blob as above; Mode: review.
**Purpose:** Challenge ensured-owner authorization, persisted fallback, replacement/HMR/liveness races, failed setup cleanup, route/test honesty, privacy, and interaction with the accepted complete S4B source before resuming browser validation.
**Outcome:** Verdict `FINDINGS`. The reviewer accepts the central correction: immutable manager-derived identity, exact current registry owner and hub revalidation, durable-marker safety, ordinary persisted/HMR-old fallback, same-host/one-use opaque tickets, actual gateway/ready/prompt ordering, protocol boundaries, and privacy. It finds two bounded major defects on the touched new-session route. First, model or thinking setup (and other post-start setup) can fail before identity marking, while destruction is scoped only to the marker block, leaving an unreachable published wrapper/hub/timer/registry owner until idle. Second, the claimed one-time startup key remains `__new__${Date.now()}`; concurrent requests in one millisecond share `getOrCreateRpcSession`'s start promise and can receive one wrapper/session ID. Neither is material divergence.
**Evidence:** The reviewer independently passed TypeScript, lint, whitespace, changed-surface `112/112`, and exact S4B focused `154/154`; verified HEAD/no-stage boundaries; and used read-only dependency probes to observe a 500 model-setup response with zero destruction and identical temporary keys for same-tick requests. Root directly confirms both source paths: publication precedes model/thinking setup, the outer catch does not own the session, and start locks are keyed by the millisecond-derived temporary string.
**Uncertainty / gaps:** Cleanup must remain limited to an ensure request whose owner has not been successfully returned; ordinary prompt HTTP ambiguity must not destroy accepted work. The extension-binding case must be tested through an actually awaited setup/send failure without changing ensure into a new binding barrier. Collision correction must preserve the existing real-ID publication/cache path. Browser/provider evidence remains blocked, not waived.
**Recommended use:** Launch one focused sequential sole writer to make per-request startup keys collision-resistant and guarantee exact-once destruction for every failed ensure setup/identity path, with concurrent-route and model/thinking/binding failure tests. Then repeat root verification and a fresh independent review before browser rerun.

## Handoff

**Source:** Pi subagents run `f1be69e1-4ef0-4ac5-b03b-c24a379c3d95`, `milestone-implementer`; child session `019fc4fe-13c3-7478-bcd9-d97e6c8cb46e`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/f1be69e1-4ef0-4ac5-b03b-c24a379c3d95/output-0.log`. Recorder: orchestration root. Kind: sequential S4B new-session cleanup/start-key fix handoff. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Correct the rejecting review's two bounded route defects while preserving the accepted ensured-owner ticket path and ordinary prompt ambiguity semantics.
**Outcome:** The route now creates a collision-resistant `__pi_web_new_request__:<randomUUID>` lock key per request. For exact `ensure_session`, it captures cleanup ownership immediately after startup; any later allowed-root, list-invalidation, model, thinking, ID, identity-marker, or response-construction failure reaches one centralized idempotent destroy. Successful ensure returns without cleanup. Non-ensure prompt failures still leave potentially accepted target-owned work intact and no new extension-binding barrier was introduced. Focused tests cover every named failed setup with native disposal, hub close, registry removal, exact route destroy count, successful retention, ordinary binding ambiguity, and deterministic distinct same-tick keys/owners.
**Evidence:** Root confirmed terminal/no-overlap state, unchanged HEAD/blob/no-stage, inspected the two-file delta, and independently reproduced TypeScript, lint, whitespace/boundary gates, changed server `121/121`, and exact S4B focused `163/163`. The writer reports non-real-Next `535/535`, literal `537` plus only the two permitted missing-`.next/BUILD_ID` failures, isolated terminal/development success, package dry-run, privacy/source/EventSource/route/hash gates, and no build.
**Uncertainty / gaps:** Fresh independent review remains mandatory. The concurrent test injects deterministic distinct keys and a shared-start model rather than relying on timing; review must verify production `randomUUID` invocation and ensure cleanup cannot cross into an ordinary ambiguous prompt. Real browsers/provider/network remain unaccepted.
**Recommended use:** Obtain a fresh no-edit/no-delegation review focused on exact cleanup ownership, success return, ordinary prompt ambiguity, startup-lock isolation, and the retained secure ticket path. If accepted, restart real-browser validation from the previously failing Chromium flow.

## Handoff

**Source:** Pi subagents run `1c0b995b-0267-4afe-9c90-f9dadabd265b`, fresh `reviewer`; child session `019fc508-d87a-7520-b74c-39930748c504`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/1c0b995b-0267-4afe-9c90-f9dadabd265b/output-0.log`. Recorder: orchestration root. Kind: fresh post-cleanup/start-key independent S4B review. Governing master and immutable S4B milestone/blob as above; Mode: review.
**Purpose:** Verify cleanup ownership, success/ordinary-prompt separation, per-request lock isolation, secure ensured tickets, persisted fallback, actual route/gateway ordering, and protected boundaries after the latest fix.
**Outcome:** Verdict `FINDINGS`. The reviewer accepts random-UUID request keys; exact ensure-only cleanup and successful retention; ordinary prompt target ownership; no binding barrier; ensured wrapper/manager/file/cwd/current-owner/hub validation; persisted/HMR fallback; actual ready/snapshot/prompt test; same-host/one-use opacity; SSE removal; and all protected source boundaries. It finds one bounded major lifecycle defect: real startup caches both `sessionId → allocated file` and reverse `file → sessionId` before route setup. Failed ensure destruction removes the wrapper/hub/timer/registry owner but does not invalidate either cache, so repeated setup failures retain nonexistent native session paths in process-global maps. Existing injected failure tests bypass real cache publication and cannot see it.
**Evidence:** The reviewer traced cache publication to `getOrCreateRpcSession`, destruction to `AgentSessionWrapper.destroy`, and dual invalidation to `invalidateSessionPathCache`; a read-only real-publication probe observed a dead/unregistered owner with both mappings still present. Root verifies `destroy()` has no session-reader cache cleanup and the route catch currently only destroys. Focused and changed-server suites remained passing.
**Uncertainty / gaps:** Invalidation must address exactly the failed ensure's `realSessionId` and preserve the successful ensure cache used before persistence; it must not delete unrelated replacement state or alter ordinary prompt ambiguity. Browser evidence remains blocked, not waived.
**Recommended use:** Launch one narrow sequential sole writer to invalidate both path-cache directions after failed ensure destruction and add real `getOrCreateRpcSession` publication tests for failure removal and success retention. Then repeat root verification and fresh independent review before Chromium.

## Handoff

**Source:** Pi subagents run `c1a27435-68b9-465e-8a7f-7ec22f61006b`, `milestone-implementer`; child session `019fc50d-45f7-7739-803a-fa289b59c616`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/c1a27435-68b9-465e-8a7f-7ec22f61006b/output-0.log`. Recorder: orchestration root. Kind: sequential S4B failed-ensure path-cache cleanup handoff. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Remove the last reviewed failed-ensure owned-resource leak without changing successful pre-persistence resolution or ordinary prompt ambiguity.
**Outcome:** The route now captures the failed ensure's exact returned real ID with its cleanup owner. Its guaranteed catch destroys that owner and then invokes existing ownership-aware `invalidateSessionPathCache(realSessionId)`, removing forward and matching reverse mappings. Successful ensure retains both mappings; non-ensure failure neither destroys nor invalidates. Real `getOrCreateRpcSession` publication tests cover failure removal, reverse-owner replacement preservation, successful pre-persistence retention, and ordinary prompt-failure retention.
**Evidence:** Root confirmed terminal/no-overlap state, unchanged HEAD/blob/no-stage, inspected the route and real-publication tests, and independently reproduced TypeScript, lint, whitespace/boundary gates, and changed server `125/125`. The writer reports exact S4B focused `167/167`, non-real-Next `539/539`, package dry-run, isolated terminal/development success with only the permitted missing production build preflight, privacy/EventSource/route/hash gates, and no build.
**Uncertainty / gaps:** Fresh independent acceptance remains mandatory. Review must verify exact-ID invalidation cannot erase replacement-owned reverse state and that successful/ordinary paths retain cache ownership. Browser/provider evidence remains pending.
**Recommended use:** Obtain a final fresh no-edit/no-delegation review of the complete ensure/ticket correction and cleanup chain. If source is accepted, restart Chromium from the formerly deadlocked first-prompt flow.

## Handoff

**Source:** Pi subagents run `8622143e-7234-44de-825a-e71d82aaa4d8`, fresh `reviewer`; child session `019fc519-3d76-71d0-b50a-f54e64fcc13f`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/8622143e-7234-44de-825a-e71d82aaa4d8/output-0.log`. Recorder: orchestration root. Kind: fresh independent full ensure/ticket/cleanup review. Governing master and immutable S4B milestone/blob as above; Mode: review.
**Purpose:** Reassess the complete new-session startup, ensured ticket, failure cleanup, path-cache, replacement, security, compatibility, privacy, and actual route/gateway evidence chain.
**Outcome:** Verdict `FINDINGS`. The reviewer accepts every prior correction and protected boundary, but finds one final bounded replacement race: failure cleanup destroys the old owner and then invalidates solely by ID. If a different same-ID wrapper becomes current while awaited model/thinking setup is pending and publishes a replacement file mapping, old-owner destruction correctly preserves the registry replacement, but ID-only invalidation then removes the replacement's current forward and reverse cache mappings. The existing replacement test uses a different replacement ID and does not cover this case.
**Evidence:** The reviewer passed TypeScript, lint, changed server `125/125`, exact focused `167/167`, non-real-Next `539/539`, package, whitespace, hash, no-stage, and inventory gates. Root confirms `invalidateSessionPathCache` unconditionally deletes the current forward mapping for the supplied ID and matching reverse owner; the route does not check current registry ownership after destroying the failed owner.
**Uncertainty / gaps:** Cleanup must skip old-cache invalidation whenever a different same-ID registry owner is current, without retaining failed-owner mappings in the ordinary no-replacement case or weakening reverse-owner checks. Browser evidence remains blocked.
**Recommended use:** Launch one narrowly focused sole writer to make failed-ensure cache invalidation same-ID-replacement-aware and add a real-publication replacement-file test. Repeat root verification and fresh review; only then resume Chromium.

## Handoff

**Source:** Pi subagents run `5af9bf65-834d-4a3c-8d12-622ada5cc418`, `milestone-implementer`; child session `019fc518-d7fc-7f35-9bbd-48ab607d36e3`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/5af9bf65-834d-4a3c-8d12-622ada5cc418/output-0.log`. Recorder: orchestration root. Kind: sequential S4B same-ID replacement cache fix handoff. Governing master and immutable S4B milestone/blob as above; Mode: fix.
**Purpose:** Preserve a current same-ID replacement's discovery mappings when an older ensured setup later fails, while retaining every prior cleanup/security invariant.
**Outcome:** After old-owner destruction, failed ensure cleanup now reads only the current registry owner for that ID. It invalidates the path cache only when there is no different current owner; it does not start, send to, destroy, or mutate a replacement. A real-publication race test blocks old model setup, publishes a different wrapper with the same ID and different file/cache pair, then fails the old setup and proves exact old disposal plus replacement liveness, registry ownership, and both cache mappings. No-replacement and different-ID cases remain covered.
**Evidence:** Root confirmed terminal/no-overlap state, unchanged HEAD/blob/no-stage, inspected the route and race test, and independently reproduced TypeScript, lint, whitespace/boundary gates, and changed server `126/126`. The writer reports exact focused `168/168`, non-real-Next `540/540`, package, isolated terminal/development success with only missing production build preflight, inventory/privacy/hash gates, and no build.
**Uncertainty / gaps:** One fresh independent review remains mandatory. Browser/provider evidence remains pending.
**Recommended use:** Obtain fresh no-edit/no-delegation acceptance of the complete ensure/ticket and cleanup chain. If accepted, immediately rerun Chromium rather than extending source review speculatively.

## Handoff

**Source:** Pi subagents run `70c56bcf-d54c-4a96-8e58-66263c932863`, fresh `reviewer`; child session `019fc52c-110c-78b0-be33-b54f32413e3c`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/70c56bcf-d54c-4a96-8e58-66263c932863/output-0.log`. Recorder: orchestration root. Kind: final independent ensure/ticket/startup/cleanup source review. Governing master and immutable S4B milestone/blob as above; Mode: review.
**Purpose:** Accept or reject the complete corrected new-session chain after exact ensured admission, UUID startup isolation, failed-setup disposal, dual cache cleanup, and same-ID replacement protection.
**Outcome:** Verdict `ACCEPTED`; no blocker, major, or minor source finding. The reviewer accepts per-request lock isolation; exact ensure-only cleanup and ordinary prompt ambiguity; owner/native/hub/timer/registry exact disposal; no-, different-ID-, and same-ID cache replacement behavior; successful pre-persistence retention; immutable manager-derived ensured identity; exact current owner/hub admission; persisted/HMR fallback; same-host one-use opacity; honest real custom-server ready/snapshot/prompt/activity evidence; privacy; SSE removal; and every protected source boundary.
**Evidence:** Independent TypeScript, lint, changed server `126/126`, exact focused `168/168`, whitespace, hash, no-stage, and EventSource inventory passed. The review cites the real-publication replacement race and actual gateway integration, not only injected unit stubs. Root had independently reproduced the same decisive gates and inspected each source transition.
**Uncertainty / gaps:** S4B-VC-009 and final S4B-VC-011 remain blocked only by real Chromium/Firefox/authenticated-provider/network evidence and final orchestration disposition. Fresh production inclusion remains release-owned.
**Recommended use:** Restart the root-owned development server and rerun the previously deadlocked Chromium hidden-first-prompt flow, followed by the remaining browser matrix if it passes.

## Handoff

**Source:** Orchestration-root real-browser runs using Chromium 149.0.7827.55 and Firefox 153.0.1; durable sanitized report `.agents/reports/2026-08-02-s4b-browser-session-migration.md`; ephemeral raw sanitized summaries `/tmp/pi-web-s4b-chromium-result.json` and `/tmp/pi-web-s4b-firefox-result.json`. Recorder: orchestration root. Kind: S4B real-browser/authenticated-provider validation. Governing master and immutable S4B milestone/blob as above; Mode: validation.
**Purpose:** Prove the formerly blocked attach-before-first-prompt flow and the required selected/hidden/reveal/reselection/reconnect/offline/reload/restart/no-SSE/HTTP behavior in both target browser engines after final source acceptance.
**Outcome:** Chromium and Firefox both passed. Each page began and ended with exactly one Pi Web global socket and one selected-session socket; the additional development socket was Next HMR and excluded. Each browser completed one ensured new session plus five accepted authenticated-provider prompts with five persisted assistant messages, hidden-before-activity selection, reveal convergence, rapid hidden/reselection, forced session-socket recovery, offline/online recovery during provider work, reload during provider work, server restart recovery, and responsive ordinary HTTP. Selection caused zero abort/Stop commands. Both pages constructed zero agent EventSources. Chromium also completed `/clone` and `/compact` dispatch waits before restart; automated suites remain authoritative for detailed command/state matrices.
**Evidence:** Durable report records finite counts and limitations only. Chromium final topology was global `1`/session `1`, with two global and nine session tickets across reload/restart, eight opened and six observed-closed session sockets, five successful prompt HTTP responses, and no page errors. Firefox final topology was identical, with two global and nine session tickets, seven opened and five observed-closed session sockets, five successful prompts, and successful WebDriver BiDi offline emulation. Both recovered their final transcript and HTTP probes after server restart. No IDs, paths, tickets, prompts, provider payloads, credentials, or content enter the report.
**Uncertainty / gaps:** Headless functional/network evidence does not replace S7 rich visual/user responsiveness acceptance. File watchers were not mounted and remain S5. OAuth was not invoked and remains the short-lived exception. Fresh production inclusion remains release-owned. Summed metrics across page reloads are cumulative document segments, not concurrent socket counts; the report distinguishes the decisive per-document topology.
**Recommended use:** Reproduce the complete final automated/package/real-development matrix on this exact source, then append the S4B Implementation Summary and commit the coherent milestone boundary if all remaining gates pass.

## Handoff

**Source:** Orchestration-root final S4B boundary validation on the source accepted by reviewer `70c56bcf-d54c-4a96-8e58-66263c932863`. Recorder: orchestration root. Kind: final automated/package/source-boundary validation. Governing master and immutable S4B milestone/blob as above; Mode: validation.
**Purpose:** Reproduce every required command class after the final source fix and browser evidence, distinguish the permitted absent-production-artifact result, and establish a commit-ready milestone boundary.
**Outcome:** Final gates pass. TypeScript and lint pass. Exact focused S4B is `168/168`; changed ensured-ticket/RPC integration is `126/126`; exact non-real-Next is `540/540`. Literal wildcard reports `542` passes and only the two expected parent/subtest failures for absent `.next/BUILD_ID`. Isolated real-Next reports terminal and development pass and the same two production preflight failures. Package dry-run passes with the expected nine-file shape. Whitespace, plan hash, HEAD, no-stage, route deletion, EventSource inventory, privacy, report presence, and source-boundary gates pass.
**Evidence:** Root executed the exact non-real command and literal wildcard separately, then real-Next without overlap; ran no build. `git diff --check` is clean; HEAD remains `86049ae`; immutable plan blob remains `7fc9e8e`; no file is staged; `.pi-subagents/` remains excluded. Production callers contain exactly four S5 file-watch EventSources and one OAuth login EventSource, with no agent EventSource or per-session SSE route. The complete intended new module/test/report set exists.
**Uncertainty / gaps:** Fresh production inclusion cannot be established without the prohibited development build and remains release-owned exactly as planned. S5 file watch, S6 lifecycle/security/shutdown, and S7 combined scale/rich visual/user acceptance remain future milestones, not S4B departures.
**Recommended use:** Accept S4B-VC-001 through S4B-VC-011, append the pending implementation summary, and commit the coherent S4B implementation/report/checkpoint boundary excluding `.pi-subagents/`.

## Implementation Summary

**Plan section:** S4B — Hook Migration and Retained Hidden Session Streams; S4B-VC-001 through S4B-VC-011.
**Work and outcome:** Added the page-owned session-view transport, pure projected-view adapter, cursor-observed HTTP reconciliation, stable prompt/completion lineage, retained-hidden lifecycle, atomic registry snapshot/effect acquisition, pending composer/draft/image ownership, and full hook/AppShell/provider integration. Removed the per-session SSE caller and route. The browser now attaches the exact new/existing session socket before prompt dispatch, preserves selected and locally active hidden streams without owning or aborting server work, converges through canonical snapshots/effects plus guarded HTTP repair, and retains current commands/product behavior. Browser validation exposed and the fix loop corrected a pre-persistence ensure/ticket deadlock, nonunique new-session lock keys, failed-ensure owner/cache leaks, and replacement races with secure exact native-owner admission. Chromium and Firefox now pass the required S4B transport/recovery/provider flows.
**Validation / evidence:** TypeScript and lint pass; focused `168/168`; changed server `126/126`; non-real-Next `540/540`; literal `542` pass plus only two permitted missing-build preflight failures; isolated terminal/development pass with the same production-artifact exception; package dry-run and whitespace pass. Final independent source review `70c56bcf-d54c-4a96-8e58-66263c932863` is clean. Sanitized browser evidence is `.agents/reports/2026-08-02-s4b-browser-session-migration.md`: Chromium and Firefox each prove one global plus one selected-session Pi Web socket, hidden-before-activity completion/reveal/reselection, reconnect, offline/online, reload, server restart, five authenticated-provider completions, responsive HTTP, zero selection abort/Stop, and zero agent EventSources.
**Departures from approved obligations:** None. Fresh production inclusion remains the plan's explicit release-owned boundary; file watch, 30-minute idle/heartbeat/shutdown, combined scale, rich visual checks, and user acceptance remain S5-S7 exactly as assigned.
**Implementation commit:** Pending.
