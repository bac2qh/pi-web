# S3 Secure Server Session WebSocket Checkpoints

Plan: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`

## Handoff

**Source:** Pi subagents run `fb86f885-46c1-424e-9731-c8a31aa7bf2e`, `scout` child 0. Recorder: orchestration root. Kind: read-only S3 bootstrap/security context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S3; Mode: investigation.
**Purpose:** Reconcile the accepted M0/S1 gateway and custom server, accepted S2 wrapper/hub, ticket route, startup locks, session reader, HMR registration, destruction, privacy, and compatibility boundaries into one contained secure server session-WebSocket milestone without editing or delegating.
**Outcome:** The scout confirmed the exact S3 trust flow: strict same-host `{channel:"session",sessionId}` bootstrap, existing `resolveSessionPath()` plus `startRpcSession()` shared lock, fail-closed compatible hub check, opaque server-created context stored only in the one-use ticket, static `session` handler, and `hub.attach()` as the sole gap-free subscription primitive. It identified the current blocking seam fact—not a material divergence—that gateway tickets retain only channel/expiry and the custom server dispatches no channel-owned context. It also confirmed consume-before-admission is an intentional fail-closed spent-ticket boundary, disconnect must never own wrapper lifecycle, pre-S2/incompatible wrappers cannot be retrofitted, and wrapper destruction needs an observable subscriber teardown path.
**Evidence:** Root verified the cited current sources: `bin/pi-web-transport-gateway.js` ticket map and deletion; `bin/pi-web-server.js` ticket/origin/admission/dispatch order; `app/api/transport/ticket/route.ts` bounded exact body; `lib/websocket-gateway.ts` structural process boundary; `lib/rpc-manager.ts` wrapper capability, registry/start Promise, and destruction observers; `lib/session-event-hub.ts` atomic attach/replay; `lib/session-reader.ts`; global channel HMR pattern; per-session SSE compatibility route; and matching gateway/route/server tests. Accepted S2 commits are `39f22f0` and `26ec788`. No advisory pointer companion exists.
**Uncertainty / gaps:** The scout left two implementation choices for the plan: reject versus recreate an incompatible inactive wrapper, and ticket/body versus post-upgrade resume metadata. The root selects the master/S2-authorized safest forms: reject every missing/incompatible/closed wrapper without mutation, and keep the exact ticket body/query by requiring one bounded post-upgrade resume control frame. Wrapper-observer accumulation is resolved in the draft through one channel-owned per-wrapper owner record preserved across compatible registration replacement. Abandoned tickets may leave a wrapper to ordinary ten-minute idle; S6 owns the later 30-minute policy.
**Recommended use:** Review the draft's exact gateway feature marker, frozen server-only authorization record, static registration/owner registry, finite error mapping, and no-S4/S6 boundaries. Implement only after the plan is approved and committed.

## Handoff

**Source:** Pi subagents run `fb86f885-46c1-424e-9731-c8a31aa7bf2e`, `scout` child 1. Recorder: orchestration root. Kind: read-only S3 resume/output/Node-client context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S3; Mode: investigation.
**Purpose:** Derive an exact transport-control, atomic catch-up, independent-subscriber, bounded-output, retryable-close, and real Node-client seam from the accepted S2 protocol/hub and current `ws` server without entering browser, heartbeat, idle, file-watch, or final shutdown scope.
**Outcome:** The scout confirmed that an exact first resume frame is necessary because the approved bootstrap/query carries no cursor, and a minimal ready frame is necessary because current-cursor attach returns no projected unit. It identified the decisive ordering rule: pause the subscriber pump, call `hub.attach()` once, place ready and returned catch-up ahead of live listener output, then drain. It also established that `socket.bufferedAmount` alone omits application-queued data; each socket needs independent UTF-8 byte accounting, one in-flight callback send, a 4 MiB live/output bound, immediate unsubscribe on overflow, retryable close, and idempotent cleanup across send/error/close/owner races. Ordinary frames can exceed the output bound and must close/recover rather than truncate; canonical snapshots may exceed 4 MiB but already consist of bounded transfer units and therefore require incremental source draining.
**Evidence:** Root verified `ProjectedSessionHubReader.attach()` and replay outcomes, snapshot/replay/unit limits in `lib/session-protocol.ts` and `lib/session-event-hub.ts`, listener publication order and tests, wrapper hub/destruction behavior, current global channel's insufficient immediate-send pattern, custom-server 16 KiB inbound ceiling/compression setting, package boundary, and installed `ws` send callback, `bufferedAmount`, close, and terminate behavior. The scout supplied deterministic fake-socket and real-loopback test seams for initial/exact/recovery, reentrant ordering, stalled versus healthy subscribers, wrapper teardown, hostile frames, ticket binding, and HTTP schedulability.
**Uncertainty / gaps:** A snapshot source larger than 4 MiB cannot be counted wholesale as socket-buffered output without making approved recovery impossible. The draft therefore distinguishes S2's separately bounded immutable catch-up source from encoded live queue/in-flight bytes, encodes one source unit at a time, and releases sent references. Standard `1013` is selected over a private close code; a bounded terminate fallback preserves cleanup when a stalled peer cannot receive it. No browser acknowledgement, heartbeat, semantic-idle touch, or HTTP generation protocol is authorized.
**Recommended use:** Independently challenge ready-before-catch-up ordering, selected-source versus live-queue accounting, exact close/timer ownership, oversized-frame recovery, real same-port Node integration, and the production-evidence boundary before plan approval.

## Handoff

**Source:** Pi subagents run `bc82504a-4bb4-457f-a664-4eb3e8787564`, `reviewer` child 0. Recorder: orchestration root. Kind: independent S3 draft-plan review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: review.
**Purpose:** Adversarially review the draft for contained authority, deterministic authorization/control/output semantics, lifecycle/security/privacy correctness, executable Node/server validation, honest package/real-Next evidence, and complete S3/master VC mapping without editing or delegating.
**Outcome:** The reviewer found no material divergence and recommends approval only after eight bounded draft corrections. It accepted the exact session body, post-upgrade resume and minimal ready control, consume-before-admission, reject-all incompatible-wrapper policy, atomic `hub.attach()`, raw-SSE preservation, no-build evidence boundary, and telemetry restrictions. It required stronger prepublication ID/file/cwd validation; exact gateway/context marker shapes; pre-resume owner registration/deletion; target-only ready cursor semantics plus `ws`-owned `1007`/`1009`; wire ordering that matches the actual attach API; bounded early-stop encoding before a >4 MiB string exists; a concrete no-provider real route fixture plus mixed cap evidence; and explicit preservation/partial mapping for ORCH-VC-002/005/010.
**Evidence:** The reviewer traced `getOrCreateRpcSession()` publication timing and hook behavior; bounded session header limitations; current gateway compatibility and silent ignored arguments; wrapper `onDestroy`; S2 `attach()` reentrant buffering and mutable exact-replay result; projected encoder allocation; installed `ws` protocol close paths; current real-loopback route gaps; master admission requirements; and production-manifest preflight. The root accepted every finding and revised the draft with exact 256-character ID and 4,096-byte path/cwd bounds, baseline plus hook prepublication checks, `ticketContextVersion:1`, the exact five-field shallow-frozen record, pre-resume owner teardown, 128-character ready fields/target semantics, `1007`/`1009`, actual attach-result ordering, bounded byte-identical encoder/cache requirements, exact timers, synthetic preseeded route integration, mixed 64/65 and deterministic 256/257 tests, and corrected ORCH mapping.
**Uncertainty / gaps:** Fresh corrected-draft verification remains required before approval. S2 still synchronously materializes a complete bounded snapshot source, and S3 can only release it incrementally after receipt; no measured fact authorizes redesign. Oversized transient one-shot effects are intentionally not recreated. Silent half-open detection remains S6 heartbeat scope. Real distributed 256/257 remains under the accepted S1 evidence boundary and cannot be represented as newly passed by deterministic S3 tests.
**Recommended use:** Obtain one fresh read-only corrected-draft review. If it finds no remaining blocker or ambiguity, change only `Status: draft` to `Status: approved`, commit the plan/checkpoint boundary excluding `.pi-subagents/`, and issue the immutable plan to one fresh milestone implementer.

## Handoff

**Source:** Pi subagents run `48d27d5d-bc1e-4b8f-aa44-07dbe2e878ae`, `reviewer` child 0. Recorder: orchestration root. Kind: final independent corrected S3 draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: review.
**Purpose:** Verify every bounded correction from the first plan review and challenge the complete corrected draft again for implementation ambiguity, security/privacy gaps, untestable validation, weaker master gates, or S4-S6 leakage before approval.
**Outcome:** The reviewer returns `APPROVE AS WRITTEN` with no blocker, major, or minor draft defect. It confirmed prepublication existing-file identity hardening remains compatible with new-session creation; exact ticket-context markers and HMR behavior are deterministic; pre-resume owner cleanup and timers are complete; ready target semantics and actual attach ordering are exact; the bounded early-stop byte-identical encoder is feasible without changing V1; the no-provider real route and mixed cap fixtures are executable; the ORCH mapping is complete; and the no-build production evidence is honest.
**Evidence:** The reviewer mapped the corrected plan directly to current wrapper publication/start locks, header reader, gateway ticket lifecycle, custom-server dispatch, wrapper destruction observers, installed `ws` close behavior, S2 attach/replay/snapshot limits, existing real 64/65 and deterministic 256/257 test precedents, package boundary, and missing production manifest. It independently passed a decisive 156-test current baseline and observed no file edit or stage. The review is recoverable by run identity.
**Uncertainty / gaps:** S2's separately bounded snapshot source may still be memory-heavy before S3 incrementally releases it, and the early-stop encoder is technically delicate; both are explicit implementation/test obligations rather than authority gaps. Fresh production route inclusion remains release-owned. No material divergence exists.
**Recommended use:** Approve and commit this exact plan/checkpoint boundary, record its immutable blob at writer launch, and issue one fresh sole milestone implementer with S3-VC-001 through S3-VC-010 and every stated stop condition.

## Handoff

**Source:** Pi subagents run `71aaad38-b188-4832-bb65-1d7f4a7937f8`, `milestone-implementer` child 0, session `019fc0f7-7fee-7d4d-9f88-6877e9e14547`. Recorder: orchestration root. Kind: initial S3 implementation handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: implementation.
**Purpose:** Implement the complete immutable S3 server session-WebSocket boundary: opaque metadata-bound tickets, exact resume/ready control, static HMR-safe channel ownership, atomic projected-hub attachment, independent bounded subscriber pumps, startup identity hardening, and deterministic plus real Node evidence, without entering browser, SSE-removal, file-watch, heartbeat, idle, or final-shutdown scope.
**Outcome:** The writer added the context-capable gateway/dispatcher and exact session ticket bootstrap; strict V1 transport control; one static session channel with wrapper-identity ownership; ready/catch-up/live ordering through one `hub.attach()`; bounded output pumping; existing-file startup identity validation; and focused fake/real tests. An implementation-side read-only review found and the writer corrected post-send `bufferedAmount` overflow and closed-before-dispatch subscriber retention. Root inspection confirmed the intended authorization, startup-identity, owner-registry, atomic-attach, output-pump, and unchanged browser/SSE boundaries. The attempt is not accepted because root scrutiny and fresh review found one P0 bounded-encoder defect and required evidence gaps.
**Evidence:** The writer reported TypeScript and lint passing; focused S3 `180/180`; exact non-real-Next `382/382`; literal full `384/386`; separate real-Next `2/4`; and package dry run, whitespace, immutable-plan, privacy/source-boundary, and no-stage gates passing. The two failures in each real-Next-inclusive run were solely the permitted absent `.next/BUILD_ID` production child/parent. Root independently passed TypeScript, lint, focused `180/180`, and whitespace inspection; inspected the actual unstaged diff; and reproduced an already marked deeply frozen canonical wide frame returning `over_limit` only after all 1,000 selected tail descriptors were inspected.
**Uncertainty / gaps:** The bounded encoder unconditionally reparses/clones a complete canonical frame before its byte-limited writer, violating S3-VC-006 early stop and allowing synchronous work/allocation proportional to an over-limit tail. Fresh production inclusion remains release-owned under the build prohibition. `.pi-subagents/` remains unrelated, untracked, and excluded. No protocol/product choice, weaker gate, scope expansion, or material divergence is implicated.
**Recommended use:** Reconcile the fresh independent review, then issue one sequential bounded fix under the same immutable milestone for true early-stop traversal plus every confirmed missing S3 evidence case. Repeat complete root verification and fresh independent review before acceptance.

## Handoff

**Source:** Pi subagents run `30a5e3eb-a06b-4502-97e0-2e3f48a15b51`, `reviewer` child 0. Recorder: orchestration root. Kind: fresh independent initial S3 implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: review.
**Purpose:** Adversarially inspect the complete uncommitted S3 diff for authorization, shared startup, opaque ticket context, resume/recovery ordering, output bounds, independent ownership, admission, shutdown, privacy, validation evidence, and later-scope containment without editing or delegating.
**Outcome:** The reviewer returns `REJECT` on bounded in-scope defects, not material divergence. It confirms the source architecture and S3-VC-001/003/004/005/008/009 behavior, including the writer's two corrected intermediate defects. It independently reproduces the S3-VC-006 blocker: `createBoundedProjectedSessionFrameEncoder()` parses and clones the complete selected graph before byte-limited serialization, so an already marked frozen frame with an early over-limit value still inspects 5,000 tail descriptors. It also finds four required evidence gaps: production ticket bootstrap is not composed with the real shared `startRpcSession()` slow path; server close is not exercised while a session socket remains open; 256/257 total-cap evidence stops at the gateway API rather than deterministic custom-server dispatch; and exact default 4 MiB/one-over, catch-up reference/cache lifetime, and transient-overflow disposition coverage is incomplete.
**Evidence:** The review cites the unconditional parse at `lib/session-protocol.ts`, fake route-start seam and separately tested runtime lock, prepublished-wrapper real integration, already closed sockets before server shutdown, gateway-only total-cap fixture, and only small injected channel byte limits. It passes the exact ticket context and startup source structure, atomic attach/ready order, real `1007`/`1009`, zero/multiple subscriber behavior, seven-open-socket HTTP scheduling, source containment, diagnostics privacy, and the implementation validation disposition. Root reproduced the encoder amplification and independently reran TypeScript, lint, and focused `180/180` successfully.
**Uncertainty / gaps:** Source review supports the startup and ownership implementation, but the immutable plan explicitly requires composed real evidence. The bounded fix must preserve byte-identical V1 output, weak-cache lifetime, canonical snapshot draining, admission authority, server ownership, and every existing test while adding deeply frozen wide-object/array trap-count regressions. No finding requires S4 browser work, S6 heartbeat/idle/final-shutdown redesign, plan revision, or weaker validation.
**Recommended use:** Launch one fresh sole fix writer under the unchanged S3 plan for the encoder blocker and all confirmed evidence gaps, then rerun root probes/full gates and obtain a fresh independent no-edit review.

## Handoff

**Source:** Pi subagents run `c41dbbb0-0574-4af9-93be-cfbab1b42d08`, `milestone-implementer` child 0, session `019fc128-9fb0-7a47-9ac0-55fff10437df`. Recorder: orchestration root. Kind: first bounded S3 fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: fix.
**Purpose:** Repair the whole-frame bounded-encoder amplification and close every required evidence gap from the initial independent S3 review, while preserving exact V1 wire semantics, authorization/ownership, browser/SSE compatibility, and all later-milestone exclusions.
**Outcome:** The writer introduced weak strict-validation identity tracking and descriptor-safe incremental bounded serialization so an already accepted, marked, deeply frozen frame can return one-over without reparsing/cloning or inspecting tail descriptors; fitting output still receives strict validation and weak identity caching. It added exact default 4 MiB/one-over and transient-recovery tests, bounded source-reference observation, production ticket `POST` composition through the real no-provider shared existing-file startup, actual unpublished-owner disposal, an open-session custom-server close/restart flow, and deterministic dispatcher-level mixed-channel 256/257 admission across direct-peer keys. Root inspection confirmed the intended implementation and source boundary, and root reran the changed focused surfaces successfully. Acceptance remains pending fresh independent review and the complete root gate matrix.
**Evidence:** Writer validation reports TypeScript and lint passing; focused S3 `186/186`; exact non-real-Next `388/388`; literal full `390/392`; separate real-Next `2/4`; package dry run, whitespace, immutable-plan, privacy/source-boundary, and no-stage checks passing. Only the permitted absent `.next/BUILD_ID` production child/parent fail. Root independently passed the six directly changed test files at `137/137`; observed the real production ticket slow path and unpublished-failure tests, dispatcher 256/257, open-socket shutdown/restart, exact 4 MiB, cache/reference, and transient disposition cases; verified plan blob `457ed24d...`; and reproduced final content-safe trap counts: a 5,000-tail object returned `over_limit` with one descriptor/one `ownKeys`, while a 5,000-tail array returned `over_limit` with two descriptors/zero `ownKeys`.
**Uncertainty / gaps:** Object traversal still obtains one bounded `Reflect.ownKeys()` list before descriptor-by-descriptor writing because JavaScript exposes no lazy own-key iterator; it no longer clones selected values or retrieves tail descriptors and remains under S2's accepted 100,000-node canonical bound. Over-limit transient effects remain intentionally unrecreated, with canonical snapshot and HTTP refresh markers explicit. Weak cache lifetime is supported by `WeakMap`, single-identity reuse, and deterministic source-reference release without claiming GC timing. Dispatcher total-cap evidence is deterministic injected-peer coverage, not a real distributed 257-socket claim. Fresh production inclusion remains release-owned. No material divergence or staged unrelated state was observed.
**Recommended use:** Obtain one fresh no-edit/no-delegation review of the complete diff, emphasizing validated-identity safety, one-over object/array work, fitting strictness, exact accounting/recovery, actual shared start, dispatcher admission, and open-socket restart. Then complete root gates and accept only if no blocker or major defect remains.

## Handoff

**Source:** Pi subagents run `958bca5a-e741-420b-a655-a088d9fe1b98`, `reviewer` child 0, session `019fc151-112b-7be0-8bd1-07607ac5d06c`. Recorder: orchestration root. Kind: fresh independent first-fix S3 review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: review.
**Purpose:** Re-test the complete S3 diff and first fix across validated-identity/cache safety, true one-over work, exact output accounting, production shared startup, dispatcher total admission, open-socket restart, authorization/ownership, privacy, compatibility, and S3-VC-001 through S3-VC-010 without editing or delegating.
**Outcome:** The reviewer returns `REJECT` on one bounded P0 S3-VC-006 defect. The first fix correctly eliminates full parser/clone and tail-descriptor amplification; its single object `Reflect.ownKeys()` remains bounded by the accepted 100,000-node canonical limit and is not a separate defect. However, `freezeCanonicalData()` can mark a stateful proxy root while the proxy hides one mutable child during descriptor traversal and exposes it during `Object.freeze()`. A later strict parse grants that root the validated token; the encoder then caches fitting output by the falsely immutable root identity. Child mutation returns stale cached wire output, and even a later strict parse rejecting a Symbol-valued child does not invalidate the cached success. Separately, a frozen proxy with legally alternating own-key order can make fitting bounded output differ byte-for-byte from `encodeProjectedSessionFrame()` even though post-write validation succeeds. All other first-review findings and S3 contracts pass.
**Evidence:** Root reproduced both decisive paths. In the cache probe, the hidden child remained unfrozen; first output was `one`, mutation made ordinary encoding `two` while the cache still returned `one`, and after mutation to a Symbol the strict parser returned `ok:false` while the cache still returned `ok:true` with `one`. In the three-order proxy probe, bounded and ordinary fitting output differed in object key order. The reviewer independently passed exact 4 MiB/one-over, source/cache release, transient recovery, real production no-provider shared startup and unpublished disposal, deterministic dispatcher 256/257, genuine open-session close/restart, static authorization/ownership, control/close codes, HTTP scheduling, privacy, and later-scope containment. Root full gates pass TypeScript, lint, exact non-real-Next `388/388`, literal `390/392`, separate real-Next `2/4`, package dry run, whitespace, immutable-plan, source/privacy, and no-stage checks; only the permitted absent `.next/BUILD_ID` production child/parent fail. One earlier deliberately parallel literal/real-Next invocation caused a transient real-development child collision; the required isolated reruns passed real development and restored the exact permitted disposition.
**Uncertainty / gaps:** A finite number of checks cannot prove future proxy trap stability, so a repair must not treat an arbitrary externally marked proxy identity as stable merely because it passed one parse/freeze sequence. The safe fast path/cache needs unforgeable canonical provenance from the parser/hub's newly reconstructed plain graph or an equivalent design; fitting output must come from one stable canonical representation. Any narrow protocol/hub branding or freeze correction must preserve accepted S2 V1 semantics and is authorized only as the proven transport blocker. No browser/S4, heartbeat/idle/S6, product choice, validation waiver, or material divergence is involved.
**Recommended use:** Launch one fresh sole bounded fix writer under the same immutable S3 milestone for robust canonical provenance, mutation-safe weak caching, and byte-identical fitting output. Require exact stateful hidden-child/cache and alternating-own-key regressions, preserve the repaired wide over-limit counts and every other S3 gate, then repeat root verification and fresh independent review.

## Handoff

**Source:** Pi subagents run `72e004a5-4a28-4408-bbad-86b35eeb9ee2`, `milestone-implementer` child 0, session `019fc14c-28da-7a03-b911-903763dd1501`. Recorder: orchestration root. Kind: second bounded S3 fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: fix.
**Purpose:** Replace forgeable caller-owned freeze/validation trust with mutation-safe canonical provenance, make fitting serialization consume one stable strict reconstruction, and retain trusted internal one-over/cache performance plus every earlier S3 correction.
**Outcome:** The writer changed `parseProjectedSessionFrame()` to freeze its newly reconstructed ordinary canonical graph before exposure and privately brand only that fresh identity; arbitrary inputs, including externally marked proxies, are strictly reconstructed on every bounded call and never cached by external identity. The bounded encoder fast-path/cache now applies only to private parser provenance and serializes the one selected stable representation. Snapshot units are canonically parsed before hub exposure so they share safe provenance without changing wire/state/replay semantics. Stateful hidden-child/cache and alternating-key regressions replace the forged-proxy trusted tests; trusted early-stop counts now instrument ordinary parser-created frames. Root inspection confirms the private weak provenance, frozen parser result, narrow snapshot canonicalization, and unchanged V1/output pump behavior. Acceptance remains pending fresh review and final root matrix.
**Evidence:** The writer reports TypeScript and lint passing; focused S3 `188/188`; exact non-real-Next `390/390`; literal `392/394`; separate real-Next `2/4`; package, whitespace, immutable-plan, privacy/source-boundary, and no-stage gates passing, with only the permitted absent `.next/BUILD_ID` production child/parent failures. Root independently passed protocol/hub/channel `57/57` and reproduced the decisive repair: the deliberately hidden child remains unfrozen, yet bounded outputs advance from `one` to `two` after mutation and a later Symbol view throws `invalid_projected_session_frame:malformed` rather than returning stale cache; consecutive three-order proxy calls produce their newly reconstructed order rather than cached bytes. Trusted object/array one-over tests retain one descriptor/one key-list and two descriptors/zero key-list respectively.
**Uncertainty / gaps:** Sequential calls on a deliberately stateful proxy may legitimately select different canonical key orders; byte identity is now defined against the one strict reconstructed graph used within each bounded call rather than an impossible future view of adversarial traps. Parser results are now frozen before exposure, a nonsemantic hardening compatible with current reducer/hub/tests. Snapshot canonicalization adds bounded per-unit parsing work but no wire/state change. Arbitrary untrusted oversized inputs still incur full strict reconstruction by design; only parser/hub-created canonical identities receive early stop/cache. Fresh production inclusion and later S4-S7 boundaries remain unchanged. No material divergence or staged unrelated state was observed.
**Recommended use:** Obtain a fresh independent no-edit review focused on private provenance, parser freezing compatibility, snapshot branding, stateful proxy semantics, cache/HMR safety, and all preserved S3 contracts. If clean, rerun the complete root gate matrix, record acceptance, and commit the coherent boundary.

## Handoff

**Source:** Pi subagents run `21e56dc6-8f1d-4a69-89e1-faa1477983b7`, `reviewer` child 0, session artifact `4099d492/run-0`. Recorder: orchestration root. Kind: final fresh independent S3 implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s3-secure-session-websocket.md`; Mode: review.
**Purpose:** Adversarially re-evaluate the complete S3 lineage after the second fix, with decisive focus on private canonical provenance, parser freeze and snapshot compatibility, stateful proxy/cache safety, stable fitting serialization, trusted one-over work, every earlier authorization/output/ownership finding, privacy, compatibility, and S3-VC-001 through S3-VC-010.
**Outcome:** The reviewer returns `ACCEPT` with no blocker, major, or minor defect. It confirms only newly reconstructed, synchronously frozen parser identities receive the private transport provenance; arbitrary externally marked proxies are strictly reconstructed per call and cannot acquire stale weak-cache state. Fitting output comes from that call's one stable canonical representation, trusted internal object/array one-over remains bounded without parser/clone or tail-descriptor amplification, and canonical snapshot units preserve replay/finality semantics. It also independently accepts metadata-bound bootstrap and real shared start, opaque context lifecycle, dispatcher admission, atomic attach/recovery, exact output pumping, open-socket restart, HTTP/SSE compatibility, privacy, and later-scope containment.
**Evidence:** The reviewer independently probed every parser-return node for deep freezing; source mutation isolation; cycle/alias/Symbol/accessor rejection; externally frozen/unbranded fallback; stateful proxy behavior; and trusted early stop/cache. It passed focused S3 `188/188`, exact non-real-Next `390/390`, package, whitespace, immutable-plan, privacy, and no-stage gates; literal is `392/394` and separate real-Next `2/4`, with only the permitted absent `.next/BUILD_ID` production child/parent. Root independently inspected the final source/test diff, reproduced stateful hidden-child and alternating-order safety, passed TypeScript, lint, focused `188/188`, exact non-real-Next `390/390`, isolated literal `392/394`, isolated real-Next `2/4`, nine-file package dry run, tracked/cached/no-index whitespace, immutable master/milestone, source/privacy/API, and no-stage checks. No build ran.
**Uncertainty / gaps:** Fresh production App Router inclusion remains release-workflow evidence. Deterministic dispatcher 256/257 is not a distributed real-socket claim. S2 still materializes its separately bounded snapshot source before S3 drains it. Oversized transient one-shot effects remain intentionally unrecreated while snapshots and HTTP markers recover durable state. Browser registry/hook/SSE removal remains S4A/S4B, file watch S5, heartbeat/30-minute idle/final shutdown S6, and cross-browser/user acceptance S7. These are exact planned boundaries, not incomplete S3 obligations or waivers.
**Recommended use:** Accept S3, create the coherent source/test/checkpoint implementation commit excluding `.pi-subagents/`, append the mandatory final implementation summary naming that commit, commit checkpoint finality, then reconcile the master and progress directly to S4A.

## Implementation Summary

**Plan section:** S3 `Design / Implementation Strategy` §§1–8 and S3-VC-001 through S3-VC-010; master S3 secure server-session-WebSocket outcome and the S3 portions of ORCH-VC-003/004/005/007/008/009/010/011/012.
**Work and outcome:** S3 is accepted at the pre-commit boundary. Pi Web now authorizes exact session targets through one-use opaque server ticket context, dispatches one static HMR-safe `session` channel, performs one strict resume/ready handshake and atomic projected-hub attachment, and independently drains ready, catch-up, and live projected V1 frames through a 4 MiB callback pump. Existing-file startup has baseline and hook/post-start identity checks; wrapper identity owns destruction while socket disconnect does not own runs. Projected frames use private parser-created frozen provenance for mutation-safe weak caching and true one-over serialization; every arbitrary proxy/input is strictly reconstructed. Real and deterministic evidence covers shared no-provider start, initial/replay/recovery, zero/multiple/stalled subscribers, exact output bounds, admission 64/65 and dispatcher 256/257, seven-socket HTTP schedulability, open-socket close/restart, and exact cleanup. Two bounded fix/review loops resolved every reproduced defect; final review accepts all S3 VCs.
**Validation / evidence:** Root passes TypeScript and lint; focused S3 `188/188`; exact non-real-Next `390/390`; literal full `392/394`; separate real-Next `2/4`; and package dry run, whitespace, immutable-plan, intended source boundary, privacy/raw/API, no-stage, stateful-proxy, and wide-tail probes. Terminal CLI and real development pass; only the expressly permitted production child/parent fail because `.next/BUILD_ID` is absent. Package output includes both modified `bin` files but makes no fresh TypeScript/App Router inclusion claim. Final independent review is run `21e56dc6-8f1d-4a69-89e1-faa1477983b7` and returns `ACCEPT`. No `next build` ran.
**Departures from approved obligations:** None. Per-session raw SSE/browser migration remains explicitly assigned to S4A/S4B; file watching to S5; heartbeat, 30-minute idle, and final shutdown ownership to S6; browser-scale and user acceptance to S7; fresh production inclusion to the release workflow. The capped object key-list allocation, pre-materialized bounded S2 snapshot source, and intentionally unrecreated oversized transient effects are disclosed accepted residuals within the exact plan, not waivers or incomplete obligations.
**Implementation commit:** pending at the coherent S3 boundary commit.
