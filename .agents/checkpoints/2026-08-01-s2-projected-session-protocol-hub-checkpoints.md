# S2 Projected Session Protocol and Wrapper-Owned Hub Checkpoints

Plan: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`

## Handoff

**Source:** Pi subagents run `be036ee9-c3bc-4590-8e09-f4802fcfea36`, `context-builder` child 0 (S2 server/protocol reconnaissance). Recorder: orchestration root. Kind: read-only milestone context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S2; Mode: investigation.
**Purpose:** Reconcile the accepted S1 boundary with installed Pi event definitions and the current wrapper/raw-SSE path, then identify a contained projected protocol, projector, reducer, replay hub, HMR seam, privacy boundary, and validation strategy without editing project files or entering S3+ scope.
**Outcome:** Confirmed that `AgentSessionWrapper.start()` receives every native event independently of browser listeners but currently forwards it unchanged; wrapper-generated events share the same broad fanout; the SSE route directly stringifies those raw objects. Installed Pi exposes actionable text, thinking, and tool-call deltas inside `message_update.assistantMessageEvent`, while `agent_end` adds `willRetry` and precedes `agent_settled`. The root adopts a wrapper-owned hub constructed before native subscription/publication, a strict projected-only API, per-wrapper stream epoch/sequence, pure reduction, dual-bounded replay, and canonical initial/recovery/final snapshots. Known unused events must be explicit no-ops, not a default raw pass-through. The hub retains live projection state only; HTTP transcript/runtime APIs remain authoritative for completed history, entry IDs, and currently HTTP-only configuration.
**Evidence:** The root directly verified `lib/rpc-manager.ts` native subscription, wrapper emit/onEvent flow, prompt/compaction accounting, extension maps, startup locks, and destruction; `app/api/agent/[id]/events/route.ts` raw JSON serialization; installed `AgentSessionEvent`, core `AgentEvent`, and `AssistantMessageEvent` declarations; S1 strict protocol/HMR patterns; and read-only Pi source/tests showing normal/tool event ordering and real delta emission. The child supplied an exhaustive consumed/omitted event map, delta-versus-snapshot classification, HMR constraints, replay/finality fixtures, and long-stream byte methodology. No project file changed.
**Uncertainty / gaps:** S2 cannot prove ticket security, WebSocket backpressure, or browser ownership; those remain S3/S4. Existing wrappers surviving a code reload cannot retroactively recover pre-capability events. Individual final messages or extension UI can exceed replay capacity and must force snapshot/HTTP recovery rather than unbounded retention. The legacy per-session SSE serializer remains raw until S4B; the new projected API must nevertheless make raw access impossible. No evidence shows material divergence because installed Pi supplies every required actionable delta.
**Recommended use:** Define one exact V1 frame/snapshot union and parser; normalize only display-required message fields; project all known inputs exhaustively; use wrapper-owned settlement after claim release for finality; attach a versioned hub capability before publication; preserve legacy SSE and all S3+ surfaces; require raw-field exclusion, final equality, zero-subscriber, replay-overflow, HMR, lifecycle, and UTF-8 byte-growth evidence before acceptance.

## Handoff

**Source:** Pi subagents run `be036ee9-c3bc-4590-8e09-f4802fcfea36`, `context-builder` child 1 (S2 consumer/state-semantics reconnaissance). Recorder: orchestration root. Kind: read-only milestone context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S2; Mode: investigation.
**Purpose:** Trace the current browser event state machine and extension/queue/retry/compaction semantics that S2 must encode canonically for later S4 consumption, while identifying bounded recovery and pure reducer tests without implementing the browser migration.
**Outcome:** Confirmed that the current hook replaces the streaming bubble with every growing `message_update.message`, reconciles optimistic user prompts at `message_end`, tracks concurrent active tools, replaces queues, handles retry/compaction, and reloads the HTTP transcript at completion to repair persisted messages and entry IDs. It currently treats every `agent_end` as final even though installed Pi distinguishes retrying attempts and emits `agent_settled` later. Durable extension recovery requires keyed statuses/widgets, pending dialogs/custom UI, their removals, and optionally latest title; notices and editor insertion are one-shot effects. Current dialog response/timeout/abort cleanup removes wrapper maps without an event, so S2 needs a projection-only close signal that does not perturb legacy SSE. The root resolves the snapshot-scope ambiguity by keeping bounded canonical event-delivery state plus explicit HTTP transcript/runtime refresh requirements, not a duplicated transcript or event-triggered claim of full runtime parity.
**Evidence:** The root directly verified `hooks/useAgentSession.ts` mount, event switch, optimistic reconciliation, polling/reconciliation, final transcript reload, queue/retry/compaction handling, and extension dispatch; `components/MessageView.tsx` use of normalized message fields/details; `lib/types.ts` message/extension shapes; `lib/normalize.ts` tool-call normalization; `lib/rpc-manager.ts` `get_state`, extension maps/UI lifecycle, prompt/compaction claims, and command paths; and installed/read-only Pi event ordering. The child supplied duplicate/out-of-order/epoch reducer rules, durable-versus-transient classifications, pure test seams, and residual risks. No project file changed.
**Uncertainty / gaps:** Model/tool/context/system state can change through HTTP or silent extension paths without a native event, so S2 cannot honestly replace HTTP runtime reconciliation. Recovery of completed message entry IDs also requires HTTP session reload. Tool/custom `details` are rendered today and need an exact JSON-safe normalization rather than blanket removal or raw copying. The subagent runtime marked this read-only child failed with `completed without making edits for an implementation task`; that is a task-classification error, not an execution or evidence failure. The root recovered and verified its complete output, and no source/planning file was modified by the child.
**Recommended use:** Preserve HTTP transcript/runtime authority and polling; encode one normalized final message plus true assistant deltas; make wrapper settlement—not retrying `agent_end`—the final boundary; distinguish durable snapshot state from replay-only effects; emit projection-only close/settle inputs; and require extension cleanup, entry-ID recovery signaling, final equality, overlap, and overflow tests before S2 acceptance.

## Handoff

**Source:** Pi subagents run `257af4ae-94dd-43d5-a552-00c09637b312`, `reviewer` child 0. Recorder: orchestration root. Kind: independent read-only draft-plan review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Adversarially check the draft S2 plan/checkpoint for master containment, executable protocol/recovery semantics, exhaustive installed-event coverage, honest validation/package evidence, privacy, and later-milestone leakage before approval.
**Outcome:** The reviewer correctly blocked the first draft on six major issues and one minor: it did not clearly preserve ORCH-VC-003 as partial while raw legacy SSE remains; it omitted installed bash-execution/branch-summary/compaction-summary message roles; it used contradictory pre-final versus post-final equality language; it overclaimed race-free HTTP transcript/runtime recovery from marker-only snapshots; oversized durable queue/extension state could make both a delta and its fallback snapshot unrepresentable; the real-Next/package evidence was not reproducible or honest about what `npm pack` contains; and standalone compaction settlement lacked an explicit success/error/abort boundary. The root accepted every finding and revised the still-draft plan without changing the S2 product direction. The corrected plan now requires partial master-row disposition, exact nested-role omission tests, two-stage equality semantics, explicit S4 HTTP-race deferral, V1 atomic snapshot transactions fragmented into individually bounded transfer units, exact test partitions and unchanged-lineage production-preflight handling, manifest-only package claims, and standalone compaction settlement fixtures.
**Evidence:** The root directly verified the raw SSE `JSON.stringify` path; the master’s S4B removal timing; installed `messages.d.ts` roles and sensitive command/output/summary/source/path fields; current HTTP response shapes without hub epoch/cursor; unbounded queue/custom/widget input surfaces; `lib/pi-web-real-next.test.mjs` production-manifest preflight and subtest structure; `package.json` packaged files; and the separate compaction claim/release path in `lib/rpc-manager.ts`. The corrected draft retains no source edit, no staged file, and no private runtime path. Because both planning files were still untracked, the root used `git diff --no-index --check` against `/dev/null` for each file and observed no whitespace diagnostics; cached-diff checking remains required after staging. Review output is recoverable by run identity.
**Uncertainty / gaps:** Snapshot transfer fragmentation is a substantive protocol clarification introduced while the plan remains mutable; it needs fresh independent rereview for sequence/group/reassembly/bounds consistency. S4A/S4B still must design and prove race-free HTTP composition. The production-artifact subtest remains release-owned when manifests are absent, and package dry run cannot prove fresh TypeScript bundling. These are explicit evidence boundaries, not hidden passes or waivers.
**Recommended use:** Obtain one fresh no-edit/no-delegation review of the corrected draft, concentrating on bounded snapshot transactions, exact master-row partial disposition, installed role coverage, finality/equality, and validation commands. Approve and commit the planning boundary only if that review finds no blocker or major ambiguity.

## Handoff

**Source:** Pi subagents run `1d7639f1-5da4-4b08-9cc1-d0e8be74ef17`, `reviewer` child 0. Recorder: orchestration root. Kind: fresh independent corrected-draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Re-evaluate every first-review correction and challenge the new snapshot-transfer design, installed event coverage, finality/equality, HTTP deferral, validation reproducibility, privacy, and S3+ containment before plan approval.
**Outcome:** The reviewer found no master-level blocker and passed the corrected partial ORCH-VC-003 disposition, installed outer message-role coverage, bounded HTTP-recovery claim, snapshot transaction direction, standalone compaction, validation/package boundaries, privacy, and scope containment. It identified two remaining major ambiguities and two minors. The root accepted them: nested assistant `done`/`error` variants and their complete terminal objects needed explicit handling; pre-final equality had to compare only delta-addressable content rather than usage/stop/error metadata; prompt claims that fail before creation of a native prompt promise still needed authoritative settlement; oversized non-retained final snapshots needed explicit replay-floor advancement; and ordinary `git diff --check` did not inspect untracked planning files. The still-mutable draft now maps every nested assistant discriminant, forbids terminal complete-object fallback, defines subset versus full post-final equality, settles every dispatched/pre-dispatch last-claim path while destruction only closes, specifies before/after oversized-snapshot cursors, and records the no-index whitespace method.
**Evidence:** Root verification covered nested `AssistantMessageEvent` `done`/`error` declarations; normalized final metadata versus delta-available fields; ordinary extension-binding rejection, synchronous `inner.prompt` failure, hosted binding failure, hosted pre-dispatch cancellation, dispatched settlement, and destruction paths in `lib/rpc-manager.ts`; replay-floor implications of an unretained logical frame; and explicit no-index whitespace checks for both untracked planning files with zero diagnostic output. The reviewer changed no file and its output is recoverable by run identity.
**Uncertainty / gaps:** The new exact language has not yet received a final pass. Implementation must keep one authoritative settlement per claim across several wrapper paths without perturbing legacy SSE or destruction. Snapshot assemblers must treat all transfer units as one logical sequence and never advance the cursor before a validated end marker.
**Recommended use:** Obtain one final focused no-edit/no-delegation plan review limited to these corrections and overall executability. If it returns no blocker or major, record its result, change only `Status` to `approved`, stage only the plan/checkpoint, run cached whitespace/privacy checks, and commit the planning boundary before launching the sole writer.

## Handoff

**Source:** Pi subagents run `1edd3926-26d0-4281-8169-ce683cf53e7a`, `reviewer` child 0. Recorder: orchestration root. Kind: final focused read-only plan review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Verify the nested assistant-event, pre-dispatch settlement, oversized-final-snapshot replay floor, and whitespace corrections while ensuring the full S2 plan remained contained and executable.
**Outcome:** The reviewer passed exhaustive assistant event handling, all wrapper settlement paths, partial ORCH-VC-003 disposition, bounded snapshot transactions, HTTP deferral, validation/package boundaries, privacy, S3+ containment, and explicit untracked whitespace evidence. It found no scope blocker and no residual minor, but identified two narrow major wording/semantic inconsistencies before freezing: the Objective still described full-message pre-final equality rather than the delta-comparable subset, and replay-floor advancement was phrased for every oversized snapshot rather than only event-driven final snapshots. The root corrected both while the plan remained draft. The Objective now reserves full equality for post-`message_completed`; event-driven oversized final snapshots advance the floor, while subscriber-generated initial/recovery transactions never change global sequence, replay floor, or another subscriber's eligibility.
**Evidence:** The reviewer inspected the exact draft, checkpoint, master, current wrapper paths, and installed event union, and independently observed zero no-index whitespace diagnostics. Root inspection confirmed the corrected Objective matches the detailed equality rule and the corrected hub/VC language distinguishes final from initial/recovery snapshot transactions. The review changed no file and is recoverable by run identity.
**Uncertainty / gaps:** Only final verification of these two exact corrections remains. Implementation must preserve the stated distinction in both hub code and tests.
**Recommended use:** Run one bounded independent verification of the two corrections and overall recommendation. If clean, approve and commit the planning boundary without further optional polish.

## Handoff

**Source:** Pi subagents run `2ae8c995-589c-4af8-9e0f-c5902810e04a`, `reviewer` child 0. Recorder: orchestration root. Kind: bounded final read-only plan verification. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Verify only the corrected delta-subset/full-message equality distinction, final-versus-initial/recovery replay-floor semantics, their test/VC consistency, and readiness to freeze.
**Outcome:** The reviewer confirmed all requested corrections and found no blocker. The Objective now limits pre-final comparison to delta-addressable content and reserves full normalized-message equality for post-`message_completed`; only non-retained event-driven final snapshots advance replay floor; subscriber-generated initial/recovery transactions leave sequence, floor, and other subscribers unchanged; and tests/VC cover both distinctions. Recommendation: `APPROVE`.
**Evidence:** Direct review of the current plan/checkpoint and bounded source/type context; recoverable run `2ae8c995-589c-4af8-9e0f-c5902810e04a`. No file, stage, or commit was changed by the reviewer.
**Uncertainty / gaps:** None at plan-approval severity. Implementation and later S3/S4 evidence remain required exactly as scoped.
**Recommended use:** Change only the plan status to `approved`, stage only this plan/checkpoint, run cached whitespace/privacy/source-boundary checks, commit the immutable planning boundary, and launch one fresh `milestone-implementer` as sole source writer.

## Handoff

**Source:** Pi subagents run `56197cff-9283-4051-b9d5-7423b7d9f4fa`, `milestone-implementer` child 0; nested read-only support runs `613a6f01` and `5e7f7a01` are recoverable through the writer run. Recorder: orchestration root. Kind: milestone implementation handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: implementation.
**Purpose:** Implement all immutable S2 protocol/projector/reducer/hub, wrapper-integration, privacy, compatibility, and validation obligations as the sole source writer without entering S3+ or modifying orchestration state.
**Outcome:** The writer added `lib/session-protocol.ts`, `lib/session-projector.ts`, `lib/session-reducer.ts`, `lib/session-event-hub.ts`, matching focused tests, and narrow `lib/rpc-manager.ts`/test integration. The delivered design includes exact projected frames, true assistant deltas, normalized final messages, snapshot transactions, sequence/replay, a wrapper capability, zero-subscriber capture, prompt/compaction settlement, extension state, and hub close. It reported typecheck, lint, 55 focused tests, 254 non-real-Next tests, package dry run, diff check, and the exact permitted two-pass/two-missing-manifest real-Next disposition. These claims were not accepted from the handoff: root inspection and fresh review found correctness, isolation, complexity, cleanup, and evidence defects requiring a bounded fix.
**Evidence:** The root read every changed production module and decisive wrapper diff, reran TypeScript, lint, the 55-test focused suite, the 254-test non-real-Next partition, package dry run, source/privacy inventories, and whitespace checks successfully, and confirmed only intended source/tests plus pre-existing `.pi-subagents/` are present with no staged file. The writer's reported literal full and real-Next counts remain consistent with the known release-manifest boundary. The root also directly reproduced malformed-event cursor mutation, strict-encoder `toJSON` substitution, false-positive tool-argument equality, reentrant subscriber/replay reordering, throwing diagnostics, deeply nested projection suppressing legacy fanout, immediate-destruction custom-UI leakage, and native zero-claim settlement remaining active.
**Uncertainty / gaps:** Repository-wide ORCH-VC-003 remains intentionally partial while raw SSE is preserved. Fresh production inclusion and race-free HTTP composition remain later/release boundaries. S2 itself is unaccepted because several P0 protocol/lifecycle contracts fail despite green happy-path tests.
**Recommended use:** Keep the implementation uncommitted. Classify the fresh findings under the same immutable S2 milestone and issue one sequential focused fix attempt covering only the reproduced protocol, hub, wrapper, performance, cleanup, and evidence defects; then repeat root validation and fresh independent review.

## Handoff

**Source:** Pi subagents run `53360130-98a1-4cb5-9e96-0e50b4b1be28`, parallel `reviewer` children 0 (protocol/reducer/hub) and 1 (wrapper/compatibility/evidence). Recorder: orchestration root. Kind: fresh independent implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Adversarially validate the actual S2 diff against S2-VC-001 through S2-VC-010, reproduce edge failures, and identify missing evidence without editing or delegating.
**Outcome:** The review blocks S2 acceptance. Reproduced protocol/hub defects are: synchronous nested publication reorders subscribers and replay; malformed known assistant events advance cursor then throw; original-object parsing permits non-enumerable `toJSON` serializer substitution; streamed tool arguments are ignored by pre-final equality; snapshot receivers do not enforce unit/declaration bounds while assembling; diagnostic sinks can escape projection/close; routine full-state cloning, chunk-array copying, repeated transfer rebuilding, and replay `shift()` create avoidable superlinear work not measured by the synthetic byte test. Reproduced wrapper/evidence defects are: deeply nested valid input can throw before preserved legacy fanout; custom UI can mount after destruction and strand its promise/component; byte-growth evidence covers text only and not the actual protocol/hub/final-snapshot path; dispatched failure, hosted dispatched failure projection, and standalone non-abort compaction-error fixtures are incomplete. Root investigation adds one plan-relevant finality defect: Pi extension `sendMessage({triggerTurn:true})`/`sendUserMessage()` can emit native `agent_start`/`agent_settled` without a wrapper claim, but the hub remains active and emits no final snapshot.
**Evidence:** Both reviewers inspected the exact diff, immutable plans, current runtime/SSE/hooks, installed event/message declarations, and read-only Pi behavior. Their 55 focused tests, typecheck, lint, 254-test non-real-Next partition, package check, and known real-Next disposition agreed with the root. The root independently reproduced the decisive failures with synthetic, content-safe inline probes and confirmed that current tests omit the required thinking/tool byte-growth and several terminal paths.
**Uncertainty / gaps:** Canonical state uniqueness/order validation and telemetry detail may need correction if implicated by the smallest safe fixes, but they are not authority to redesign the protocol. S3 network backpressure and S4 HTTP/browser composition remain excluded. No observed fact requires changing the immutable milestone or master.
**Recommended use:** Treat all findings as bounded in-scope implementation defects. Use one sole fix writer, require adversarial regression fixtures for every reproduced case plus end-to-end text/thinking/tool growth, preserve legacy delivery even on rejected projection, and stop if repair would require a new protocol/product choice rather than the already approved strict/bounded semantics.

## Handoff

**Source:** Pi subagents run `76ef5221-960f-47b4-a272-2b48d48cb50e`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Correct the 13 bounded protocol, hub, wrapper-lifecycle, extension-cleanup, complexity, telemetry, and evidence defects established by the initial review, while preserving the raw-SSE compatibility boundary and excluding every S3+ surface. The issued obligations covered publication FIFO/reentrancy; malformed-input atomicity; canonical non-invoking parsing; streamed tool equality; receiver bounds; diagnostic isolation; structural/linear work; legacy fanout under hostile input; custom-UI pre-mount cleanup; zero-wrapper-claim settlement; missing terminal fixtures; actual text/thinking/tool wire-growth evidence; and finite content-free telemetry, mapped across `S2-VC-001` through `S2-VC-010`.
**Outcome:** The writer changed only the intended S2 modules and `lib/rpc-manager.ts` plus their tests. It added accepted-input FIFO publication, precommit frame validation, fresh canonical reconstruction, strict tool-argument comparison, bounded linked snapshot assembly, structural reducer copies, a replay head-index deque, isolated diagnostics, deep-input legacy preservation, pre-mount custom-UI cleanup, zero-claim native settlement, expanded terminal fixtures, and actual projector-to-encoder growth measurements for all three delta classes. Root probes and the next fresh review confirmed that the previously reproduced reentrancy, malformed-cursor, hidden-`toJSON`, tool-equality, diagnostic, deep-fanout, custom-UI pre-mount, complexity, and missing-evidence defects were materially repaired. The attempt is not accepted because review found new or previously unexercised bounded-state, strict-receiver, nested-accessor, compaction-finality, and post-destruction-dialog defects.
**Evidence:** Recoverable writer output reports TypeScript and lint passing; focused tests `69/69`; exact non-real-Next partition `268/268`; literal suite `270/272` with only the production child and parent failing at the named missing-`BUILD_ID` preflight; separate real-Next `2/4` with the same two permitted preflight failures; package dry run, privacy/source scans, and whitespace checks passing; no staged files and no forbidden build. The root inspected the changed production paths and focused fixtures, independently reran typecheck, lint, the focused suite, and the non-real-Next partition, and verified that the raw SSE route/caller, plans, memory, gateway/tickets/browser surfaces, and package/lifecycle sources remain unchanged. The package check remains manifest-only evidence and does not prove fresh TypeScript bundling.
**Uncertainty / gaps:** Repository-wide `ORCH-VC-003` remains explicitly partial until S4B removes raw SSE. Fresh production inclusion and race-free HTTP composition remain later/release boundaries. The writer's zero-claim compaction rule overgeneralized direct-compaction settlement to automatic compaction inside a zero-wrapper-claim agent turn; accepted canonical state also lacks an aggregate representability budget. Independent review and root verification therefore still block `S2-VC-002`, `S2-VC-004`, `S2-VC-005`, `S2-VC-006`, `S2-VC-008`, and `S2-VC-010` despite the green reported gates.
**Recommended use:** Keep the diff uncommitted and issue one sequential bounded fix for the independently verified remaining defects. Preserve the efficient delta path and oversized-but-bounded snapshot semantics rather than restoring whole-state cloning or weakening recovery.

## Handoff

**Source:** Pi subagents run `abe6beb1-ee65-4cb4-b634-e509b310a3a5`, parallel `reviewer` children 0 (protocol/reducer/hub) and 1 (wrapper/lifecycle/evidence). Recorder: orchestration root. Kind: fresh independent post-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Re-test all original S2 defects against the complete uncommitted diff, challenge strict projected input/state and receiver recovery, distinguish direct from in-run compaction settlement, inspect extension teardown races, and map the result to `S2-VC-001` through `S2-VC-010` without editing or delegating.
**Outcome:** Both reviewers block acceptance. Protocol review found that individually valid durable updates can accumulate an aggregate state beyond the parser's node budget, after which the hub has already advanced but `getState()`, `snapshot()`, and wrapper settlement can throw; same-epoch snapshots at the current cursor can replace canonical state; decoded snapshots whose two required refresh markers are false are accepted; and terminal tool projection invokes a caller-owned nested accessor. Wrapper review found false `run_settled` plus a final snapshot at a non-retrying automatic `compaction_end` inside an extension-triggered zero-wrapper-claim agent run, before continued turns and `agent_settled`; it also found that a retained extension UI context can create a dialog after wrapper destruction and leave its promise pending. Its evidence-only finding that the checkpoint lacked the post-fix handoff is resolved by these root-owned entries, not by a source writer. The reviewers otherwise passed the repaired publication ordering, malformed-input atomicity, canonical reconstruction, tool equality, receiver unit/part/cumulative checks, diagnostics, structural/deque work, three-class wire growth, HMR, zero-subscriber capture, deep-input compatibility, custom-UI pre-mount cleanup, terminal-path fixtures, raw-SSE preservation, privacy, package boundary, and S3+ containment.
**Evidence:** The root reproduced every decisive remaining code defect with content-safe synthetic probes. Two individually valid custom-UI updates advanced the hub cursor from one to two; the first state was readable, while the aggregate state made both `getState()` and recovery snapshot generation fail. A same-epoch recovery transaction at the current cursor applied altered state; a separately encoded state with both refresh markers false also applied. A getter-backed nested terminal tool-call field was invoked and projected. The sequence `agent_start`, threshold `compaction_start`, non-retrying `compaction_end` emitted one settlement and one final snapshot before `agent_settled`, leaving projected activity false. A dialog requested through a retained post-destruction UI context remained pending while the hub was closed. Both reviewers independently reran TypeScript, lint, focused `69/69`, and non-real-Next `268/268`; wrapper review also confirmed the exact `270/272` literal-suite and `2/4` separate real-Next missing-manifest disposition, package dry run, and diff check.
**Uncertainty / gaps:** Reviewer VC tables differed where one review did not cover the other's surface: protocol review failed `S2-VC-005/006`, while wrapper review passed those narrower seams; wrapper review failed `S2-VC-004/008`, while protocol review did not assess those lifecycle races. Root reproduction resolves the combined disposition as blocked. Aggregate limits must be shared and injectable across accepted hub state, snapshot generation, parser, and receiver, but enforcement must use incremental or changed-branch accounting so routine growing deltas do not regain quadratic whole-state validation. Direct extension compaction must still settle, while automatic compaction nested in an active native turn must wait for `agent_settled`. These are contained implementation corrections, not a new product/protocol decision or material divergence.
**Recommended use:** Launch one fresh sole fix writer under the immutable S2 plan. Require atomic aggregate-overflow rejection with unchanged cursor/state/replay and always-usable snapshots; same-cursor and marker-mismatch receiver rejection; zero nested accessor invocation; causal native-turn versus standalone-compaction settlement; immediate default settlement for post-destruction dialogs; focused regressions; all required gates; and then another fresh independent review.

## Handoff

**Source:** Pi subagents run `ef405ee8-4b3b-427f-b926-ceda3ed306c7`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: second bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Repair the independently reproduced aggregate-state, strict-receiver, nested-accessor, zero-claim compaction-finality, and post-destruction-dialog defects without truncating state, restoring growing-state work, changing the V1 product semantics, or entering S3+ scope. Issued obligations mapped to `S2-VC-001` through `S2-VC-010`, principally `S2-VC-002/003/004/005/006/007/008/009`.
**Outcome:** The writer added shared injectable canonical depth/node/snapshot-byte/snapshot-part limits; cached structural state metrics with exact incremental string-byte accounting; atomic aggregate and multi-draft precommit rejection; same-epoch current/stale snapshot rejection and decoded refresh-marker enforcement; descriptor-safe nested terminal tool normalization; native-turn versus standalone-compaction tracking; and an alive guard for all retained post-destruction dialog methods. Seven regressions raised the focused suite from 69 to 76. Root probes confirmed the original aggregate test now rejects its second update without advancing, state/snapshots remain readable, same-cursor state replacement fails, nested getters are not invoked, in-run automatic compaction waits for `agent_settled`, and retained dialogs return documented defaults. The attempt is not accepted because root and fresh review found additional strict-receiver, limit-configuration, and causal-order overlap defects.
**Evidence:** The writer reported typecheck and lint passing; focused `76/76`; exact non-real-Next `275/275`; literal suite `277/279` and separate real-Next `2/4`, with only the permitted missing-`BUILD_ID` production child/parent failures; package dry run with the honest nine-file manifest boundary; privacy/source/whitespace checks passing; no stage and no forbidden build. The root read the changed protocol, projector, reducer, hub, wrapper, and focused tests; verified the immutable plan hash and intended source boundary; independently reran typecheck, lint, focused `76/76`, and non-real-Next `275/275`; and ran randomized content-safe metric checks that matched every sampled snapshot byte declaration across text, Unicode, escapes, split surrogates, queue, status, and widget updates.
**Uncertainty / gaps:** Repository-wide `ORCH-VC-003`, fresh production inclusion, and race-free HTTP/browser composition remain explicitly deferred. The second fix correctly handles automatic compaction after `agent_start` but not pre-prompt automatic compaction before `agent_start`; it also allows a wrapper claim release to settle an overlapping standalone native compaction. The receiver still applies the snapshot-unit ceiling to ordinary logical frames even though the immutable plan permits oversized ordinary frames, and it can reduce past its own canonical limits. Configured depth greater than the supported recursive maximum can throw. These are bounded S2 implementation defects, not material divergence.
**Recommended use:** Keep the diff uncommitted. Reconcile the fresh review, then issue one focused sequential fix that uses the immutable plan's snapshot-only unit ceiling, shared incremental receiver precommit, explicit safe-integer guards, manual-only standalone native compaction classification plus overlap gating, and a fail-closed supported depth range.

## Handoff

**Source:** Pi subagents run `6cca0a6f-9c9c-4749-a5f8-c698ddff819e`, parallel `reviewer` children 0 (protocol/projector/reducer/hub) and 1 (wrapper/lifecycle/evidence). Recorder: orchestration root. Kind: fresh independent second-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Adversarially inspect the second-fix diff for exact aggregate accounting and receiver representability, strict parser behavior, native lifecycle ordering, extension cleanup, compatibility, scope, privacy, and current validation under `S2-VC-001` through `S2-VC-010`, without editing or delegating.
**Outcome:** Review remains blocking. Protocol review found three P0 defects and one major: an ordinary logical frame accepted by a hub can exceed the matching receiver's configured encoded-unit ceiling and be rejected live even though snapshot recovery works; sequential logical frames can advance a receiver into state beyond its node/byte limits; `transcriptRevision` can overflow to an unsafe integer while advancing the cursor; and a configured recursive depth of 10,000 can escape as `RangeError`. It also found a lifecycle blocker: release of a failing wrapper prompt can settle an overlapping standalone native compaction before its `compaction_end`. Wrapper review independently confirmed the root's pre-prompt blocker: threshold/overflow compaction before `agent_start` emits false finality at `compaction_end` and again at eventual `agent_settled`, across success/abort/error orderings. Its note that the checkpoint lacked current evidence is resolved by these root-owned entries. Both reviews otherwise passed aggregate/multi-draft atomicity, cached Unicode accounting, snapshot assembly/current-cursor/marker checks, nested accessor exclusion, FIFO/replay, true-delta growth, HMR/zero-subscriber/close, retained dialog/custom cleanup, raw-SSE compatibility, privacy, and S3+ containment.
**Evidence:** The root reproduced every decisive new defect with synthetic content-safe probes. A hub and receiver sharing a 512-byte snapshot-unit limit produced a 793-byte ordinary custom-UI frame that the hub delivered and the receiver rejected. A receiver node budget sized for one status applied a second status, advanced to cursor two, and became unparsable; a valid snapshot state at maximum transcript revision applied one change and advanced to an unsafe integer. A supported API call with configured depth 10,000 threw `RangeError`. A direct native manual compaction overlapped by a synchronously failing wrapper prompt emitted settlement and a final snapshot before `compaction_end`. The pre-prompt sequence `threshold compaction start/end`, then `agent_start`, continued event, and `agent_settled` produced two settlements. Root source verification of read-only Pi shows pre-prompt auto-compaction occurs before `_runAgentPrompt` and its eventual `agent_settled`. Reviewers and root agreed that the immutable plan's 64 KiB encoded-unit ceiling is specific to snapshot-transfer units; oversized ordinary logical frames are deliberately delivered live or recovered by snapshot.
**Uncertainty / gaps:** Reviewer VC tables differed because wrapper review treated receiver surfaces as passing while protocol review directly probed them; root reproduction resolves `S2-VC-005` as failed. A receiver fix must share the hub's incremental metrics rather than reparsing/stringifying growing state per delta. Injected depth is needed only for deterministic tighter tests and must not accept an unsafe larger recursive maximum. Manual direct compaction remains independently settleable; threshold/overflow is automatic and must wait for native settlement even when it precedes `agent_start`; wrapper releases must also respect an active standalone manual compaction. No new product choice or weaker gate is required.
**Recommended use:** Launch one fresh sole fix writer under the same immutable milestone. Require matching-hub ordinary frames to bypass the snapshot-unit ceiling, receiver-side atomic semantic/aggregate precommit with no effect/cursor mutation, transcript-revision overflow rejection, safe depth caps, pre-`agent_start` threshold/overflow fixtures, and standalone-manual overlap fixtures; then repeat root probes, all gates, and fresh review.

## Handoff

**Source:** Pi subagents run `04077734-cb19-477d-86aa-f1ac3c944570`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: third bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Correct the snapshot-unit/ordinary-frame mismatch, receiver post-reduction aggregate and semantic overflow, unsafe configurable recursion depth, pre-prompt automatic-compaction finality, and standalone-native-compaction overlap defects while retaining all prior S2 protocol, replay, privacy, compatibility, and performance obligations.
**Outcome:** The writer made the 64 KiB-style option explicitly snapshot-transfer-only; factored shared canonical state metrics and transfer representability; added receiver candidate precommit and maximum-safe revision guards; capped recursive depth at the supported maximum; distinguished manual native compaction from threshold/overflow automatic compaction; and tracked native turns and standalone manual compaction independently across wrapper claims and synchronous fanout reentrancy. Nineteen focused cases raised the S2/RPC suite from 76 to 95. Root probes confirmed oversized ordinary live/recovery convergence, receiver node overflow rejection, safe revision rejection, fail-closed deep configuration, one final boundary for pre-prompt threshold/overflow, and no settlement before an overlapping manual compaction ends. The attempt is not accepted because fresh review exposed mandatory-finality headroom, prototype-key inspection, receiver mutability, and legacy-fanout defects.
**Evidence:** The writer and root independently observed typecheck and lint passing; focused `95/95`; exact non-real-Next `294/294`; literal suite `296/298` and separate real-Next `2/4`, with only the permitted missing-`BUILD_ID` production child/parent failures; package dry run with nine manifest files; clean tracked and untracked whitespace checks; unchanged lifecycle/package/SSE/hook/S3+ boundaries; immutable milestone hash; no stage and no forbidden build. Root inspected the factored metrics, receiver commit path, hub precommit, native flags, and new fixtures and reran every decisive synthetic probe.
**Uncertainty / gaps:** Repository-wide raw serialization, fresh production inclusion, and race-free HTTP/browser composition remain deferred exactly as before. Exact state limits now protect current transitions, but the hub did not yet reserve the possibly one-byte-larger mandatory settled form. Shared receiver metrics remained cacheable against mutable returned state. The wrapper's newly correct terminal flags still depended on a mutable, exception-propagating legacy listener loop. These are contained S2 defects, not a reason to weaken the plan or change V1.
**Recommended use:** Keep the diff uncommitted, record the fresh independent review, and issue one sequential fix for finality reserve, strict descriptor copying, immutable cache inputs, and stable exception-isolated legacy fanout. Do not disguise the separate many-block index optimization as a protocol rewrite.

## Handoff

**Source:** Pi subagents run `f5bbd7f0-57c8-45b4-b0e8-70ac3aa80d61`, parallel `reviewer` children 0 (protocol/reducer/hub) and 1 (wrapper/lifecycle/evidence). Recorder: orchestration root. Kind: fresh independent third-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Re-test every prior defect and adversarially inspect shared metrics, parser strictness, receiver recovery, finality order, extension teardown, legacy compatibility, privacy, scope, and all current validation evidence without editing or delegating.
**Outcome:** Review blocks acceptance on three P0 areas. First, a currently valid active state can exactly fill its byte budget while `run_settled` is one byte larger, so the mandatory settlement and final snapshot are silently rejected and activity remains stranded. Second, `inspectDataRecord()` assigns an own `__proto__` data property into `{}`, changing the temporary prototype and permitting a later inherited accessor read. Third, legacy wrapper fanout iterates the mutable listener array directly and propagates exceptions: self-unsubscribe can skip later observers yet still finalize, while a throw at `prompt_done`, `agent_settled`, or `compaction_end` can prevent claim/flag release and strand activity. Protocol review also found that a receiver's publicly mutable state/metrics/limits can invalidate identity-keyed metric caches and bypass later precommit. Its simultaneous-many-block/many-delta timing finding is real but required separate scope classification. The review otherwise passed the third-fix unit semantics, receiver bounds, depth cap, replay/snapshot strictness, delta equality/growth, causal native paths, post-destruction UI, HMR/zero-subscriber/close, raw-SSE compatibility, privacy, scope, and test/package dispositions.
**Evidence:** Root reproduced each decisive blocker with content-safe probes. An accepted 441-byte active/native-settled state required 442 bytes after settlement; `wrapper_settled` left cursor unchanged and activity true. An own enumerable `__proto__` data property invoked one inherited `message` getter and projected it. Mutating returned receiver status state from 313 to 1,312 exact bytes left cached metrics at 313 and allowed another frame/cursor. A self-unsubscribing terminal listener skipped the next listener while the hub finalized; a throwing `agent_settled` listener escaped, left activity true, and emitted no settlement. Reviewer/root validation agreed on focused `95/95`, non-real-Next `294/294`, literal `296/298`, real-Next `2/4`, the exact permitted manifest failures, package boundary, clean diff, and no stage.
**Uncertainty / gaps:** The reviewer classified immutable ordered draft-block copying as a major `S2-VC-003` complexity defect and proposed a separate indexed/persistent representation. That recommendation would split internal and canonical wire state and therefore needed authority review before inclusion. The checkpoint-evidence note is resolved by these root-owned append-only entries. No other finding requires changing scope, weakening validation, or entering S3+.
**Recommended use:** Obtain a narrow decision-consistency review of the many-block finding. Then launch one sole fix writer for the four independently reproduced contained defects, preserving incremental delta accounting and exact canonical state, and repeat all verification/review gates.

## Handoff

**Source:** Pi subagents run `71d6ad86-801d-404d-8c7d-517705ced097`, `oracle` child 0. Recorder: orchestration root. Kind: read-only decision-consistency advisory review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Determine whether the measured `O(block-count × delta-count)` immutable draft-array cost is an S2 acceptance defect or whether the proposed persistent/indexed representation would exceed the exact canonical-state and pure-reducer authority.
**Outcome:** The advisory review confirms the timing trend is real but classifies it as a non-blocking residual, at most minor under the approved S2 contract. `ORCH-VC-003` and `S2-VC-003` define the blocked quadratic fallback through repeated growing wire snapshots/full transcripts and require encoded-byte evidence; all three actual delta classes pass that evidence. The canonical state intentionally contains an exact ordered plain block array and the same pure reducer is to serve tests/future clients. A separate persistent internal representation materialized only at snapshot/final boundaries would silently redesign that seam and cross the master's explicit secondary index-optimization scope stop. No current real provider or S7 responsiveness evidence shows this synthetic cross-dimensional workload blocks acceptance.
**Evidence:** Direct comparison of the master and immutable S2 plan with `lib/session-reducer.ts` array operations and the passing projector-to-encoder growth fixtures. The output is recoverable by run identity and changed no project file.
**Uncertainty / gaps:** Very large single assistant messages containing hundreds or thousands of content blocks could still stall the Node event loop under concurrency; supported-provider block limits are not established. A local ordered locate-and-copy replacement may remove gratuitous filter/sort and repeated lookup overhead while preserving the exact array, but it cannot remove the intrinsic `O(block-count)` immutable copy and must not be represented as doing so.
**Recommended use:** Do not add a persistent map, parallel canonical representation, mutable cache, dependency, or timing acceptance gate in S2. Optionally fold a narrow locate-and-copy/last-block fast path into an already-authorized reducer fix. Record the benchmark as a residual S7 responsiveness risk and escalate only if a real supported-provider or acceptance workload is materially affected.

## Handoff

**Source:** Pi subagents run `f54ee3dc-83b9-4bf5-a6ac-6cdfd7b8b79c`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: fourth bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Repair the mandatory-settlement representability reserve, prototype-key descriptor inspection, receiver/cache immutability, and hostile legacy-fanout defects established by the third-fix review, while allowing only the advisory-approved local ordered draft-array optimization and preserving every S2 scope, privacy, recovery, and compatibility boundary.
**Outcome:** The writer added settlement-form representability checks, null-prototype descriptor copying, deeply frozen hub/receiver/cache/assembly graphs, stable exception-isolated legacy fanout, and a contained ordered locate-and-copy reducer path. Ten regressions raised the focused S2/RPC suite from 95 to 105. Root probes confirmed that exact-limit growth is rejected before consuming the state-byte finality reserve, own `__proto__` inputs invoke no inherited getter, returned hub/receiver/assembly graphs reject mutation, and self-unsubscribing or throwing terminal observers no longer skip peers, escape, or strand ordinary cleanup. The attempt is not accepted because fresh review found safe-sequence finality headroom, legacy-alias settlement, reentrantly queued-input mutability, and same-kind lifecycle-reentrancy defects.
**Evidence:** The writer reported TypeScript and lint passing; focused `105/105`; exact non-real-Next `304/304`; literal suite `306/308`; and separate real-Next `2/4`, with only the permitted absent `.next/BUILD_ID` production child/parent failures. Package dry run contained the existing nine manifest files and remains manifest-only evidence. Privacy/raw-field/source-boundary scans, tracked and no-index untracked whitespace checks, immutable-plan hash comparison, and no-stage inspection passed; no forbidden build ran. The root inspected the fourth-fix production/test changes, verified the unchanged milestone hash and source boundary, reran decisive content-safe probes for settlement bytes, prototype keys, immutable graphs, and hostile fanout, and observed no staged file.
**Uncertainty / gaps:** Repository-wide `ORCH-VC-003` remains intentionally partial until S4B removes legacy raw SSE; fresh production inclusion and race-free HTTP/browser composition remain later boundaries. The intrinsic `O(block-count)` immutable array copy remains a non-blocking S7 responsiveness risk for unusually large provider output. The implementation-side command matrix is reported but cannot satisfy acceptance while the newly reproduced P0 finality and input-isolation defects remain.
**Recommended use:** Keep the S2 diff uncommitted. Reconcile the fresh review, reproduce every decisive finding at root, then issue one sequential bounded fix under the same immutable milestone before repeating all root gates and independent review.

## Handoff

**Source:** Pi subagents run `cae8b3df-9104-4f4e-866a-3408541fa8f0`, parallel `reviewer` children 0 and 1. Recorder: orchestration root. Kind: fresh independent fourth-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Challenge the complete fourth-fix diff for immutable-cache safety, safe finality sequence and state headroom, snapshot assembly/recovery, hostile fanout and destruction ordering, causal wrapper settlement, all prior defects, validation evidence, privacy, and S3+ containment without editing or delegating.
**Outcome:** Review remains blocking on four bounded implementation defects. First, settlement reserves state/transfer capacity but not the two remaining safe sequence values: from `MAX_SAFE_INTEGER - 1`, `wrapper_settled` discards the whole planned settlement when its final snapshot would overflow. Second, accepted legacy manual compaction aliases project start/end frames but wrapper lifecycle tracking never releases them, leaving canonical activity true. Third, `accept()` retains a caller-owned input reference when invoked reentrantly during publication, so the caller can mutate it after `accept()` returns but before state-dependent planning. Fourth, native-turn and standalone-manual-compaction booleans are not causal under same-kind synchronous reentrancy: a nested start during outer terminal fanout is cleared by the outer terminal, producing false `run_settled` and a final snapshot. The reviewers otherwise passed strict V1 shapes/raw exclusion, true deltas and final equality, aggregate state and snapshot bounds, immutable graphs, ordinary stable fanout, extension cleanup, zero-subscriber/HMR/close behavior, privacy, raw-SSE deferral, and later-surface containment. Their checkpoint-continuity finding is resolved only by these root-owned append-only entries; it does not clear the code blockers.
**Evidence:** Root reproduced every decisive defect with content-safe synthetic probes. Settlement at `MAX_SAFE_INTEGER - 2` emitted `run_settled` plus a final snapshot at the last safe cursor, while starts at `MAX_SAFE_INTEGER - 1` and `MAX_SAFE_INTEGER` emitted nothing and remained active. A listener reentrantly accepted an extension status as `original`, mutated it before the outer publication returned, and canonical state recorded the mutated value at cursor two. Reentrant `agent_start` during `agent_settled` yielded `native_settled`, nested `activity_started`, then false settlement/final snapshot with `active:false`; the same failure occurred for nested manual `compaction_start` during `compaction_end`. A legacy manual `auto_compaction_start/end` emitted only compaction frames and remained `active:true`. Reviewers independently observed TypeScript, lint, focused `105/105`, and non-real-Next `304/304` passing; the wrapper reviewer also confirmed literal `306/308`, real-Next `2/4`, the exact permitted missing-manifest disposition, nine-file package dry run, and no staged file.
**Uncertainty / gaps:** Current tests cover reentrant terminal fanout only with a different-kind `turn_start`, which cannot expose same-kind causal overwrite. The smallest safe repair must preserve FIFO and state-dependent planning while snapshotting only accepted projection input, use safe causal counters/tokens for current and legacy native activity, and reserve two sequence positions without weakening safe-integer parsing or final snapshot semantics. No finding requires protocol redesign, product choice, scope expansion, weaker validation, or S3+ work.
**Recommended use:** Launch one fresh sole fix writer under the immutable S2 plan. Require exact safe-sequence boundary tests, immutable descriptor-safe accepted-input capture, current and legacy manual-compaction settlement, same-kind native/manual reentrancy regressions, preservation of all prior terminal and compatibility paths, all milestone gates, and then fresh independent review.

## Handoff

**Source:** Pi subagents run `5cac0f69-495e-45c8-9a06-297b8f3849c6`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: fifth bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Repair the safe-sequence finality reserve, caller-mutable reentrant accepted-input queue, current and legacy manual-compaction settlement, and same-kind start-during-terminal lifecycle defects established by the fourth-fix review, while preserving every prior S2 protocol, privacy, replay, compatibility, and scope obligation.
**Outcome:** The writer reserved two safe logical sequence positions for settlement and its final snapshot, guarded implicated arithmetic, and added exact maximum-boundary/multi-draft tests. It introduced descriptor-safe projection-input capture before enqueue so caller aliases cannot mutate reentrantly queued work, while preserving the original raw object for legacy fanout. It replaced native-turn and standalone-manual-compaction booleans with counters, covered current and legacy manual aliases, and kept threshold/overflow aliases nested. Eleven regressions raised the focused suite from 105 to 116. Root probes confirmed the safe sequence boundary, atomic multi-draft rejection, canonical maximum-cursor final snapshot, ordinary reentrant alias isolation, getter non-invocation, same-kind nested starts, legacy manual settlement, and threshold/overflow preservation. The attempt is not accepted because deeper root probes found that one hostile captured input can still throw and same-kind reentrant terminal events can claim one counter instance twice, drive a counter negative, and strand the next run.
**Evidence:** The writer reported TypeScript and lint passing; focused `116/116`; exact non-real-Next `315/315`; literal suite `317/319`; and separate real-Next `2/4`, with only the permitted absent `.next/BUILD_ID` production child/parent failures. Package dry run contained nine manifest files and remains manifest-only evidence. Tracked/cached/no-index whitespace checks, source/privacy/raw-field inventories, immutable-plan comparison, and no-stage inspection passed; no forbidden build ran. Root inspection confirmed only the approved S2 modules/tests changed, the checkpoint remained root-owned, the milestone blob hash remains identical to `HEAD`, and no file is staged. Root content-safe probes observed settlement succeeding from `MAX_SAFE_INTEGER - 2` and rejecting atomically from `-1`/maximum; a reentrantly queued status retained its original value after caller mutation; and nested current/legacy starts remained active until their own terminal.
**Uncertainty / gaps:** A revoked proxy used as ignored assistant `content` escapes `captureSessionProjectionInput()` through `Array.isArray`, so `accept()` throws instead of failing closed, although cursor/state remain unchanged. More critically, an `agent_settled` listener that reenters with another `agent_settled` lets both terminals reserve the same pre-fanout count: nested cleanup settles, outer cleanup decrements to `-1`, and the next native run starts at zero and remains active forever. Reentrant duplicate current/legacy manual compaction ends have the same failure. Repository-wide `ORCH-VC-003`, fresh production inclusion, race-free HTTP/browser composition, and the immutable block-array responsiveness residual remain deferred exactly as before.
**Recommended use:** Keep the diff uncommitted. Complete the already-launched fresh independent review, record its disposition, then issue one sequential bounded fix for fail-closed capture under engine/proxy failures and exclusive causal terminal-token reservation if review confirms no authority gap.

## Handoff

**Source:** Pi subagents run `a2e0d680-5b71-4f8e-9032-9975f6aad191`, parallel `reviewer` children 0 (protocol/hub) and 1 (wrapper/lifecycle/evidence). Recorder: orchestration root. Kind: fresh independent fifth-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Adversarially verify the fifth-fix safe-sequence reserve, descriptor-safe accepted-input capture, current/legacy lifecycle counters, replay/recovery, privacy, compatibility, validation evidence, and every prior defect without editing or delegating.
**Outcome:** Review remains blocking on two implementation areas with four concrete effects. Accepted-input capture copies every nested assistant-event field and complete non-summary messages before later normalization, retaining forbidden provider fields, final tool metadata, and accumulated block-end content in the reentrant queue. Its node budget restarts for each copied property, so one malformed event can multiply aliases beyond the advertised aggregate limit. Revoked proxies can also escape unguarded `Array.isArray` checks and throw from `accept()` instead of failing closed. Separately, current counters reserve terminal release only logically before fanout: a same-kind terminal reentered during that fanout claims the same positive count, nested cleanup settles, outer cleanup decrements negative, finality occurs before every outer observer receives the terminal, and the next balanced run strands active. New tests cover nested starts and sequential duplicate terminals but not these adversarial cases. Safe sequence headroom, atomic multi-draft rejection, replay/snapshot/receiver behavior, ordinary alias isolation, true-delta wire growth, extension cleanup, HMR/zero-subscriber/destruction, raw legacy identity, scope, and staged deferrals otherwise pass.
**Evidence:** Root probes reproduced every decisive finding. Three reentrantly queued inputs retained assistant `responseId`, accumulated `text_end.content`, tool `thoughtSignature`, and completed-message `responseId`. An unknown assistant event with 101 properties aliasing one 1,000-element array copied 101,000 elements despite the 100,000-node advertised limit. A revoked proxy in ignored assistant content threw from `accept()` with unchanged cursor/state. Reentrant `agent_settled` produced the outer/nested observer order `listener1:outer`, `listener1:nested`, `listener2:nested`, final snapshot, then `listener2:outer`; the count became `-1`, and the next balanced native run ended at count zero with `active:true` and no settlement. Root independently reran TypeScript, lint, focused `116/116`, tracked/no-index whitespace checks, immutable-plan comparison, and no-stage inspection successfully. Reviewer 0's checkpoint-continuity note observed a stale overlapping read; the fifth-fix handoff was already appended by the root and reviewer 1 verified it.
**Uncertainty / gaps:** The corrected capture must retain only exact canonical projection inputs while preserving later state-dependent delta comparison and strict malformed/unknown classification. Block-end accumulated content is needed only for type/presence validation and must not be retained as a growing value. All selected values need one shared depth/node budget, and any proxy/engine failure must be isolated. Terminal release must be exclusively reserved before reentrant fanout while still preventing any nested wrapper operation from settling before the outer public terminal finishes. These are contained S2 corrections; no product/protocol decision, scope expansion, weaker gate, or S3+ work is required.
**Recommended use:** Launch one fresh sole fix writer under the same immutable milestone. Require minimal canonical accepted-input capture with one aggregate budget and no forbidden raw retention, complete fail-closed proxy isolation, exclusive terminal-token reservation that preserves public-before-final ordering under nested terminals and starts, focused adversarial regressions, all milestone gates, and then another fresh independent review.

## Handoff

**Source:** Pi subagents run `9f09389d-c28e-4c11-8e38-23158ef4961f`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: sixth bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Replace raw/growing accepted-input retention with minimal canonical capture under one aggregate budget, make every hostile capture path fail closed, and reserve native/current-legacy manual terminal tokens exclusively through complete public fanout, while preserving all earlier S2 behavior and later-milestone boundaries.
**Outcome:** The writer added projection-specific capture that strips growing partial/terminal messages, replaces accumulated block-end content with an empty validation sentinel, normalizes final tool calls and completed display messages before enqueue, and omits unknown event values. Selected JSON branches now share depth/node accounting, and hub `accept()` isolates all capture failures. Wrapper lifecycle now moves one unreserved activity into a reservation before fanout and keeps that reservation active until every start-of-fanout observer returns; nested duplicate terminals cannot claim it. Seven regressions raised the focused suite from 116 to 123. Root probes confirmed forbidden provider values and accumulated content are absent from queued canonical values, selected repeated aliases over the configured node limit reject, revoked proxies do not throw or mutate the hub, native/current-manual/legacy-manual nested duplicate terminals preserve observer-before-final ordering, counters/reservations return to zero, and the next balanced run settles. The attempt remains unaccepted pending fresh independent review and full root gate verification.
**Evidence:** The writer reported TypeScript and lint passing; focused `123/123`; exact non-real-Next `322/322`; literal suite `324/326`; and separate real-Next `2/4`, with only the permitted absent `.next/BUILD_ID` production child/parent failures. Package dry run contained nine manifest files and remains manifest-only evidence. Corrected tracked/cached/no-index whitespace checks, immutable master/milestone comparisons, privacy/raw/source-boundary scans, and no-stage inspection passed; no forbidden build ran. Root inspected the capture budget, normalized message/tool capture, fail-closed hub boundary, descriptor-safe lifecycle classification, reservation counters, and adversarial tests; confirmed the milestone hash and source boundary; and reran the decisive content-safe probes described above.
**Uncertainty / gaps:** Known assistant excess-field names are currently retained only as null-valued sentinels so later strict projection rejects them; no excess value is traversed or retained, but fresh review must decide whether immediate capture rejection is required by the exact no-unused-field boundary. Ignored assistant content arrays are descriptor-validated and then discarded without counting each omitted element against the selected-node budget; this retains no item or value but still performs work proportional to block count. These are scrutiny points, not authority for weakening malformed classification or adding a new representation. Repository-wide `ORCH-VC-003`, fresh production inclusion, S4 HTTP/browser composition, and the immutable block-array responsiveness residual remain deferred exactly as before.
**Recommended use:** Run fresh independent no-edit/no-delegation review of the complete diff, emphasizing exact canonical capture/budget semantics and terminal reservation under nested starts, terminals, claims, hostile listeners, and destruction. Then rerun the complete root gate matrix and accept only if no blocker or major defect remains.

## Handoff

**Source:** Pi subagents run `404df78a-5150-4ae9-93cd-d3cd65efcf1e`, parallel `reviewer` children 0 (protocol/capture/hub) and 1 (wrapper/lifecycle/evidence). Recorder: orchestration root. Kind: fresh independent sixth-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Re-test every prior S2 defect and adversarially challenge minimal canonical capture, aggregate work/output limits, exact assistant/message/extension semantics, hostile inputs, nested lifecycle reservations, public ordering, malformed native interference, recovery, compatibility, privacy, scope, and the complete current validation evidence without editing or delegating.
**Outcome:** Review remains blocking in two bounded areas. Canonical capture still retains raw model/provider values inside validation-only ignored assistant markers, null-valued attacker-controlled excess field names, and the union of every extension method's fields rather than the selected method's exact subset. Validation-only partial content arrays scan all keys/descriptors while charging one node, reconstructed display objects do not consistently count toward the same output budget, and repeated delta capture can therefore regain block-count-by-delta-count work. Separately, a start listener can synchronously emit its matching terminal; the nested terminal releases and finalizes before later observers receive the enclosing start. Lifecycle accounting also accepts malformed terminals independently of projected capture, so a current manual end missing Boolean `willRetry`, an end with invalid `aborted`, or an accessor-bearing `agent_settled` can erase valid activity and emit finality without the corresponding projected terminal frame. Safe sequence/finality headroom, ordinary FIFO/alias isolation, terminal-within-terminal reservation, replay/snapshot/receiver bounds, immutable state, true-delta encoded growth, extension teardown, HMR/zero-subscriber/destruction, raw legacy identity, S3+ containment, and staged deferrals otherwise pass.
**Evidence:** Root probes reproduced every decisive finding. Ignored markers retained raw model/provider labels; one known assistant event queued a null sentinel under an attacker-controlled one-million-character key with a node limit of 20; `setStatus` queued unused `id`, `options`, and `message`; a 1,000-block ignored content array passed a node limit of 10. A 20-block final message captured under limit 50 even though `cloneJsonSafe` rejected the resulting graph under the same limit, and a matching hub emitted `message_completed`. Native/current-manual/legacy-manual start-to-terminal reentrancy produced `listener1:start`, nested terminal observer, final snapshot, then `listener2:start`. Malformed manual terminals missing `willRetry` or carrying non-Boolean `aborted`, and an accessor-bearing `agent_settled`, each emitted settlement/final snapshot without `compaction_finished` or `native_settled`. Root independently ran TypeScript, lint, focused `123/123`, exact non-real-Next `322/322`, literal `324/326`, separate real-Next `2/4`, nine-file package dry run, whitespace, immutable-plan, source-boundary, privacy/log, and no-stage gates; the only suite failures were the permitted missing `.next/BUILD_ID` production child and parent.
**Uncertainty / gaps:** Minimal capture should use content-free fixed validation sentinels, reject known excess keys before queueing, select exact extension method fields, avoid traversing ignored content, and validate the final reconstructed canonical graph against the same configured depth/node limit. Native event settlement needs an outer-fanout deferral barrier so nested terminals cannot finalize before the enclosing event completes. Lifecycle mutation must derive from the same canonical accepted input and exact semantic validation as projection; independent permissive classification is unsafe. These corrections require no new protocol/product choice, scope expansion, validation waiver, or S3+ work.
**Recommended use:** Launch one fresh sole fix writer under the immutable S2 milestone. Require exact minimal/bounded capture, one final canonical output-budget check, constant-work ignored assistant validation, outermost native-fanout settlement deferral, projection-consistent lifecycle classification, adversarial regressions for every reproduced case, all milestone gates, and then fresh independent review.

## Handoff

**Source:** Pi subagents run `529f7d2a-f6ef-47db-b569-a02e373205fb`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: seventh bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Correct the sixth-review defects in exact minimal capture, reconstructed canonical graph accounting, outermost fanout settlement ordering, and projection-consistent lifecycle classification, while preserving every prior S2 protocol, replay, privacy, compatibility, and S3+ exclusion obligation.
**Outcome:** The writer replaced ignored assistant labels/content with fixed empty validation sentinels, rejects excess keys on known assistant variants, selects extension fields by method, strictly clones the reconstructed captured graph under the configured limits, and adds a wrapper-only prepared-input seam so canonical capture is shared by lifecycle and projection. It also adds an outer event-fanout depth barrier and real deferred settlement request. Regressions raise the focused suite from 123 to 138 and repair the sixth-review reproductions for raw labels, excess-key queueing, method-field leakage, ignored-content scanning, reconstructed-output overflow, start-to-terminal ordering, malformed manual ends, accessor terminals, and stateful-proxy inspection. The attempt is not accepted because root verification found two further bounded defects: unknown assistant/method classification still performs unbounded shallow descriptor scans before returning its sentinel, and wrapper lifecycle releases activity even when the hub rejects the matching valid projected terminal.
**Evidence:** Root inspection confirmed only the intended S2 modules/tests plus this root-owned checkpoint differ; master and milestone blobs remain identical to `HEAD` and planning commit `29bc0a6`; no file is staged; package/server/real-Next harness and later transport/browser/SSE surfaces are unchanged. Root independently passed TypeScript, lint, focused `138/138`, exact non-real-Next `337/337`, tracked/cached/no-index whitespace checks, source/privacy/log inventories, and the nine-file manifest-only package dry run. Literal full tests are `339/341` and separate real-Next is `2/4`; both failures are solely the permitted absent `.next/BUILD_ID` production child and parent, while terminal and real-development subtests pass. No build ran. A root proxy probe with node limit ten observed 1,001 and 1,002 descriptor traps for unknown assistant and extension inputs respectively before bounded sentinels were returned. At sequence `MAX_SAFE_INTEGER - 3`, a valid native start published at `-2`, its valid `agent_settled` frame was rejected for headroom, yet wrapper release emitted `run_settled` and a final snapshot with no `native_settled`. A constrained-state manual compaction likewise rejected an oversized `compaction_finished` precommit but still emitted settlement/final snapshot.
**Uncertainty / gaps:** Fresh independent review run `8684fca9-b695-43c8-aaf7-e64411c3406e` is in progress and must classify these findings and challenge the rest of the diff. The smallest lifecycle repair must preserve exclusive reservation before raw fanout and projected FIFO under reentrancy while making release/finality depend on actual terminal projection commit, not merely successful canonical capture/enqueue. Repository-wide `ORCH-VC-003`, fresh production inclusion, S4 HTTP/browser composition, and the intrinsic immutable block-array responsiveness residual remain deferred exactly as before. No observed fact requires a new product/protocol choice or scope expansion.
**Recommended use:** Keep S2 uncommitted. Complete and record the fresh review, then issue one sequential bounded fix for bounded discriminant/method inspection and actual projected-terminal commit receipts if review confirms no additional authority gap.

## Handoff

**Source:** Pi subagents run `8684fca9-b695-43c8-aaf7-e64411c3406e`, `reviewer` child 0. Recorder: orchestration root. Kind: fresh independent seventh-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Re-test the complete seventh-fix lineage across exact capture/output bounds, prepared lifecycle consistency, outermost fanout settlement, all prior protocol/replay/reducer/extension defects, compatibility, privacy, scope, and the full `S2-VC-001` through `S2-VC-010` matrix without editing or delegating.
**Outcome:** Review rejects acceptance on four bounded implementation areas. First, wrapper lifecycle treats successful canonical enqueue as terminal projection success: sequence or aggregate-state precommit can reject `native_settled` or `compaction_finished`, after which the wrapper still releases its reservation and emits false finality. Second, discriminant/method classification and selected normalizers still call full descriptor inspection, so unknown, known-unused, and selected inputs can scan arbitrarily many discarded keys outside the configured node budget. Third, a current manual `compaction_end` with Boolean `willRetry:true` emits `compaction_finished` but lifecycle classification leaves its standalone activity permanently claimed. Fourth, capture and `cloneJsonSafe()` reject cycles but duplicate repeated selected aliases instead of failing closed under the explicitly issued alias invariant. The review otherwise passes true-delta growth/finality equality, exact protocol parsing, immutable reducer/receiver state, replay/snapshot bounds, extension teardown, HMR/zero-subscriber capture, diagnostics/privacy, ten-minute compatibility, and S3+ containment.
**Evidence:** Reviewer and root independently passed TypeScript, lint, focused `138/138`, exact non-real-Next `337/337`, literal `339/341`, separate real-Next `2/4`, nine-file package dry run, whitespace, immutable-plan, source/privacy, and no-stage checks; the only suite failures are the permitted absent `.next/BUILD_ID` production child and parent. Reviewer reproduced the root's sequence and constrained-state false-finality probes and descriptor-trap counts, then additionally reproduced 1,001 discarded descriptor traps for a known-unused `entry_appended` input, a stranded manual `willRetry:true` completion, and small repeated aliases accepted by both capture and strict clone. Root confirmed that one shared queue array is copied into two accepted arrays and that the valid manual retry completion projects inactive compaction state while lifecycle classification remains null and overall activity remains true. No forbidden build ran.
**Uncertainty / gaps:** The actual-commit repair needs a single-use internal receipt/outcome that resolves FIFO for reentrant queued inputs; start promotion and terminal release must depend on commit, while raw identity still fans out exactly and finality waits for that event's complete public fanout plus the outer barrier. Sequence headroom alone cannot repair aggregate-state rejection. Discriminant-first fixed descriptor reads must avoid scanning discarded fields; exact-key validation remains necessary only where the protocol requires it. Repository-wide raw SSE, S4 HTTP/browser composition, fresh production inclusion, and the immutable block-array responsiveness residual remain deferred. No finding requires changing the protocol/product, widening scope, or weakening validation.
**Recommended use:** Launch one fresh sole bounded fix writer under the same immutable milestone for commit receipts, bounded selected descriptor inspection, consistent manual-retry terminal semantics, and full-traversal alias rejection. Add exact adversarial regressions, then repeat root verification and fresh independent review.

## Handoff

**Source:** Pi subagents run `b851b91c-4123-46f2-bf9a-d1faca4b21ba`, `milestone-implementer` child 0. Recorder: orchestration root. Kind: eighth bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Repair actual projected-commit-dependent lifecycle, discarded-field inspection amplification, repeated selected aliases, and current manual `willRetry:true` semantics from the seventh review while preserving all prior S2 protocol, replay, privacy, compatibility, and S3+ boundaries.
**Outcome:** The writer added opaque single-use hub commit receipts resolved after FIFO plan/precommit, causal pending/committed start claims with exclusive terminal reservation and commit-dependent release, current manual retry terminal unification, discriminant-first fixed descriptor selection, and traversal-wide identity rejection in capture and strict clone. Fifteen focused tests raise the focused suite from 138 to 153 and repair the seventh-review sequence/state false-finality, unknown/known-unused shallow descriptor scans, sibling alias, and manual-retry reproductions. The attempt remains unaccepted: root scrutiny found that selected arbitrary JSON graphs still fetch all descriptors before the node limit can reject them, and nested `capturePickedRecord()` boundaries for message-start metadata and compaction results do not select the raw object identity, allowing aliases back to the already-selected outer input.
**Evidence:** Root confirmed master/milestone immutability, intended source scope, no stage, unchanged package/server/real-Next/SSE/hook boundaries, and no build. Root independently passed TypeScript, lint, focused `153/153`, exact non-real-Next `352/352`, tracked/cached/no-index whitespace, privacy/API/source inventories, and nine-file manifest-only package dry run. Literal full tests are `354/356` and separate real-Next `2/4`; only the permitted absent `.next/BUILD_ID` production child and parent fail, while terminal and real-development subtests pass. Root rechecked receipt/causal code and the decisive repaired lifecycle tests. A selected tool-input proxy with 1,000 primitive keys and node limit 20 still received 1,000 descriptor traps before capture rejection; `cloneJsonSafe()` did the same. A `message_start` whose `message` references its outer event and a `compaction_end` whose `result` references its outer event were both accepted and reconstructed despite the every-repeated-selected-identity obligation.
**Uncertainty / gaps:** Fresh independent review run `00ac83bb-2bb7-4f83-a786-d7594c89f603` is in progress and must challenge receipt/causal behavior and classify the root findings. The likely bounded inspection correction is to check selected own-key/array length against remaining node capacity before fetching individual descriptors, while the nested metadata/result helpers must select identity unless their caller proves it was already selected. Repository-wide raw SSE, S4 composition, fresh production inclusion, and the immutable block-array responsiveness residual remain deferred. No observed fact requires protocol/product change or weaker validation.
**Recommended use:** Keep S2 uncommitted. Complete and record the fresh review, then issue one sequential bounded fix for selected-graph descriptor preflight and nested ancestor-alias detection if review confirms no additional authority gap.

## Handoff

**Source:** Pi subagents run `00ac83bb-2bb7-4f83-a786-d7594c89f603`, `reviewer` child 0. Recorder: orchestration root. Kind: fresh independent eighth-fix review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Adversarially verify opaque commit receipts, wrapper causal claims, manual retry semantics, bounded selected capture, full-traversal aliases, all prior S2 defects, validation evidence, privacy, compatibility, and later-scope containment without editing or delegating.
**Outcome:** Review rejects current acceptance on two blockers and one major bounded defect. Selected arbitrary objects and dense arrays still bulk-fetch all descriptors before the configured node budget charges their children, so hostile selected work is unbounded even though output rejects. Nested message-start metadata and compaction-result helpers fail to select the raw nested identity, allowing each to alias the already-selected outer event and then erase that alias through reconstruction. Separately, a diagnostic that calls `hub.close()` during projection/precommit sets `closeRequested`, but the hub still publishes the current input and resolves its receipt committed before closing. Commit receipts, causal pending/committed claims, false-finality repair, current manual `willRetry:true`, and all other reviewed protocol/replay/reducer/extension/lifecycle paths otherwise pass.
**Evidence:** Reviewer independently reproduced the root's 1,000 selected descriptor traps and outer-root alias bypasses, including hub cursor/state mutation for accepted aliases. Root reproduced the additional close ordering with a durable status input: timeline `diagnostic → close-return → receipt:committed`, cursor advanced to one, canonical status changed, then the hub closed. Reviewer and root independently passed TypeScript, lint, focused `153/153`, exact non-real-Next `352/352`, literal `354/356`, separate real-Next `2/4`, nine-file package dry run, whitespace, immutable-plan, source/privacy/API, and no-stage checks; only the permitted absent `.next/BUILD_ID` production child and parent fail. No build ran.
**Uncertainty / gaps:** The smallest bounded traversal fix must preflight own-key/array length against remaining nodes before individual descriptor retrieval, then fetch/charge one selected descriptor/value at a time in capture, dense arrays, and strict clone. Nested `capturePickedRecord()` calls need an explicit raw-identity selection mode distinct from already-selected top-level reconstruction. Hub close needs an explicit commit point after complete planning: close during diagnostic/precommit rejects current and queued receipts without publication, while close during publication finishes the already-committed logical input atomically. Raw SSE, S4 composition, fresh production inclusion, and the immutable block-array residual remain deferred. No material divergence or authority gap exists.
**Recommended use:** Launch one fresh sole bounded fix writer under the same immutable milestone for selected descriptor preflight, ancestor-alias closure, and precommit-close rejection, add exact regressions, then repeat full root verification and fresh independent review.

## Handoff

**Source:** Pi subagents run `0563ada8-5942-48ba-94cc-acf0dc699189`, `milestone-implementer` child 0, session `019fc0bc-68a4-703f-adc4-eebed5ad2bb4`. Recorder: orchestration root. Kind: ninth bounded milestone fix handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: fix.
**Purpose:** Bound selected object/array descriptor retrieval before node-budget rejection, reject nested aliases to selected ancestors and roots, and distinguish precommit close cancellation from atomic close after publication begins, while preserving every prior S2 protocol, receipt, lifecycle, replay, privacy, compatibility, and later-scope boundary.
**Outcome:** The writer replaced bulk descriptor retrieval on selected arbitrary objects and arrays with own-key/length capacity preflight followed by bounded per-child retrieval; added explicit `select` versus `already_selected` fixed-record identity modes for nested message metadata and compaction results; and made the hub reject the current receipt when close is requested during planning while completing all groups of an input whose publication has begun. Six directly implicated production/test files changed; reducer and wrapper production code were left untouched. Focused regressions now cover wide proxy trap counts, strict-clone/state-parser limits, root/ancestor aliases, unchanged hub surfaces after rejection, precommit diagnostic close, multi-group listener close, and final-snapshot frame-diagnostic close.
**Evidence:** The writer reported TypeScript and lint passing; focused `159/159`; exact non-real-Next `358/358`; literal `360/362`; separate real-Next `2/4`; and nine-file manifest-only package dry run, with only the permitted absent `.next/BUILD_ID` production child and parent failures and no build. The root inspected the three changed production modules and exact regressions, confirmed every `capturePickedRecord()` identity mode and the hub publication boundary, and independently passed TypeScript, lint, focused `159/159`, exact non-real-Next `358/358`, literal `360/362`, separate real-Next `2/4`, package dry run, tracked/cached/no-index whitespace, immutable master/milestone, source-boundary, privacy/API, and no-stage checks. Root content-safe probes observed zero descriptor traps for a rejected 1,000-key selected object, one for a rejected 1,000-element selected array, atomic rejection of both root aliases, `diagnostic → close → rejected` with identity-stable state/replay, and complete two-frame publication followed by queued rejection when close occurs after publication starts.
**Uncertainty / gaps:** Fresh independent review run `adcb9bb6-8d83-45e1-8deb-858c1f6f1b25` is in progress and remains mandatory before acceptance. Repository-wide raw SSE removal remains intentionally deferred to S4B, race-free browser/HTTP composition to S4A/S4B, fresh production inclusion to the release workflow, and the intrinsic immutable block-array copy to the already-classified non-blocking S7 responsiveness residual. No departure, blocker, material divergence, staged file, or forbidden build was observed in root verification.
**Recommended use:** Complete and record the fresh no-edit review. If it finds no blocker or major defect, append the final S2 implementation summary, create the coherent S2 boundary commit, reconcile the master and repository at that commit, and progress directly to the next contained milestone.

## Handoff

**Source:** Pi subagents run `adcb9bb6-8d83-45e1-8deb-858c1f6f1b25`, `reviewer` child 0. Recorder: orchestration root. Kind: final fresh independent S2 implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Adversarially re-evaluate the complete uncommitted S2 lineage, with decisive focus on ninth-fix descriptor preflight, nested identity selection, close/commit semantics, receipt-dependent wrapper lifecycle, all earlier protocol/reducer/replay/finality defects, privacy, compatibility, and S2-VC-001 through S2-VC-010.
**Outcome:** The reviewer returns `ACCEPT` with no blocker or major defect. It confirmed that selected object and array descriptor work is rejected before exceeding remaining node capacity; nested root, ancestor, sibling, cycle, and repeated selected aliases fail while distinct and deliberately ignored values retain intended behavior; precommit close rejects without mutation; and close after publication begins completes the full ordinary or final-snapshot logical input before rejecting queued work. It also passed commit receipt single-resolution/FIFO behavior, commit-dependent wrapper causal claims, true deltas/final equality, exact protocol and raw boundary, immutable canonical reduction, replay/snapshot bounds, extension semantics, zero-subscriber/HMR lifecycle, diagnostics, and scope containment.
**Evidence:** The reviewer independently passed TypeScript, lint, focused S2/RPC `159/159`, exact non-real-Next `358/358`, whitespace/no-index checks, immutable-plan and no-stage inspection, plus an independent alias/close adversarial probe. It verified the exact source/test ranges for descriptor preflight, `select`/`already_selected` identities, publication commit point, receipts, and wrapper outcome consumption. The root's broader final matrix additionally records literal `360/362`, separate real-Next `2/4`, and nine-file package dry run; only the permitted absent `.next/BUILD_ID` production child and parent fail.
**Uncertainty / gaps:** Legacy raw per-session SSE remains deliberately deferred to S4B, browser/HTTP composition to S4A/S4B, and fresh production inclusion to the release workflow. The intrinsic immutable block-array copy remains the previously classified non-blocking S7 responsiveness residual. These are exact planned boundaries rather than S2 defects or waivers; no current evidence creates material divergence.
**Recommended use:** Accept S2, commit the coherent source/test/checkpoint boundary excluding `.pi-subagents/`, append the mandatory post-commit summary naming that implementation commit, and then reconcile and progress directly to S3.

## Implementation Summary

**Plan section:** S2 `Design / Implementation Strategy` §§1–8 and S2-VC-001 through S2-VC-010; master S2 projected-session-protocol outcome and ORCH-VC-001/003/004/007/008/009/011/012.
**Work and outcome:** S2 is accepted at the pre-commit boundary. Pi Web now defines an exact V1 projected session frame/snapshot protocol, descriptor-safe bounded capture and strict parsing, exhaustive installed native/wrapper classification, actionable text/thinking/tool-argument deltas with final equality, a pure immutable canonical reducer and receiver, and one wrapper-owned monotonic hub with bounded atomic replay, transactional initial/recovery/final snapshots, opaque commit receipts, zero-subscriber projection, HMR-safe capability access, and exact close semantics. `AgentSessionWrapper` publishes native and wrapper lifecycle into the hub before stable raw compatibility fanout and promotes/releases causal activity only after the matching projected input commits. Nine sequential bounded fixes resolved every independently reproduced protocol, resource-bound, replay, finality, alias, reentrancy, lifecycle, and close defect; final fresh review recommends acceptance.
**Validation / evidence:** TypeScript and lint pass; focused S2/RPC tests pass `159/159`; exact non-real-Next tests pass `358/358`; literal full tests are `360/362`; separate real-Next is `2/4`, with terminal CLI and real development passing and only the production child/parent failing at the expressly permitted absent `.next/BUILD_ID` preflight. The nine-file `npm pack --dry-run` manifest passes but makes no fresh TypeScript-inclusion claim. Tracked, cached, and no-index S2 whitespace; immutable master/milestone; intended source boundary; privacy/raw/API inventories; no-stage inspection; and root descriptor/alias/close probes pass. Final independent review is run `adcb9bb6-8d83-45e1-8deb-858c1f6f1b25` and returns `ACCEPT` across S2-VC-001 through S2-VC-010. No `next build` ran.
**Departures from approved obligations:** None. Repository-wide ORCH-VC-003 remains explicitly partial only because the immutable S2 plan preserves the legacy per-session raw SSE path until S4B; S4A/S4B retain browser/HTTP composition and recovery integration; fresh production inclusion remains release-owned under the build prohibition. The immutable ordered block-array copy is a disclosed non-blocking S7 responsiveness residual and did not justify an unauthorized representation redesign. No S2 obligation is incomplete, blocked, waived, superseded, or divergent.
**Implementation commit:** pending at the coherent S2 boundary commit.

## Implementation Summary

**Plan section:** Final S2 disposition for `Design / Implementation Strategy` §§1–8 and S2-VC-001 through S2-VC-010; master S2 projected-session-protocol outcome and ORCH-VC-001/003/004/007/008/009/011/012.
**Work and outcome:** Accepted S2 is committed. Pi Web now owns an exact projected V1 frame/snapshot protocol, bounded descriptor-safe native capture, strict parser and immutable reducer/receiver, actionable text/thinking/tool-argument deltas with final equality, and one HMR-safe wrapper hub whose monotonic sequence, bounded atomic replay, transactional snapshots, canonical state, opaque commit receipts, and event capture are independent of browser subscriber count. Wrapper lifecycle claims now promote and release only after their causal projected inputs commit, while exact raw fanout remains available to the intentionally preserved compatibility path. Nine bounded fix/review loops resolved every reproduced S2 defect; the final independent review found no blocker or major.
**Validation / evidence:** Root evidence passes TypeScript, lint, focused S2/RPC `159/159`, exact non-real-Next `358/358`, package dry run, whitespace, immutable-plan, source/privacy/API, no-stage, and decisive descriptor/alias/close probes. Literal full tests are `360/362` and separate real-Next is `2/4`; terminal CLI and real development pass, and only the production child/parent fail at the permitted absent `.next/BUILD_ID` preflight. Final fresh review run `adcb9bb6-8d83-45e1-8deb-858c1f6f1b25` independently accepts S2-VC-001 through S2-VC-010. The implementation tree and eleven-path committed boundary are recoverable at the commit below; no build ran.
**Departures from approved obligations:** None. Legacy raw per-session SSE remains only under the S2 plan's explicit S4B deferral, browser/HTTP composition remains with S4A/S4B, and fresh production inclusion remains release-owned. The immutable ordered block-array copy remains a disclosed non-blocking S7 responsiveness residual. These are planned later boundaries, not waivers or incomplete S2 work. There are no other incomplete, blocked, waived, superseded, or divergent S2 obligations.
**Implementation commit:** `39f22f0eafa418a3bac5e664b35764ff43213f27` (`feat: add projected session protocol hub`).
