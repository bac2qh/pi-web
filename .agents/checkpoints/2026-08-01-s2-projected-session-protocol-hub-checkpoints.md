# S2 Projected Session Protocol and Wrapper-Owned Hub Checkpoints

Plan: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`

## Handoff

**Source:** Pi subagents run `be036ee9-c3bc-4590-8e09-f4802fcfea36`, `context-builder` child 0 (S2 server/protocol reconnaissance). Recorder: orchestration root. Kind: read-only milestone context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S2; Mode: investigation.
**Purpose:** Reconcile the accepted S1 boundary with installed Pi event definitions and the current wrapper/raw-SSE path, then identify a contained projected protocol, projector, reducer, replay hub, HMR seam, privacy boundary, and validation strategy without editing project files or entering S3+ scope.
**Outcome:** Confirmed that `AgentSessionWrapper.start()` receives every native event independently of browser listeners but currently forwards it unchanged; wrapper-generated events share the same broad fanout; the SSE route directly stringifies those raw objects. Installed Pi exposes actionable text, thinking, and tool-call deltas inside `message_update.assistantMessageEvent`, while `agent_end` adds `willRetry` and precedes `agent_settled`. The root adopts a wrapper-owned hub constructed before native subscription/publication, a strict projected-only API, per-wrapper stream epoch/sequence, pure reduction, dual-bounded replay, and canonical initial/recovery/final snapshots. Known unused events must be explicit no-ops, not a default raw pass-through. The hub retains live projection state only; HTTP transcript/runtime APIs remain authoritative for completed history, entry IDs, and currently HTTP-only configuration.
**Evidence:** The root directly verified `lib/rpc-manager.ts` native subscription, wrapper emit/onEvent flow, prompt/compaction accounting, extension maps, startup locks, and destruction; `app/api/agent/[id]/events/route.ts` raw JSON serialization; installed `AgentSessionEvent`, core `AgentEvent`, and `AssistantMessageEvent` declarations; S1 strict protocol/HMR patterns; and read-only Pi source/tests showing normal/tool event ordering and real delta emission. The child supplied an exhaustive consumed/omitted event map, delta-versus-snapshot classification, HMR constraints, replay/finality fixtures, and long-stream byte methodology. No project file changed.
**Uncertainty / gaps:** S2 cannot prove ticket security, WebSocket backpressure, or browser ownership; those remain S3/S4. Existing wrappers surviving a code reload cannot retroactively recover pre-capability events. Individual final messages or extension UI can exceed replay capacity and must force snapshot/HTTP recovery rather than unbounded retention. The legacy per-session SSE serializer remains raw until S4B; the new projected API must nevertheless make raw access impossible. No evidence shows material divergence because installed Pi supplies every required actionable delta.
**Recommended use:** Define one exact V1 frame/snapshot union and parser; normalize only display-required message fields; project all known inputs exhaustively; use wrapper-owned settlement after claim release for finality; attach a versioned hub capability before publication; preserve legacy SSE and all S3+ surfaces; require raw-field exclusion, final equality, zero-subscriber, replay-overflow, HMR, lifecycle, and UTF-8 byte-growth evidence before acceptance.

## Handoff

**Source:** Pi subagents run `be036ee9-c3bc-4590-8e09-f4802fcfea36`, `context-builder` child 1 (S2 consumer/state-semantics reconnaissance). Recorder: orchestration root. Kind: read-only milestone context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S2; Mode: investigation.
**Purpose:** Trace the current browser event state machine and extension/queue/retry/compaction semantics that S2 must encode canonically for later S4 consumption, while identifying bounded recovery and pure reducer tests without implementing the browser migration.
**Outcome:** Confirmed that the current hook replaces the streaming bubble with every growing `message_update.message`, reconciles optimistic user prompts at `message_end`, tracks concurrent active tools, replaces queues, handles retry/compaction, and reloads the HTTP transcript at completion to repair persisted messages and entry IDs. It currently treats every `agent_end` as final even though installed Pi distinguishes retrying attempts and emits `agent_settled` later. Durable extension recovery requires keyed statuses/widgets, pending dialogs/custom UI, their removals, and optionally latest title; notices and editor insertion are one-shot effects. Current dialog response/timeout/abort cleanup removes wrapper maps without an event, so S2 needs a projection-only close signal that does not perturb legacy SSE. The root resolves the snapshot-scope ambiguity by keeping bounded canonical event-delivery state plus explicit HTTP transcript/runtime refresh requirements, not a duplicated transcript or event-triggered claim of full runtime parity.
**Evidence:** The root directly verified `hooks/useAgentSession.ts` mount, event switch, optimistic reconciliation, polling/reconciliation, final transcript reload, queue/retry/compaction handling, and extension dispatch; `components/MessageView.tsx` use of normalized message fields/details; `lib/types.ts` message/extension shapes; `lib/normalize.ts` tool-call normalization; `lib/rpc-manager.ts` `get_state`, extension maps/UI lifecycle, prompt/compaction claims, and command paths; and installed/read-only Pi event ordering. The child supplied duplicate/out-of-order/epoch reducer rules, durable-versus-transient classifications, pure test seams, and residual risks. No project file changed.
**Uncertainty / gaps:** Model/tool/context/system state can change through HTTP or silent extension paths without a native event, so S2 cannot honestly replace HTTP runtime reconciliation. Recovery of completed message entry IDs also requires HTTP session reload. Tool/custom `details` are rendered today and need an exact JSON-safe normalization rather than blanket removal or raw copying. The subagent runtime marked this read-only child failed with `completed without making edits for an implementation task`; that is a task-classification error, not an execution or evidence failure. The root recovered and verified its complete output, and no source/planning file was modified by the child.
**Recommended use:** Preserve HTTP transcript/runtime authority and polling; encode one normalized final message plus true assistant deltas; make wrapper settlement—not retrying `agent_end`—the final boundary; distinguish durable snapshot state from replay-only effects; emit projection-only close/settle inputs; and require extension cleanup, entry-ID recovery signaling, final equality, overlap, and overflow tests before S2 acceptance.

## Handoff

**Source:** Pi subagents run `257af4ae-94dd-43d5-a552-00c09637b312`, `reviewer` child 0. Recorder: orchestration root. Kind: independent read-only draft-plan review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Adversarially check the draft S2 plan/checkpoint for master containment, executable protocol/recovery semantics, exhaustive installed-event coverage, honest validation/package evidence, privacy, and later-milestone leakage before approval.
**Outcome:** The reviewer correctly blocked the first draft on six major issues and one minor: it did not clearly preserve ORCH-VC-003 as partial while raw legacy SSE remains; it omitted installed bash-execution/branch-summary/compaction-summary message roles; it used contradictory pre-final versus post-final equality language; it overclaimed race-free HTTP transcript/runtime recovery from marker-only snapshots; oversized durable queue/extension state could make both a delta and its fallback snapshot unrepresentable; the real-Next/package evidence was not reproducible or honest about what `npm pack` contains; and standalone compaction settlement lacked an explicit success/error/abort boundary. The root accepted every finding and revised the still-draft plan without changing the S2 product direction. The corrected plan now requires partial master-row disposition, exact nested-role omission tests, two-stage equality semantics, explicit S4 HTTP-race deferral, V1 atomic snapshot transactions fragmented into individually bounded transfer units, exact test partitions and unchanged-lineage production-preflight handling, manifest-only package claims, and standalone compaction settlement fixtures.
**Evidence:** The root directly verified the raw SSE `JSON.stringify` path; the master’s S4B removal timing; installed `messages.d.ts` roles and sensitive command/output/summary/source/path fields; current HTTP response shapes without hub epoch/cursor; unbounded queue/custom/widget input surfaces; `lib/pi-web-real-next.test.mjs` production-manifest preflight and subtest structure; `package.json` packaged files; and the separate compaction claim/release path in `lib/rpc-manager.ts`. The corrected draft retains no source edit, no staged file, and no private runtime path. Because both planning files were still untracked, the root used `git diff --no-index --check` against `/dev/null` for each file and observed no whitespace diagnostics; cached-diff checking remains required after staging. Review output is recoverable by run identity.
**Uncertainty / gaps:** Snapshot transfer fragmentation is a substantive protocol clarification introduced while the plan remains mutable; it needs fresh independent rereview for sequence/group/reassembly/bounds consistency. S4A/S4B still must design and prove race-free HTTP composition. The production-artifact subtest remains release-owned when manifests are absent, and package dry run cannot prove fresh TypeScript bundling. These are explicit evidence boundaries, not hidden passes or waivers.
**Recommended use:** Obtain one fresh no-edit/no-delegation review of the corrected draft, concentrating on bounded snapshot transactions, exact master-row partial disposition, installed role coverage, finality/equality, and validation commands. Approve and commit the planning boundary only if that review finds no blocker or major ambiguity.

## Handoff

**Source:** Pi subagents run `1d7639f1-5da4-4b08-9cc1-d0e8be74ef17`, `reviewer` child 0. Recorder: orchestration root. Kind: fresh independent corrected-draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Re-evaluate every first-review correction and challenge the new snapshot-transfer design, installed event coverage, finality/equality, HTTP deferral, validation reproducibility, privacy, and S3+ containment before plan approval.
**Outcome:** The reviewer found no master-level blocker and passed the corrected partial ORCH-VC-003 disposition, installed outer message-role coverage, bounded HTTP-recovery claim, snapshot transaction direction, standalone compaction, validation/package boundaries, privacy, and scope containment. It identified two remaining major ambiguities and two minors. The root accepted them: nested assistant `done`/`error` variants and their complete terminal objects needed explicit handling; pre-final equality had to compare only delta-addressable content rather than usage/stop/error metadata; prompt claims that fail before creation of a native prompt promise still needed authoritative settlement; oversized non-retained final snapshots needed explicit replay-floor advancement; and ordinary `git diff --check` did not inspect untracked planning files. The still-mutable draft now maps every nested assistant discriminant, forbids terminal complete-object fallback, defines subset versus full post-final equality, settles every dispatched/pre-dispatch last-claim path while destruction only closes, specifies before/after oversized-snapshot cursors, and records the no-index whitespace method.
**Evidence:** Root verification covered nested `AssistantMessageEvent` `done`/`error` declarations; normalized final metadata versus delta-available fields; ordinary extension-binding rejection, synchronous `inner.prompt` failure, hosted binding failure, hosted pre-dispatch cancellation, dispatched settlement, and destruction paths in `lib/rpc-manager.ts`; replay-floor implications of an unretained logical frame; and explicit no-index whitespace checks for both untracked planning files with zero diagnostic output. The reviewer changed no file and its output is recoverable by run identity.
**Uncertainty / gaps:** The new exact language has not yet received a final pass. Implementation must keep one authoritative settlement per claim across several wrapper paths without perturbing legacy SSE or destruction. Snapshot assemblers must treat all transfer units as one logical sequence and never advance the cursor before a validated end marker.
**Recommended use:** Obtain one final focused no-edit/no-delegation plan review limited to these corrections and overall executability. If it returns no blocker or major, record its result, change only `Status` to `approved`, stage only the plan/checkpoint, run cached whitespace/privacy checks, and commit the planning boundary before launching the sole writer.

## Handoff

**Source:** Pi subagents run `1edd3926-26d0-4281-8169-ce683cf53e7a`, `reviewer` child 0. Recorder: orchestration root. Kind: final focused read-only plan review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Verify the nested assistant-event, pre-dispatch settlement, oversized-final-snapshot replay floor, and whitespace corrections while ensuring the full S2 plan remained contained and executable.
**Outcome:** The reviewer passed exhaustive assistant event handling, all wrapper settlement paths, partial ORCH-VC-003 disposition, bounded snapshot transactions, HTTP deferral, validation/package boundaries, privacy, S3+ containment, and explicit untracked whitespace evidence. It found no scope blocker and no residual minor, but identified two narrow major wording/semantic inconsistencies before freezing: the Objective still described full-message pre-final equality rather than the delta-comparable subset, and replay-floor advancement was phrased for every oversized snapshot rather than only event-driven final snapshots. The root corrected both while the plan remained draft. The Objective now reserves full equality for post-`message_completed`; event-driven oversized final snapshots advance the floor, while subscriber-generated initial/recovery transactions never change global sequence, replay floor, or another subscriber's eligibility.
**Evidence:** The reviewer inspected the exact draft, checkpoint, master, current wrapper paths, and installed event union, and independently observed zero no-index whitespace diagnostics. Root inspection confirmed the corrected Objective matches the detailed equality rule and the corrected hub/VC language distinguishes final from initial/recovery snapshot transactions. The review changed no file and is recoverable by run identity.
**Uncertainty / gaps:** Only final verification of these two exact corrections remains. Implementation must preserve the stated distinction in both hub code and tests.
**Recommended use:** Run one bounded independent verification of the two corrections and overall recommendation. If clean, approve and commit the planning boundary without further optional polish.

## Handoff

**Source:** Pi subagents run `2ae8c995-589c-4af8-9e0f-c5902810e04a`, `reviewer` child 0. Recorder: orchestration root. Kind: bounded final read-only plan verification. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-01-s2-projected-session-protocol-hub.md`; Mode: review.
**Purpose:** Verify only the corrected delta-subset/full-message equality distinction, final-versus-initial/recovery replay-floor semantics, their test/VC consistency, and readiness to freeze.
**Outcome:** The reviewer confirmed all requested corrections and found no blocker. The Objective now limits pre-final comparison to delta-addressable content and reserves full normalized-message equality for post-`message_completed`; only non-retained event-driven final snapshots advance replay floor; subscriber-generated initial/recovery transactions leave sequence, floor, and other subscribers unchanged; and tests/VC cover both distinctions. Recommendation: `APPROVE`.
**Evidence:** Direct review of the current plan/checkpoint and bounded source/type context; recoverable run `2ae8c995-589c-4af8-9e0f-c5902810e04a`. No file, stage, or commit was changed by the reviewer.
**Uncertainty / gaps:** None at plan-approval severity. Implementation and later S3/S4 evidence remain required exactly as scoped.
**Recommended use:** Change only the plan status to `approved`, stage only this plan/checkpoint, run cached whitespace/privacy/source-boundary checks, commit the immutable planning boundary, and launch one fresh `milestone-implementer` as sole source writer.
