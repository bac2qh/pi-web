# S4A Browser Session Registry and Recovery Reducer Checkpoints

Plan: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`

## Handoff

**Source:** Pi subagents parallel batch `b4ac57b6`, `scout` child 0, recoverable output `.pi-subagents/artifacts/outputs/b4ac57b6/s4a-browser-context.md`. Recorder: orchestration root. Kind: read-only S4A browser ownership context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S4A; Mode: investigation.
**Purpose:** Reconcile the accepted S3 server transport with the current page/provider, AppShell selection, keyed ChatWindow, hook/SSE, sidebar, draft, and S1 browser-client architecture into one independently testable S4A registry/provider boundary without editing or delegating.
**Outcome:** The scout confirmed that one inert page provider can live above the keyed chat lifetime while current product behavior remains untouched. It derived a page-local registry keyed by exact session ID, independent client entries, generation-safe visible/retained-hidden ownership handles, and start-on-first/stop-on-last semantics. It identified the controlling boundary: S4A may represent ownership primitives and mount the provider, but cannot acquire from AppShell/hook, choose hidden-retention qualification/duration, promote ID-less new sessions, or let WebSocket and SSE both write chat state. Draft, URL, selection, sidebar, prompt, polling, reconciliation, branch, and rendering lifetimes remain unchanged.
**Evidence:** Root verified the cited sources: `app/page.tsx` provider root; `AppShell.tsx` selection/session-key lifetime; `ChatWindow.tsx` hook/draft ownership; `useAgentSession.ts` EventSource and reconciliation; `GlobalStatusProvider`/client lifecycle; accepted S2/S3 protocol; and current static provider tests. S3 implementation/finality are `2f3eb50`/`43b9e0c`. The provider mount can be tested without opening any session socket because no production owner exists in S4A.
**Uncertainty / gaps:** The current UI has one keyed chat, not internal chat-tab persistence. `retained_hidden` therefore remains a registry label/primitive only; S4B must decide and wire actual qualifying hidden lifetimes under the master. New-session connect-before-prompt also remains S4B because a stable session ID does not exist before HTTP creation. No observed fact requires a product choice or material divergence for S4A.
**Recommended use:** Review the draft for an inert provider, exact generation-safe registry handles, atomic ownership setup, and strong static exclusions for AppShell/ChatWindow/hook/SSE integration.

## Handoff

**Source:** Pi subagents parallel batch `b4ac57b6`, `scout` child 1, recoverable output `.pi-subagents/artifacts/outputs/b4ac57b6/s4a-protocol-context.md`. Recorder: orchestration root. Kind: read-only S4A protocol/recovery context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S4A; Mode: investigation.
**Purpose:** Derive the strict browser ticket/socket, ready-target, receiver, snapshot, effect, reconnect, stale-resource, registry, and privacy contract from accepted S2/S3 without changing server protocol or entering S4B.
**Outcome:** The scout confirmed that S4A should wrap—not duplicate—the immutable `SessionReceiver`; retain the committed cursor through disconnect; treat ready as target-only; hold old state during snapshot assembly; and deliver one-shot effects separately after applied sequence commit. It supplied the exact same-origin ticket/page URL/resume flow, S1 epoch/resource-identity pattern, 250 ms–10 s backoff, terminal unsupported-version rule, finite error classes, fake-socket tests, and privacy bounds. Root corrected one advisory overrestriction while drafting: exact retained replay may include an accepted final-snapshot transaction, so S4A follows receiver outcomes rather than forbidding every snapshot under `exact`.
**Evidence:** Root verified `session-transport-protocol.ts` strict ready/resume vocabulary; `session-protocol.ts` frame/effect parser; `session-reducer.ts` duplicate/gap/wrong-epoch and atomic assembly semantics; `session-event-hub.ts` replay/final snapshot behavior; `session-channel.ts` ready/catch-up/live ordering; the exact ticket route; and S1 client fake-resource tests. Accepted S2 commits are `39f22f0`/`26ec788`; accepted S3 commits are `2f3eb50`/`43b9e0c`.
**Uncertainty / gaps:** S3 supplies no negotiation or user-facing retry policy. The draft resolves unsupported versions as terminal until explicit stop/start or page recreation and treats other fail-closed faults as bounded reconnect from committed state; this is a contained transport rule, not new UI. A duplicate snapshot start at or behind the committed cursor cannot satisfy a promised recovery target. HTTP generation/cursor composition and refresh-marker acknowledgement do not exist in V1 and remain explicitly S4B work.
**Recommended use:** Independently challenge target gating, exact replay containing final snapshots, assembly discard, effect ordering, stale callback suppression, unsupported-version terminal behavior, no commands/acks, and complete S4B source exclusions before approving the plan.

## Handoff

**Source:** Pi subagents run `0fcce0fd-3744-474c-84cb-c9daea0296a9`, `reviewer` child 0, session artifact `b01c210f/run-0`. Recorder: orchestration root. Kind: independent initial S4A draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: review.
**Purpose:** Adversarially test the draft for contained authority, exact attach/ready/recovery semantics, snapshot reason/phase safety, atomic effect ownership, stable public APIs, deterministic ticket/client behavior, executable tests, privacy, and complete S4B/later-scope exclusions without editing or delegating.
**Outcome:** The reviewer rejects the initial draft. Its decisive probe shows `ProjectedSessionEventHub.attach()` currently overwrites a selected cursor when reentrant buffered units advance the hub: an initial snapshot ended at zero while ready targeted one, and an `empty` outcome targeted one while returning unit one. That conflicts with the frozen S3 empty/target semantics and the draft's excluded server boundary. The reviewer also requires bounded draft corrections for snapshot reason/phase/sequence authorization, atomic first-owner effect attachment before synchronous client start, identity-stable `useSyncExternalStore` snapshots and literal controller APIs, an exact focused command, deterministic expiry parsing, and literal provider nesting.
**Evidence:** Root inspected `session-event-hub.ts` selection/attach and `session-channel.ts` ready construction. The unambiguous bounded correction is to preserve the selected `ReplayResult.cursor/outcome/epoch` while appending buffered frames as post-target live units; no frame, sequence, replay, or wire shape changes. The corrected draft adds exact reentrant empty/initial/recovery/exact hub/channel tests; strict initial/recovery/final snapshot matrices; `acquire(..., { ownership, onEffect })` atomic setup; stable frozen snapshot/controller contracts; S1-compatible nonnegative safe expiry without LAN clock comparison; literal provider nesting; and an exact focused command.
**Uncertainty / gaps:** The reviewer classified the initial source-boundary mismatch as material relative to that draft but not a master-architecture conflict. Because the accepted S3 target-only contract dictates one implementation—preserve selection and treat reentrant buffered units as later live output—the root can resolve it in the still-mutable draft without a product choice, master scope change, weaker validation, or human ambiguity. Any broader S2/S3 change remains prohibited. Fresh corrected-draft review is mandatory before approval.
**Recommended use:** Review the corrected §0 seam and complete revised plan. Approve only if the narrow hub correction, snapshot matrix, atomic effect API, stable snapshot semantics, ticket rule, provider nesting, exact commands, and S4B exclusions are deterministic and fully contained.

## Handoff

**Source:** Pi subagents run `e168149a-c227-483b-8154-226072506c15`, `reviewer` child 0, session artifact `3caa4c04/run-0`. Recorder: orchestration root. Kind: final independent corrected S4A draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: review.
**Purpose:** Verify every initial finding and the full corrected draft for a bounded §0 attach fix, exact target/snapshot matrices, stable client APIs, atomic registry effects, deterministic bootstrap/reconnect, inert provider topology, executable tests, privacy, and complete S4B/later-scope exclusions without editing or delegating.
**Outcome:** The reviewer returns `APPROVE AS WRITTEN` with no blocker, major, or minor defect and no unresolved material divergence. It confirms §0 preserves selected cursor/outcome/epoch while appending reentrant units post-target and authorizes no wider S2/S3 change; exact initial/recovery/final snapshot reason/epoch/sequence gates close receiver permissiveness; public snapshots are stable/frozen with literal subscribe semantics; first-owner `onEffect` is installed before synchronous start; expiry parsing is LAN-safe and deterministic; provider nesting and focused filenames are literal; and S4B/S5/S6 boundaries remain strict.
**Evidence:** The reviewer traced the corrected plan to current `session-event-hub.ts` attach overwrite, `session-channel.ts` ready construction, lower-level receiver snapshot acceptance, S1 ticket parsing, page provider/keyed-chat topology, and unchanged hook EventSource/reconciliation sources. The root verified the final draft remains `Status: draft`, complete Objective/strategy/scope/tests/telemetry/VC/handoff sections exist, no pointer companion exists, and only the two new planning files plus unrelated `.pi-subagents/` are untracked.
**Uncertainty / gaps:** §0 is an implementation obligation, not already repaired. S4B still owns production acquisition, hook adaptation, retained-hidden policy, cursor-bound HTTP convergence, and SSE removal; S5/S6/S7 remain unchanged. No product choice, validation waiver, master scope change, or human ambiguity remains.
**Recommended use:** Change only the plan status to `approved`, commit this exact plan/checkpoint boundary excluding `.pi-subagents/`, record the immutable blob, and launch one fresh sole S4A milestone implementer with every VC and stop condition.
