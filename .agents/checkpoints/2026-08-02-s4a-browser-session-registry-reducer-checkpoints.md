# S4A Browser Session Registry and Recovery Reducer Checkpoints

Plan: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`

## Handoff

**Source:** Pi subagents parallel batch `b4ac57b6`, `scout` child 0, recoverable output `.pi-subagents/artifacts/outputs/b4ac57b6/s4a-browser-context.md`. Recorder: orchestration root. Kind: read-only S4A browser ownership context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S4A; Mode: investigation.
**Purpose:** Reconcile the accepted S3 server transport with the current page/provider, AppShell selection, keyed ChatWindow, hook/SSE, sidebar, draft, and S1 browser-client architecture into one independently testable S4A registry/provider boundary without editing or delegating.
**Outcome:** The scout confirmed that one inert page provider can live above the keyed chat lifetime while current product behavior remains untouched. It derived a page-local registry keyed by exact session ID, independent client entries, generation-safe visible/retained-hidden ownership handles, and start-on-first/stop-on-last semantics. It identified the controlling boundary: S4A may represent ownership primitives and mount the provider, but cannot acquire from AppShell/hook, choose hidden-retention qualification/duration, promote ID-less new sessions, or let WebSocket and SSE both write chat state. Draft, URL, selection, sidebar, prompt, polling, reconciliation, branch, and rendering lifetimes remain unchanged.
**Evidence:** Root verified the cited sources: `app/page.tsx` provider root; `AppShell.tsx` selection/session-key lifetime; `ChatWindow.tsx` hook/draft ownership; `useAgentSession.ts` EventSource and reconciliation; `GlobalStatusProvider`/client lifecycle; accepted S2/S3 protocol; and current static provider tests. S3 implementation/finality are `2f3eb50`/`43b9e0c`. The provider mount can be tested without opening any session socket because no production owner exists in S4A.
**Uncertainty / gaps:** The current UI has one keyed chat, not internal chat-tab persistence. `retained_hidden` therefore remains a registry label/primitive only; S4B must decide and wire actual qualifying hidden lifetimes under the master. New-session connect-before-prompt also remains S4B because a stable session ID does not exist before HTTP creation. No observed fact requires a product choice or material divergence for S4A.
**Recommended use:** Review the draft for an inert provider, exact generation-safe registry handles, atomic ownership setup, and strong static exclusions for AppShell/ChatWindow/hook/SSE integration.

## Handoff

**Source:** Pi subagents parallel batch `b4ac57b6`, `scout` child 1, recoverable output `.pi-subagents/artifacts/outputs/b4ac57b6/s4a-protocol-context.md`. Recorder: orchestration root. Kind: read-only S4A protocol/recovery context. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; candidate milestone: S4A; Mode: investigation.
**Purpose:** Derive the strict browser ticket/socket, ready-target, receiver, snapshot, effect, reconnect, stale-resource, registry, and privacy contract from accepted S2/S3 without changing server protocol or entering S4B.
**Outcome:** The scout confirmed that S4A should wrap—not duplicate—the immutable `SessionReceiver`; retain the committed cursor through disconnect; treat ready as target-only; hold old state during snapshot assembly; and deliver one-shot effects separately after applied sequence commit. It supplied the exact same-origin ticket/page URL/resume flow, S1 epoch/resource-identity pattern, 250 ms–10 s backoff, terminal unsupported-version rule, finite error classes, fake-socket tests, and privacy bounds. Root corrected one advisory overrestriction while drafting: exact retained replay may include an accepted final-snapshot transaction, so S4A follows receiver outcomes rather than forbidding every snapshot under `exact`.
**Evidence:** Root verified `session-transport-protocol.ts` strict ready/resume vocabulary; `session-protocol.ts` frame/effect parser; `session-reducer.ts` duplicate/gap/wrong-epoch and atomic assembly semantics; `session-event-hub.ts` replay/final snapshot behavior; `session-channel.ts` ready/catch-up/live ordering; the exact ticket route; and S1 client fake-resource tests. Accepted S2 commits are `39f22f0`/`26ec788`; accepted S3 commits are `2f3eb50`/`43b9e0c`.
**Uncertainty / gaps:** S3 supplies no negotiation or user-facing retry policy. The draft resolves unsupported versions as terminal until explicit stop/start or page recreation and treats other fail-closed faults as bounded reconnect from committed state; this is a contained transport rule, not new UI. A duplicate snapshot start at or behind the committed cursor cannot satisfy a promised recovery target. HTTP generation/cursor composition and refresh-marker acknowledgement do not exist in V1 and remain explicitly S4B work.
**Recommended use:** Independently challenge target gating, exact replay containing final snapshots, assembly discard, effect ordering, stale callback suppression, unsupported-version terminal behavior, no commands/acks, and complete S4B source exclusions before approving the plan.

## Handoff

**Source:** Pi subagents run `0fcce0fd-3744-474c-84cb-c9daea0296a9`, `reviewer` child 0, session artifact `b01c210f/run-0`. Recorder: orchestration root. Kind: independent initial S4A draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: review.
**Purpose:** Adversarially test the draft for contained authority, exact attach/ready/recovery semantics, snapshot reason/phase safety, atomic effect ownership, stable public APIs, deterministic ticket/client behavior, executable tests, privacy, and complete S4B/later-scope exclusions without editing or delegating.
**Outcome:** The reviewer rejects the initial draft. Its decisive probe shows `ProjectedSessionEventHub.attach()` currently overwrites a selected cursor when reentrant buffered units advance the hub: an initial snapshot ended at zero while ready targeted one, and an `empty` outcome targeted one while returning unit one. That conflicts with the frozen S3 empty/target semantics and the draft's excluded server boundary. The reviewer also requires bounded draft corrections for snapshot reason/phase/sequence authorization, atomic first-owner effect attachment before synchronous client start, identity-stable `useSyncExternalStore` snapshots and literal controller APIs, an exact focused command, deterministic expiry parsing, and literal provider nesting.
**Evidence:** Root inspected `session-event-hub.ts` selection/attach and `session-channel.ts` ready construction. The unambiguous bounded correction is to preserve the selected `ReplayResult.cursor/outcome/epoch` while appending buffered frames as post-target live units; no frame, sequence, replay, or wire shape changes. The corrected draft adds exact reentrant empty/initial/recovery/exact hub/channel tests; strict initial/recovery/final snapshot matrices; `acquire(..., { ownership, onEffect })` atomic setup; stable frozen snapshot/controller contracts; S1-compatible nonnegative safe expiry without LAN clock comparison; literal provider nesting; and an exact focused command.
**Uncertainty / gaps:** The reviewer classified the initial source-boundary mismatch as material relative to that draft but not a master-architecture conflict. Because the accepted S3 target-only contract dictates one implementation—preserve selection and treat reentrant buffered units as later live output—the root can resolve it in the still-mutable draft without a product choice, master scope change, weaker validation, or human ambiguity. Any broader S2/S3 change remains prohibited. Fresh corrected-draft review is mandatory before approval.
**Recommended use:** Review the corrected §0 seam and complete revised plan. Approve only if the narrow hub correction, snapshot matrix, atomic effect API, stable snapshot semantics, ticket rule, provider nesting, exact commands, and S4B exclusions are deterministic and fully contained.

## Handoff

**Source:** Pi subagents run `e168149a-c227-483b-8154-226072506c15`, `reviewer` child 0, session artifact `3caa4c04/run-0`. Recorder: orchestration root. Kind: final independent corrected S4A draft review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; reviewed milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: review.
**Purpose:** Verify every initial finding and the full corrected draft for a bounded §0 attach fix, exact target/snapshot matrices, stable client APIs, atomic registry effects, deterministic bootstrap/reconnect, inert provider topology, executable tests, privacy, and complete S4B/later-scope exclusions without editing or delegating.
**Outcome:** The reviewer returns `APPROVE AS WRITTEN` with no blocker, major, or minor defect and no unresolved material divergence. It confirms §0 preserves selected cursor/outcome/epoch while appending reentrant units post-target and authorizes no wider S2/S3 change; exact initial/recovery/final snapshot reason/epoch/sequence gates close receiver permissiveness; public snapshots are stable/frozen with literal subscribe semantics; first-owner `onEffect` is installed before synchronous start; expiry parsing is LAN-safe and deterministic; provider nesting and focused filenames are literal; and S4B/S5/S6 boundaries remain strict.
**Evidence:** The reviewer traced the corrected plan to current `session-event-hub.ts` attach overwrite, `session-channel.ts` ready construction, lower-level receiver snapshot acceptance, S1 ticket parsing, page provider/keyed-chat topology, and unchanged hook EventSource/reconciliation sources. The root verified the final draft remains `Status: draft`, complete Objective/strategy/scope/tests/telemetry/VC/handoff sections exist, no pointer companion exists, and only the two new planning files plus unrelated `.pi-subagents/` are untracked.
**Uncertainty / gaps:** §0 is an implementation obligation, not already repaired. S4B still owns production acquisition, hook adaptation, retained-hidden policy, cursor-bound HTTP convergence, and SSE removal; S5/S6/S7 remain unchanged. No product choice, validation waiver, master scope change, or human ambiguity remains.
**Recommended use:** Change only the plan status to `approved`, commit this exact plan/checkpoint boundary excluding `.pi-subagents/`, record the immutable blob, and launch one fresh sole S4A milestone implementer with every VC and stop condition.

## Handoff

**Source:** Pi subagents run `44112021-dde5-4016-97b2-0aa1504661ba`, `milestone-implementer` child 0, session `019fc17a-0fee-74f6-a174-1ca7dbb32a6b`. Recorder: orchestration root. Kind: initial S4A implementation handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: implementation.
**Purpose:** Implement §0's selected-target attach correction plus the complete pure browser recovery/client/registry/inert-provider seam while preserving current hook/SSE/HTTP product authority and all later-milestone boundaries.
**Outcome:** The writer preserved selected attach cursor/outcome/epoch while appending reentrant units post-target; added a target-gated `SessionReceiver` wrapper with strict initial/recovery/final snapshot matrices; implemented an injectable same-origin session client with committed-cursor resume, stable frozen snapshots, ordered effects, bounded reconnect and terminal unsupported-version handling; implemented generation-safe visible/retained-hidden registry handles with atomic first-owner effect setup; and mounted one inert page provider. Root inspection confirms the expected seven production surfaces, exact four new test files, and no protected hook/chat/sidebar/SSE/package change. The attempt is not yet accepted pending complete root scrutiny and fresh independent review.
**Evidence:** Writer validation reports TypeScript and lint passing; exact focused `102/102`; exact non-real-Next `420/420`; literal full `422/424`; separate real-Next terminal/development passing with only the permitted absent `.next/BUILD_ID` production child/parent; package dry run, whitespace, immutable-plan, privacy/source-boundary, and no-stage gates passing. Root independently passed TypeScript, lint, and focused `102/102`; inspected the pure stream/client/registry/provider source and §0/channel/protocol diffs; confirmed milestone blob `b0585ab...`; and observed only intended source/tests plus unrelated `.pi-subagents/` in status.
**Uncertainty / gaps:** Fresh review must challenge lower-level receiver/candidate fault atomicity, exact ready outcome relations, malformed bootstrap/resource races, registry setup rollback and diagnostics, stable snapshot/effect reentrancy, provider inertness, and all claimed test layers. S4B production acquisition, hook adaptation, hidden policy, HTTP cursor composition, and SSE removal remain deliberately absent. No material divergence or implementation departure is currently established.
**Recommended use:** Obtain one fresh no-edit/no-delegation review of the complete diff and rerun decisive root probes/full gates. Send any bounded defect through the immutable S4A fix loop; commit only after all VCs are independently supported.

## Handoff

**Source:** Pi subagents run `46310de1-5509-46a2-9741-19b378415a56`, `reviewer` child 0; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/46310de1-5509-46a2-9741-19b378415a56/output-0.log`. Recorder: orchestration root. Kind: fresh independent initial S4A implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: review.
**Purpose:** Challenge all S4A VCs and the complete tracked/untracked implementation, especially attach targets, receiver target matrices, reentrant snapshot/effect order, synchronous resource faults, transactional registry cleanup, StrictMode provider lifecycle, privacy, protected boundaries, and the exact validation matrix without editing or delegating.
**Outcome:** Verdict `FINDINGS — DO NOT ACCEPT`. Five P0 implementation defects are bounded by the immutable milestone: nested client/registry fanout can omit/duplicate snapshot revisions and reverse effect sequences; relay-subscription exceptions poison an unstarted registry entry and cleanup throws can interrupt teardown; synchronous injected bootstrap/timer/handler failures can escape or strand resources; StrictMode effect replay disposes and republishes the same terminal registry; and recovery ready outcomes accept impossible held-cursor/epoch relationships. The review also finds the corresponding adversarial test layers incomplete. The §0 attach correction, ordinary ticket/URL/resume/parser security, happy-path topology, committed state immutability, ordinary stale-resource/backoff handling, protected product boundary, and privacy classes were verified correct.
**Evidence:** Reviewer reproductions included snapshot deliveries `[[4,"connected",0],[6,"idle",1],[6,"idle",1]]`, nested effects observed as `[2,1]`, poisoned first relay acquisition reused with `start 0/stop 0`, synchronous fetch escaping with public state stuck `connecting`, React StrictMode setup-cleanup-setup retaining the disposed controller, and fresh receivers accepting `wrong_epoch`, `overflow_snapshot`, and `invalid_cursor`. Root inspected each cited code path and S3 `selectReplay()`: null resume yields only `initial_snapshot`; `wrong_epoch` requires a non-null differing held epoch; `overflow_snapshot` requires the same held epoch and ready cursor above held cursor; `invalid_cursor` requires the same held epoch and ready cursor below held cursor. Root full gates independently produced focused `102/102`, non-real-Next `420/420`, literal `422/424`, isolated real-Next `2/4`, with only the authorized absent `.next/BUILD_ID` child/parent failures; TypeScript, lint, package dry run, whitespace, plan hash, no-stage, protected-source, inventory, and privacy scans passed.
**Uncertainty / gaps:** Fix design must serialize complete snapshot/effect notification batches under nested publication, make registry acquisition/teardown rollback-safe despite every injected throw, contain all synchronous dependency boundaries without parallel resources or unbounded retry, and make actual React StrictMode replay safe while retaining one inert `useRef`-owned page registry and final unmount disposal. An actual mounted StrictMode test can use React DOM against a minimal test container without adding dependencies. Aggregate ownership diagnostics should also be corrected and tested as the plan requires. No wire change, server redesign, UI acquisition, hidden policy, SSE change, dependency, or other material divergence is needed.
**Recommended use:** Launch one focused sole S4A fix writer under the same immutable milestone. Require exact adversarial regressions for every reproduction and missing matrix, then repeat root validation and a new fresh independent review before acceptance.

## Handoff

**Source:** Pi subagents run `30d79e52-5b3b-4bc0-bc0f-1662fe964899`, `milestone-implementer` child 0, session `019fc19c-25bd-7aef-9e4a-82dac71a7568`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/30d79e52-5b3b-4bc0-bc0f-1662fe964899/output-0.log`. Recorder: orchestration root. Kind: bounded S4A fix implementation handoff. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: fix.
**Purpose:** Resolve the five independent-review P0s and their in-scope adversarial coverage gaps without changing wire authority, current product acquisition/SSE/HTTP behavior, dependencies, or later milestones.
**Outcome:** The fix queues complete client and registry snapshot/effect notification batches under nested publication; makes first-entry setup and all teardown rollback-safe across injected throws; contains synchronous bootstrap/socket/timer boundaries with a resource-free explicitly restartable scheduling-failure state; defers provider disposal across StrictMode effect replay and performs final unmount disposal once; enforces exact fresh/wrong-epoch/overflow/invalid-cursor relations; discards candidates on every fault; validates complete duplicate final transactions; and expands the four S4A test files. Aggregate registry diagnostics now report entry, visible-owner, and retained-hidden-owner count classes. The implementation process reached a complete handoff, but the subagent run is mechanically marked `failed` because root requested generic `acceptance:"verified"` without runtime-configured verify commands; this is an orchestration-tool acceptance configuration failure, not a reported source/test failure. Root therefore treats all claims as unaccepted until direct scrutiny and fresh review.
**Evidence:** Writer reports TypeScript/lint passing; focused `118/118`; literal `438/440`; non-real-Next `436/436`; isolated real-Next terminal/development passing with only absent `.next/BUILD_ID` child/parent; package, whitespace, hash, privacy/boundary, and no-stage gates passing. Root inspected all four production modules and expanded tests, confirmed no source outside the approved S4A inventory changed, and independently passed TypeScript, lint, focused `118/118`, and non-real-Next `436/436`. Direct source inspection confirms queued immutable batch identities, transaction teardown continuation, exact S3 ready relations, and an actual React DOM StrictMode test with no dependency.
**Uncertainty / gaps:** The complete post-fix literal/real-Next/package/privacy gates still require root rerun, and a fresh independent reviewer must challenge listener-subscription timing, queued stale entry delivery, scheduling-failure restart semantics, handler-setter reentrancy, duplicate snapshot handling, provider render/effect lifetime, and every original reproduction. The writer's mechanical acceptance rejection remains recorded; it neither accepts nor invalidates the source result. S4B product acquisition/retention/convergence/SSE removal remains absent.
**Recommended use:** Finish root gates and launch a new fresh no-edit/no-delegation review of the whole corrected diff. Return any bounded defect to one sequential fix attempt; otherwise record acceptance and commit S4A.

## Handoff

**Source:** Pi subagents run `c3077fe5-f768-402c-91e2-c9d8947c4e7c`, `reviewer` child 0; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/c3077fe5-f768-402c-91e2-c9d8947c4e7c/output-0.log`. Recorder: orchestration root. Kind: fresh independent post-fix S4A review. Governing master and milestone are the persistent-stream master and immutable S4A plan; Mode: review.
**Purpose:** Reproduce every original P0 after the first fix and challenge queued notification ownership, rollback, resource containment, ready matrices, StrictMode lifecycle, privacy, boundaries, and all S4A VCs without editing or delegating.
**Outcome:** Verdict `FINDINGS — DO NOT ACCEPT`. All five original P0 classes are corrected in their ordinary paths, but one bounded P0 remains: a registry listener can enqueue a later snapshot/effect and then last-release or dispose the entry; `deleteEntry()` does not purge that queued batch and the drain does not revalidate it, so the deleted entry mutates and captured listeners receive sequence/cursor two after disposal. This blocks S4A-VC-004/005 and finalizes no milestone. Every other VC surface and the expanded validation matrix was accepted as implemented.
**Evidence:** Reviewer reproduced stale post-disposal snapshot `{ snaps: [1,2], stops: 1, staleSnapshot: 2 }` and effect `{ seen: [1,2], stops: 1 }` at `lib/session-registry.ts` notification drain/deletion. Root had independently inspected the same queue lifetime and agrees that already-shifted current fanout may finish under listener-set snapshot semantics, while nested batches belonging to a deleted generation must be suppressed. Root gates after the first fix passed TypeScript, lint, focused `118/118`, non-real-Next `436/436`, package, whitespace/hash/stage/protected/privacy checks; literal `438/440` and isolated real-Next `2/4` had only the authorized missing `.next/BUILD_ID` child/parent failures.
**Uncertainty / gaps:** Tests lack nested snapshot/effect plus last-release, whole-registry disposal, and same-ID recreation coverage. The correction must not suppress the currently executing batch, reorder active-entry batches, or let a deleted generation affect its replacement. No protocol, provider, client, UI, SSE, dependency, or planning change is needed.
**Recommended use:** Run one narrow sequential S4A fix that generation-checks or purges only not-yet-shifted deleted-entry batches and adds exact lifecycle/recreation regressions. Then rerun root gates and obtain another fresh review.

## Handoff

**Source:** Pi subagents run `b61ea853-4fe2-4f1d-be71-3c49ced560e7`, `milestone-implementer` child 0, session `019fc1be-bb78-71c1-8828-2e1baa139c41`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/b61ea853-4fe2-4f1d-be71-3c49ced560e7/output-0.log`. Recorder: orchestration root. Kind: second bounded S4A fix handoff. Governing master and immutable S4A milestone as above; Mode: fix.
**Purpose:** Suppress only queued, not-yet-shifted notification batches belonging to a deleted/stopped registry generation while preserving completion of the current captured batch and all active-entry ordering.
**Outcome:** The writer added one liveness gate immediately after queue shift and six registry regressions: snapshot/effect nested publication followed by last release, registry disposal, and same-ID recreation. Current captured listeners finish; stale cursor/effect two does not deliver; the stale entry remains at cursor one; old client stops once; replacement cursor/effect advances independently. No other source or test file changed in this attempt.
**Evidence:** Writer reports registry `19/19`, focused `124/124`, non-real-Next `442/442`, literal `444/446`, isolated real-Next `2/4`, TypeScript/lint/package/whitespace/hash/boundary/privacy/no-stage gates, with only authorized absent `.next/BUILD_ID` child/parent failures. Root inspected the exact liveness gate and six tests: validation occurs once for each shifted batch before its fanout, so the already-shifted current batch completes, while nested old-generation batches encounter `entry.stopped` or missing identity and are discarded before mutation/fanout.
**Uncertainty / gaps:** Root must rerun final gates and a fresh reviewer must independently reproduce the original stale post-disposal cases plus same-ID replacement. Production inclusion remains release-owned; no S4B or later behavior was added.
**Recommended use:** Perform final root validation and fresh no-edit/no-delegation review. Accept only if every S4A VC and original finding is closed.

## Handoff

**Source:** Pi subagents run `67659d72-1a05-4175-945e-a0ee0b919f03`, `reviewer` child 0; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/67659d72-1a05-4175-945e-a0ee0b919f03/output-0.log`. Recorder: orchestration root. Kind: fresh independent review after deleted-generation fix. Governing master/S4A milestone as above; Mode: review.
**Purpose:** Reproduce every prior P0 and verify stale-generation suppression, current-batch completion, replacement isolation, all S4A VCs, and final evidence without edits or delegation.
**Outcome:** Verdict `FINDINGS — DO NOT ACCEPT`. The intended stale-generation P0 is fixed, but one bounded P0 remains in replacement subscription setup: when same-ID recreation occurs during an existing registry drain, the new client's synchronous initial subscription queues its current snapshot; a later handle subscription delivers that current identity synchronously and then blindly joins the queued equal-identity batch, receiving revision zero twice. This blocks S4A-VC-005/009. Every stale old-generation snapshot/effect, stop, current-batch, replacement isolation, prior P0, scope, privacy, protocol, provider, and resource check otherwise passed.
**Evidence:** Reviewer reproduction delivered `[[0,0],[0,0],[1,9]]` with the first two values the same identity, while stale cursor remained one, replacement reached nine, and old stop remained one. Root inspected `RegistryHandle.subscribe()` plus `includeInQueuedSnapshots()` and agrees: queued snapshots equal to the just-synchronously-delivered current identity must be skipped, while genuinely newer ordered identities must still be attached. Existing same-ID tests assert only eventual `getSnapshot()`, not subscription cardinality/identity.
**Uncertainty / gaps:** The correction must track identity through queued batches, avoiding duplicate adjacent identities while retaining later genuinely new identities and all established listener reentrancy semantics. No source outside registry/test is needed.
**Recommended use:** Run one final narrow sequential fix and exact reentrant replacement subscription regression; repeat focused/root gates and fresh review before accepting.

## Handoff

**Source:** Pi subagents run `76089753-975d-4829-b1a4-9b143efdf904`, `milestone-implementer` child 0, session `019fc1ca-4d13-7b2f-a605-f9befbd8da14`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/76089753-975d-4829-b1a4-9b143efdf904/output-0.log`. Recorder: orchestration root. Kind: third narrow S4A fix handoff. Governing master/immutable milestone as above; Mode: fix.
**Purpose:** Ensure a reentrant same-ID replacement subscription receives its synchronous current snapshot identity once and only later distinct queued identities in FIFO order.
**Outcome:** The writer wraps each snapshot subscription with a distinct internal listener identity, reconciles queued batches from the synchronously delivered current identity, removes already-captured equal-identity membership, and adds later distinct identities once. Two tests reproduce replacement delivery `[[0,0],[1,9]]` and repeated queued identities reducing to `[[0,0],[1,7],[2,8]]`, while retaining stale-generation suppression/current-batch completion/exact stops/replacement isolation. Only registry source/test changed.
**Evidence:** Writer reports registry `21/21`, focused `126/126`, non-real-Next `444/444`, literal `446/448`, isolated real-Next `2/4`, and passing TypeScript/lint/package/whitespace/hash/protected/privacy/no-stage gates, with only authorized absent `.next/BUILD_ID` child/parent failures. Root inspected wrapper identity and queued FIFO identity reconciliation; no wire, client, provider, product, or boundary source changed.
**Uncertainty / gaps:** Fresh review must reproduce exact cardinality/identity and challenge multiple subscriptions using the same callback, synchronous callback reentrancy/release, queued equal/distinct identities, unsubscribe/current-batch semantics, and all prior fixes. Root final gates remain required after this source state.
**Recommended use:** Run final root matrix and fresh no-edit/no-delegation review; accept only with no remaining finding.

## Handoff

**Source:** Pi subagents run `e51bdc46-f5b5-4f31-acbf-9a4151d128a2`, `reviewer` child 0; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/e51bdc46-f5b5-4f31-acbf-9a4151d128a2/output-0.log`. Recorder: orchestration root. Kind: fresh independent review after replacement-identity fix. Governing master/S4A milestone as above; Mode: review.
**Purpose:** Reproduce all prior findings and adversarially challenge replacement identity cardinality, duplicate callback subscriptions, synchronous callback release, queued identities, stale generations, and the entire VC set.
**Outcome:** Verdict `FINDINGS — DO NOT ACCEPT`. Same-ID replacement identity/order and every earlier P0 now pass, but one bounded synchronous-subscription release P0 remains: when another owner retains the entry, a newcomer's synchronous current callback can release itself and then publish cursor two; unconditional queued reconciliation re-adds that inactive listener, delivering `[1,2]` after release. This blocks S4A-VC-004/005/009/010.
**Evidence:** Reviewer reproduction observed `{ seen:[1,2], stops:0, current:2 }`. Root had identified the same edge during source scrutiny and agrees on the exact contained rule: after synchronous current delivery, reconcile only if the handle remains active; if publication preceded release, its already-captured listener membership remains untouched, but publication after release must never gain that listener.
**Uncertainty / gaps:** One keeper-owner regression is missing. No other code or behavior requires change.
**Recommended use:** Apply one final one-condition registry/test fix, run gates, and obtain fresh acceptance review.

## Handoff

**Source:** Pi subagents run `e06ea557-8c64-413a-bab4-e28a7d215075`, `milestone-implementer` child 0, session `019fc1d7-0395-75ff-ba2e-b83781c9e0e7`; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/e06ea557-8c64-413a-bab4-e28a7d215075/output-0.log`. Recorder: orchestration root. Kind: fourth narrow S4A fix handoff. Governing master/milestone as above; Mode: fix.
**Purpose:** Prevent a released synchronous subscriber from gaining later queued snapshot membership while preserving pre-release captured membership with another owner retaining the entry.
**Outcome:** Queued reconciliation now receives the generation-bound handle and runs only while that handle remains current and the wrapper remains installed. Keeper-owner tests prove release-then-publish delivers only `[1]`, while publish-then-release preserves captured `[1,2]`; the keeper advances and client stops only on final release. Only registry source/test changed.
**Evidence:** Writer reports registry `23/23`, focused `128/128`, non-real-Next `446/446`, literal `448/450`, isolated real-Next `2/4`, plus TypeScript/lint/package/whitespace/hash/protected/privacy/no-stage passing; only authorized missing `.next/BUILD_ID` failures remain. Root inspected the current-handle/listener guard and exact paired tests and finds the distinction correctly implemented.
**Uncertainty / gaps:** Fresh no-edit review and final root matrix remain mandatory. No other known source gap remains.
**Recommended use:** Obtain fresh acceptance review, complete root gates, then summarize/commit only if no finding remains.

## Handoff

**Source:** Pi subagents run `d9d3e8e2-0732-4de5-81f8-bf16257fc242`, `reviewer` child 0; recoverable output `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/d9d3e8e2-0732-4de5-81f8-bf16257fc242/output-0.log`. Recorder: orchestration root. Kind: final fresh independent S4A implementation review. Governing master: `.agents/plans/2026-07-30-persistent-stream-websocket-migration.md`; governing milestone: `.agents/plans/2026-08-02-s4a-browser-session-registry-reducer.md`; Mode: review.
**Purpose:** Re-evaluate the complete S4A implementation and every prior finding, with direct keeper-owner release/publish ordering, same-ID identity/FIFO, same-callback subscription, stale-generation, rollback, resource, recovery, StrictMode, privacy, and protected-boundary probes.
**Outcome:** The reviewer returns `ACCEPT` with no blocker, major, minor, or meaningful coverage defect. Release-then-publish excludes the released subscriber; publish-then-release preserves already-captured membership; same-ID replacement and same-callback subscriptions retain distinct exact identities; stale generations, disposal, effects, current-batch completion, and exact stops are correct. It independently accepts nested browser-client/registry notification order, transaction rollback, synchronous resource containment, complete ready/snapshot matrices, duplicate finals, inert StrictMode provider lifecycle, privacy, scope, and S4A-VC-001 through S4A-VC-009. S4A-VC-010 is ready for root summary and commit.
**Evidence:** Root independently inspected the complete 14-path source/test inventory and every fix; passed TypeScript, lint, focused `128/128`, exact non-real-Next `446/446`, literal `448/450`, isolated real-Next `2/4`, nine-file package dry run, whitespace, immutable-plan/master, protected-source, inventory, EventSource/source/privacy, and no-stage checks. Terminal CLI and real development pass; only the explicitly permitted absent `.next/BUILD_ID` production child/parent fail. No build ran. Final review session is recoverable above.
**Uncertainty / gaps:** Fresh production inclusion remains release-workflow evidence. S4B still owns production acquisition, hidden-view retention, HTTP cursor composition, hook adaptation, and per-session SSE removal; S5-S7 remain unchanged. The initial broad fix run's generic `verified` acceptance was mechanically rejected because no runtime verify commands were configured, but its source claims were not used as acceptance: root gates plus five successive independent reviews and the final `ACCEPT` establish the result.
**Recommended use:** Accept S4A, create the coherent source/test/checkpoint implementation commit excluding `.pi-subagents/`, append the mandatory final implementation summary naming that commit, commit checkpoint finality, then reconcile the master and progress directly to S4B.

## Implementation Summary

**Plan section:** S4A `Design / Implementation Strategy` §§0–8 and S4A-VC-001 through S4A-VC-010; master S4A browser session registry/reducer outcome and the S4A portions of ORCH-VC-003/004/005/008/011/012.
**Work and outcome:** S4A is accepted at the pre-commit boundary. The S3 hub now preserves selected ready cursor/outcome/epoch across reentrant attach publication while ordered later units remain post-target live. A pure target-gated stream wrapper enforces every ready relationship, initial/recovery/final snapshot reason/epoch/sequence rule, committed-cursor resume, partial-candidate discard, duplicate/final behavior, and sequence-addressed effects. A strict same-origin browser session client provides ticket-only page-derived WebSockets, one resume, stable frozen external-store snapshots, serialized nested snapshot/effect batches, bounded reconnect, terminal unsupported versions, stale-resource suppression, and contained injected failures. A generation-safe page registry shares one client per exact ID, isolates distinct IDs, installs first effects before start, serializes active fanout, rolls setup/teardown back across throws, suppresses deleted generations, preserves exact listener identities under reentrancy/recreation, and reports only aggregate count classes. One inert StrictMode-safe provider is mounted above keyed chat lifetime without acquiring or changing current product state. Four bounded fix/review loops closed every reproduced defect; final review accepts all S4A VCs.
**Validation / evidence:** Root passes TypeScript and lint; focused S4A `128/128`; exact non-real-Next `446/446`; literal full `448/450`; separate real-Next `2/4`; package dry run; whitespace; immutable master/milestone; intended 14-path source/test inventory; protected hook/AppShell/chat/sidebar/SSE/HTTP/package sources; privacy/EventSource/source-boundary; and no-stage checks. Terminal CLI and real development pass; only the expressly permitted absent `.next/BUILD_ID` production child/parent fail. Final independent review run `d9d3e8e2-0732-4de5-81f8-bf16257fc242` returns `ACCEPT`. No `next build` ran.
**Departures from approved obligations:** None. S4A remains deliberately inert at the product boundary. Production acquisition, connect-before-prompt composition, actual retained-hidden policy, cursor-bound HTTP convergence, hook/render adaptation, and per-session SSE removal remain S4B; file watch remains S5; heartbeat/30-minute idle/final shutdown remains S6; cross-browser scale/user acceptance remains S7; fresh production inclusion remains release-owned. The absent production artifact and intentionally unrecreated oversized transient effects are disclosed planned boundaries, not waivers or incomplete S4A obligations.
**Implementation commit:** pending at the coherent S4A boundary commit.

## Implementation Summary

**Plan section:** Final S4A disposition for `Design / Implementation Strategy` §§0–8 and S4A-VC-001 through S4A-VC-010; master S4A browser session registry/reducer outcome and the S4A portions of ORCH-VC-003/004/005/008/011/012.
**Work and outcome:** Accepted S4A is committed. Reentrant S3 attachment now preserves the selected ready target while later units remain ordered live output. The new pure stream wrapper exposes only fully committed canonical state, enforces exact ready/replay/snapshot matrices, and separates ordered one-shot effects. The strict page-host session client owns one ticket/bootstrap/socket/timer path, resumes only committed cursors, serializes nested snapshot/effect publication, bounds retry, suppresses stale resources, and fails unsupported versions terminally. The page registry shares exact-ID clients, isolates distinct IDs, transactionally creates and tears down generations, preserves stable external-store identity and exact listener semantics through reentrancy/recreation, and suppresses stale generation work. One inert StrictMode-safe provider is mounted without changing the current SSE/HTTP product authority. Four bounded fix/review loops resolved every reproduced target, fanout, rollback, resource, lifecycle, and ownership defect; final fresh review accepted all S4A VCs.
**Validation / evidence:** Root passes TypeScript, lint, focused S4A `128/128`, exact non-real-Next `446/446`, package dry run, whitespace, immutable master/milestone, intended source boundary, protected-source, privacy/EventSource, and no-stage gates. Literal full is `448/450` and separate real-Next is `2/4`; terminal CLI and real development pass, with only the expressly permitted absent `.next/BUILD_ID` production child/parent failing. Actual React StrictMode, complete ready/snapshot/final transactions, nested client/registry fanout, every setup/teardown throw, synchronous resource faults, stale generation/replacement identity, and release-order cases are directly covered. Final review run `d9d3e8e2-0732-4de5-81f8-bf16257fc242` returns `ACCEPT`. The 14-path implementation plus checkpoint boundary is recoverable at the commit below; no build ran and `.pi-subagents/` remains excluded.
**Departures from approved obligations:** None. No S4A obligation is incomplete, blocked, waived, superseded, or divergent. Production acquisition, connect-before-prompt, actual retained-hidden policy, HTTP cursor composition, hook/render adaptation, and per-session SSE removal remain exactly S4B; file-watch migration remains S5; heartbeat/30-minute idle/final shutdown remains S6; cross-browser scale/user acceptance remains S7; fresh production inclusion remains the release workflow. The absent production artifact and intentionally unrecreated oversized transient effects are planned disclosed boundaries, not departures.
**Implementation commit:** `11036f6b2f829b7e27bad9e2fee4148d5925915d` (`feat: add browser session transport registry`).
