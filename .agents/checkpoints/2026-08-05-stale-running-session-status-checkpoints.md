# Stale Running Session Status Checkpoints

Plan: `.agents/plans/2026-08-05-stale-running-session-status.md`

## Handoff

**Source:** Pi subagents run `fc9d2f3b-eea8-45a0-851c-cf338937ed28`, fresh read-only `scout`; child session `5ce6149b/run-0`; recoverable result `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-results/fc9d2f3b-eea8-45a0-851c-cf338937ed28.json`, output log `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-runs/fc9d2f3b-eea8-45a0-851c-cf338937ed28/output-0.log`, and native session `/Users/xin/.pi/agent/sessions/--Users-xin-Documents-repos-pi-web-.agents-worktrees-2026-08-05-stale-running-session-status--/2026-08-05T23-56-03-797Z_019fd45b-0955-7d30-82f0-0ea4adaa843d/5ce6149b/run-0/session.jsonl`.
**Purpose:** Independently map the ordinary-event publication defect, delayed projected-receipt risk, reentrancy and replacement-authority constraints, smallest approved production seam, and focused validation surface without editing or delegation.
**Outcome:** The scout confirmed that `eventFanoutDepth` is a necessary internal cleanup barrier but is sampled too early by `publishRunningState()` inside native delivery. It recommended publishing only when the outer fanout depth returns to zero, plus publishing a delayed receipt outcome only when no fanout barrier remains. It found no reason to change publisher identity, projector/hub semantics, global-status transport, sidebar, HTTP, persistence, timeout policy, or SDK lifecycle.
**Evidence:** The parent verified the decisive paths in `lib/rpc-manager.ts`, `lib/session-event-hub.ts`, `lib/session-projector.ts`, `lib/global-status-channel.ts`, and existing lifecycle/global-status tests. Baseline `NODE_ENV=test node --test lib/rpc-manager.test.mjs` passed `105/105`. Before production changes, four new focused tests produced the required red baseline: the balanced native lifecycle passed, while idle metadata, nested idle delivery, and delayed rejected-receipt cleanup failed because the global set retained the session ID even though `wrapper.isRunning()` was false.
**Uncertainty / gaps:** The delayed-receipt regression uses the hub's existing private test seam to hold processing and force a rejected queued start; final review must ensure that this models the real queued-receipt ordering without weakening finality. Hosted success/failure global membership, replacement authority, full reentrancy, and sanitized browser behavior remain pending validation.
**Recommended use:** Keep production changes bounded to the stable outer-fanout and delayed-receipt publication sites in `lib/rpc-manager.ts`; extend global membership assertions around existing hosted/replacement coverage, then run the approved focused, full, static, and browser gates.

## Handoff

**Source:** Pi subagents run `0a095d9a-71e5-4667-a62f-f071c1d795eb`, two fresh read-only `reviewer` children; recoverable result `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-results/0a095d9a-71e5-4667-a62f-f071c1d795eb.json`, output logs in the sibling async run directory, and child sessions `2be3d3f7/run-0` and `2be3d3f7/run-1` under the parent session directory recorded by that result.
**Purpose:** Adversarially review the first implementation for fanout/receipt concurrency, projected-finality ordering, replacement authority, scope fidelity, test sufficiency, and validation-contract completion without editing or delegation.
**Outcome:** One reviewer found a material delayed committed-terminal defect: while the hub was already processing, the native terminal receipt could queue `wrapper_settled`; all immediate status publications then observed projected activity as still active, and no publication followed the queued finality commit. The parent reproduced the contradiction and made wrapper-settlement publication receipt-aware. A follow-up authority test also exposed that a delayed obsolete wrapper could reclaim publisher ownership through an intermediate positive publication before clearing it; delayed callbacks are now release-only and publish only an idle result. The second reviewer requested explicit global membership assertions for synchronous prompt failure and independent compaction error/abort paths; those were added. Its browser/full-gate findings reflected validation still in progress rather than a code defect.
**Evidence:** The new delayed committed-terminal regression failed before the review fix with `wrapper.isRunning() === false` and projected activity false while the global set retained the ID. After the receipt-aware change it passes and proves `running -> projected-final -> idle` ordering. The revised same-ID test proves a queued obsolete settlement cannot clear replacement-owned running status. Focused prompt failure, compaction success/error/abort/overlap, rejected receipt, committed receipt, reentrant settlement, and replacement tests pass. The parent also completed the concurrently pending sanitized browser smoke: initial/final spinner absent, initial/final authoritative set idle, metadata command accepted, visible and persisted transcript counts `2/2`, persisted leaf present, and zero page errors; all disposable browser/session/server artifacts were removed.
**Uncertainty / gaps:** The delayed tests intentionally use existing private hub processing/sequence seams; consolidating them into a helper is optional maintenance polish, not a correctness gap. The exact full-suite gate still requires a clean run with worktree-local dependency resolution, followed by final fresh review after all fixes.
**Recommended use:** Retain receipt-aware final publication and release-only delayed callbacks; do not let delayed positive state reclaim same-ID authority. Complete the exact full/static gates, then obtain final clean-room review of the post-fix diff.

## Implementation Summary

**Plan section:** Objective; Design / Implementation Strategy items 1–6; Test Strategy; Validation Contract VC-001 through VC-006.
**Work and outcome:** Moved native-event running publication to the stable outer fanout boundary while retaining `eventFanoutDepth` as the internal cleanup guard. Delayed native receipt callbacks publish only an actual idle release, preventing an obsolete wrapper from reclaiming replacement authority. Wrapper settlement now observes its own projected commit receipt so final snapshots commit before global idle publication, including queued/reentrant terminal delivery. Expanded focused global-membership coverage for accepted idle metadata, nested delivery, rejected and committed delayed receipts, real native settlement, retry/compaction overlap, prompt success/failure, compaction success/error/abort, hosted success/failure/Stop/destruction, same-ID replacement, and initial/reconnected global-status frames. No sidebar, HTTP, WebSocket protocol, persistence, timeout, Hide/Restore, deletion, SDK lifecycle, or publisher-map semantics changed.
**Validation / evidence:** The red baseline passed only the balanced native case and failed idle metadata, nested idle, and delayed rejected-release cases on old production code. Review then found a distinct delayed committed-terminal failure, which the parent reproduced before the receipt-aware fix. Post-fix focused RPC tests pass; the exact repository gate `NODE_ENV=test node --test lib/*.test.mjs components/*.test.mjs` passes `673/673`; literal `node_modules/.bin/tsc --noEmit`, `npm run lint`, and `git diff --check` pass using a temporary worktree dependency symlink that was removed afterward. The sanitized isolated Chromium smoke verified no initial/final spinner, no initial/final authoritative running membership, successful metadata command, visible and persisted transcript counts `2/2`, a persisted leaf, and zero page errors; its disposable session, server, browser files, development output, and runtime cwd were removed.
**Departures from approved obligations:** None. Production remains limited to `lib/rpc-manager.ts`; the additional receipt-aware finality and release-only authority guard were required by deterministic review reproducers inside the approved lifecycle seam.
**Implementation commit:** Pending final independent review and final validation.

## Handoff

**Source:** Pi subagents run `051f8129-5d32-4f44-a3bd-a4083184bebd`, two fresh read-only `reviewer` children; recoverable result `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-results/051f8129-5d32-4f44-a3bd-a4083184bebd.json`, output logs in the sibling async run directory, and child sessions `6d8d0937/run-0` and `6d8d0937/run-1` under the parent session directory recorded by that result.
**Purpose:** Perform clean-room post-fix review of receipt-aware finality, delayed authority, full VC coverage, validation evidence, and repository hygiene.
**Outcome:** The concurrency reviewer found one remaining same-ID authority race: an obsolete terminal listener could start a replacement reentrantly, after which the obsolete wrapper's unconditional stable-end positive publication reclaimed authority and its idle finality cleared the still-running replacement. The parent reproduced the reported `replacement running / global empty` contradiction. Stable status updates now preserve another publisher's authority by default; only wrapper startup, explicit prompt/compaction claims, or a native start accepted in the current outer fanout may claim authority. A deterministic reentrant same-ID test proves the replacement publisher and sole running frame survive obsolete finality. The validation reviewer otherwise signed off the code/test scope and identified only removal of untracked subagent artifacts before closeout.
**Evidence:** The new reentrant replacement test failed before the authority-mode fix with an empty global set while the replacement wrapper remained running, then passed after the fix. Full RPC lifecycle tests pass `114/114`; the focused concurrency set passed 30 repeated process-isolated runs; the exact full suite now passes `674/674`; literal TypeScript, lint, and whitespace gates pass with the temporary dependency symlink removed afterward.
**Uncertainty / gaps:** The prior browser smoke predates only the authority-preserving hardening; the idle metadata path remains directly covered by the final deterministic test and no browser/protocol code changed. One final fresh reviewer must inspect the new claim-versus-preserve boundary. Runtime `.pi-subagents/` artifacts remain intentionally untracked until that review finishes and must then be removed.
**Recommended use:** Require explicit authority acquisition only at true wrapper/run starts, preserve authority for settlement and ordinary stable publications, and obtain final no-findings review before commit.

## Handoff

**Source:** Pi subagents run `c3d3ee60-c7b7-4132-89ae-fd9c40371f4f`, two fresh read-only `reviewer` children; recoverable result `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-results/c3d3ee60-c7b7-4132-89ae-fd9c40371f4f.json`, output logs in the sibling async run directory, and child sessions `bb30df81/run-0` and `bb30df81/run-1` under the parent session directory recorded by that result.
**Purpose:** Re-review the claim-versus-preserve authority boundary after terminal-fanout hardening and challenge inverse nested-start ordering.
**Outcome:** Both reviewers independently found the inverse same-ID race: an obsolete outer native start captured authority, a replacement started inside its raw listener and claimed newer authority, then the obsolete outer completion used a wrapper-wide Boolean to reclaim. The parent reproduced it with a current-code-failing inverse regression. Start publications now capture both publisher identity and a weakly held authority epoch before any reentrant work and may claim only if that baseline is still current; stable updates otherwise preserve the newer publisher. The same ordering rule now covers wrapper startup, prompt claims, compaction claims, and native starts. Additional deterministic cases prove a newer nested wrapper start, a newer same-publisher reclaim after an ABA authority cycle, and a newer reentrant prompt claim all outrank the older outer start.
**Evidence:** The inverse nested-start test failed before the ordering-aware change because the publisher map held the obsolete identity and later became globally idle while the replacement remained running. It and the ABA/reentrant-prompt cases now pass. RPC lifecycle tests pass `117/117`; the expanded focused race set passes 30 repeated process-isolated runs; the exact full suite passes `677/677`; TypeScript, lint, and whitespace checks pass.
**Uncertainty / gaps:** Authority epochs are HMR-stable weak keys tied to opaque wrapper publisher identities; they add no protocol or persistence state. Final review must inspect the baseline capture timing and confirm there is no remaining older-start path that bypasses it.
**Recommended use:** Treat start authority as an ordering claim captured before projection/subscription reentrancy, not as an unconditional positive publication. Keep non-start and delayed publications release-only or authority-preserving.

## Handoff

**Source:** Pi subagents run `459a48b0-d93f-426c-9bc1-f740c9c1ddb7`, two fresh read-only `reviewer` children; recoverable result `/var/folders/_j/ttksft5934g0fj8z6l4rg6_w0000gn/T/pi-subagents-uid-501/async-subagent-results/459a48b0-d93f-426c-9bc1-f740c9c1ddb7.json`, output logs in the sibling async run directory, and child sessions `e8233c26/run-0` and `e8233c26/run-1` under the parent session directory recorded by that result.
**Purpose:** Exhaustively review the final authority baseline/epoch model across wrapper startup, prompt, compaction, native fanout, delayed outcomes, same-ID nesting, ABA cycles, finality, destruction, and HMR state.
**Outcome:** One reviewer caught the newly added pending-start reproducer before its fix and correctly required pending versus committed authority handling. The parent changed native fanout to retain each start request and outcome, exclude rejected nested starts, and let a pending start preserve rather than replace existing authority. The other reviewer received the live post-fix diff, inspected the added pending/rejected request cases, and returned an explicit no-findings signoff: no older start or non-start path could overwrite or clear a newer same-ID owner, and no real-run visibility gap remained. The parent additionally applied the same ordering baseline to wrapper startup and prompt/compaction claims and added faithful reentrant prompt and same-publisher epoch cases.
**Evidence:** Final `lib/rpc-manager.test.mjs` passes `119/119`. The expanded authority/receipt set passes 30 repeated process-isolated runs. The exact full suite passes `679/679`; literal TypeScript, lint, and whitespace checks pass. The signing reviewer independently ran focused RPC, TypeScript, lint, and whitespace checks and explicitly signed off with no findings. Durable current-state and memory guidance now record stable fanout, receipt-aware finality, and ordering-aware authority semantics.
**Uncertainty / gaps:** Private hub controls are used only to deterministically hold FIFO receipt processing and force bounded rejection; public reentrant listener cases independently cover the real queue/fanout behavior. The earlier sanitized browser smoke remains representative because later hardening changed only same-ID authority ordering and did not change UI, protocol, or the idle metadata transport path.
**Recommended use:** Accept the implementation for commit after removing untracked subagent/runtime artifacts and repeating final hygiene checks.
