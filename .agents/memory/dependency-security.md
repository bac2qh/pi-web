# Dependency Security And Local Production Runtime

## 2026-07-23

- Dependency refresh commit `caaa1bd9393b9c5a4abfcf383f1fa99c2249e85c` updates Next/ESLint to `16.2.11`, Mermaid intent to `^11.15.0`, and project-local Pi AI/coding-agent/TUI intent to `^0.81.1` while keeping package identity `@agegr/pi-web@0.7.16`.
- The parent-scoped Next override is deliberately exact: `next@16.2.11` uses `postcss@8.5.12` and `sharp@0.35.0`. Remove or revise it only after a stable compatible Next line natively selects fixed versions and full build/native/runtime validation passes.
- The published Pi coding-agent shrinkwrap still installs `protobufjs@7.6.4`. Both production and full audit intentionally expose exactly its moderate `GHSA-j3f2-48v5-ccww` record at the nested coding-agent path; no private Pi fork, repack, manual lock surgery, or audit suppression is allowed.
- `npm ls --all` may remain nonzero only for `@emoji-mart/react@1.1.1` declaring React support through 18 while the app uses React 19. Do not hide it with legacy-peer-deps, force, or fabricated metadata.
- npm is authoritative for this refresh. `bun.lock` remains unchanged and unvalidated.
- The local production runtime is the complete checkout: local `node_modules`, `.next`, and executable `bin/pi-web.js`. After source or dependency changes, rerun `npm ci --include=dev`, validation, and `npm run build`; launch never rebuilds automatically.

## 2026-08-07

- Pi Web now keeps the compatibility key `@earendil-works/pi-coding-agent` while installing the untracked sibling artifact whose actual identity is `@bac2qh/pi-coding-agent@0.84.0-bac2qh.734502cb8`, built from exact `bac2qh/pi` commit `734502cb86eaf631e1ceeb403dbd717e3b78404f`. Runtime imports remain unchanged; package metadata, npm alias output, and `VERSION` expose the fork identity.
- `scripts/build-local-pi-fork.mjs` requires Node `24.19.0`/npm `11.17.0`, a clean exact-commit sibling fork, two fresh full fork validations, metadata-only repacking, and byte-identical output before atomic publication. The retained artifact digest is SHA-256 `f5a5caf1f5d34e9830480afb5707b5dc412a0e10a3016349240904bc12a47de2` / SHA-512 `sha512-T/AN5JeYYvGiDU7uupAkgiibNLD3Q8iUzQTpG401YJ9IbCIMsZE8ICtTdEcnGWaPqtwsP1UkNFH9VMOeJnchYw==`.
- Exact-commit validation must not consume mutable live model catalogs. The helper hydrates ignored generated model data from integrity-pinned official `@earendil-works/pi-ai@0.84.0`, then runs the exact fork checks, full suite/build/local release, and focused faux-provider between-turn compaction regression.
- Every official Pi companion in the npm graph is locked at exact `0.84.0` (AI, TUI, agent core, client, protocol, telemetry). npm 11 does not apply the packaged shrinkwrap as a resolver for this local alias, so lock creation used npm operations to temporarily anchor all six exact companions and then remove the four transient direct intents; the committed manifest still directly declares only AI/TUI plus the compatibility key.
- The old coding-agent `0.82.1` audit findings are superseded. At adoption time, production/full audits retain only already-baselined `brace-expansion`, Mermaid, Next, and PostCSS findings; the coding-agent/Undici findings disappear. `npm ls --all` remains nonzero only for the established Emoji Mart React-18 peer range against React 19.
- Pi `0.84.0` credential mutations can commit before post-commit model/auth synchronization throws. All Pi Web login/logout/API-key mutation routes invalidate the independent 60-second model cache in `finally`, while preserving the native success/error result.
- `npm run install:local-pi-fork` verifies the sibling artifact identity and committed integrity before running `npm ci --ignore-scripts --include=dev`; explicit dev inclusion keeps the development workflow complete even under an ambient production environment, and the preflight is required because a warm npm cache can otherwise satisfy a missing local `file:` dependency without proving the on-disk prerequisite.
- The local-fork manifest is deliberately non-publishable: npm does not embed the external `file:../pi/.artifacts/...` tarball. `npm run release` remains unchanged but must not run until a separately approved change restores a self-contained dependency. `bun.lock` and the global Pi installation remain outside this workflow.
